# Wave 7 session prompts — hand each to a fresh Opus session

Written 2026-07-08. Each prompt below is a standalone task for one fresh session. **Prepend the "COMMON PREAMBLE" to every prompt** (it carries the base, docs, and build/gate discipline). Dependency order at the bottom.

---

## COMMON PREAMBLE (prepend to every prompt)

You are implementing ONE PR in the RecoilEngine sim/draw decoupling program (C++23 RTS engine). Work in the git worktree `/www/projects/RecoilEngine/.claude/worktrees/epoch-integration` (branch `epoch-integration`; build on current HEAD — do NOT `git reset`). NEVER modify or build the operator's main checkout at `/www/projects/RecoilEngine` — build only in the worktree.

⚠️ PATH TRAP (a past session wasted an hour on this): EVERY Read/Edit/Write path MUST be worktree-absolute — start it with `/www/projects/RecoilEngine/.claude/worktrees/epoch-integration/...`. The main checkout `/www/projects/RecoilEngine/rts/...` is a DIFFERENT branch (`bruno/poc-split-sim-draw`) whose files DIVERGE from the worktree; editing `/www/projects/RecoilEngine/rts/...` by habit silently edits the wrong branch and your worktree build never sees it. If you catch yourself having done this: `git -C /www/projects/RecoilEngine checkout -- <files>` to revert, then re-apply against the worktree (re-read the worktree versions — they differ). NOTE the two non-code exceptions that DO live only in the main checkout: the planning docs (`/www/projects/RecoilEngine/doc/sim-draw-*.md`, read them from there) and the replays (`/www/projects/RecoilEngine/replays/*.sdfz`) — see the gate section. Put any NEW doc you author in the WORKTREE's `doc/`.

Read before coding (absolute paths; these docs are untracked, read directly):
- `doc/sim-draw-pr43-44-contract-refresh.md` — THE governing Wave-7 contract, incl. `§7 OPERATOR RULINGS` (BINDING) and the `§0` landed-boundary map. Read all of it.
- `doc/sim-draw-pr44-prerequisites.md` — the prerequisite specs.
- `doc/sim-draw-epoch-publish-specs.md` + `doc/sim-draw-epoch-publish-plan.md` — program specs/plan (all amendments binding).
- `doc/sim-draw-thread-decoupling-research.md` — landed PR 1–27b architecture + the "Build & gate runbook".

Discipline (learned the hard way — follow exactly):
- The Bash sandbox is broken on this host — run shell with `dangerouslyDisableSandbox: true`.
- EXACTLY ONE spring process at a time. Before any run: `pgrep -af build/spring` must be empty (kill strays by PID; confirm port 8453 free with `ss -tulpn | grep 8453`). Never overlap runs — overlapping causes port clashes + CPU-contention that corrupts timings.
- Build: `ninja -C build -j28 engine-legacy` in the worktree. IGNORE clangd "file not found" / "no member" diagnostics — they are worktree-vs-main-checkout compile-DB noise; judge only by ninja's result.
- Bound every build/run with `timeout -k 30 <secs>`; treat rc 124/137 as HUNG = a failed gate (never a pass); decide pass/fail from the exit code + a crash/DESYNC/Lua-error log scan, never from a sentinel's presence. Runs over ~9 min go in the background (`run_in_background: true`).
- ESCALATION RULE: implement the narrowest flag-off-safe change; if you hit a genuine design ambiguity not covered by the docs, STOP and record it as a question for the operator — do NOT improvise design. Append rulings you receive to the specs doc.
- PARTIAL-LAND RULE (learned from SCOPE-1): a task bullet can prove blocked at GATE time, not design time — it compiles and even resim-cleans, then fails only the flag-ON headful matrix because it's coupled to infrastructure that isn't landed yet (SCOPE-1's `IdToObject` retirement needed PR-43's DEAD_THIS_BATCH; the headful gate surfaced it as a `RecvFromSynced` nil-storm). When that happens: REVERT just that bullet, land + fully-gate the independent bullets, document the blocker WITH the gate evidence + the finish-path in the design doc AND move the bullet to the PR that unblocks it (edit THIS prompts file), and report it. Do NOT block the whole PR on one coupled bullet, and do NOT force a bullet past a red headful gate. Headful is the ONLY gate that exercises draw-side serving — a green compile + green resim + green armed diff-gate does NOT clear a draw/serving change.
- Land IN PLACE: commit directly on `epoch-integration` (the program is one-PR-at-a-time, land-in-place). Commit-message trailers:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
  `Claude-Session: <your session URL>`

Gate tooling: `test/callout-diff-gate/run_diff_gate.sh "<ABSOLUTE demo path>" <label> <worktree>/build/spring` (run it with cwd = the worktree root so the widget/config setup resolves).
- Env: `DG_ARM=1` armed diff-gate (mirror/serving bit-correctness + resim-clean), `DG_ARM=0` plain resim; `DG_FF=1` fast-forward; `DG_QUIT_FRAME=0` full-length. `SPRING_DATADIR=/www/projects/bar-data2`. Flag via that dir's `springsettings.cfg`: `SimDrawSplit=1` (split on) / `0` (off), `SplitWindowShrink=1` (PR-42 shrink on). For flag-OFF resim, `sed -i 's/^SimDrawSplit = 1/SimDrawSplit = 0/'` that cfg (back it up first) and RESTORE it to 1 after.
- Headful adds: `DISPLAY=:0 SDL_VIDEODRIVER=x11 SDL_AUDIODRIVER=dummy ALSOFT_DRIVERS=null`. The driver widget is already registered in bar-data2.
- ⚠️ Two replays live in the MAIN checkout — pass them by ABSOLUTE path (a relative `replays/...` resolves against the worktree, which has no `replays/`, and the runner then launches spring with a bogus demo path → headful pops a blocking `xmessage "Demofile not found"` dialog and the run hangs until timeout; kill stray `xmessage` by PID). Use exactly:
  - `/www/projects/RecoilEngine/replays/2026-07-04_15-21-42-198_Rosetta 1.4.4_2026.06.10.sdfz`
  - `/www/projects/RecoilEngine/replays/2026-07-04_15-57-11-635_All That Glitters Extended v1.0.2_2026.06.10.sdfz`
- Reading results: the runner's own summary prints `SYNC   : clean (no DESYNC in infolog)` — that line CONTAINS the substring "DESYNC", so `grep -c DESYNC` on the runner log false-positives; check `grep -c DESYNC /www/projects/bar-data2/infolog.txt` (the real count) or grep the runner log for `MISMATCH`/`GATE   :`/`Invalid Feature id`. A full-length flag-ON headful run reaches ~f=44500; a run that dies at `f=-1` (pre-game) failed to load (usually the demo-path trap above).
- KNOWN non-bug: flag-OFF Rosetta throws `gfx_decals_gl4.lua:564 table index is NaN` at ~f=39535 — a pre-existing stock-BAR widget bug, NOT branch-caused. Any OTHER lua error is yours. In particular a `gl.SetFeatureBufferUniforms() Invalid Feature id` nil-storm from `unit_healthbars_widget_forwarding.lua` starting ~f=2474 is the 38b class = you broke died-in-batch serving.
- The 38b widget-error class is headful-GL4-only + draw-timing-flaky — headless drops the GL4 draw entirely (so headless gates do NOT exercise draw-side record reads at all; only headful does). Run the headful matrix (both replays × ≥2 flag-ON runs each, per `§7.8`). Report gate numbers explicitly (rc, last frame, desync count, lua-error count, diff-gate PASS/FAIL).
- DEBUG-headless gate: the operator has waived DEBUG builds for these sessions ("don't run debug builds") — skip it unless told otherwise. TSan (a separate `USE_TSAN=ON` sanitizer build, NOT a DEBUG build) is still wanted where a prompt's GATE lists it; confirm with the operator if unsure.

Your specific task follows.

---

## PROMPT — Gap A: draw-owned UI control-groups

TASK: Make UI control-groups (`uiGroupHandlers`) fully draw-owned so the sim thread never touches them — a prerequisite for the PR-44 producer flip (under the flip a sim-thread group poke races the draw thread's group reads). The complete edit spec is in `doc/sim-draw-pr44-prerequisites.md` "Gap A" (Edits 1–4 + the AICallback scope-out). Implement it exactly:
- Move the sim-thread group pokes to the boundary: `CUnit::PreDestruct` `SetGroup(nullptr)` → `CGame::DeliverBoundaryDeaths`; `CUnit::ChangeTeam` + `CFactory::FinishBuild` group inherit → `UnsyncedBoundaryQueue::Defer` id-capturing closures (re-resolve by id at drain; add `CGroupHandler::RemoveUnitFromGroups(int)`).
- Replace the sim-thread `CCommandAI` idle-gate `owner->GetGroup()` map read with an atomic `inUiGroup` mirror on `CUnit` (set in `CGroup::AddUnit`, cleared in `RemoveUnit`/`SetUnitGroup(id,nullptr)`/`RemoveUnitFromGroups`).
- Every edit wraps the existing call in `if (!SimDrawSplit::Enabled()) { …verbatim… }` so flag-off is byte-identical. Scope OUT the deprecated `CAICallback` engine-group callbacks (note them for a possible narrow lock later).
- Per `§3.8` of the refresh doc: the FinishBuild drain-time re-resolve must be epoch-safe (nullptr-guard the `GetUnit`s).
GATE: flag-OFF resim BOTH replays (`DG_ARM=0 SimDrawSplit=0`, full-length) → 0 DESYNC (proves the boundary-moved pokes change no synced state/ordering) + flag-ON headful BOTH replays → groups work, 0 death/capture crash, 0 lua errors + DEBUG-headless replay. Commit on epoch-integration.

---

## PROMPT — Gap B: serve the deep-CAI default-command (unparks guihandler)

TASK: Serve the default-command + available-commands surface so `CGuiHandler::Update`→`SetCursorIcon`→`GetDefaultCommand` and `LayoutIcons`→`GetAvailableCommands` no longer read live sim (`leaderUnit->commandAI->GetDefaultCmd`, `commandAI->GetPossibleCommands`, `lastSelectedCommandPage`). This is the `§7.2` operator ruling (SERVE, option A) and `§4.2` of the refresh doc; background in `doc/sim-draw-pr44-prerequisites.md` "Gap B". It is a Wave-5-style serving addition (follow the §E.2 serving recipe / diff-gate pattern in the specs):
- Add a decoded default-command row per unit (the value `GetDefaultCmd` would return), refreshed with the command-queue cache at the barrier (generation/version-gated like `RefreshCommandQueues`).
- Extend the PR-30 `UnitCmdQueueSlot` with `lastSelectedCommandPage`; export a C++ accessor so `CSelectedUnitsHandler::GetAvailableCommands` reads the boundary cache instead of live `commandAI`.
- Add a `SnapshotDiffGate` field-pass for the new served value(s) (armed dual-run must prove the served default-command bit-matches the live `GetDefaultCmd`).
- Then REMOVE the guihandler "keep parked" carve-out: fold `guihandler->Update` into the PR-42 sim-live set (it can now run with the sim live). Verify the context cursor is correct sim-live.
GATE: armed diff-gate BOTH replays (`DG_ARM=1`) PASS incl. the new field-pass + flag-ON headful BOTH replays 0 desync / 0 lua + a windowed note (cursor default-command visually correct — the operator will confirm interactively at the end). Commit on epoch-integration. If serving `GetDefaultCmd` faithfully proves infeasible, STOP and escalate (the fallback is `§7.2` option C, a ≤1-boundary-stale cached cursor — operator ruling required).

---

## PROMPT — SCOPE-1: handler-resolution-free render records (units/features) — ✅ LANDED @ `86eb7b26fa` (2026-07-09)

STATUS: DONE and gated — do NOT re-implement. Details in the WORKTREE's `doc/sim-draw-pr39-scope1-design.md`. What landed (3 of the 4 original bullets):
- Per-object drawer-owned render record (id-keyed, in the drawer data): immutable header `{model, def, localModel}` + the sim-owned mutable scalars the draw passes read (team, allyteam, engineDrawMask, buildProgress, beingBuilt, buildFacing, radius, pos, luaDraw, isInVoid, noMinimap). Captured producer-side at `RenderUnit/FeaturePreCreated`, refreshed each `Update`. Every unit/feature draw/shadow/alpha/icon/ghost pass (GLSL+GL4) + `AddToSubmission` reads the record; LOS via SimSnapshot; selection stays live (draw-owned).
- PR-10 deferred evictions DONE: `LocalModel::luaMaterialData` + `LocalModelPiece::lodDispLists` → the record (threaded into `LocalModel::Draw/DrawPiecesLOD/SetLODCount`; `CSolidObject::GetLuaMaterialData` removed).
- `BuildSplitResolveCache`/`splitResolveCache` DELETED (units/features) + the `ModelDrawer.h` wrappers + the barrier-step-1c calls. `DrawerGetObjectByID` resolves via the record's deferred-safe handle (no handler walk); non-model features via `RegisterNonModelFeatureRecords`. Projectile drawer untouched (that's the Projectile-pass PR).

FRAMING CORRECTION: this is "handler-resolution-FREE", NOT "pointer-free". The record retains a deferred-deletion-safe `obj` handle + a `localModel` handle (used only for the legacy immediate-mode piece path + id-keyed SSBO offsets); those reads are pre-existing tolerated torn-reads, NOT handler-table walks. Full pointer-freedom of the immediate-mode piece path (extract piece transforms + scriptVisible) is a 44-adjacent follow-up, out of SCOPE-1.

BLOCKED / MOVED TO PR 43 — the 4th bullet (retire the `IdToObject` pointer-path shell fallback): EMPIRICALLY CANNOT be done in SCOPE-1. Dropping it nil-storms `gl.SetFeatureBufferUniforms` from deferred `RecvFromSynced` handlers (`unit_healthbars_widget_forwarding.lua`), because the boundary-dead shell (`renderEventQueue.ResolveBoundaryDeadFeature`) covers died-in-batch feature ids the record's cold-miss (`FindPendingDestroyFeature`) does NOT — SCOPE-1 CLEARS the record's `obj` at RenderDestroyed, so a died-in-batch id has no at-death row. Retiring it REQUIRES the record to RETAIN the at-death row through the deferred-dispatch window = **PR 43's DEAD_THIS_BATCH**. So this bullet is folded into PR 43 (see that prompt). `LuaUtils::IdToObject` still calls `ShellFallbackUnit/Feature` and is documented in-code.

---

## PROMPT — Projectile pass conversion (pointer-free projectiles)

TASK: Stop `renderProjectiles`/`UpdateDrawFlags` from resolving through `projectileHandler` (plan PR 40 [O]; refresh `§4.4`). Extract per-class projectile visual state into POD draw packets (the §C projectile family becomes extracted, not torn-tolerated); delete the projectile resolve cache. NOTE the projectile drawer is STRUCTURALLY SEPARATE from the unit/feature machinery SCOPE-1 touched: `CProjectileDrawer` is a `CEventClient` (NOT a `CModelDrawerBase`) with its OWN `splitResolveCache`/`BuildSplitResolveCache` (in `ProjectileDrawer.cpp`); SCOPE-1 deliberately left it intact + kept its barrier call — this PR deletes it. PRECEDENT: SCOPE-1's landed render record (`doc/sim-draw-pr39-scope1-design.md`) is the pattern for the "record-captured stable pointers" interim — an id-keyed drawer record holding a deferred-deletion-safe handle + extracted scalar/POD fields the draw passes read, captured producer-side, never a handler walk. This is decision-point 4 (projectile extraction budget): MEASURE the per-boundary copy cost on both replays first (S0-style, est. 1–2 MB/boundary heavy); if over budget, the documented interim is the SCOPE-1-style deferred-safe handle under epoch lifetime — escalate the budget number to the operator. GATE: flag-OFF resim ×2 + armed diff-gate both replays + flag-ON headful both replays + TSan segment. Commit on epoch-integration.

---

## PROMPT — Pre-44c pause-surface clearing (ScopedExternalSimPause inventory + ctrl remainders + races)

TASK: Clear the mid-gameplay sim-park surface so PR 44c can demote `RequestPause` to lifecycle-only. Work the inventory in refresh `§4.5` + `§4.6` + `§4.8`:
- For EACH mid-gameplay `ScopedExternalSimPause` site (GuiHandler.cpp:1157 TryTarget, :2187, :2521/:3567 DrawMapStuff blocking/yardmap, MouseHandler.cpp:484 MouseRelease, MiniMap.cpp:1421): SERVE it (via the existing snapshot / trace channel / blocking mirror / placement channel), or explicitly classify it as an accepted rare park with measured frequency. `Path.cpp:208` is operator-ruled to STAY a park (display-gated) — leave it. `GetDefaultCommand` (:1694) is Gap B — leave it.
- `LuaUnsyncedCtrl.cpp:603 SendCommands` — do the per-action net/UI/sim classification the Batch-1 amendment owes before 44 (executor-park deadlock check).
- PR-41-escalated ctrl remainders (`§4.6`): `SetUnitNoGroup`/`SetUnitIconDraw`/`SetUnitIcon` (live `unit->team` + live `CUnit*` to draw-owned ops) + the TraceScreenRay minimap `GetSelectUnit()->id` deref — close via the id→owner seam.
- Known races (`§4.8`): the MT-spawn `RenderEventQueue::Push` append race, the guRNG per-side stream split, the TSan camera-read note.
This is an audit-heavy PR — produce a disposition table (site → served/converted/accepted-park + evidence) in the commit. GATE per touched surface: armed diff-gate + flag-ON headful both replays + TSan segment. Escalate any site whose disposition needs an operator call. Commit on epoch-integration. (Can be split into sub-commits if large.)

---

## PROMPT — PR 43: the epoch ring (foundation)

TASK: Package today's boundary output into an explicit N=3, refcount-retired epoch ring, still PRODUCED AT THE BARRIER UNDER THE PARK (consumer unchanged) — a pure representation swap, flag-off byte-identical / flag-on value+timing-identical. Full contract in refresh `§1` (channel list) + `§2` (the PR-43 spec) + `§7` (rulings). Implement per `§2`:
1. The ring: N=3 epoch slots, each `{epochId (u64 monotonic); frame span; all §1 channels}`; publish = newest-complete; draw acquires (ref++) / releases prev (ref--); recycle at refcount 0. The double buffer + `cmdQueueCacheGeneration`/`pieceCacheGeneration` + mirror drained-versions become per-slot state. Per `§7.1`: FLAT copies everywhere (no refcount-shared blocks); add a STANDING TODO to measure per-epoch bytes later.
2. Rekey `DeferredObjectDeleter` release to epoch retirement (replaces end-of-Draw `ReleaseAcked` + the relocated-ack special case): destroy record pins its shell to its epoch; ACK at that epoch's record dispatch; RELEASE at epoch retirement. Under 43's lockstep these coincide (that's what keeps 43 flag-on-identical).
3. Dead-in-batch rows (resurrect from tag `pr38b-flip-wip`, AMENDED per `§7.7`: the producer extracts dying ids' rows from their DeferredObjectDeleter shells at the frame edge — genuine at-death state, DELETE the 38b genuineness-guard class; keep the `fd41dbdd92` projectile-namespace filter). Barrier dispatches STILL read live under `barrierLive` here (43 does NOT convert them — that's 44b). DEAD_THIS_BATCH replaces the shell-fallback READ maps for ROW-served twins.
   3b. RETIRE THE `IdToObject` SHELL FALLBACK (deferred from SCOPE-1, which empirically could NOT — see the SCOPE-1 prompt). The SCOPE-1 render record clears its `obj` handle at RenderDestroyed, so a died-in-batch id is unservable from it and `LuaUtils::IdToObject<CUnit/CFeature>` still calls `ShellFallbackUnit/Feature`. Once DEAD_THIS_BATCH makes the record RETAIN the id's at-death row through the deferred-dispatch window, convert `IdToObject` to resolve from the record with NO explicit shell (i.e. `IdToObject → DrawerGetObjectByID`, whose cold-miss now finds the retained dead row). GATE THIS SPECIFICALLY: re-run the headful matrix and confirm ZERO `gl.SetFeatureBufferUniforms() Invalid Feature id` / `RecvFromSynced` nil-storm (that error class from `unit_healthbars_widget_forwarding.lua` ~f=2474 is the exact regression that blocked it in SCOPE-1). If it reappears, the record is not retaining the dead row for long enough — extend retention to ACK, do not restore the shell blindly.
4. Land the query/reply keying fix (`§7.3` + Batch-4): grid-snapped `TestBuildOrder` key + standing-latest-query for `ClosestBuildPos`/`TestMoveOrder`/pos-form traces.
5. Re-key every `Generation()`-keyed draw cache to `epochId`.
6. LAND THE ARMED ID-COVERAGE GATE (`§2.6`/`§3.6a`): a per-record pre-dispatch assert that every id referenced by an epoch's records/closures resolves in that epoch (ACTIVE or DEAD_THIS_BATCH). This is 44b's make-or-break invariant; prove it holds under the park now.
7. Settle the piece/SSBO channel representation (`§4.7`/`§7.6`): for 43 the piece transforms are a per-epoch flat channel produced at the barrier (the move to the sim thread is 44a). Re-measure S0 churn (non-gating, feeds the §7.1 TODO).
8. Land `/epochstats` telemetry (per-channel bytes + ring occupancy).
GATE (`§2.7`): standing battery + DEBUG-headless + TSan segment (ack/release rekeyed) + armed diff-gate both replays + strict `SplitDrawContract=2` zero-denial re-proof + headful matrix (both replays × ≥2 flag-ON). Invariants in `§2.6` — escalate if threatened. This is the biggest foundational PR; high effort. Commit on epoch-integration.

---

## PROMPT — PR 44a: extraction on the sim thread (residual consume-park) — ✅ LANDED (2026-07-09/10, `epoch-integration`; design record in the WORKTREE's `doc/sim-draw-pr44a-producer-flip-design.md`)

TASK: Move epoch production onto the sim thread at its frame edges, keeping a RESIDUAL tiny park at consume (deferred dispatches still run sim-quiescent). This is the `§7.5` split's first step — isolate the threading move from the serving semantics (do NOT touch the deferred-serving conversion; that's 44b). Contract in refresh `§3.1/§3.2/§3.7` + `§7.5`:
- Producer (sim thread, at the `YieldIfPauseRequested` edge in `SimThreadProc`): drain the ctrl-poke queue, evaluate standing/pending queries, `Extract*` all namespaces (+ shells for dying ids), refresh queue/piece blocks, capture dirty mirror layers, package records, publish the epoch. (Precedent that Extract* is sim-thread-safe: `HashCompletedFrame` already runs it on the sim thread; the one exception `CacheMapStartPositions` moves to load.)
- Move `transformsMemStorage` production to the sim thread with per-epoch staging (`§7.6` ruling A — touch the uploader seam; no double copy).
- Skip-if-unconsumed pacing; net-mutation-between-frames re-mark is producer-local (sim owns ClientReadNet).
- Consumer keeps a MINIMAL park for the acquire + deferred dispatch (still reads live under `barrierLive` as today — unchanged serving).
- REQUIRES first: SCOPE-1 render records (✅ LANDED @ `86eb7b26fa` — its deferred `IdToObject` shell-retirement bullet moved to PR 43, which is fine: 44a keeps `barrierLive`/live serving, so it does not depend on that retirement) + projectile pass conversion + Gap A (groups). Confirm the projectile pass + Gap A have landed before starting.
GATE: full battery — resim ×2 both flags, DEBUG-headless, TSan segment (the threading move is the whole point — TSan is load-bearing), armed diff-gate, headful matrix both replays, ON-vs-OFF frame-time telemetry (the parked window should shrink to ~acquire time). Commit on epoch-integration. This is lifetime + threading — highest effort.

---

## PROMPT — PR 44b: remove the park + creation/event-time deferred serving — ✅ LANDED (2026-07-10, commits 1/6–5/6 + 6/8–8/8 through `021ebbed03` on `epoch-integration`; design record in the WORKTREE's `doc/sim-draw-pr44b-noport-park-design.md`)

STATUS: FULLY LANDED, park removal included. The §3.6 DEFERRED-SERVING CONVERSION (the 38b resolution) landed first — `barrierLive` removed, acquire-first dispatch, per-epoch window scoping, ParseRaw/ParseFeature shell retirement, the §3.8 closure conversions, the default-cmd re-host (draw-side pick + sim-side leader walk; a sim-side pick was gate-found to corrupt the heap — see the design record §4), the wait-AI served-queue conversion, and all the cross-thread channel mechanics. The `SYNCED` cross-hop blocker was then RULED (contract refresh §9, BINDING: option 3+1 hybrid — read-set-driven epoch mirror of SYNCED scalar globals + a dispatch-scoped lazy-park fallback for first-touch/non-scalar reads) and the PARK REMOVAL LANDED per the ruling: `CGame::Draw` no longer parks, consume-window exclusion via the explicit `SignalEpochConsumeComplete` pacing stamp, valve → `ServicePoolValveOnce` (consume + force-produce the mid-frame tail in place + no-wait resume), `valveLive` retired → the ScopedLiveException survivor set is EXACTLY the 8 audited. Two gate-found no-park fixes landed en route: per-epoch-batch SendToUnsynced mailbox rotation (7/8, fire/drain race) and flip→lockstep teardown consume (7b/8, orphaned sealed-batch quit storm).

FINAL GATE MATRIX (all green, design record §5 + §10): armed diff-gate flag-OFF full-length PASS(0)/0 DESYNC on BOTH replays (ATG + Rosetta); strict ATG flag-ON full-length rc=0, 0 DESYNC, 0 denials, idCoverage 275,156:0, `[SimParkStats]` parks=22 totalMs=240.7 (parked ≈ 0), mirror-served 44,865 / lazy-park engages 28, zero 38b signatures; serial headful Rosetta flag-ON full-length (f=44544) rc=0, 0 DESYNC, 0 lua errors, 0 `Invalid Feature id`, 0 denials, idCoverage 186,036:0, parks=29 totalMs=196.0, mirror-served 44,537 / lazy-park engages 33, zero 38b signatures. Residuals for the operator (in-budget, design record §5/§10): 2 deterministic instancevbo zombie echoes (ATG only, ≤ the accepted 4-echo budget), the quit-frame `gui_build_eta` nil (one-shot, did not recur), and a non-fatal no-park-only `invalid unit/featureID while resizing` unitlights warning class (watch at end-of-stack). Per the operator directive, TSan/DEBUG/multi-run matrix remain end-of-stack items.

ORIGINAL TASK (for reference): Remove the residual consume-park; the deferred unsynced create/damage/event handlers now read PUBLISHED epoch state (no `barrierLive` fallback exists — the sim runs concurrent). This is THE 38b-resolution; it failed once (revert `981cbfea44`) so follow refresh `§3.6` EXACTLY:
- (a) Enforce the id-coverage invariant via the armed gate landed in PR 43 — every record/closure id must resolve in its epoch.
- (b) Generalize event-time presentation using the LANDED override machinery (fire-time `CaptureCmdQueueEvent` + `ScopedCmdQueueEventOverride` + top-of-callout consults + the 38l LOS-exit retained-pos serving). Extend fire-time capture to every deferred event whose handler reads state the event changed (UnitCreated def/team/pos, UnitDamaged/FeatureReclaim value args). Do NOT use the 38h blanket-reassert (it force-nil'd unrelated reads — reverted).
- (c) Scope the dispatch window per epoch (DEAD_THIS_BATCH validity + overrides scope to "epoch E dispatching"; E can't retire until its bracket closes).
- REQUIRES first: Gap B (deep-CAI served — guihandler has no park to fall back on) + PR 44a + PR 43's id-coverage gate.
GATE (`§3.6d` — the class is headful-GL4-only + flaky): the headful matrix run MULTIPLE times per replay, orchestrator-instrumented, expecting 0 lua errors of the create/damage/event class across ALL runs (one clean run is NOT proof) + full battery (resim ×2, DEBUG, TSan) + strict zero-denial. If the deferred nils reappear, the id-coverage gate should now catch them DETERMINISTICALLY — root-cause from there, do not blind-loop. Commit on epoch-integration. Highest risk in the program.

---

## PROMPT — PR 44c: demote RequestPause + rekey valve/backpressure — ✅ LANDED (2026-07-10, commits `e9767c3138`/`c5ff10df98`/`f9c4f02a94` on `epoch-integration`; design record in the WORKTREE's `doc/sim-draw-pr44c-lifecycle-pause-design.md`)

STATUS: LANDED and gated (lean policy, operator-ratified). (1/3) The pool valve is the real §3.4 form: the main-thread in-place drain service is DELETED (`ServicePoolValve/Once`, `ParkAtValve`/`ResumeFromValve*`); a pool-pressured sim publishes its mid-frame tail as a normal pacing-gated epoch (`ProduceEpochForPoolValve`, sim thread) and waits for epoch retirement in `ValveParkWait` rounds (presents as a parked sim; holds fully quiescent while a pause is pending; only the sim resumes itself). Proven live by a forced-pressure smoke (80 engages / 391 rounds / 0 DESYNC / no hang). (2/3) Backpressure rekeyed to "sim runs ahead ≤ N-1 unretired epochs" (`SimSnapshot::UnretiredEpochCount`, retirement stamped at the consumer's acquire), `LastBoundaryFrame`/`PublishBoundaryFrame` plumbing deleted; FF/catch-up/skip/capture carve-outs preserved verbatim (all FF gate legs 3:26–5:33 wallclock, `[BackpressureStats]` 0 ring-blocks). (3/3) RequestPause is lifecycle-only by audit: survivors = saves/checkpoint serialize, Lua handle create/reload/kill brackets, Reload/Restart/Start, the operator-ruled `CPathTexture` park, the accepted input-gated GUI/mouse/GiveOrder/SendCommands parks (≈0 in replays) and the §9 lazy dispatch park — full grep table in the design record. Ctrl-poke pre-extraction consumption was already 44a's p1 (no work remained).

GATE MATRIX (all green): armed diff-gate ATG flag-OFF full-length **PASS (0 mismatches)**, 0 DESYNC (f=44867); strict Rosetta flag-ON full-length rc=0, 0 DESYNC, 0 denials, idCoverage 186,036:0, parks=29 totalMs=193.8; serial headful ATG flag-ON full-length rc=0 (f=44863), 0 DESYNC, 0 denials, idCoverage 275,152:0, **0 lua errors, 0 `Invalid Feature id`, 0 zombie echoes, 0 no-park VBO-resize warnings**, `[SimParkStats]` parks=21 totalMs=160.1 (parked ≈ 0, lifecycle-only), `[SimPauseSurvey]` LUA_SEND_COMMANDS=5 only, mirror-served 44,855 / lazy 29. Enumerated deviation flagged for the operator: a lifecycle park catching a valve-parked sim gets MID-frame quiescence (rare²; design record §1). TSan/DEBUG/multi-run matrix remain end-of-stack; NEXT: the operator's final live windowed skirmish (`§7.8`).

---

## PROMPT — Prereq D: world-GUI serving (unpark DrawMapStuff + MiniMap frustum)

CONTEXT: The pre-44c pause-surface audit (LANDED @ `6ff670e440`; full disposition + measured park-engage frequencies in the WORKTREE's `doc/sim-draw-pre44c-pause-surface-clearing.md`, and `doc/sim-draw-pr44-prerequisites.md` "NEW 44b prerequisites … Prereq D") measured THREE mid-gameplay `ScopedExternalSimPause` sites that re-park the running sim EVERY draw frame (they move the *parked ≈ 0* headline and so gate 44b, not just 44c). The audit landed per-site telemetry (`CGame::SimPauseSite` + the `[SimPauseSurvey]` shutdown dump) — use it to measure your progress.

TASK: Serve the per-frame world-GUI reads so these sites no longer park. All the serving machinery already exists — this is a wiring PR (Wave-5-style serving; follow the §E.2 recipe / diff-gate pattern in the specs). The three sites and their served sources:
- **`CGuiHandler::DrawMapStuff`** (GuiHandler.cpp, tag `GUI_DRAW_MAPSTUFF`, measured ~12–13k/game headful) — the world-space GUI overlay parks for the whole pass. Route its reads: selected-unit command queues → the LANDED command-queue cache (`LuaSnapshotServe::RefreshCommandQueues` / `cmdQueueCache`); hover/`GuiTraceRay` targets → the PR-35 trace query/reply channel (standing-query model, the shape Gap B's `EvaluateDefaultCmdQuery` already uses, `§7.3` rides-with-epoch); build previews / `Pos2BuildPos` / `TestUnitBuildSquare` / yardmap → the blocking mirror (`DrawMapMirrors`, PR 29) + the placement channel (`EvaluatePlacementQueries`, `§7.3` keying: grid-snapped `TestBuildOrder`, standing-latest-query for `ClosestBuildPos`/`TestMoveOrder`). Drop the whole-pass park once its reads are served; the nested `GetCommand`/`GetBuildPositions`/`TryTarget` parks then become genuine no-ops (or serve them via the same channels).
- **`CMiniMap::DrawCameraFrustumAndMouseSelection`** (MiniMap.cpp, tag `MINIMAP_FRUSTUM`, per-frame headful — measured 0 in the two gate replays' minimap state, but per-frame by construction) — the `GuiTraceRay` hover pick → the trace channel. Drop the park.
- **`GetDefaultCommand` non-cursor fallback** (GuiHandler.cpp:1726, tag `GUI_GET_DEFAULT_CMD`, measured ~10k/game headful) — Gap B unparked only the every-frame *cursor* path (via the default-command query/reply); the *non-cursor* callers (`DrawMapStuff`, `CMouseHandler::DrawCentroidCursor`) still park. Route them through the same served default-command reply (generalize the Gap-B standing query to these callers), or fold into the DrawMapStuff serving above.

PARTIAL-LAND EXPECTATION: some DrawMapStuff reads may have no landed channel — the audit flagged `TryTarget`'s weapon-state / range-ring reads as the likely residual (weapon range/target state is not currently served). For any residual live read with no channel: serve it (add the row/channel) if cheap, else keep a NARROW internal park around only that read (not the whole pass) with the `SimPauseSurvey` count as evidence, and ESCALATE if the residual is itself per-frame (that would need its own serving decision). Do NOT force the whole-pass park to stay.

GATE: armed diff-gate BOTH replays (`DG_ARM=1`) PASS incl. any new field-pass for served values + flag-ON headful matrix (both replays × ≥2, per `§7.8`) 0 desync / 0 lua + the `[SimPauseSurvey]` dump showing `GUI_DRAW_MAPSTUFF` / `MINIMAP_FRUSTUM` / `GUI_GET_DEFAULT_CMD` dropped to ~0 (any residual narrow-park count documented). The interactive hover/build-preview correctness is not fully replay-reachable — note it for the operator's final skirmish. Commit on epoch-integration. (Can be split into sub-commits: minimap frustum + GetDefaultCommand fallback are smaller and independent of the DrawMapStuff bulk.)

---

## PROMPT — Prereq E: SendCommands per-action park scoping

CONTEXT: The pre-44c audit (`6ff670e440`) measured `Spring.SendCommands` (LuaUnsyncedCtrl.cpp:603, tag `LUA_SEND_COMMANDS`) at **~10k park-engages/game HEADFUL** (vs 22 headless) — stock BAR widgets call it ~per draw frame (verified NOT the diff-gate driver). So its conservative full-batch `ScopedExternalSimPause` is a per-frame park headful → it moves the *parked ≈ 0* headline and gates 44b. The per-action net/UI/sim classification the Batch-1 amendment owed is DONE (audit doc §4.5-SendCommands, and `sim-draw-pr44-prerequisites.md` Prereq E): the DRAW-UI and NET-SEND families (the large majority) touch no live sim; only the SIM-POKE readers do (`DumpState`, `ViewSelection`, `Give`, `Destroy`/`Remove`, `MaxParticles`/`MaxNanoParticles`, and the `gs->`/`teamHandler`/`selectedUnitsHandler` reads). Deadlock-safety is established (no executor blocks on the sim), so removing the park for the no-touch majority cannot deadlock.

TASK: Scope the park PER ACTION instead of per batch, so the per-frame DRAW-UI/NET widget traffic stops parking. Pick the mechanism (implementer's call, keep it conservative-by-default):
- Preferred: a `touchesSimState` classification on the unsynced action executor (`IUnsyncedActionExecutor` registration in `UnsyncedGameCommands.cpp`), defaulting to TRUE (park) for anything unclassified so the safe default is preserved; explicitly mark the DRAW-UI / NET-SEND families FALSE. Then in `CGuiHandler::RunCustomCommands` / `SendCommands`, take the `ScopedExternalSimPause` only around actions whose executor is classified sim-touching (park per-action, not per-batch). Mind the `RunCustomCommands` recursion (depth guard) and multi-command batches.
- Alternative: a small static allowlist of the known SIM-POKE action names (the minority) and park only for those; simpler but the classification must not silently miss a sim-touching action — default-park-if-unknown is safer.
Remove the single full-batch park at LuaUnsyncedCtrl.cpp:603; the flag-off path stays byte-identical (park still acquired for the sim-touching actions; no-op flag-off as today). Keep the `LUA_SEND_COMMANDS` telemetry tag on whatever park(s) remain.

GATE: flag-OFF resim BOTH replays 0 DESYNC (proves no synced-ordering change — actions still dispatch in order, sim-touching ones still park) + flag-ON headful BOTH replays 0 desync / 0 lua + the `[SimPauseSurvey]` `LUA_SEND_COMMANDS` count DROPS sharply (the ~10k/game DRAW-UI/NET traffic no longer parks; only the rare SIM-POKE actions remain). Report the before/after count. The interactive `SendCommands` executor-park behaviour under real input is the operator's final-skirmish coverage (`§7.8`). Commit on epoch-integration.

---

## Dependency order

- **Independent, do first / in parallel:** ~~Gap A~~ (✅ LANDED @ `f0d51df471`), ~~Gap B~~ (✅ LANDED @ `a8c90334ce`), ~~SCOPE-1~~ (✅ LANDED @ `86eb7b26fa`), ~~Projectile pass~~ (✅ LANDED @ `7e8895c312`), ~~Pre-44c pause-surface clearing~~ (✅ LANDED @ `6ff670e440` + §8.1 @ `a22b710ce5`). (Gap B unparks guihandler; SCOPE-1 + Projectile make the draw side handler-resolution-free — draw passes no longer walk `unitHandler`/`featureHandler`/`projectileHandler`; they still hold deferred-safe handles, i.e. not literally pointer-free.)
- **Prereq D** (world-GUI serving) + **Prereq E** (SendCommands scoping): surfaced by the pre-44c audit as the per-frame parks that gate 44b (not just 44c). Independent of 43 and of each other; both land before **44b** (a per-frame park defeats 44b's *parked ≈ 0* headline). D reuses the trace/placement channels + blocking mirror + command-queue cache; E reuses the classification already in the audit doc.
- **PR 43** (epoch ring): needs only the piece/SSBO channel decision (settled, §7.6) — can run alongside the prereqs. ALSO now owns the deferred `IdToObject` shell-fallback retirement (item 3b), which SCOPE-1 proved needs DEAD_THIS_BATCH.
- **PR 44a**: needs SCOPE-1 + Projectile + Gap A + PR 43.
- **PR 44b**: needs Gap B + ~~PR 44a~~ (✅ LANDED) + PR 43's id-coverage gate (armed + green under 44a). ✅ LANDED 2026-07-10 (through `021ebbed03`): §3.6 serving conversion + THE PARK REMOVAL per the §9 ruling (option 3+1 hybrid: SYNCED-globals epoch mirror + dispatch-scoped lazy-park fallback); full gate matrix green (see the 44b prompt's STATUS + the worktree design record §10).
- **PR 44c**: ✅ LANDED 2026-07-10 (`e9767c3138`/`c5ff10df98`/`f9c4f02a94`): valve retirement-wait (in-place service deleted), N-1-epochs backpressure rekey, RequestPause lifecycle-only audit; full gate matrix green (see the 44c prompt's STATUS). THE ENGINE STACK IS COMPLETE — remaining: end-of-stack TSan/DEBUG/multi-run matrix + the operator's final live skirmish (`§7.8`).
- **PR 45** (follow-ups/cleanup, program close-out): ✅ LANDED 2026-07-10 on `epoch-integration` (docs-only; zero engine-code change). Dead-machinery audit (the plan's deletion list verified already-collected by the landing PRs — nothing left to delete; full mechanism→status table in the design record); FF wall-time re-measured (headful Rosetta FF full-length: flag-ON 2:58 vs flag-OFF 3:26, ratio 0.86 — 44a's regression resolved by 44c, the producer pacing knob DROPPED); telemetry index + "Wave 7 — LANDED" consolidation appended to `doc/sim-draw-thread-decoupling-research.md`; the program deferred-items punch list compiled. Design record: the WORKTREE's `doc/sim-draw-pr45-followups-design.md`. THE PROGRAM IS CLOSED — open items are exactly the punch list (§5 there): end-of-stack TSan/DEBUG/multi-run matrix + the operator's final live skirmish, plus the trigger-conditioned deferrals.
- Between PRs: orchestrator DIY-gates a consolidated headful run per replay (one spring at a time). The operator merges `epoch-integration` → `bruno/poc-split-sim-draw` at a moment of his choosing, and runs the final live skirmish after 44c.
