# BAR → modern-GL migration: living inventory & plan

Working document for moving Beyond-All-Reason (`/www/projects/Beyond-All-Reason`) and the Recoil engine off deprecated fixed-function / immediate-mode OpenGL, so a live BAR frame can be captured in RenderDoc. Iterate on this as we learn more — especially after runtime tracing (see Runtime findings log).

## Goal

**Definition #1 — a RenderDoc-capturable BAR frame.** Remove every OpenGL call RenderDoc rejects (immediate mode, fixed-function matrix stack, display lists, attrib stack, alpha test, line stipple, point sprite, fixed-function lighting) from a normal in-match frame. The engine can keep requesting a **compatibility** GL context (`GlobalRendering.cpp:512`); RenderDoc tolerates a compat context, it only chokes on the rejected *calls*. We are **not** targeting a strict GL4 core-profile context (that would additionally require the whole engine, not just BAR-triggered paths, to be core-clean, and the engine to flip its context request).

**Constraint #1 — non-BAR games must stay frame-perfect, and are exempt from RenderDoc.** Any game that is not BAR (Zero-K, TA-derived content, anything else still drawing through legacy GL) must keep rendering **byte-for-byte identically** to a pre-migration engine. Those games do **not** have to become RenderDoc-capturable — Definition #1 is scoped to BAR-triggered paths only. Three consequences, and they bind the whole plan:

- **Conversion is per draw-shape, not per game — and engine-side draws convert outright.** Engine drawers (ROAM terrain, particles, models, water, decals, fonts, RenderBuffers) can go modern for *all* games with no fallback, and largely already have: the 2026-07-31 census measured ROAM 0 rejected / 1153 calls, particles 0 / 68, opaque models 8 / 1144 (attrib stack only) — Zero-K gets that today. The same holds for deprecated **Lua API surface whose implementation is engine-side**: `gl.AlphaTest` → threshold uniform + `discard`, `gl.LineStipple` → shader pattern, `gl.PointSprite` → core point sprites. The widget calling them never changes, so those are engine work, not content work. What must keep an exact-legacy fallback is only the **draw shapes modern cannot yet prove parity on** — today that is dense-MV composition (see the un-matchable-composition finding), which cuts across BAR and non-BAR content identically. FF retires shape by shape as parity is proven; it is not pinned open by non-BAR games as a class.
- **"Remove the FF scaffolding" in this document means "stop *emitting* it from BAR-triggered paths", never "delete it from the engine".** The Phase-0 items below (`ResetGLState`'s FF resets, `SetupScreenMatrices`, the attrib stack) therefore become **conditional on the frame's content already being modern**, not unconditional removals. A legacy-content frame must still get the full FF scaffolding it gets today.
- **Other games are verified by the same A/B gate, not a different one.** `GLFrameABCompare` is engine-level (`CGame::Draw`) and content-agnostic — pointing it at Zero-K or any other game is the same mechanism, and a 0-pixel result there is the same proof it is for BAR. The per-game cost is not a new harness, it is **pass-idempotency guards for that game's content**: control(L↔L) only reaches 0 once no widget advances per-draw state (BAR needed 11 files; see `Spring.GetABCompareActive` / `GetABDuplicatePass`). A *legacy-vs-legacy* cross-build comparison is needed only for changes that alter the **legacy path itself** (e.g. making `ResetGLState` conditional) — not for conversions sitting behind a modern/legacy switch, which the in-frame gate already covers.

## Status / changelog

- 2026-08-02 (latest) — **The shaders stop naming the builtins, and the compatibility work list is measured: 11 constructs left.**
  - **Definition #1 was not sufficient, and the capture is what showed it.** RenderDoc replays on a **core**-profile context, so a shader whose `#version` says `compatibility` cannot be compiled there at all and therefore cannot be *reflected* — which is most of why one captures. Zero rejected *calls* is necessary; core-clean shader **text** is the other half.
  - **The both-sources selector is gone from generated sources.** For fog and the matrices that is a reasoning step — the engine picks the source once at startup, and under suppression it no longer issues the fixed-function calls at all, so selecting the builtin would read an identity rather than merely go unused. For position and texcoord it is a **measurement**: those arrive per draw, so a submission reaching a rewritten program outside `DrawFFAttribStream` would read an unfed attribute and corrupt geometry silently. Measured **zero** on watertest and idletest with a positive control, and the census stays in permanently at `L_ERROR`.
  - **Two errors the gate caught that reading did not.** A uniform nothing references is dropped by the linker, so keeping the selector "declared for identification" classified every rewritten program as unfed and fed none of them — 250k px on every frame. Identification now keys on the generated attribute and on the matrix uniforms. And that same path gated its uploads behind `mvp >= 0`, skipping any shader that reads `gl_ModelViewMatrix` without the composed MVP.
  - **The rewrite now runs on fragment sources too.** `gl_Fog` and the matrices are fixed-function state read identically from either stage, but `Shader.cpp` only ever handed it vertex sources — so fragment shaders kept naming builtins the vertex half had already dropped. Two blockers retired with no new machinery.
  - **What still forces `compatibility` in a BAR frame** (measured on the text as compiled, both stages):

    | blocker | what it needs |
    |---|---|
    | `gl_Normal` | a normal channel in the attribute stream — and it **cascades**: a shader reading it has its position and texcoord channels declined, which is the only reason `gl_Vertex` and `gl_MultiTexCoord0` are still listed |
    | `gl_FragColor`, `gl_FragData` | a single-stage `out` declaration |
    | `gl_TexCoord` | cross-stage: a matching `out`/`in` pair emitted at both funnels |
    | `gl_TextureMatrix`, `gl_LightSource` | new uniform families, same shape as the matrix one |
    | `gl_ClipVertex` | `gl_ClipDistance` |
    | `attribute`, `varying` | keywords removed from core |

  - **The fragment-output builtins retire.** `gl_FragColor` / `gl_FragData` are not fixed-function state — they are the pre-130 spelling of a fragment output — so replacing them is a declaration plus a rename with no engine side at all. Both go through one array when a shader names both, which is normal rather than illegal: they sit in different `#ifdef` branches of the same deferred source, and declaring a scalar beside the array would put two outputs on location 0.
  - **WHERE THE REST OF THE LIST LIVES, and it is not BAR.** Tracing the declines to a source excerpt put every remaining blocker in the engine's **own shipped GLSL**, `cont/base/springcontent/shaders/GLSL/`:

    | file | `#version` | blockers |
    |---|---|---|
    | `ModelFragProg.glsl` | **none** (so 110) | `gl_FragColor` `gl_FragData` `gl_LightSource` `gl_TexCoord` `varying` |
    | `ModelVertProg.glsl` | yes | `attribute` `gl_ClipVertex` `gl_Normal` `gl_TexCoord` `varying` |
    | `ShadowGen{Vert,VertMap,Frag}Prog.glsl` | **none** | `gl_ClipVertex` `gl_TexCoord` `gl_Normal` |
    | `Grass{Vert,Frag}Prog.glsl` | mixed | `gl_Normal` `gl_FragColor` `varying` |
    | `SMF{Vert,Frag}Prog.glsl`, `BumpWaterFS.glsl` | yes | `gl_ClipVertex` `varying` `gl_FragColor` |

    **And every one of them declares a GLSL version a core context cannot accept at all.** ModelFragProg, ShadowGen* and GrassFragProg have no `#version` (so 110); ModelVertProg and GrassVertProg say 120; SMF{Vert,Frag}Prog and BumpWaterFS say 130. A core profile supports GLSL 1.40 and later only — so these cannot be reflected regardless of which builtins they name, and replacing constructs one at a time can never finish the job. The engine's GL4 variants (`ModelVertProgGL4`, `ShadowGenVertProgGL4`, …) are already fully core-clean, which is what the modern path uses.

    These are compiled at load whether or not BAR draws with them, and RenderDoc reflects every live shader object, so an unused-but-compiled one blocks capture reflection exactly as a drawn one does. So there are two ways out, and they differ by an order of magnitude. Porting all ten to core GLSL 150 is ~1375 lines of shader work that no gate here can verify. **Not compiling them is the cheaper and more honest one**: they are the legacy fallback for a path BAR does not take, the modern equivalents already exist and are already clean, and skipping or deferring their compilation under the migration knob removes them from a capture without touching a line of GLSL. **Deferral is in, and is not enough on its own.** `IModelDrawerState::EnsureInstance` lets the legacy state be constructible-but-unconstructed under the migration knob when GL4 is available, and every reader goes through it so the fallback survives. Measured: `deferLegacy=1` (rewrite=1, haveGL4=1), gate 825 frames control 0 / signal 0 — and the compat census UNCHANGED at eleven, with `gl_LightSource` and `gl_FragData` still present, which are ModelFragProg-only. So something still asks for the legacy slot; the suspect is an early `SelectImplementation` before the GL4 *drawer* exists, since `qualifyDrawerFunc` only ever qualifies `t >= MODEL_DRAWER_GL4` and `best` falls through to `MODEL_DRAWER_GLSL` when none does. Log `EnsureInstance` to confirm before changing that default. `ShadowGen*` is compiled by `ShadowHandler` on a separate path this does not touch at all. The care needed is precise. The legacy shaders are built in `CModelDrawerStateGLSL`'s CONSTRUCTOR, so "compile lazily" means CONSTRUCT lazily, and `CModelDrawerConcept::InitStatic` builds both states unconditionally. Two places in `ModelDrawer.h` then matter: `qualifyDrawerFunc` reads `IsValid()`, which is false for a state that has not compiled; and `SelectImplementation(bool,...)` starts from `int best = MODEL_DRAWER_GLSL` and only ever *upgrades* from it, so the legacy state is selected WITHOUT being qualified whenever no GL4 state qualifies. Both paths must force construction rather than read a null or an uncompiled state — otherwise the fallback stops existing silently, which is the one outcome worse than compiling four unused shaders. Because those files ship to every game, the ports have to sit behind the migration knob (a core-clean variant selected when it is on) rather than replacing the originals — constraint 1 by construction, as everywhere else here.
  - **The dual-variant harness, and the obstacle it runs into.** The way to prove a migrated shader equals the original is to compile BOTH and let the in-frame A/B harness bind the other one on the candidate pass — `FFRemovalExperiment::Active()` is already exactly that hook, and `GLSLProgramObject::EnableRaw` is the single bind point. What blocks a naive twin is **uniform locations**: `GetUniformLoc` resolves against `objID` and each `UniformState` caches one location, so binding a twin would push every uniform to locations resolved for the other program. The linker assigns them independently and they will not match; `layout(location=)` on uniforms needs GL 4.3 and cannot be written into a `#version 120` file anyway. So the twin needs either per-variant location caches or a re-resolve when the bound variant changes (a stale-locations flag flipped at bind, paid only on the candidate pass). `GLSLCopyState` already remaps uniform state between programs by name on reload, which is the same problem solved once per reload rather than once per pass.
  - **The gate cannot see any of this, and screenshots cannot replace it.** An engine-side shader substitution moves all four in-frame passes together. A cross-configuration screenshot comparison was built to cover that and its **control failed**: two runs of the *same* configuration differ by thousands of pixels, so run-to-run screenshots gate nothing. What the remaining ports need is dual-variant programs — compile original and rewritten, and let the A/B harness bind the other one on the candidate pass — which is the program-level form of the both-sources pattern already used for fog and matrices.
  - **Two ports proven, one refuted.** `SMF{Vert,Frag}Prog` (the ground) and `BumpWater{VS,FS}` (the water) migrate to core 150 and measure **0 px** against the originals — 824-889 frames on watertest and 585 on idletest, control 0 on both, with the twins named in the coverage log so the zero is about programs that were bound. Two independent contents, so neither result rests on one map happening to exercise the shader. `ShadowGen{VertMap,Frag}Prog` **fails**: its twin builds and binds and the signal is 114662 px on every frame, so that port is wrong and was backed out rather than kept. The suspects are `gl_ClipVertex` (dropped to a local on the assumption its write has no consumer — true for BAR's ground pass, not established for the map SHADOW pass) and prepending `#version 150` to a source that had none, i.e. was 110.
  - **Cross-stage builtins are not the uniform case.** `gl_FogFragCoord` and `gl_TexCoord` are written by the vertex stage and read by the fragment stage, so they need a declared `out`/`in` pair with matching names on both sides — not the engine-fed uniform the matrices and fog get. `BumpWaterVS` failed to compile on exactly this.
  - **`ninja base/springcontent.sdz` does not drop removed files.** A deleted `.core.glsl` kept being served from the stale archive, so the gate kept failing on a shader that no longer existed on disk. `rm` the `.sdz` before rebuilding, or a removal is invisible.
  - **Gate throughout:** watertest 946, 938, 949, 847 frames, control 0 / signal 0. Unchanged with the rewrite off, where `RewriteFFBuiltins` returns before touching the source.

- 2026-08-02 — **A capture exists. RenderDoc takes a frame of BAR, and its replay side opens it: 3053 draws, 13 clears.**
  - **The meter was a prediction; this is the experiment.** `run_rdoc_capture.sh` preloads `rdoc_trigger.so` next to librenderdoc and asks for a capture through RenderDoc's in-application API — the UI and F12 do not exist here, and a capture has to be *requested*. With the migration knobs on: `rdoc_capture_frame5005.rdc`, 787 MB, 0 unsupported functions. With `--legacy`: 32 unsupported functions and no file, so the harness demonstrably tells the two apart.
  - **"A file appeared" is not the claim.** `rdoc_validate_capture.cpp` links librenderdoc's replay side and opens the capture: driver OpenGL, local replay supported, 2048×1152 thumbnail of a real BAR frame, then a full replay reporting **3148 actions / 3053 draws / 13 clears**. The runner does this on every capture.
  - **Opening it crashes on Mesa, and the cause is not ours.** Mesa's GLSL disk cache is not keyed on GL profile: the `#version 150 compatibility` shaders the capturing run just compiled in its compatibility context are served back to RenderDoc's **core**-profile replay context, bypassing the `compatibility profile is not supported` rejection, and the linker then segfaults on them. `mesa_compat_cache_repro.c` reproduces all three outcomes in 40 lines (compat ctx → links; core ctx warm cache → segfault; core ctx cold/disabled cache → clean rejection). `MESA_SHADER_CACHE_DISABLE=true` is the workaround the runner sets.
  - **The engine-side lesson, not yet acted on.** Zero rejected *calls* is necessary but not sufficient: RenderDoc replays in a core context, so a shader that merely *declares* `compatibility` cannot be reflected there. The FF rewrite keeps the builtin compiled in behind a selector uniform — deliberately, because the in-frame gate is blind to an engine-side substitution otherwise — and that reference is exactly what forces the declaration. Making the builtin arm gate-only would let the version line drop `compatibility`. 16 BAR files declare it too, so this is joint engine + content work.
  - **A harness widget was painting the world white.** `ab_unitshape_driver.lua` — the widget the *offender meter* installs to manufacture coverage of the legacy model path — leaks GL state and leaves the world 100% white, with the migration knobs on **and** off, so it is the harness and not the engine. Bisected to it out of 117 enabled widgets. It does not affect what the meter measures (functions reached, not pixels), but anything looking at the image must keep it off: `run_gate.sh` now forces `ABUnitShapeDriver = 0` rather than inheriting it from a previous meter run through the shared config, and `run_rdoc_capture.sh` dies if the widget activated.
  - **Two ways a bisect over widgets lies.** `luaui disablewidget` **persists** to `LuaUI/Config/BYAR.lua`, so without restoring that file per run every iteration inherits every previous disable and the search converges on whichever name happens to sort first — it named the wrong widget twice before the control (`disable nothing` must reproduce the symptom) was added. And deleting an order entry is not the same as re-enabling it: absent means "the widget's own default", which for many BAR widgets is disabled.

- 2026-08-01 — **ZERO. The matrix family retires and the offender meter reads 0 of 47. With `FFVertexAttribRewrite` on, a BAR frame makes no call RenderDoc rejects.**
  - **Measured.** Meter **0** on watertest. Whole-frame gate with the migration on: watertest **833** frames, idletest **557**, control 0 / signal 0. With the rewrite **off**: meter 21, gate 802 frames 0 px — an unupdated game is untouched, which is constraint 1 intact at the end exactly as at the start.
  - **What phase C needed, in the end.** `GL::InstallFFMatrixTracking` feeds `GL::ffMirror` from the glad entry points and, under `FFMatrixSuppress`, does not issue the call; every shader reads its matrices from the uniform the rewrite injected; every C++ reader goes through `GL::ReadFFMatrix`; and every fixed-function emitter calls `GL::MaterializeFFState` first, which puts the mirrored colour, fog, rasterization parameters *and matrices* back into GL for a draw that really is going to be rasterized by fixed function.
  - **`FFMatrixSuppress` defaults ON, and that is not a behaviour change for anyone.** It is inert without `FFVertexAttribRewrite` — `InstallFFMatrixTracking` returns early and the mirror is never fed — so the default cannot reach a game that has not enabled the migration. It makes turning the migration on one knob rather than two. The knobs whose defaults *do* change behaviour for every game are still off and still the user's call.
  - **THE LESSON OF THE WHOLE PHASE, and it cost six rounds: the blank world was the harness.** With suppression on the world vanished while the UI stayed correct, and every engine-side hypothesis — mirror taint, unwrapped draw entry points, unfed programs, wrong fed values, clip planes enabled with unwritten equations, `glGetDoublev` readers — was eliminated in turn, each costing a build and a run. The cause was `ab_fogprobe.lua`, a probe widget of *mine* in the write-dir that draws world-space `gl.BeginEnd` quads out to 43,500 elmos; its geometry took the legacy path, whose stack suppression had emptied, and a few enormous quads covered the screen.
  - **What actually found it: making the symptom a number.** PIL reads the world region of a screenshot and reports how much of it is white, which turns "does the world render" into something a script can decide — and BAR's 206 enabled widgets bisected to one culprit in eight runs. When a whole-frame symptom resists reasoning, stop reasoning and bisect the content.
  - **And the residual after that was mine too.** `FlushLegacy`/`FlushTexRectLegacy` materialised the mirrored state; `gl.Shape`'s emit loops, `gl.BeginEnd`'s, `gl.Rect`'s `glRectf` and the command-list replay's Rect did not, so they drew through an empty stack — 2 frames at delta 181, confined to the minimap in pregame, reproducible across three runs and both scripts. Every emitter materialises now.
  - **Two process traps, both previously documented, both hit again.** `FFMatrixSuppress` was stripped from the write-dir config between runs (the engine drops any key at its default on exit), which made two runs look like suppression working; `run_gate.sh` reports all seven knobs now, and reports a default-on knob as `default-on` rather than `off`, since absence had come to mean the opposite of what the line said.
  - **The 47 → 0 path, for the record.** Immediate mode, client arrays, display lists, the attrib stack, fixed-function lighting, shade model, texenv, materials, point sprites, `gl.UnitShape`'s client arrays, the ground-circle draws, the current colour, the fog family, alpha test, line stipple, the clip planes, and finally the ten matrix functions. Every one of them verified by the whole-frame gate at 0 pixels, and the three that could not be verified that way — where an engine-side change moves all four passes together — by compiling both sources in behind a selector the harness flips per pass.

- 2026-08-01 — **Phase C, two steps of three. With the matrix calls suppressed the meter reads ZERO — and the world does not render, so this is not yet a configuration anyone can use.**
  - **Step 1, done and verified.** Every matrix builtin now compiles with both sources behind the one selector: `gl_ModelViewProjectionMatrix`, `gl_ModelViewMatrix`, `gl_ProjectionMatrix`, `gl_NormalMatrix` and the three inverses. The inverses and the normal matrix are computed where the matrices are, rather than asked of a driver that will not have them. `FFExperiment` 14 over the full set: watertest **674** frames, idletest **427**, 0 px.
  - **Dead PREPROCESSOR code drives the decision the same way commented-out code did.** `cus_gl4.vert.glsl` reads `gl_ModelViewMatrix` exactly once, inside `#if 0`, and that alone made the generated accessor reference a builtin the live shader never touches — 16 BAR vertex shaders failing to compile. `MaskDisabledBlocks` blanks `#if 0` bodies up to their own `#else`/`#elif`. Caught by the gate's compile check, not by reading 0/0 over a broken frame.
  - **Step 2, done.** Every C++ consumer of the fixed-function matrices goes through `GL::ReadFFMatrix`/`ReadFFMatrices` instead of `glGetFloatv`: the immediate backend's texture matrix, LuaOpenGL's matrix bridge, the font renderer, `RenderBuffers`' MVP uniform, the shader feed itself, and `gl.GetMatrixData`. The accessor returns GL while the set-calls still reach it and the mirror once they do not — after suppression that query returns whatever GL was last left holding, an identity, and reads exactly like a working one.
  - **Step 3, implemented and NOT working.** `FFMatrixSuppress` (default off, requires `FFVertexAttribRewrite`) makes the glad wrappers mirror the call and not issue it; `GL::MaterializeFFMatrices` loads the mirrored state back for a draw that fell back to fixed function, behind a re-entrancy guard so the mirror does not drift by its own replay. **The meter with it on reads 0, which is the goal.** But the dumped frame shows UI, minimap, panels and text correctly placed and the ground, sky, water and models absent, so something in the world path still takes its transform from the fixed-function stack.
  - **Ruled out already:** the mirror being tainted by a real display list — under suppression it is used anyway, since nothing else describes the state, and what a real list's own matrix ops did is lost either way because they replay into a stack nothing reads; and the multi/indirect draw entry points being unwrapped, which they no longer are.
  - **Next diagnostic, and it discriminates:** log the MVP actually pushed for a world program under suppression and compare it against what the same draw computed without. A blank world is either a zero/identity matrix reaching the shader or geometry sent off-screen, and those two point at different halves of the feed.
  - **The knob is off and the frame is unchanged with it off** — meter **10**, gate 664 and 665 frames 0 px on consecutive runs. This is a dead-ended step, not a regression.

- 2026-08-01 — **Phase C's appearance objection is gone, both halves of it. The CPU mirror renders pixel-identically to the driver's builtin while differing from it by 7e-4 relative. What is left is plumbing.**
  - **The two halves.** Retiring the matrix family means shaders read an engine-supplied MVP instead of `gl_ModelViewProjectionMatrix`, which needs (a) the *product* to match and (b) the *operands* to match, since deleting the set-calls also deletes the `glGetFloatv` source. `FFExperiment` 13 settled (a). This entry settles (b).
  - **The mirror had to be made complete first, and that is work phase C needed anyway.** `GL::ffMirror` was fed only from `LuaOpenGL` — every matrix call the engine makes for itself was invisible to it, including the 130k `glMultMatrixf` a run from `S3DModelPiece::DrawStaticLegacy`. `GL::InstallFFMatrixTracking` feeds it from the glad entry points, where one pointer per function sees every caller, and `FFMirrorOps()` stands down while it is installed rather than applying each op twice.
  - **Verified EXACTLY, not within tolerance.** The pre-existing shadow-compare uses 1e-4 relative, which is not the bar for replacing a builtin. `GL::VerifyFFMatrixMirror` memcmps the mirrored matrix against `glGetFloatv` after every op and names the op that diverged — which op it is decides where the tracker is wrong, and one of them fires 130k times a run.
  - **It caught a bug in the wrapper before it caught anything about the tracker.** `CMatrix44f` has **no constructor from a float pointer**, and passing one *compiles*: the pointer converts to `float3` and selects the position-only constructor, so a 4×4 load silently became a **translation built from the first three floats**. Whole matrices wrong, reported as `glLoadMatrixf` diverging by 2.0 relative — and without the verifier it would have read as "the mirror cannot track GL" and killed the whole approach. Same conversion trap as `float3`'s array-assignment operator.
  - **The real residue, once that was fixed:** absolute loads (`glLoadMatrixf`) are **exact**; divergence originates at `glRotatef`, whose trigonometry the tracker re-derives in CPU float (4.3e-6 relative), and propagates from there through push/pop/mult (2.1e-5).
  - **`FFExperiment` 14: the MVP uniform composed from the mirror instead of from `glGetFloatv`. watertest 654 frames, idletest 410, control 0 / signal 0, 13 programs, and the mirror was never tainted** — every push used it. Crucially the same run logs how far the mirror's MVP got from the driver's product: **up to 7.05e-4 relative**, three orders of magnitude past a ULP. A 0-pixel result over values that agreed would have proved nothing; over that gap it is the answer.
  - **So the matrix family is no longer an appearance decision.** What remains is plumbing of known shape: rewrite the other matrix builtins (`gl_ModelViewMatrix`, `gl_ProjectionMatrix`, `gl_NormalMatrix`, `gl_TextureMatrix`) the same way `gl_ModelViewProjectionMatrix` now is; move the C++ `glGetFloatv(GL_*_MATRIX)` readers and `gl.GetMatrixData` onto the mirror; then delete the set-calls, all ten together, since a family dies when its last reached site does.
  - **Two things to carry into that work.** `gl.CallList` replays matrix ops inside the driver where glad cannot see them, so the mirror can be **tainted** at a draw — it never happened in these runs, and the feed counts and reports it rather than absorbing it, because post-C there is no `glGetFloatv` to fall back to. And the per-draw feed exists because the matrices move **while a program stays bound** (`PushTransform`, then a transform per piece); a bind-time upload is not enough, which is the upload-discipline hazard the three-phase plan flagged.

- 2026-08-01 — **The composition objection that blocked the matrix family is REFUTED by measurement: a CPU-composed `P*MV` is pixel-identical to the driver's `gl_ModelViewProjectionMatrix`, on the ground shader included. What phase C still needs proving is the *mirror*, not the multiply.**
  - **The claim being tested.** Every engine-side conversion so far reads the builtin rather than a uniform because "no CPU-composed `P*MV` reproduces the driver's own bit-for-bit on dense matrices, and under perspective the divide turns those ULPs into whole shades". That is what made phase C a change to what BAR looks like rather than a plumbing job.
  - **`FFExperiment` 13.** `gl_ModelViewProjectionMatrix` is rewritten to an accessor with both sources compiled in — the shape the fog conversion established — and the candidate pass takes a uniform composed on the CPU. **Both** matrices are read back with `glGetFloatv`, so the only variable is who multiplies them. **watertest 669 frames and idletest 421, control 0 / signal 0, across 13 distinct programs**, with a log line per program so the coverage is a number rather than an assumption.
  - **The coverage is the case the objection is about.** The 13 include `SMFVertProg` — the ground, a dense world modelview under perspective — plus `BumpWaterVS`, `GroundDecalsVertProg`, `Icons3DVS`, `ModernSkyVS`, `CubeMapVS`, the engine's RenderBuffer / font / immediate / `FFStandIn` programs, and BAR's `gui_pip` and attack-AoE shaders.
  - **This does not contradict the un-matchable-composition finding; it is a different comparison.** That one measured a CPU `P*MV` against *true fixed-function transform* — `glBegin` geometry going through `P * (MV * v)` in eye space, a per-vertex two-step. Here both sides are shader draws and only the matrix product differs. The `LuaImmediateBuffer` gates that exist because of the earlier finding are still right for what they gate.
  - **The feed is per DRAW, not per bind**, because the fixed-function matrices move while a program stays bound: `CModelDrawerStateGLSL::Enable` binds, then `PushTransform` and `S3DModelPiece::DrawStaticLegacy` push a transform per object and per piece. That is the upload-discipline hazard the three-phase plan flagged, and the draw-entry-point wrapper is the answer to it. Armed only when this experiment is selected — the per-draw `glGetFloatv` pair is a measurement cost, not something a shipping frame pays.
  - **What is left for phase C, stated precisely.** Deleting the FF matrix calls means the uniform can no longer be sourced from `glGetFloatv`, so the matrices themselves have to come from `GL::ffMirror`. Experiment 13 shows the *product* is free; it says nothing about the *operands*. So the remaining question is whether the CPU mirror reproduces the driver's matrix stack — and it is measurable exactly the same way, by sourcing the same uniform from the mirror instead. Two things stand in the way first: the mirror is fed only from `LuaOpenGL`, so the engine's own matrix calls (`Camera`, `ModelDrawerHelpers`, `DrawStaticLegacy`'s 130k `glMultMatrixf` a run) are invisible to it; and its existing shadow-compare uses a 1e-4 relative tolerance, which is not the bar. Wrapping the ten entry points at glad level fixes both, and is work phase C needs regardless.

- 2026-08-01 — **13 → 10, and the ten are the matrix family. Nothing else in a BAR frame calls a function RenderDoc rejects; the whole remaining board is the one open decision.**
  - **The plan for alpha test, line stipple and the clip planes was to reproduce each operation in the shader** — a `discard` against a fed threshold, `gl_ClipDistance` from fed plane equations, a stipple pattern from a screen-space line parameter — because unlike `gl_Color` and `gl_Fog` these are not values a shader reads but operations the pipeline applies to shader draws. **Measuring first made all three unnecessary.**
  - **`FFExperiment` 10, 11 and 12 hold each capability OFF for the candidate pass.** watertest **644 / 617 / 643** frames and idletest **412 / 409 / 405**, control 0 / signal 0 every time. Every shader that draws while they are on already does its own alpha handling and writes its own `gl_ClipDistance` — BAR's GL4 material shaders declare the output and feed their own plane uniforms. The engine was configuring operations nothing was left to apply.
  - **A capability held off that was never switched on reads exactly like an inert one**, which is this project's most-repeated failure mode, so the enable hook logs each of the three the first time it sees it. All six (alpha test, stipple, `GL_CLIP_PLANE0..3`) appear in both runs, so the zeros are about inertness rather than absence.
  - **Two-stage retirement, and the stages have different warrants.** `GL::ffRaster` first stopped writing any parameter while its operation was DISABLED — `ResetGLState` set an alpha-test function immediately after disabling alpha test 5,464 times a run, the attrib restore wrote parameter and enable together 16,483 times — which is a GL-semantics argument and holds for every game. Then, **under the shader rewrite only**, the parameters are not written at all: that half is a measurement about BAR, so it is gated on the same knob as the rest of the migration and an unenabled game writes everything exactly as before. A draw that falls back to fixed function still gets them, through `GL::MaterializeFFState`.
  - **`glPopAttrib` restores enables without going through `glEnable`**, so it is wrapped too and re-reads them; the engine's own brackets are explicit save/restore and already go through the hook.
  - **Measured.** Meter **10** on watertest *and* idletest. Gates with the rewrite on: watertest **638**, idletest **415**, control 0 / signal 0; with it off, watertest **621**, 0/0. (Both configurations threw a 1-frame signal on one run each and were clean on re-run — the documented async-flake class, which the gate's own note covers.)
  - **What is left.** `glMatrixMode`, `glLoadIdentity`, `glTranslatef`, `glScalef`, `glOrtho`, `glPushMatrix`, `glPopMatrix`, `glLoadMatrixf`, `glRotatef`, `glMultMatrixf` — the matrix family, entire, and the open decision recorded below. Everything that could be retired without changing what BAR looks like has been. The one thing that has changed in the decision's favour is that **phase A's validation vehicle now exists and has been exercised**: the fog conversion compiles both sources in behind a selector the A/B harness flips per pass, which is exactly what the matrix rewrite needs to prove a uniform against the builtin before anything is deleted.

- 2026-08-01 — **16 → 13: the fog family retires. `gl_Fog` becomes a uniform the engine feeds per program bind, and the substitution is proven by compiling both sources in and flipping between them per pass.**
  - **Why fog could go at all.** `glFogf`/`glFogfv`/`glFogi` fill `gl_Fog` for shaders and nothing else — with zero fixed-function draws left (entry below), no rasterizer reads them. `GL::ffFog` holds the parameters; the rewrite substitutes `gl_Fog` with a struct-returning accessor and `GL::PushFFUniforms` fills it. `scale` is derived, matching the builtin's `1/(end - start)`, which is why nothing ever read the mode.
  - **The rewrite reaches FRAGMENT shaders now**, because `gl_Fog` is read in both stages (`SMFFragProg`, `BumpWaterFS`, BAR's `defaultMaterialTemplate`). The vertex-input channels stay vertex-only: in a fragment shader `gl_Color` is the *interpolated varying*, not the attribute, and substituting it there would read an input that does not exist.
  - **A per-bind feed, wrapping `glUseProgram` the way the draw census does.** Locations are cached per program and evicted by `glLinkProgram`/`glDeleteProgram` (ids are recycled), and the upload is skipped while the mirrored state has not moved, so a bound-but-unchanged program costs a map lookup. **Suspended inside a `gl.CreateList` body**: one `glUniform1i` there turns a capture into a real display list and every legacy call in it into a real GL call — that alone put `glBegin`, `glVertex2f`, `glEnd`, `glTexCoord2f` and `glColor4fv` back on the meter, reading **21**, before it was understood.
  - **THE POINT WORTH CARRYING: the substitution is proven, not assumed, and the gate could not have done it.** `run_gate.sh` compares Lua backends within one build, so a wrong uniform feed moves all four passes together and reads a clean 0/0 — the same vacuous-pass class as the dead shader set. So **both sources are compiled in behind a selector** (`recoil_ff_useFogU`) that the A/B harness flips per pass, and `FFExperiment` 9 measures uniform-against-builtin directly: **668 frames, 0 px**, with a one-shot log proving the selector actually reached GL. That is the same validation vehicle the matrix plan's phase A calls for, now built and exercised.
  - **Every consumer that read fog out of GL had to move to the mirror**, and missing one does not fail loudly — it reads a stale value and silently takes a legacy fallback. Found the hard way: the immediate backend's `REASON_FOG_MODE` gate still called `glGetIntegerv(GL_FOG_MODE)`, got the `GL_EXP` default, and pushed a `gl.TexRect` back onto `glBegin`. Converted: the immediate backend's fog gates, `GL::FFStandIn::StateReproducible`, the attrib brackets' `Capture`, and `gl.GetFixedState`.
  - **No `Verify` against GL for this mirror, unlike `GL::ffColor`'s.** GL is not an oracle here: an attrib bracket's `glPopAttrib` restores GL's copy without telling the mirror, and a `glFog*` issued inside a list body is swallowed by the command-list recorder so GL never sees it. Both are correct behaviour and both make the copies differ — 16 reported "bypasses" that were nothing of the kind. A check that cannot be right is worse than no check; the pixel experiment is the proof.
  - **Measured.** Meter **13** on watertest *and* idletest. Gates: watertest **663**, idletest **419**, control 0 / signal 0; with the rewrite off, watertest **662**, 0/0. Experiment 9 as above.
  - **What the remaining 13 are.** Matrix family **10**, unchanged and still the open decision. State **3**: `glAlphaFunc`, `glClipPlane`, `glLineStipple` — and none of them is another `gl_Fog`. They are not values a shader reads; they are per-fragment and per-rasterization *operations* that apply to shader draws too, so retiring them means reproducing the operation in the fragment stage (an alpha-test `discard` against a fed threshold, `gl_ClipDistance` written from fed plane equations, a stipple pattern from a screen-space line parameter) rather than redirecting a read.
  - **`GL::ffRaster` mirrors those three anyway, and retires every write made while the operation is DISABLED — which was most of them.** `ResetGLState` set an alpha-test function immediately *after* disabling alpha test, 5,464 times a run; the attrib-bracket restore wrote parameter and enable together with no idea whether the parameter could be observed, 16,483 times. The mirror writes GL only while the operation is on and puts the value in place from a `glEnable` hook, so it is never observed stale. Skipping a write to state nothing can read cannot change a pixel — GL semantics, not a measurement — and the hook is installed whether or not the rewrite is on, since that argument does not depend on it. `glPopAttrib` restores enables without going through `glEnable`, so it is wrapped too and re-reads them.
  - **What that leaves is exactly the effective set, which is what the conversions need to know.** `glAlphaFunc`: the `glEnable` hook (17,190), `IModelDrawerState::SetupAlphaDrawing` (11,016), `CGuiHandler::Draw` (1,377), `gl.AlphaTest` (1,126). `glLineStipple`: the hook (1,376) and `gl.LineStipple` (207). `glClipPlane`: the hook only (5,512) — every equation write is now made at enable time. Each of the three changes what a shader *computes*, so each needs its own selector experiment in the shape `FFExperiment` 9 established.

- 2026-08-01 — **17 → 16, and the 17 was only true of content that never places a building. Nothing in a BAR frame is drawn by fixed function any more, which is what makes the rest of the board a shader problem rather than a rasterizer one.**
  - **The score was content-specific and the census could not say so.** The offender meter runs `watertest`, which never opens a build placement, so it never calls `gl.DrawGroundCircle` — and the whole client-array family (`glEnableClientState`, `glVertexPointer`, `glColorPointer`, `glDisableClientState`) came back on `idletest`, at **21**. Those four had been recorded as retired one entry above. They were retired *for that run*. This is the coverage hazard the meter already documents, caught this time by a different instrument rather than by luck.
  - **A new instrument, because the question the remaining board turns on is not answerable statically.** Alpha test, fog, clip planes, line stipple and the current colour are read by the **rasterizer** on a draw with no program bound, where no shader-side substitution can reach them — so "is anything still drawn by fixed function?" decides whether any of them can go. `GL::FFDrawCensus` (config `GLFFDrawCensus`) wraps the glad draw entry points and tracks `glUseProgram`, and counts every draw issued at program 0, keyed by backtrace. Tracking the bind rather than querying `GL_CURRENT_PROGRAM` per draw is exact, not sampled: glad has one pointer, so engine and Lua binds both come through it.
  - **Result: with the knobs on, `gl.DrawGroundCircle` was the entire remaining fixed-function draw surface** — 1631 draws on idletest, zero on watertest, one call site. The positive control (knobs off) reports **436k** draws from the legacy model path, so the zero is the instrument working rather than the instrument missing.
  - **`run_ffdraw_census.sh` re-applies the knobs per run and then asserts the engine echoed each one back, and that check earned itself immediately.** The engine **rewrites `springsettings.cfg` on exit and drops every key sitting at its default value**, so setting a knob to its default deletes it, and the next run silently measures a different configuration. The first idletest census read 378k fixed-function draws that were not there for exactly that reason.
  - **The circle conversion.** `glSurfaceCircleLua` / `glBallisticCircleLua` are the Lua variants of circles the engine already draws through a shader; they kept fixed function so that a game shader bound around the call would still receive the vertices. That case is honoured — a bound program is left to draw its own way — but with none bound, fixed function is just the four-line shader the legacy model path already stands in with. `GL::FFStandIn` is that code lifted out of `3DModelVAO`, unchanged except for writing `gl_ClipVertex`: user clip planes reach a fixed-function draw by themselves and a shader-bound one only if the vertex stage says where the vertex is, which the minimap's clipped Lua draws need. **`FFExperiment` 7: 420 frames, 0 px**, stand-in logged ACTIVE; experiment 6 re-run over the now-shared shader is still **0 px on 667 frames**, both variants. idletest **21 → 17**, and the draw census reports zero sites on both scripts.
  - **`glColor4fv` retires: 17 → 16.** 127k calls a run over seven sites, every one of them feeding something that reads the value back — a shader through `gl_Color`, or the engine through `glGetFloatv(GL_CURRENT_COLOR)`. So keep the value and move where it lives. `GL::ffColor` is a CPU mirror that, with the rewrite on, writes a **generic attribute's current value** instead of GL's: that has exactly `gl_Color`'s semantics, per-vertex when an array is enabled for the slot and one global value otherwise.
  - **The colour slot is PINNED (15) where position and texcoord are queried, and that asymmetry is the whole design.** An attribute's current value lives in a context slot rather than in the program, so the one thing a per-program queried location cannot express is "set the colour now, for whichever program is bound later" — which is precisely what `gl.Color` does. Pinning is done with `glBindAttribLocation` at both link sites; binding a name a program does not declare is a no-op, so it is unconditional. 15 is the last slot GL guarantees, the engine binds up to 6 and BAR up to 10.
  - **The rewrite therefore stopped declining a shader wholesale.** Reading `gl_Normal` declines the **stream** channels, which come as a set and would silently zero one of them; the colour channel is complete on its own and converts either way. `recoil_ff_Color()` has no `useAttrs` branch for the same reason — the slot's current value is already right, and a stream draw overrides it per vertex exactly as a colour array overrode `gl_Color`.
  - **All 133 `glColor*` sites in `rts/` route through the mirror, not the seven the meter names.** A writer that bypasses it is invisible until some shader draws the wrong colour. `Verify()` proves there is none: with the rewrite **off** the mirror also writes GL, so GL is the oracle — 665 frames, zero mismatches, with a one-shot ACTIVE line proving the check itself ran.
  - **`writeThrough` is what keeps the gate honest.** The legacy immediate backend transforms and shades through fixed function, so it is the one reader that still needs the colour *in* GL — and it is what the A/B gate renders on three of its four passes. Without it the first gate run failed **406/406 frames at delta 255**: the legacy pass had lost its colours entirely. It is set from `LuaOpenGL::SetModernImmediate`, so the shipped configuration never takes it, and the paths that do are issuing `glBegin` anyway.
  - **Measured.** Meter **16** on watertest *and* idletest. Gates with the rewrite on: watertest **642**, idletest **416**, control 0 / signal 0. With it off: watertest **665**, 0/0, verifier active. (The first watertest run reported 1/655 frames — the known async flake, gone on re-run, which is what the gate's own note about that class is for.)
  - **What the remaining 16 are.** Matrix family **10**, still the open decision below and still not a plumbing job. State **6**: `glAlphaFunc`, `glFogfv`, `glFogi`, `glFogf`, `glClipPlane`, `glLineStipple`. With zero fixed-function draws left, all six are now read only by shaders through compatibility builtins or applied by the rasterizer to shader draws — so `gl_Fog` and `gl_ClipPlane` are the same source-rewrite shape `gl_Color` just took, except that both are read in **fragment** shaders too, which the rewrite does not touch yet. `glAlphaFunc` and `glLineStipple` are per-fragment/per-rasterization state that applies to shader draws as well, so they need the substitution in the fragment stage rather than a channel.
  - **A refutation re-tested, and it expired.** `FFExperiment` 5 concluded "something still rasterizes fog through fixed function" — measured *before* `ModernModelFFShader` existed, when the legacy model path was drawing 436k fixed-function draws a run. Re-armed as **8** and re-run in the shipped configuration: **667 frames, 0 px**. It had found those draws. A candidate refuted against a board that then moves is not settled, and the enumerator now records the measurement rather than the verdict.
  - **`glFogi` still does not retire, and the reason moved from "something reads it" to "something falls back to it".** `GL::FFStandIn::StateReproducible` rejects a non-`GL_LINEAR` fog mode, and a rejected draw falls back to fixed function — which would then fog *exponentially* where it used to fog linearly. So the mode goes with the rest of the fog family after all, for a reason about the fallback rather than about any reader. Trying it anyway cost a meter reading of **26**: dropping the `GL_LINEAR` write turned every textured stand-in rejection back into a client-array draw, which is the same coupling seen from the other side.
  - **Two process traps, both already documented here, both walked into anyway.** The re-run's first attempt *skipped* the `glFogi` write on the candidate pass instead of **holding `GL_EXP`** — `GL_FOG_MODE` is sticky and written once a run, so the skip inherited the `GL_LINEAR` an earlier pass had set and measured nothing; that is the exact hazard the experiment-5 entry below records. And `FFVertexAttribRewrite` vanished from the write-dir config between runs, because setting it to its default value makes the engine drop the key on exit and a substituting `sed` then has nothing to substitute. `run_gate.sh` now prints the state of all six migration knobs as the engine echoed them, which is what turned the 26 from a mystery into two lines of log.

- 2026-08-01 — **21 → 17: the immediate-mode family retires. `glBegin`, `glEnd`, `glVertex2f` and `glTexCoord2f` are gone, by rewriting the fixed-function vertex builtins out of the GLSL the engine compiles.**
  - **The blocker was never the call sites, it was the contract.** Every surviving immediate-mode call existed because a game-supplied shader was bound and expected `gl_Vertex` / `gl_Color` / `gl_MultiTexCoord0` to arrive through fixed function — which leaves `glBegin`/`glVertex` as the only way to deliver them. `GL::RewriteFFVertexBuiltins` (`Rendering/GL/FFShaderRewrite.cpp`) substitutes those identifiers in the source at the two compile funnels (`Shader.cpp:CompileShaderObject`, `LuaShaders.cpp:CompileObject`) and prepends the matching declarations, so a game's **own unmodified shaders** get the modern feed with no content change and no `gl.*` API change. `#define` of a `gl_` name is illegal in GLSL, hence textual identifier substitution; `ftransform()` becomes `gl_ModelViewProjectionMatrix * recoil_ff_Vertex()`.
  - **A uniform branch, not a hard substitution, and that is what satisfies constraint 1.** The generated accessors read `recoil_ff_useAttrs ? recoil_ff_aVertex : gl_Vertex`, and the uniform defaults to false. Every draw path that exists today therefore keeps reading the builtins, byte for byte; only `GL::DrawFFAttribStream` raises the flag, for the length of one `glDrawArrays`, and lowers it again — because the next draw through the same program may well be one fixed function still feeds. Attribute locations are **queried**, never pinned, so an injected attribute cannot collide with one the shader declares.
  - **Five consumers, because a function dies only when its last reached site does.** `LuaOpenGL::BeginEnd` and `TexRect` lose the `NoShaderBound()` gate in favour of `ModernFeedable()`; `gl.Shape` gets a modern path it never had (it now parses the whole table first, since a per-vertex normal has no channel in the immediate buffer and that is only knowable after reading them); `CInfoTextureCombiner::Update`'s "old nonsence" quad draws from a VBO; and the modern flushes stop **priming** the fixed-function current texcoord, which was 54k `glTexCoord2f` a run issued purely on behalf of a path they had just replaced.
  - **Two content fixes, both the same shape, and both the sort constraint (b) exists for.** `gfx_guishader.lua` and `luaintro/Addons/main.lua` create **fragment-only** programs and read `gl_TexCoord[0]` — supplied by fixed-function *vertex* processing, so there is no vertex shader to rewrite and the engine cannot invent one (attaching a generated vertex stage would delete FF vertex processing for every other draw through that program, i.e. exactly the general FF emulator this project has refused to build). Each gets a four-line vertex shader stating what fixed function was doing implicitly, `gl_TextureMatrix[0] * gl_MultiTexCoord0` included — fixed function applies the texture matrix, and both fragment shaders then apply it again, so reproducing FF exactly preserves that (harmless, identity-matrix) double-application rather than silently changing it.
  - **THE TRAP, and it is the worst one this project has hit: a shader that fails to compile draws nothing in every pass alike, so the pixel gate reports a clean run over a broken frame.** The first run of this change gated **646 frames, control 0 / signal 0** — with BAR's *entire* CUS unit-material set failing to compile. That reads exactly like a pass. Two fixes, both permanent: `run_gate.sh` now greps the infolog for compile failures and dies, and **both compile funnels retry with the original source if the rewritten one fails**, which makes "a game nobody updated keeps working" structural rather than argued.
  - **What broke those shaders is worth carrying separately.** (1) **Commented-out code drove the decision**: `cus_gl4.vert.glsl` mentions `gl_MultiTexCoord0` exactly once, on line 616, commented out — and that alone injected a declaration into a shader that reads no builtin at all. Detection and substitution now run against a comment-masked copy. (2) **The `#version` was not in the source**: BAR's CUS shaders carry theirs in the `defines` block ("shader version is added via widget"), so probing only the source returned 0, and guessing GLSL 110 emitted `attribute` — a *reserved word* at the version it actually compiles at. (3) BAR indents `#version` inside a Lua long string, and treating that as "not a directive" put the declarations above it, the one place GLSL forbids anything.
  - **Finding the last two sites needed a refusal log, not the census.** After the engine work the meter still read 21, and the census could only say *which* function still fired and from which C++ line — never which widget produced it or which gate turned it away. `ReportImmLegacy` prints one line per distinct (draw, reason, Lua call site) under `LuaImmediateFallbackStats`, and it named `gfx_guishader.lua:419` (1499 calls) and then the loading screen (264 calls) directly. Refusals are otherwise indistinguishable from conversions at the call site, which is how a run can look finished and not be.
  - **Display-list-compile refusals are not capture blockers.** The log shows ~40 of them, all at load time. Inside a `gl.CreateList` body under `LuaCommandLists` the recordable entry points are pointer-swapped recorders, so the `glBegin` those draws "emit" never reaches GL — confirmed by the census, which attributes zero calls to `LuaOpenGL::BeginEnd`.
  - **Measured.** Meter **17**, with no immediate-family site left in the census at all. Whole-frame gate with `FFVertexAttribRewrite=1`: watertest **644 frames** and idletest **416 frames**, control 0 / signal 0 on both, each with the one-shot `[FFAttribFeed] ACTIVE` line proving the converted path ran and zero shader-compile failures proving the frame was the frame. With the knob **off**, watertest **634 frames** 0/0 as well — which covers `gl.Shape`, the one piece that converts regardless of it (its modern path is new; there was none before, so a no-shader Shape draw now goes through the immediate buffer either way) and the two BAR vertex shaders, which are pure content and knob-independent.
  - **What the remaining 17 are.** Matrix family **10** (`glMatrixMode`, `glLoadIdentity`, `glTranslatef`, `glScalef`, `glOrtho`, `glPushMatrix`, `glPopMatrix`, `glLoadMatrixf`, `glRotatef`, `glMultMatrixf`) — the open decision recorded below, not a plumbing job. State **7** (`glAlphaFunc`, `glColor4fv`, `glFogfv`, `glFogi`, `glFogf`, `glClipPlane`, `glLineStipple`). `glColor4fv` is now the interesting one: the rewrite gives `gl_Color` an attribute channel, so the same treatment could reach `gl.Color`'s 61k calls — but that needs a location pinned across *all* programs, since the fixed-function current colour is global state and a queried per-program location cannot model it.

- 2026-08-01 (later still) — **25 → 21: the client-array family retires, by standing in for fixed function with a shader instead of emulating it.**
  - **What it is.** `glEnableClientState`, `glVertexPointer`, `glNormalPointer`, `glTexCoordPointer` existed for exactly one thing: model draws where the caller bound no program of its own, so fixed function drew them. `gl.UnitShape`'s `rawState` defaults to true and BAR's one reachable site is `unit_icongenerator`'s offscreen atlas. Fixed function with lighting off is `texture * current colour` — a four-line shader — so the draw moves to the modern VAO (attributes 0 and 4) and the client arrays go with it. Behind `ModernModelFFShader`, default off, requires `ModernModelAttribs`.
  - **The design point that makes it safe for an unupdated game.** It is not a general fixed-function emulator, and does not try to be. It reproduces one state; `FFStateReproducible` checks the *live* state against that (lighting, fog mode and coordinate source, active unit, competing targets, texenv, second units) using only `glGet*`/`glIsEnabled`, which RenderDoc supports; anything else keeps the old path. The fallback is per **draw**, not per run, so the worst case for a game nobody has touched is exactly today's behaviour. Fog is *reproduced* rather than refused — the linear recipe the modern Lua backend is already byte-parity-proven on, `|eye z|` via `gl_ModelViewMatrix`, state from `gl_Fog`.
  - **Proven as `FFExperiment` 6**, `[L, L, L, L+substitute]`: **665 frames, 0 px**, with the log confirming both untextured variants (fogged and unfogged) actually ran and nothing was rejected. Whole-frame gate with the knob on: watertest **674**, idletest **428**, control 0 / signal 0.
  - **The textured case is refused, and that is the interesting half.** The first run of experiment 6 reported a clean **646 frames 0/0** — and it was worthless, because the log said `textured=0`: `gl.UnitShapeTextures` *binds* without enabling `GL_TEXTURE_2D`, so the textured branch never executed. Teaching the driver to texture half its shapes turned that same experiment into **15 px a frame at delta 3, on every frame**. Same texture, same `GL_MODULATE`, same current colour, and still not zero — small and consistent enough to suggest a per-edge or per-texel effect rather than a wrong transform, but the bar is zero, so the substitute now returns a reject reason for it. The family retires regardless: `LuaOpenGL::ResetGLState` disables `GL_TEXTURE_2D` and BAR's icon generator never enables it, so BAR's own draw is the untextured case.
  - **Two pieces of tooling this needed, both instances of the same recurring hazard.** The path is a load-time one-shot that no pixel gate can see, so `ab_unitshape_driver.lua` grew a `rawState` row that draws it every frame — the same fix experiment 3 needed on this same function. And because that row is an *amplifier*, it would have kept the client arrays alive in the offender meter and hidden the retirement, so it is switchable with `ABUnitShapeTextured=0`: the gate wants maximum coverage, the meter wants what BAR actually reaches, and those are different questions. Verified both ways — meter reports 21 with no client-array sites in the census at all.
  - **Also added: a one-shot ACTIVE/REJECTED report per outcome.** A rejected draw falls back silently and reads exactly like a converted one. Both of the findings above came from that line rather than from the pixel counts.
  - **CORRECTION to the three-phase matrix plan in the entry above — phase C has a hole, and this project already measured it.** The plan said: rewrite `gl_ModelViewProjectionMatrix` to an engine-supplied uniform, then stop issuing the FF matrix calls. The second half is not reachable at zero pixels, because **no CPU-composed `P*MV` reproduces the driver's own composition bit-for-bit on dense matrices** — float-composed, double-composed and two-step `P*(MV*v)` variants each flip *different* thin-primitive edge pixels (see the un-matchable-composition finding and `LuaImmediateBuffer`'s `ScreenAlignedMV`/`OrthoProjection` gates, which exist for exactly this). Screen-aligned MV under an ortho projection is measured exact; dense world MV under perspective is not, and under perspective the divide amplifies composition ULPs into whole shades on linear-sampled textures.
    - This is *why* the engine-side conversions done so far all read the **builtin** rather than a uniform: reading it costs nothing at the capture gate and gets the driver's exact composition. That route works precisely as long as the FF matrix set-calls survive to feed it — which is the thing phase C removes. The two cannot both be true.
    - So the matrix family is not a plumbing problem, it is a **decision**: retiring it means world-space geometry is transformed by a composition that differs from today's by ULPs, visible as edge pixels on thin primitives and as shade differences on perspective-sampled text/icons. That is a change to what BAR looks like, small but real, and it is the user's call under constraint 3 — not something to slip in behind a knob. It also cannot be proven either way by the in-frame harness, which has no reference once the builtin is gone; it needs the cross-build hash.
    - Phase A (rewrite + uniform feed, builtin still available behind a runtime branch) remains sound and is still the right first step. What must not be assumed is that phase C follows from it for free.

- 2026-08-01 (later) — **25 remain, and every one of them is the same problem wearing four hats: shaders read fixed-function state through compatibility builtins, so the FF *setters* cannot go. The unlock is to rewrite the builtins out of the source the engine compiles.**
  - **Current census, driven and reproducible** (meter with both command-list knobs and `ModernModelAttribs` on; `RDOC_SITE_CENSUS=1`): **25 distinct functions**. Matrix family **10** (`glMatrixMode`, `glPushMatrix`, `glPopMatrix`, `glLoadIdentity`, `glLoadMatrixf`, `glMultMatrixf`, `glTranslatef`, `glScalef`, `glRotatef`, `glOrtho`); Lua/engine immediate **4** (`glBegin`, `glEnd`, `glVertex2f`, `glTexCoord2f`); client arrays **4** (`glEnableClientState`, `glVertexPointer`, `glNormalPointer`, `glTexCoordPointer`); state **6** (`glAlphaFunc`, `glColor4fv`, `glFogf`, `glFogfv`, `glFogi`, `glClipPlane`, `glLineStipple` — 7 by count, `glColor4fv` doubles as immediate-mode state). Pixel gate re-verified the same day at four configurations: shipped-default watertest 747/0/0, shipped-default idletest 473/0/0, all-knobs watertest 741/0/0 (after one 1-px flake that did not repeat), all-knobs idletest 481/0/0, unit-shape driver active in each.
  - **Why the remaining board does not decompose into independent wins.** Every family bottoms out in the same dependency. `glMultMatrixf` survives because `ModelVertProg.glsl` and BAR's shaders read `gl_ModelViewProjectionMatrix`. `glFogi` survives at **one call a run** — the initial transition off the GL default — because `SMFFragProg`, `ModelFragProg`, `BumpWaterFS`, `GrassFragProg` and BAR's `defaultMaterialTemplate` read `gl_Fog.color`/`.end`/`.scale`. `glVertexPointer` survives at **40 calls a run** because `gl.UnitShape(rawState=true)` with no program bound has nothing but `gl_Vertex` to feed. `glColor4fv` survives partly because the *modern* Lua flush deliberately re-issues it to leave the FF current colour where a legacy `glEnd` would have (`FlushModern`, `FlushTexRectModern`) — later shaders read that as `gl_Color`. In each case the setter is alive to serve a compatibility **builtin**, not to draw anything.
  - **So the enabling change is a source rewrite, not a per-call-site migration.** The engine compiles GLSL in exactly **two** places — `GLSLShaderObject::CompileShaderObject` (`Shader.cpp:202`) and `LuaShaders`' `CompileObject` (`LuaShaders.cpp:435`) — and both already prepend a generated defines block. Substituting `gl_ModelViewProjectionMatrix` → `recoil_ff_mvp` (etc.) in the source and prepending the matching `uniform` declaration makes every shader in the process, **including a game's own unmodified shaders**, read engine-supplied values instead of FF state. That is what satisfies all three constraints at once: unchanged games keep rendering (their source is rewritten for them), no `gl.*` API changes, and the values are bit-identical because they are the same matrices. `#define gl_… ` is *not* an option — GLSL forbids macro names beginning with `gl_` — so it has to be textual substitution of the identifier plus a declaration, which is why the two-funnel property matters.
  - **Three phases, each independently verifiable, in this order.** **A** — rewrite the builtins to uniforms and feed them from `glGetFloatv(GL_MODELVIEW_MATRIX)` etc. at upload time. Retires nothing; proves the plumbing, because the uniform is sourced from the very state it replaces, so any divergence is a *when-do-we-upload* bug and shows up as gross geometry. **B** — switch the source from `glGetFloatv` to the existing CPU mirror (`GL::ffMirror`, whose value stacks already exist and whose `shadowCompare` flag already exists to assert mirror-vs-`glGetFloatv` equality on every read). **C** — stop issuing the FF calls. Only C retires functions, and it retires all ten at once, because a family only dies when its *last* reached site does.
  - **Validating A needs a per-pass switch, and there is a cheap one.** `run_gate.sh` compares Lua backends within one frame; it cannot compare two builds, and `--ff-experiment` toggles engine state per pass but cannot toggle how a shader was *compiled*. Dual-compiling every program to get a per-pass variant would be heavy. Instead compile **one** program that carries both: `mvp = recoil_ff_use ? recoil_ff_mvp : gl_ModelViewProjectionMatrix`, with `recoil_ff_use` set per pass by the harness. A runtime branch is the validation vehicle; phase C replaces it with a compile-time one. This costs one bool uniform per program during A and nothing afterwards.
  - **The upload discipline is the real design risk, and it is not solved by uploading at program-bind time.** Matrices change *while a program stays bound* — `CModelDrawerStateGLSL::Enable` binds, then `CModelDrawerHelper::PushTransform` and `S3DModelPiece::DrawStaticLegacy` push per-object and per-piece transforms (76,172 `glMultMatrixf` a run from that one function, the largest single site on the board). So the uniform has to be re-uploaded on matrix mutation, not just on bind — which means the FF matrix calls need wrappers, which is work phase C needs anyway. Order the work so A's wrappers *are* C's wrappers.
  - **Reached matrix sites, for scoping C:** 14 files — `Lua/LuaOpenGL.cpp` (the per-callin scaffolding plus the `gl.*` matrix callouts, by far the most), `Game/Camera.cpp`, `Game/CameraHandler.cpp`, `Game/Game.cpp`, `Game/UI/MiniMap.cpp`, `Rendering/WorldDrawer.cpp`, `Rendering/GL/myGL.cpp` (`ClearScreen`), `Rendering/GL/myGL.h` (`__spring_glOrtho`/`gluOrtho2D`/`glFrustum`), `Rendering/Common/ModelDrawerHelpers.cpp`, `Rendering/Common/ModelDrawerState.cpp`, `Rendering/Units/UnitDrawer.cpp`, `Rendering/Env/SkyBox.cpp`, `Map/SMF/SMFRenderState.cpp`, `Rendering/Models/3DModelPiece.cpp`. Consumers to redirect alongside: 20 shader files in `cont/base/springcontent/shaders/GLSL/` (61 builtin references total), the `glGetFloatv(GL_*_MATRIX)` readers in C++, and `gl.GetMatrixData`.
  - **The client-array family is the one exception, and it is smaller than it looks.** Its entire reachable surface is `unit_icongenerator.lua:703` — `gl.UnitShape(udid, teamID, true)` rendering the offscreen icon atlas with no program bound. (`gui_commands_fx.lua`'s legacy pass did not fire in this run; everything else BAR calls goes through its own `WG.DrawUnitShapeGL4`.) With no shader and no FF lighting the fixed-function result is just `texture * current colour`, which is exactly what `LuaImmediateBuffer`'s modern textured program already computes, so the engine could bind a small FF-equivalent program instead of falling back to client arrays — gated on `glIsEnabled(GL_LIGHTING)` being false so a game that *does* light that path keeps the old behaviour. Four functions for one program. The catch is verification: icon generation is a **load-time one-shot**, so neither gate can see it (same class as list capture), and it needs the cross-build band hash.
  - **One candidate looked cheap enough to just test, and it was: `FFExperiment` 5 is REFUTED, and `glFogi` stays.** The reasoning for it was good — one reachable site, fires exactly **once a run** (the transition off the `GL_EXP` default in `ISky::SetupFog`), and nothing reads `gl_Fog.mode`, since shaders take `.end`/`.scale` and those derive from `GL_FOG_START`/`END` under either mode. Measured instead of argued: holding `GL_EXP` on the candidate pass diverges **2/747 frames at up to 46 levels**, reproducibly (identical frames and pixel counts across re-runs, two orders of magnitude above the 1-px flake class). Something in a BAR frame still rasterizes fog through fixed function. `glFogi` can only go when the whole fog family does.
    - Two things about *how* that was measured are worth keeping. The candidate pass has to **hold `GL_EXP`**, not skip the write: `GL_FOG_MODE` is sticky and written once, so a skip inherits the `GL_LINEAR` an earlier pass set and reports a vacuous zero — the same coverage hazard the meter documents, in a new place. And the first run used `--force-legacy`, which maximises fixed-function draws and therefore could not distinguish "FF fog is live in BAR" from "FF fog is live only on the forced-legacy Lua path the shipped config never takes". `run_gate.sh` grew `--mixed-ok` for exactly that second question: it permits `--ff-experiment` *without* `--force-legacy`, which conflates the backend with the removal and is sound only because the backend's own signal had been measured at 0 on this same content minutes earlier. It diverged the same way, so the answer is the shipped configuration, not the harness.
  - **Method note, since this cuts against how the earlier entries read.** Three of the four retired experiments were confirmations, which makes the mechanism look like a formality. It is not: the argument for this one was at least as strong as for those, and it was wrong. Candidates get run.
  - **A latent bug found while reading that path, worth fixing regardless of the above.** `S3DModelVAO::UnbindLegacyVertexAttribsAndVBOs` returns after `vao.Unbind()` whenever `ModernModelAttribs` is on — but the *bind* side falls back to client arrays per draw when no program is bound, so on exactly those draws the unbind skips `glDisableClientState`/`indxVBO.Unbind()`/`vertVBO.Unbind()` and leaves the arrays enabled and the buffers bound. The two sides must agree per draw, not per config.

- 2026-08-01 — **The meter was silently under-reporting by five; group C's content blocker is exactly two call sites; group D is not the clean win it looked like.**
  - **The score moved 35 → 30 with no code change, because it counts only what the run happened to reach.** The client-array family (`glVertexPointer`, `glNormalPointer`, `glTexCoordPointer`, `glEnableClientState`, `glDisableClientState`) is reachable *only* through `gl.UnitShape`, and BAR's two call sites are both demand-driven: an on-screen build menu, and `unit_icongenerator` sitting behind `gui_cache_icons`' `nonStartUnitCacheDelay = 6` **`os.clock()` CPU-time** gate. A run slow enough to burn six CPU-seconds before the script's `quitforce` trips the delay and generates icons; a fast one does not. The 35 came from the run with `RDOC_SITE_CENSUS=1` (a backtrace per call site makes everything slower); every run without it read 30. Same binary, same content, same config.
  - **The fix is to drive the path, not to wait for it.** `run_rdoc_offenders.sh` now installs and enables `ab_unitshape_driver.lua` itself. That widget already existed for the pixel gate *for this same reason*, but it was installed by hand — and a hand step that stops happening is invisible. Enabling it needs **two** settings, not one: the widget calls `widgetHandler:RemoveWidget()` when its knob is off, and BAR persists that as `order = 0` in `LuaUI/Config/BYAR.lua`, so one knob-off run disables it for every run afterwards *regardless of the knob*. The meter repairs the saved order too, then **requires** the widget's activation line in the log — without that check the failure mode is a smaller number rather than an error, which is the way round that gets believed. Baseline with the path driven: **35**, stable across runs.
  - **Group C's content blocker is two `gl.UnitShape` calls, both `rawState=true`.** BAR has already migrated the bulk of unit-shape drawing to its own `WG.DrawUnitShapeGL4` (`gfx_DrawUnitShape_GL4.lua`); `gui_pregameui`, `unit_ghostradar_gl4`, `gfx_showbuilderqueue` and `gui_pregame_build` only *look* like call sites because each defines a local `addUnitShape`/`removeUnitShape` wrapper around it. The real engine-API draws are `unit_icongenerator.lua:703` (load-time offscreen icon atlas) and `gui_commands_fx.lua:1040` (build-queue ghosts, self-described as the "legacy pass"). `gl.UnitShapeTextures` has three more users, but it binds textures rather than model geometry and never reaches the legacy VAO bind, so it is not a group-C blocker.
  - **Group D is entangled with a deliberate performance cap, so it is not 6 free functions.** Real display lists in a BAR run have four sources, not one: an unrecordable opcode mid-body (`glTexParameteri`, `glNormal3f`), the **`CMDLIST_MAX_CAPTURED_VERTS = 4096`** size cap at `gl.CreateList` time, the font renderer's own `ffTextureSpaceMatrix`, and `GrassDrawer`. The size cap exists because replaying a large captured list burns one CPU `glVertex`/`glColor` per vertex per frame, and it bites immediately: `gfx_unit_shield_effects.lua` builds two icosahedra, `gl.CreateList(DrawIcosahedron, 5, …)` at **15,360 verts** (over the cap) and subd-4 at 3,840 (under it). So the `large` shield list would keep materialising on **size** even if `glNormal3f` became recordable. Lifting the cap is a frame-time-for-capturability trade that needs measurement, not a bug to fix.
  - **`glNormal3f` cannot be made recordable "for free", and the reason is informative.** `ImmStreamData` carries pos/uv/colour and no normals, and neither backend replays them — so a *captured* stream already loses normals today, and only materialisation preserves them. The live modern path loses them too: `LuaOpenGL::Normal` calls `glNormal3f` straight through to FF state that the modern flush never consumes. Swallowing in-`Begin` normals is therefore exactly what every non-materialised path already does; the only behaviour that changes is for the two shield lists. That is a real pixel question, and it is **not** answerable by either existing gate — `run_gate.sh` moves both passes together, and `--ff-experiment` toggles per *pass* within a frame while list capture is a one-shot load-time event. It needs the cross-build band hash.
  - **`CmdListMaterialize`'s report never named a site, for a reason unrelated to its config.** `createSite` was assigned at *end* of capture, but materialisation happens *during* the body, so the field was always still empty and every report read `<unknown: LuaImmediateFallbackStats off>` **with the setting on**. Now captured before the body runs — and with it working, the 14 real display lists in a BAR run resolve to **12 `glTexParameteri` + 2 `glNormal3f`**, not the 2 the deduplicated report had been implying.
  - **Those 12 are not Lua recording a texture upload — they are engine object construction leaking into the capture.** BAR has **zero** `gl.TexParameter` call sites. Every one of the 12 is a first `gl.Texture("…")` of an unloaded name inside a list body, reaching `CNamedTextures::Load`, which builds a texture *object*: `glTexImage2D`, then `glTexParameteri` for the clamp qualifiers. Under a real display list that is actively broken — both are compiled rather than executed, so the texture is undefined until the first `gl.CallList` and its parameters land on whatever is bound then. The capture inherited the problem and materialised instead. Fixed with a `LuaCmdListCapture::SuspendScope` around `Load`, the same treatment `glGen*`/`glBindBuffer` already get by being left unwrapped.
  - **…but it is not shippable yet, and the reason is the useful part.** Behind `LuaCmdListSuspendOnObjectCreate`, **default off**: converting those 12 from display lists to the command-list replay takes the whole-frame gate from **1/744** frames diverging to **110/715**. Their materialising had been *masking* a fidelity gap in the replay for that content. Fixing that gap is now the next concrete piece of group D, and the knob is the lever for it — list capture is a load-time one-shot, so neither `run_gate.sh` (moves both passes together) nor `--ff-experiment` (toggles per pass *within* a frame) can vary it, while two runs of one build can.
  - **35 → 34: `glColor4f` retired, and the font renderer stops creating a display list it usually never uses.** `glColor4f` had exactly two reachable sites — `LuaOpenGL::ResetGLState` and `CShadowHandler::CreateShadows` — both setting opaque white, and `glColor4fv` is already unavoidable elsewhere (`gl.Color`, 84k calls a run). Spelling both the same way removes an entry for no behaviour change; the target is zero *distinct* functions, so a redundant spelling is one fewer thing to retire separately. Separately, `ffTextureSpaceMatrix` was built in the font renderer's constructor, spending `glGenLists`/`glNewList`/`glEndList`/`glDeleteLists` in *every* run even though it is reachable only from `DrawTraingleElementsRecordable` — which runs only inside an open compile, i.e. exactly the state the whole effort is trying to reach zero of. Now created lazily from `HandleTextureUpdate`, the only place that can (both calls are illegal inside the compile the flush runs in). `GrassDrawer` also deleted `grassDL` unconditionally, and it is 0 on a map with no grass. Whole-frame gate **PASS, 729 frames, control 0 / signal 0**.
  - **Read-back-then-write: ~35k unsupported calls a run removed, by the measure that actually counts.** Following the caveat below, the useful lever is call *sites*, and two of the biggest producers were writing state back unconditionally. `GL::AttribSnapshot::Restore` is the `glPopAttrib` replacement and runs on every draw callin, and `LuaOpenGL::ResetGLState` runs just before it; both now read the current value and skip the write when it already matches, which cannot change rendering. The queries (`glGet*`, `glIsEnabled`) are supported functions and the setters are not, which is what makes the trade worth making. Measured: `glLineStipple` 8,465 → **60**, `glFogfv` 2,664 → **0**, `glFogi` 2,664 → **0**, `glColor4fv` 1,175 → **0** from `Restore`, and `glAlphaFunc` in `ResetGLState` 19,108 → 5,930. `glAlphaFunc` in `Restore` barely moves (17,999) — honest, and worth keeping visible: content really does leave alpha state dirty most callins. No function retires from this, because each still has a site elsewhere; the meter stays at 33 while roughly a third of the run's remaining unsupported traffic is gone. Gate PASS on both changes.
  - **32 → 26: the entire display-list family retires, via a static-VBO bake.** `gl.CreateList` compiled any capture over `CMDLIST_MAX_CAPTURED_VERTS` into a real GL display list because replaying a large one re-interleaved and re-uploaded its geometry every frame. But a captured stream is **immutable**, so that interleave is identical on every replay: build and upload it once into a `GL_STATIC_DRAW` buffer (`LuaCommandList::StreamBake`, the persistent twin of `ImmFloatStream` with the same attribute layout) and replay becomes bind + `glDrawArrays`. Every gate in `FlushModern` still runs per replay — they read live GL state — so this changes where the geometry comes from, never which program draws it. Streams with seed-class vertices are excluded: those take the REPLAY-time current colour, so their content is not fixed at capture. One more site fell out of it: `CLuaDisplayLists`' destructor called `glDeleteLists` on **every** active entry including id 0, and under `LuaCommandLists` every captured list has id 0 — so it made that call once per list while creating none. Its own comment observed that deleting id 0 is not a GL error, which is true and irrelevant to the capture gate. Retired together: **`glGenLists`, `glNewList`, `glEndList`, `glCallList`, `glDeleteLists`, `glColor4ub`**.
  - **Isolating that result, because the combined run looks like a regression and is not.** With both knobs on the gate reports 102/671 frames diverging, which reads as the bake's fault. It is not: **baked streams alone gate PASS at 0/722**, and the divergence is entirely `LuaCmdListSuspendOnObjectCreate` (independently measured at 99/702 before the bake existed). So the two knobs are `LuaCmdListBakedStreams` — pixel-clean, ready for default-on review on its own merits — and `LuaCmdListSuspendOnObjectCreate`, still blocked. **26 is reachable the moment that one replay divergence is fixed**, and it is now the single thing standing between the current 33 and it. Worth noting what it is: the 12 lists it converts are textured UI content, so the suspect is the modern MODULATE path versus FF texturing on those specific streams, not the conversion itself.
  - **Hunting the ~100/700 divergence: one real bug found and fixed, and the search narrowed sharply.** `CNamedTextures::Bind` has two paths, and only the display-list one binds: inside a compile it calls `GenInsertTex(.., bindTex=true, ..)`, otherwise `GenLoadTex` → `Load`, whose last act on the uncompressed branch is `glBindTexture(GL_TEXTURE_2D, 0)`. So a **first** `gl.Texture` of a name left nothing bound while every later one bound it from the cache — a latent bug on its own, and specifically hostile to capture: a command-list capture has `GL_LIST_INDEX == 0`, so it takes the load path, where `SuspendScope` would swallow a bind issued inside `Load`. Fixed by binding in `Bind` after the load, outside the construction scope, so a capture records it. **It is not the cause** though: 86/648 after versus ~100/700 before, the same rate.
    - What the run *did* settle is where not to look. With the census on, modern→legacy fallbacks total **0 / 0** — no stream is being rejected by any gate. So this is not a missing gate letting unmatchable content through; it is content the modern path already claims to handle exactly and does not. Both passes replay identical captured commands and the control channel is 0, so the content is deterministic and only the backend differs. Next suspect worth measuring is colour precision: `LoadCaptured` stores 8-bit `ClampedColor` in `verts` and exact floats in `vertColorsF`, and the two flushes do not read the same one — though a 1/255 difference does not obviously explain a max delta of 25, so check that assumption before building on it.
  - **The divergence is not caused by the conversion — it is EXPOSED by it, and that changes who owns it.** With `LuaCmdListBakedStreams` alone the gate is 0/722, and every list that already took the command-list path is clean. The 12 that `LuaCmdListSuspendOnObjectCreate` converts are clean too *until* they convert. The reason is structural: as real display lists their content rendered from the compiled list in **both** passes, so legacy-vs-modern could not differ — the display list was hiding the comparison, not passing it. Once they become command lists, that content flows through the Lua immediate backend for the first time and the difference appears. So this is **pre-existing modern-backend infidelity on `gui_top_bar` / `gui_pip` / `gui_options` content**, not a bug in the conversion, and the same pixels would diverge today if a widget drew that content without wrapping it in a list. It belongs to the divergence-hunting work below rather than to the display-list burn-down, and fixing it is what unlocks 26.
  - **`glClipPlane` checked as a candidate and ruled out — it needs content coordination, confirming the group-E assessment.** Six sites, all engine, no Lua API, which made it look like a clean self-contained retirement. Four are `CMiniMap::SetClipPlanes`, axis-aligned planes bounding the minimap rectangle in a space that maps linearly to a screen rect — `glScissor` replaces those exactly, and both scissor calls are supported. The other two are `CWorldDrawer::DrawAlphaObjects` clipping alpha models and projectiles to below/above the water plane, and those cannot follow: replacing them means every shader in those passes writes `gl_ClipDistance`, which includes BAR's custom unit shaders, not just the engine's. (Worth noting the current behaviour is already driver-dependent — a compat-profile shader that writes neither `gl_ClipVertex` nor `gl_ClipDistance` leaves `GL_CLIP_PLANE` undefined per spec.) Four of six retires nothing, so this stays parked with the rest of group E.
  - **The last two display lists in a BAR run, named.** The size-cap conversion was *silent* — unlike `CmdListMaterialize`, it logged nothing — so it now reports itself, and with `LuaCmdListSuspendOnObjectCreate=1` the entire remaining display-list surface is two bodies: **`gfx_unit_shield_effects.lua:740` at 15,360 verts** (the subd-5 icosahedron; subd-4 at 3,840 now passes cleanly since the dead normals went) and **`gui_options.lua:7176` at 12,712 verts**. The second is what keeps `glColor4ub` alive: it carries text, and `CmdListEmitIntoCompile` replays captured `Op::Font` commands into the open compile. Clear these two and `glGenLists`, `glNewList`, `glEndList`, `glDeleteLists`, `glCallList` and `glColor4ub` retire together, **32 → 26**. Both are BAR content, so constraint (b) allows a content fix — but the honest options there are reducing shield subdivision (a visual-quality call, not one to make silently) or splitting the options panel into sub-cap lists (mechanical but touches UI that is hard to eyeball from a headless gate). The engine-side static-VBO bake handles both without touching content, and remains the chosen route.
  - **CAVEAT on the score, and a correction to the entry below it.** The capture gate is **any call to any unsupported function**, not the number of *distinct* ones — the distinct count is a work-tracking proxy, and the two come apart. Retiring `glColor4f` by respelling its two sites as `glColor4fv` moved the meter 35 → 34 and brought capturability **no closer at all**: the same calls still execute under a different name. The same trick is available for at least `glColor4ub` → `glColor4fv`, `glVertex2f` → `glVertex3f` (z = 0) and `glFogi` → `glFogf` (`GL_FOG_MODE` accepts a float), and would read as 33 → 30 while changing nothing. **Do not do those.** Only eliminating call *sites* moves the real goal; score movement without call elimination is noise. Judge every future entry here by which of the two it did.
  - **34 → 33: `glNormal3f` retired content-side, and it cost three deleted lines.** `gfx_unit_shield_effects.lua` emitted a per-vertex `gl.Normal` for each icosahedron face — but `ShieldSphereColor.vert` reads `gl_Vertex` and **never `gl_Normal`**, and the draw is shader-bound with no fixed-function lighting, so every one of those normals was consumed by nothing. They were also BAR's only `gl.Normal` call sites, so deleting them retires the function outright. Worth noting what they were costing: `glNormal3f` is unrecordable into a command list, so those three dead lines were single-handedly forcing both shield lists to compile as real GL display lists — and one display-list call anywhere disables capture process-wide. Gate PASS, 702 frames, control 0 / signal 0 (a first run showed 1/728 on the *control* channel at max delta 2, the known flake class; it did not repeat). This is exactly the shape constraint (b) exists for: no engine change, no API change, no pixel change.
  - **Down to a single remaining source, and it is the vert cap — the shields were never the real obstacle.** With `LuaCmdListSuspendOnObjectCreate=1` the meter reads **32**, and the census resolves every surviving display-list call to just two places: `LuaOpenGL::CreateList:7510/7513/7515` — which is the `CMDLIST_MAX_CAPTURED_VERTS` block, **exactly 2 lists** — and the font renderer's now-lazy list, which only exists because those two trigger it. `glColor4ub` survives at 12,320 calls for the same reason: `CmdListEmitIntoCompile` replays captured `Op::Font` commands *into* the open compile, so at least one of the two oversized lists carries text. They are therefore **not** the shield icosahedra (no text in those) but large UI lists. Clearing the cap retires `glGenLists`, `glNewList`, `glEndList`, `glDeleteLists`, `glCallList` and `glColor4ub` **together — six functions, 32 → 26** — and it is now the only thing standing between here and that.
  - **The display-list family is now one blocker from falling entirely.** With `LuaCmdListSuspendOnObjectCreate=1` the meter reads **33** and `glCallList` retires too — no text-bearing list materialises any more, so the recordable font path never runs. What is left creating real display lists is *only* the two `gfx_unit_shield_effects` icosahedra. Clear those and `glGenLists`, `glNewList`, `glEndList`, `glDeleteLists`, `glCallList` and `glColor4ub` all go together — **six functions**. The two obstacles are independent: subd-4 (3,840 verts) materialises on `glNormal3f`, subd-5 (15,360 verts) on the 4096-vert cap, which needs the static-VBO bake. And the knob itself is still blocked on the replay divergence — re-measured after the font change at **99/702**, so it is the replay, not the font list.
  - **Two verification failures on the way here, both the same shape.** A "baseline" rebuild ran `cmake --build .` from the source root, which has no cache; the error message said `Error:` and the filter grepped for `error|FAILED`, so it reported success and the gate then compared a binary against **itself**. And toggling the new knob with a substitute-only `sed` silently did nothing, because the engine had already stripped the default-valued line from `springsettings.cfg` on exit — the same trap the meter documents for `GLFrameABCompare`. Both now assert: builds check that the binary's mtime advanced, config writes append-or-substitute and then re-read.

- 2026-07-31 — **Baseline corrected to 47 (the meter was measuring itself), the meter now resolves call SITES, and the first batch lands: 47 → 43, pixel-clean.**
  - **The 49 included two functions the A/B harness itself makes.** `GLFrameABCompare` was left on in the write-dir config, and its legacy-pass blit uses `glWindowPos2i`/`glDrawPixels` (`ABDisplayLegacy`, `Game.cpp`). With it off the true baseline is **47**. `run_rdoc_offenders.sh` now forces `GLFrameABCompare=0` for its own run rather than inheriting whatever the last gate left behind — a meter that measures the measuring apparatus is worse than no meter.
  - **The patch now emits backtraces, so the burn-down is addressed by source line instead of by grep.** Two additions to `renderdoc-unsupported-log.patch`: each function's **first use** prints a backtrace (bounded — once per distinct function), and `RDOC_SITE_CENSUS=1` counts **every** reachable call site. Two resolvers turn the addresses into source: `rdoc_offender_sites.sh` (first-use, i.e. the head of the list) and `rdoc_site_census.sh` (all sites + call counts). The census is the **1-minute replacement for the ~20-minute apitrace census**, and it answers the question static grep cannot.
  - **Reachable sites are a fraction of static ones — grep was overstating the job by 3-4x.** Measured 434 reachable sites for the 47 functions. `glMatrixMode` greps to 185 sites but only **55** are reachable; `glEnableClientState` 39 → **8**; `glShadeModel` 10 → **4**. Most legacy call sites in the engine are dead for any given game, and only the reached ones block capture. Work the census, never the grep.
  - **Batch A landed: `glTexEnvi`, `glMaterialfv`, `glMaterialf`, `glGetTexEnviv` retired (47 → 43).** All four were reachable **only** through `LuaOpenGL::ResetGLState`, which restores each family to its **GL default** before every draw callin (15,092 calls/run). New `GL::ffResetState` (`Rendering/GL/FFStateTracker.h`) mirrors that state so a restore is emitted only when a set-call actually moved the family off its default — which for BAR is never: the game has **zero** `gl.Material`, `gl.ShadeModel`, `gl.TexEnv`, `gl.Light` and `gl.ClipPlane` call sites. The calls stay in the code and still run for content that uses them, so non-BAR games keep their exact behaviour (Constraint #1); they simply stop executing in a BAR frame.
    - `glGetTexEnviv` went with them: `PlainModulateTexturing` only needed it to confirm `GL_MODULATE`, which the mirror already knows.
    - `glShadeModel` also lost its startup call (`InitGLState` was setting `GL_SMOOTH`, already the initial value — the very first unsupported call of the run, before any frame existed to capture). It survives at **first-use position 35 instead of 1**, now reachable only through `CShadowHandler::CreateShadows`'s `GL_FLAT`/`GL_SMOOTH` bracket. That pair is the whole of what is left for it.
  - **Why this is provably pixel-identical, and how that was checked.** Skipping a write of the value a state already holds cannot change rendering, so the argument is GL semantics, not a pixel measurement — what has to hold is the *tracking*. `FFResetState::Verify()` queries real GL and reports any place the mirror claims "clean" while GL disagrees, driven by the same `shadowCompare` switch the matrix mirror uses. Result: **0 desyncs across 788 gate frames**, and the pixel gate itself passed 0/0 (control and signal).
    - The zero was falsified rather than trusted: `Verify` is the **only** caller of `glGetMaterialfv` in the engine, so running the meter with the A/B compare on must make `glGetMaterialfv` appear in the offender list. It does — which proves the check executed rather than returning early.
    - The subtle bug this caught: `LuaOpenGL::EnableCommon` wraps every draw callin in `glPushAttrib(AttribBits)` and **`AttribBits` contains `GL_LIGHTING_BIT`**, so a pop can restore a shade model no set-call ever announced. The mirror therefore models the attrib save-stack (`NotePushAttrib`/`NotePopAttrib`) instead of only watching setters. An audit of every `glPushAttrib` mask in the engine confirms only `AttribBits` and `gl.PushAttrib` carry that bit.
  - **The gate's blind spot, now closed — see the next entry.** `run_gate.sh` compares modern-vs-legacy *within one build*, so an engine-side change that moves both passes together leaves it reading 0/0 while pixels changed against the previous build. Batch A did not need that gate (its changes are no-ops by construction), but anything altering real GL state does.

- 2026-07-31 — **`--ff-experiment`: a same-frame gate for engine-side FF removals, and `glShadeModel` retired with it (43 → 42).**
  - **Don't compare across builds — vary the call inside one frame.** The obvious instrument for "does deleting this fixed-function call change a pixel" is a this-build-vs-last-build image compare, but that means reproducing a pinned draw clock and draw RNG across two processes, and wall-clock-driven visuals still differ. The whole-frame harness already re-renders one frame four times with exactly those pinned (`CGame::Draw`), so the cheaper and stricter answer is to make the toggled variable the **fixed-function call** rather than the Lua backend. `GLFFRemovalExperiment=<n>` drops candidate *n* on the last pass only; `run_gate.sh --ff-experiment N` sets it and implies `--force-legacy` so the Lua backend stays constant and the removal is the only variable. Control and signal must both be 0.
  - **The mechanism is self-retiring by design.** Add a `GL::FFExperiment` enumerator plus its `Skip()` call site, prove the candidate inert, then delete the call outright and retire the enumerator. An empty enum is the correct steady state, not an unused mechanism.
  - **First use: `CShadowHandler::CreateShadows`' `GL_FLAT`/`GL_SMOOTH` bracket, 785 frames, 0 px.** The shadow passes are depth-only and shader-driven, so the shade model — which only governs interpolation of the fixed-function `gl_Color` — never reached a fragment. That was the last reachable site, so **`glShadeModel` is retired**: 43 → 42.
  - **Validate the instrument before trusting its zero.** A wiring bug gives a vacuous 0/0, so the skip was confirmed to actually fire by censusing the call counts with the experiment off vs on: **1432 → 1128** `glShadeModel` calls at the ShadowHandler sites, exactly 2 fewer per compared frame — one whole pass's worth.
  - **The residual control flake is still live and still non-deterministic:** one post-removal gate run showed control=4 px on 1 frame of 801 with signal 0 and pass 1 the lone pairwise outlier; the re-run was 766 frames clean. Matches the documented ~1-in-750 rate. Re-run before believing a control blip.
  - **The mechanism is not removal-only.** The candidate pass can run *any* alternative code path, so this is a general same-frame A/B for engine-side rendering changes — the thing that was missing for everything outside the Lua immediate backend.

- 2026-07-31 — **State at 41 / 407 reachable sites. Phase 0's precondition is MET for engine draws; what blocks the rest is content shaders reading fixed-function builtins.**
  - **Phase 0 re-check (the prompt's priority 3): the uniform-MVP path is already proven.** The whole-frame harness toggles `RenderBuffer::SetUseMVPUniform` and `CglShaderFontRenderer::SetUseMVPUniform` on the modern pass only, and the gate's signal is **0 over 782 frames**. So a CPU-composed `uMVP` is bit-identical to the `gl_ModelViewProjectionMatrix` builtin for every RenderBuffer and font draw the gate exercises. The documented "dense-MV composition is un-matchable" result is specific to the **Lua immediate** backend, and does not condemn the engine-side conversions. (Not yet confirmed: whether the gate's RenderBuffer coverage includes a genuinely dense world MV. Confirm before generalising.)
  - **The actual Phase-0 blocker is 27 shaders, not the matrix calls.** `gl_ModelViewProjectionMatrix` / `gl_ModelViewMatrix` / `gl_NormalMatrix` / `ftransform()` are read by **21 engine content shaders** under `cont/base/springcontent/shaders/GLSL/` plus **6 BAR shaders**. Deleting the FF matrix set-calls turns those matrices into identity, so every one of them must move to explicit uniforms *first*. That is what the 55-site `glMatrixMode` group really costs.
- 2026-07-31 — **The attrib stack is the largest remaining CONTRACT-FREE target, and the font bracket shows the pattern (and the trap).**
  - **`glPushAttrib`/`glPopAttrib` are ~24 + ~32 reachable sites and essentially all engine-side.** BAR's one `gl.PushAttrib` (customformations2, drag-formation UI) does not even appear in the reachable census. So unlike fog, clip planes and alpha test, **this pair needs no content change and no back-compat decision** — just mechanical conversion of each bracket to an explicit save/restore. `glIsEnabled`/`glGetIntegerv`/`glGetBooleanv` are **not** on RenderDoc's list, so reading the state is free.
  - **Converted the busiest one:** `CglShaderFontRenderer::PushGLState` / `CglFont::End`, ~66k pairs per run — the single biggest site of either function.
  - **A narrow save is not equivalent to a wide push** — `glPushAttrib(GL_ENABLE_BIT)` saves *every* enable, so the saved set must cover whatever the bracket's whole dynamic extent modifies, not just the lines next to the push. **CORRECTED below: the specific `GL_TEXTURE_2D` diagnosis was wrong.** Original (mistaken) reasoning: `glPushAttrib(GL_ENABLE_BIT)` saves *every* enable. Saving only what the bracket visibly writes (depth/alpha/blend) missed `GL_TEXTURE_2D`, because `DrawTraingleElementsRecordable` enables it and ends by **disabling** it — the wide pop used to restore the caller's value, the narrow save left texturing off for the next draw. Symptom: **1 diverging frame per run, max delta 73**, intermittent because it needs a display-list compile to hit the recordable path. Two of three runs failed; one passed, which is exactly how this reads like the ~1-in-750 flake. It is not — **the flake has a max delta of a few LSB, this had 73.** With `GL_TEXTURE_2D` added: 3/3 clean (798, 799, 807 frames).
  - **Two more converted, both exact by construction rather than by measurement.** `LuaOpenGL::RenderToTexture` pushed only `GL_VIEWPORT_BIT`, which *is* exactly {viewport, depth range} — saving both is an exact substitute, and it matters because the `lua_pcall` in between runs arbitrary Lua that may set either. `CLineDrawer::DrawAllBuffered` pushed `GL_ENABLE_BIT` and modifies exactly three enables; `TypedRenderBuffer::DrawArrays` touches none, and its `glEnableVertexAttribArray` is client state, not `GL_ENABLE_BIT`. Attrib pairs **65,773 → 38,726**, total offending calls **1,917,948 → 1,722,719**.
  - **NOTE the gate is weak for this class.** A save/restore bug corrupts state identically in all four passes, so the A/B compare cancels it out and reads 0/0 — the same blind spot as any engine-side change. The font conversion was caught only because its recordable path runs on *some* passes (list compile) and not others. So for attrib conversions, the by-construction argument (what does the pushed bit-mask contain, and what does the bracket's whole dynamic extent modify) is the primary evidence; the gate is a backstop.
  - **So the recipe for the remaining ~54 sites is:** enumerate every state the bracket's *whole dynamic extent* can leave modified — not just the lines next to the push — and diff against the pushed bit-mask. Delta magnitude distinguishes a real divergence from the flake.

- 2026-07-31 — **`gl.TexRect` no longer refuses untextured draws — correct, but inert for BAR. And what the remaining 35 are actually blocked on.**
  - `FlushTexRectModern` always picked the *textured* program, so `LuaOpenGL::TexRect` gated the modern path on `glIsEnabled(GL_TEXTURE_2D)`. It now selects the untextured program when FF texturing is off or the bound texture is mip-incomplete — the case where the legacy quad rasterizes a flat current colour and samples nothing, which the untextured program reproduces exactly. Same treatment `FlushModern` already had.
  - **Measured honestly: it changed nothing for BAR.** TexRect's legacy count went 1,996 → 2,009, i.e. unchanged. The texture enable was never the binding constraint — those draws refuse because a **widget has its own shader bound** (`noShader`), which is a legitimate refusal: the widget's shader must be the one that draws. Kept because it removes a real over-refusal and is gate-verified, but it buys this content nothing.
  - **Where the 35 actually stand**, from the live census rather than assumption:
    - **FF matrix group is 226 of the 333 remaining sites** — `glMatrixMode` 72, `glLoadIdentity` 38, `glPushMatrix` 27, `glPopMatrix` 26, `glLoadMatrixf` 25, `glScalef` 14, `glTranslatef` 12, `glOrtho` 6, `glMultMatrixf` 4, `glRotatef` 2. Removing them turns `gl_ModelViewProjectionMatrix` into identity, which is precisely what the modern immediate backend now reads, plus 21 engine and 6 BAR shaders. Content-gated.
    - **Lua immediate remnants** (`glBegin`/`glEnd`/`glVertex*`/`glTexCoord2f`/`glColor4fv`) survive through `gl.BeginEnd`, `gl.Vertex`, `gl.TexRect` and `CInfoTextureCombiner` — all refusing for the same reason: **a game-supplied shader is bound and expects the FF attribute channels**. `CInfoTextureCombiner` says so in its own comment ("need to keep this old nonsence intact to keep Lua shaders compatible").
    - **Fog, clip planes, alpha test, the client-array five, the display-list cluster** — each already established as content- or structure-blocked in the entries above.
  - Note two of the attrib restores now *add* calls to already-reachable offenders (`glFog*`, `glColor4fv`, `glLineStipple`): total calls rose 1,693,736 → ~1,715,000 while functions fell 37 → 35. Function count is the unit, so that is the right trade, but it is a trade.

- 2026-08-01 — **`ModernModelAttribs` landed (default OFF): flipping it retires 5 functions but moves pixels. The new gate caught that immediately.**
  - Engine + engine-shipped shader now convert together: `S3DModelVAO` binds the modern VAO instead of client arrays, and `ModelVertProg.glsl` gains a `GENERIC_ATTRIBS` variant reading `aPos`/`aNormal`/`aUV` at locations **0/1/4** — the same layout `cus_gl4.vert.glsl` already uses. Config `ModernModelAttribs`, **default false**, so unmodified games are untouched (verified: gate 748 frames 0/0 with it off, and the band hash returns to its original value).
  - **Measured with the band hash, and the numbers are unambiguous:** off → 35 functions, `hash=1358954496`. On → **30 functions** (`glVertexPointer`, `glNormalPointer`, `glTexCoordPointer`, `glEnableClientState`, `glDisableClientState` all retired) but `hash=2390867968`, reproduced twice. **The conversion works mechanically and is not yet pixel-identical.**
  - **ISOLATED, and the engine half is PROVEN.** Re-running the driver with `useLuaMat=false` for every shape — so only the engine's `ModelVertProg` draws — gives **`hash=1140318208` with the mode both off and on**, while the count still goes 35 → 30. So the engine-side conversion (VAO bind + `GENERIC_ATTRIBS` shader) is **pixel-identical and retires 5 functions**; the earlier hash difference came entirely from the `useLuaMat=true` half.
  - **CORRECTION — it is NOT BAR's material shader.** That was the obvious guess and it is wrong: `modelmaterials_gl4/templates/` (the only templates BAR actually loads — `cus_gl4.lua` is the sole CUS gadget and it includes the GL4 ones) contains **no** `gl_Vertex`/`gl_Normal`/`gl_MultiTexCoord*` read at all, just one commented-out line. The legacy `modelmaterials/Templates/` is dead in BAR: nothing includes it.
  - **ROOT-CAUSED at runtime: `program=0`.** Logging `GL_CURRENT_PROGRAM` at every `BindLegacyAttrVBOs` shows two distinct values — a shader (`35`) and **zero, i.e. no shader bound at all**. Those draws are pure fixed-function, and the FF pipeline can *only* be fed by client arrays; generic vertex attributes feed it nothing. So the blocker was never a shader needing conversion — part of this path has no shader to convert.
  - **`program=0` IDENTIFIED: it is `gl.UnitShape`'s `doRawDraw` contract, not a bug.** Arg 3 of `gl.UnitShape` is `rawState`, **defaulting to true**, and `CUnitDrawerGLSL::DrawIndividualDefOpaque` skips `PushIndividualOpaqueState` entirely when it is set — so no shader is bound and the caller owns the state. Those draws are deliberately fixed-function. (The `ab_unitshape_driver` passes `false`, which is why its own draws *do* have a program and why the engine-shader half measured pixel-identical.)
  - **So the rule under `ModernModelAttribs` is a documented mode requirement, not an engine fix:** a raw `gl.UnitShape` draw must have a generic-attrib shader bound by its caller. Auditing BAR's raw-draw call sites and converting their shaders is content work — permitted under the "modify BAR however is necessary" constraint — and needs no Lua API change, since `gl.UnitShape`'s signature and semantics are untouched. A game that does not opt into the mode keeps the fixed-function feed and is unaffected.
  - **What that means for the five client-array functions.** They cannot retire by swapping the bind, however the shaders are written. Either those FF draws get a shader (which is a real behavioural change needing its own parity work, since FF fragment processing must then be reproduced), or the bind stays conditional on whether a program is bound — which keeps the functions reachable. `ModernModelAttribs` remains correct and gated for the shader-bound half; it just cannot be the whole answer.
  - *(superseded reasoning below)* The `useLuaMat=true` divergence looked like a shader problem. Candidates: `gl.UnitShape` with `useLuaMat` may route through `LuaObjectDrawer` and bind a different program, or take a draw path that does not call `BindLegacyAttrVBOs` at all — in which case the VAO bind leaves state that path does not expect. The way to find out is to log the bound program id and the actual draw path for both `useLuaMat` values; the band hash then confirms the fix. **Do not start by editing a BAR shader** — this entry exists because that would have been wasted work.
  - **The original remaining-consumer guess, for the record:** `ab_unitshape_driver` alternates `useLuaMat`, so half the shapes draw through BAR's Lua material rather than the engine's `ModelVertProg`. Converting the engine shader alone leaves that half reading FF builtins from a VAO that no longer feeds them. Next step is to identify exactly which material program those draws bind and convert it in the same step — the hash then says yes or no in one run.
  - **The gate proved itself on first real use.** The A/B harness reports a clean 0/0 for this change (both passes move together, its documented blind spot); the pixel difference is visible *only* in the cross-build hash. Note also the first attempt at this comparison was **vacuous** — the engine rewrites `springsettings.cfg` on exit and drops the line, so a substitute-only `sed` silently left the mode off and produced two identical hashes that looked like success. Re-assert the setting immediately before every run and check it took.

- 2026-08-01 — **The cross-build gate finally exists — and it needed no engine change at all.**
  - The gap this programme has carried from the start: the whole-frame harness compares two passes of ONE frame, so it can only vary what the engine can switch per pass. A change moving engine **and** shader together — the attribute contract — is invisible to it, and it reports a clean 0/0 over broken output.
  - **`ab_unitshape_driver.lua` now emits a `BANDHASH`.** Its band is deterministic across *processes*, not just passes: fixed unit defs sorted by id, fixed screen positions and rotations, an opaque backing rect so the (non-reproducible) world cannot show through, and no sim or clock input. It hashes those pixels with `gl.ReadPixels` and echoes one line. Comparing two builds is then diffing one log line — **no engine support, no file I/O, no image compare**, because `gl.ReadPixels` was already exposed.
  - **Validated both ways**, which is the part that matters: identical across two separate processes (`hash=1358954496` twice), and it *moves* when pixels move — perturbing one shape's rotation by a single degree gave `3892314112`, and restoring it returned the original. A gate that only ever agrees is worth nothing; this one was shown to disagree on demand.
  - **Two traps worth carrying:** the band geometry is FIXED (640x160), not viewport-relative, or two runs at different window sizes would silently hash different regions. And the hash uses no bitwise operators — this is **Lua 5.1**, where `~` is not xor and `&` does not parse, and the syntax error made the whole widget fail to load with no error line, presenting as "the driver just isn't running".
  - **This unblocks the attribute conversion**: change `S3DModelVAO` and the legacy material template together, run twice, diff the hash. Same method covers any future engine+content change the A/B harness cannot express.

- 2026-08-01 — **Attribute-contract pilot MEASURED: the engine half alone breaks everything. 571/571 frames.**
  - Swapped `S3DModelVAO::BindLegacyVertexAttribsAndVBOs` for `vao.Bind()` (the modern VAO, generic attribs 0..5) behind experiment 5, with `ab_unitshape_driver.lua` active so the path runs 6,845×/run rather than ~31. Result: **571 of 571 compared frames diverge.** Reverted; baseline 784 frames 0/0.
  - **This is the confirmation the shader-source reading needed.** `defaultMaterialTemplate.lua` reads `gl_Vertex` (213), `gl_Normal` (215), `gl_MultiTexCoord0` (233); feeding the same draw from generic attributes leaves those builtins empty, so nothing lands. The five client-array functions (`glVertexPointer`, `glNormalPointer`, `glTexCoordPointer`, `glEnableClientState`, `glDisableClientState`) are **not** retirable by an engine change alone — engine and shader must move together, in one step.
  - **The conversion itself is mechanical:** the mapping is 1:1 onto locations the modern VAO already provides, and `modelmaterials_gl4/templates/cus_gl4.vert.glsl` already declares exactly that layout (`0=pos, 1=normal, 2=T, 3=B, 4=uv`). `gl_Vertex`→`vec4(pos,1)`, `gl_Normal`→`normal`, `gl_MultiTexCoord0`→`uv.xy`, `gl_MultiTexCoord5/6`→`T`/`B`.
  - **CORRECTION — experiment 5 CANNOT validate that conversion, and it is worth understanding why before planning around it.** `--ff-experiment` varies one thing between passes of a single frame while everything else, *including the bound shader program*, stays identical. Engine-and-shader-together is precisely a change the harness cannot express: a shader reads FF builtins or generic attributes, never both, so whichever pass does not match its feed renders nothing. Experiment 5 measured the engine half **alone** (571/571) — that is all it can measure.
  - **So this conversion has no gate today.** The A/B harness is blind to it for the usual reason (both passes move together), and the pixel gate would read a clean 0/0 over broken output. Options, none free: teach BAR's material to hold two program variants and select on `Spring.GetABPassIndex()` so the harness *can* vary it; or build the cross-build image compare this programme has wanted since the beginning — the `ab_unitshape_driver` band is the natural subject for it, since 8 unit shapes at pinned transforms are deterministic across processes in a way a live frame is not. **Do not start the conversion before one of those exists**, or it will land unverified.

- 2026-08-01 — **Display-list materialization: two of three causes ARE fixable, but making them recordable diverges. Discarded, with the measurement.**
  - Goal was the display-list cluster — `glGenLists`, `glNewList`, `glEndList`, `glCallList`, `glDeleteLists`, `glColor4ub`, 6 functions — which exists only because some `gl.CreateList` bodies **materialize** into real GL lists instead of being captured. Zero materializations would make the font's recordable path dead and let the whole cluster go. Purely engine-side: no API and no content change.
  - **Causes, after `glPushAttrib` retirement changed the picture:** `glTexParameteri`, `glNormal3f`, `glTexImage2D`. Making the first two recordable worked *as intended* — materializing lists **16 → 12**, then `glNormal3f` off the list entirely, leaving `glTexImage2D` as the sole cause. Note `glTexParameteri` had been tried before and appeared useless; it only helps now that `glPushAttrib` no longer materializes first.
  - **But it diverges.** With both changes: 4 failures in 7 gate runs, including a **recurring 16-17 px at max delta ~90** class — well above the 1 px residual. Reverting them: **3/3 clean**. That A/B is the whole argument; the mechanism is unidentified.
  - **ROOT CAUSE, found by reading rather than re-running.** `CmdRecColor` branches on `cap.inBegin`: inside a `gl.BeginEnd` stream it updates *stream* state (`cap.stream.sawColor`) and emits **no** standalone `Cmd`, because the stream and the command list are two ordered sequences that must not interleave. My `CmdRec_Normal3f` emitted a `Cmd` unconditionally, so a `gl.Normal` **inside** `gl.BeginEnd` inside `gl.CreateList` injected a command mid-stream and corrupted the ordering. That is exactly the profile of the failure: rare (needs all three nested), small (a few vertices land wrong), and non-deterministic-looking. **Any new recorder must follow the `inBegin` discipline** — `CmdRecColor` and `CmdRecTexCoord` are the templates.
  - **Discarded rather than kept**, because they retire **no function** on their own (`glTexImage2D` still materializes lists, and `gl.Normal` keeps `glNormal3f` reachable regardless) while adding a real divergence. Recorded so the next attempt starts from "this diverges, find out why" rather than repeating it.
  - **Method note:** the 16 px class was nearly waved through as the documented ~1-in-750 flake, because per-run failure rates look similar. What separated them was **pixel count**, and then a **3-run baseline A/B with the changes reverted** — which is the check that should follow any suspicious class, since per-run pass/fail is far too noisy to read directly.

- 2026-08-01 — **FALSE ALARM, recorded so it is not re-run: the attrib conversions are NOT expensive. `build/` was `CMAKE_BUILD_TYPE=Debug`.**
  - Symptom: the meter and the gate both went from working to timing out, and a diagnostic run exceeded **20 minutes** for content the meter documents as ~1 minute. I chased it as a performance regression from the attrib work — ~130 state queries per bracket replacing one `glPushAttrib` is a plausible-sounding culprit — and tested two hypotheses, both wrong: uncommitted changes (the last commit timed out identically) and the per-unit texture walk (`NUM_UNITS=1` gave 11:42 against 11:39).
  - Cause: `build/` had been left on **Debug**, not the canonical `RELWITHDEBINFO`. Reconfiguring with the standard command and rebuilding: **meter 54s**, gate 785 and 777 frames 0/0, verifier silent. The conversions cost nothing measurable.
  - Two tells that should have been checked first: the version banner reads `(Debug)`, and the startscript's `NullAI` trips `SkirmishAIHandler.cpp:287 Assertion '!resKey.IsUnspecified()'` — an abort that only fires because asserts are live in Debug.
  - **`grep CMAKE_BUILD_TYPE build/CMakeCache.txt` before drawing any performance conclusion.** The existing memory already says to check the *cache* rather than the binary for the compiler; the same applies to build type. Correctness results are build-type independent and were never in doubt — only the timing conclusions were, and they were entirely wrong.

- 2026-07-31 — **`glPushAttrib` and `glPopAttrib` RETIRED (37 → 35). The whole attrib-stack family is gone, with no content change and no back-compat decision.**
  - Final eight brackets: `CGuiHandler::Draw`, five in `CUnitDrawer`/`CUnitDrawerGL4`, plus the two `GL_TEXTURE_BIT` users, `CBitmap::CreateDDSTexture` and `CNamedTextures::Update`. `CUnitDrawerGLSL::Push/PopIndividualOpaqueState` split push and pop across functions and took a file-scope snapshot, same as the Lua callin bracket.
  - **`GL_TEXTURE_BIT` turned out to be modellable after all.** Both remaining users wrap texture *creation* and only need the caller's binding back, so the snapshot models per-unit bindings for 1D/2D/3D/cube. Texture-object parameters are deliberately not modelled and do not need to be: `glPushAttrib` saves those only for objects bound **at push time**, and a texture created inside the bracket is not among them — which is exactly why these call sites work. Texture-env is not restored either (that needs `glTexEnvi`, retired) but *is* captured and diffed under shadowCompare, so the "nothing writes texenv" assumption stays checked.
  - **One more spec detail, caught by the verifier while the gate passed:** `CBitmap::CreateDDSTexture: ... at unit 0 enable 0x0de1`, i.e. `GL_TEXTURE_2D`. **`GL_TEXTURE_BIT` saves the per-unit texture ENABLES as well as the bindings** — they belong to both it and `GL_ENABLE_BIT`. 183 mismatches in a run the pixel gate called clean.
  - **Final instrument scorecard, 3–3.** The gate caught the per-unit-texture bug (456/456) and the active-unit bug (463/463); the verifier caught enable-ownership per bit, unmodelled `AttribBits` states (including a latent bug in an already-committed conversion), and this texture-enable one. **Every single one would have shipped had only one instrument been used.**
  - Total offending calls **1,707,073**, reachable sites **333** (from 2,605,050 and ~434).

- 2026-07-31 — **Five more brackets: `glPushAttrib` 6,134 → 2,393 calls, 9 → 4 sites. Four left, each blocked for a named reason.**
  - Converted `CSMFGroundDrawer::DrawForwardPass` (dynamic mask — `(GL_ENABLE_BIT * alphaTest) | (GL_POLYGON_BIT * wireframe)` — stored in a local so the restore matches the push), `ShowSplashScreen`, `LuaFBOs::ActiveFBO`, `CMiniMap::DrawForReal`, `CMiniMap::RenderCachedTexture`. Verifier silent throughout.
  - **The four that remain, and why:**
    - `CBitmap::CreateDDSTexture` (674) and `CNamedTextures::Update` (2) push **`GL_TEXTURE_BIT`**, which saves texture bindings and per-unit texture parameters — state the snapshot does not model at all. Both are texture-upload paths, not per-frame rendering.
    - `CGuiHandler::Draw` (1,715) pushes in one file and pops in **another** (`CInputReceiver::DrawReceivers`), so it needs shared state rather than a local snapshot.
    - `CUnitDrawerGL4` (2) pushes `GL_POLYGON_BIT` with the matching pop in a different function.
  - **Flake rate characterised, since three runs failed consecutively and that needed ruling on rather than waving through.** Over six normal runs: 3 clean, 3 with a **single** diverging frame (1 px of 3.7M, deltas 38/87). The `--force-legacy` null test — all four passes legacy, so the modern backend is out of the picture — was **2/2 clean**. That is the documented ~1-frame-in-750 residual, whose per-run hit probability over ~760-frame runs is ~63%, i.e. failing about half the time is the *expected* behaviour, not a signal. Judge by pixel count and delta, never by "a frame diverged".

- 2026-07-31 — **The Lua-callin bracket converted; two more real bugs, one found by each instrument. `glPushAttrib` 16,975 → 6,134 calls, 15 → 9 sites.**
  - `LuaOpenGL::EnableCommon`/`DisableCommon` wraps **every** Lua draw callin and pushes the full `AttribBits`, so it is the widest-extent bracket in the engine. Converting it needed the snapshot to actually model `AttribBits`, and it did not.
  - **Bug 1 — states the snapshot never modelled (verifier).** With the missing states added to capture+diff, the verifier named them immediately: `clearColor` (5,518 hits) and `polygonOffsetFactor` (1,820). It also flagged **`CShadowHandler::DrawShadowPasses` at `polygonOffsetFactor`** — a bug in a conversion committed one entry earlier, which nothing had caught because the state was outside the model. Added: clear colour, draw buffer, blend colour, depth clear value, polygon offset factor/units, line stipple pattern/repeat. **A verifier only checks what it models; extending the model is part of extending the conversion.**
  - **Bug 2 — the active texture unit (gate).** The per-unit walk read the active unit *after* the loop instead of before, so it "restored" unit 7 and parked every subsequent draw there: **463/463 frames, max delta 255**. Invisible to the verifier, because the active unit is not part of the compared snapshot — `glPushAttrib(GL_ENABLE_BIT)` does not save it (that is `GL_TEXTURE_BIT`), so the selector must be put back exactly as the *caller* left it.
  - **`GL_LIGHTING_BIT` is settled, not deferred.** `AttribBits` contains it, and it is deliberately not restored — that would need `glShadeModel`/`glMaterial*`/`glLight*`, four functions already retired here. Safe because nothing writes that state any more, and now *provably* so: the snapshot captures materials, `GL_LIGHT1` parameters, both light-model flags and shade model **under shadowCompare only** (their getters are unsupported functions) and diffs them. Silent across 760-frame runs.
  - Gate 762/758/760 frames 0/0, verifier silent. Total offending calls **1,693,736**.

- 2026-07-31 — **Four more attrib brackets, and the verifier earns its keep by catching a spec error.**
  - Converted `CShadowHandler::DrawShadowPasses`, `CCursorIcons::Draw`, `CBasicWater::Draw`, `CubeMapHandler::UpdateReflectionFace`. `glPushAttrib` **23,205 → 16,975** calls, 19 → **15** sites.
  - **`GL_FOG_BIT` and `GL_CURRENT_BIT` are now restorable**, which is what made three of those four convertible — but note the trade: restoring them needs `glFogfv`/`glFogi`/`glFogf` and `glColor4fv`, unsupported functions in their own right. Both families are already reachable elsewhere (`ISky::SetupFog`, `gl.Color`), so this keeps nothing alive that was not, and it buys the attrib pair's retirement. When those families are eventually retired these restores become blockers and must go with them. `GL_LIGHTING_BIT` is deliberately **not** restored — that would need `glShadeModel`/`glMaterial*`/`glLight*`, four functions this programme has already retired, so restoring them would resurrect the dead; `shadeModel` is captured and diffed but never restored, so the verifier says so if the "nothing writes it any more" assumption ever breaks.
  - **The verifier caught a genuine spec error before it shipped:** `CubeMapHandler::UpdateReflectionFace: explicit restore differs from glPopAttrib at enable 0x0b60`. That is `GL_FOG` — and **several attrib bits own enables of their own**: `GL_FOG_BIT` includes the `GL_FOG` enable, `GL_DEPTH_BUFFER_BIT` the `GL_DEPTH_TEST` enable, `GL_POLYGON_BIT` the cull-face and polygon-offset enables, `GL_COLOR_BUFFER_BIT` alpha-test/blend/dither/logic-op. Restoring a bit's *values* without its *enables* is wrong, and reading the bracket would never have revealed it. Fixed with `RestoreCap` per owning bit; gate 756 frames 0/0 and verifier silent afterwards.
  - **Running score for the two instruments:** the gate caught the per-unit texture bug the verifier structurally could not see; the verifier caught this enable-ownership bug the gate barely registered (2 frames, max delta 2 — indistinguishable from flake). Neither is redundant.

- 2026-07-31 — **Per-unit texture enables are a hard limit on attrib conversion — and the verifier is blind to them.**
  - `AttribSnapshot::Restore(mask)` (capture everything, restore by bit-mask) was meant to make every remaining bracket a two-line conversion, exact without per-site analysis. It works for brackets that stay on one texture unit; the three landed ones (font, `RenderToTexture`, `CLineDrawer`) qualify.
  - **It does not work where the active texture unit moves.** `GL_TEXTURE_2D`, `GL_TEXTURE_1D/3D/CUBE_MAP` and `GL_TEXTURE_GEN_*` are **per texture unit**. `glIsEnabled`/`glEnable` only ever see the *active* unit, while `glPushAttrib` saves all of them. Converting `IModelDrawerState::Setup/ResetOpaqueDrawing` and `Setup/ResetAlphaDrawing` this way broke **456 of 456 frames at max delta 253**, because the model drawer switches units between Setup and Reset and the restore wrote one unit's saved value onto another. Reverted.
  - **The verifier did not catch it, and could not:** `FirstDifference` reads the same per-unit-blind `glIsEnabled`, so it compared the active unit only and saw no difference. **The pixel gate caught it.** So the two instruments are complementary rather than redundant — the verifier sees state the gate cancels out, the gate sees breakage the verifier cannot express. Neither alone is sufficient for this class.
  - **RESOLVED the same day.** `AttribSnapshot` now splits its cap list into global and **per-unit** (`UNIT_CAPS`: `GL_TEXTURE_1D/2D/3D/CUBE_MAP`, `GL_TEXTURE_GEN_S/T/R/Q`) and walks units 0-7 in `Capture`, `Restore` and `FirstDifference`. Eight units covers the deepest the engine's legacy paths reach (unit 6, `S3DModelVAO`'s tangent channels) without paying for 32. One subtlety: `glPushAttrib(GL_ENABLE_BIT)` does **not** save the active unit — that is `GL_TEXTURE_BIT` — so `Restore` puts back the caller's current selector rather than the captured one.
  - **Re-applied to `IModelDrawerState`, the same conversion that broke 456/456: now 751 frames 0/0, verifier silent.** `glPushAttrib` 38,726 → **23,205** calls, 21 → 19 sites; total offending calls 1,722,719 → **1,688,130**. The helper is now usable on multi-unit brackets, which unblocks `SMFGroundDrawer` and the `EnableDrawWorld` family.

- 2026-07-31 — **CORRECTION: the `GL_TEXTURE_2D` diagnosis was a misread flake. Built an oracle verifier, and it refuted my own claim.**
  - **What I claimed:** that omitting `GL_TEXTURE_2D` from the font bracket's saved set caused a real divergence (1 frame, max delta 73), and that adding it fixed the gate (3/3 clean).
  - **What is actually true:** `GL::VerifyAttribRestore` (new, `Rendering/GL/AttribStateVerify.*`) keeps the original `glPushAttrib` alive under `shadowCompare`, snapshots the state the explicit restore produced, calls `glPopAttrib`, snapshots again, and names any state that differs. With `GL_TEXTURE_2D` deliberately removed again it reports **zero mismatches over ~150k active calls**, and the accompanying gate failures were **1 pixel of 3.7M at max delta 2** — the ambient residual flake. Removing the `GL_DEPTH_TEST` restore likewise produced no mismatch: in this content both states are already at the value the bracket sets, so those saves are defensive rather than load-bearing. I attributed a flake to a mechanism and then credited a fix to it; the pass/fail streaks were noise.
  - **The verifier is proven, not assumed.** Two negative controls that removed real saves detected nothing — because they were states that never differ here, which made them useless as controls. A third, corrupting `glBlendFunc` *inside* the pushed mask, was named correctly and immediately: `explicit restore differs from glPopAttrib at blendSrcRGB`. **A negative control has to perturb something that actually varies**; the first two were as vacuous as the zeros they were meant to test.
  - **Standing result:** the three converted brackets (font, `RenderToTexture`, `CLineDrawer`) are **state-exact against the real `glPushAttrib`**, which is far stronger evidence than the pixel gate can give for this class — the gate is nearly blind here, since a missed state corrupts all four passes equally.
  - **Note the verifier is self-healing:** its own `glPopAttrib` puts the correct state back, so under `shadowCompare` a state bug is masked in the pixels and visible only in its log. Read the log, not the gate result.
  - **Flake discriminator, restated because I got it wrong:** pixel COUNT and delta magnitude, not frame count. The residual is ~1 px at a few LSB. "1 frame diverged" says nothing on its own.

- 2026-07-31 — **The dense-MV / perspective limitation is SOLVED, not fundamental: all 132,206 Lua-immediate fallbacks eliminated (396 → 341 reachable sites).**
  - **The old conclusion was scoped too widely.** "Dense-MV composition is un-matchable" is true of a **CPU-composed** `P*MV` fed as a uniform — no arithmetic recipe reproduces the driver's undocumented ordering. It is *not* true of the transform generally: a shader reading the compatibility **`gl_ModelViewProjectionMatrix`** builtin gets the driver's own composition, exact for any matrices. And reading a builtin is not a GL call, so it costs nothing at the capture gate; only the FF matrix **set**-calls do, and those are a separate group. The `CLineDrawer` conversion earlier in the session is what surfaced this; applying it here was the payoff.
  - **Eight shader variants now:** `{untextured, textured} × {fogless, fogged} × {uniform MVP, builtin MVP}`. Separate programs rather than a uniform branch, for the documented reason — touching the parity-proven shader source perturbs the compiled `gl_Position` math enough to move thin-primitive edge pixels.
  - **Both gates in `FlushModern`/`FlushTexRectModern` now select the builtin variant instead of falling back.** Fallback census: **132,206 flushes / 854,216 vertices → 0 / 0.** Every `DENSE_MV` and `PERSPECTIVE_P` fallback is gone, which retires the entire legacy *geometry* replay for Lua immediate mode — including the top offender `modules/graphics/r2thelper.lua:48` (90,490 flushes) and the three `gui_top_bar` list sites, with **no BAR content change at all**.
  - **Verified:** watertest 788 frames 0/0; idletest showed a 1-frame signal on one run and 0/0 on two re-runs (533, 529) — non-deterministic, the documented ~1-in-750 flake, not a regression.
  - **No function retired yet, and the census says exactly why.** `glColor4fv`/`glTexCoord2f` survive because `FlushModern` still replays the FF **current-colour and current-texcoord end-state** (`LuaImmediateBuffer.cpp:703/800`) — BAR's `gui_pip` minimap shader reads `gl_Color`, so that side effect is load-bearing. `glBegin`/`glEnd`/`glVertex*` survive through `LuaOpenGL::BeginEnd`, `::Vertex`, `::TexRect`, `CInfoTextureCombiner`, and the font's recordable path. Those are the next targets, and they are now the *only* users.

- 2026-07-31 — **Negative results: fog, clip planes and alpha test are genuinely live for BAR. Do not spend gate cycles on them.**
  - **Fog (`glFogfv`/`glFogi`/`glFogf`) feeds real shader uniforms.** Three BAR files read `gl_Fog`, including `modelmaterials_gl4/templates/cus_gl4.vert.glsl` — the GL4 material template, i.e. the actively-used one — plus `gfx_volumetric_clouds.lua` and the legacy `defaultMaterialTemplate.lua`. The engine's `LuaImmediateBuffer` fog shader variant reads it too. Retiring these means rehosting fog as explicit uniforms across engine **and** BAR content.
  - **Clip planes (`glClipPlane`) actually clip BAR's units.** `defaultMaterialTemplate.lua:321` writes `gl_ClipVertex`, which is exactly what makes fixed-function clip planes bite a shader draw. `CWorldDrawer::DrawAlphaObjects` (water-line split) and `CMiniMap::SetClipPlanes` are therefore doing visible work.
  - **Alpha test (`glAlphaFunc`) is held alive by content regardless of the engine sites.** Three of its four reachable sites are engine-side (`ResetGLState`, `CGuiHandler::Draw`, `IModelDrawerState::SetupOpaqueDrawing`), but BAR calls `gl.AlphaTest` from 9 places, so the fourth keeps the function reachable no matter what the engine does. Retiring it means shader `discard` in content.
  - **`glColor4f` likewise.** `CShadowHandler`'s `glColor4f(1,1,1,1)` before a depth-only pass is plausibly inert (same shape as the shade-model bracket already retired), but `ResetGLState`'s is load-bearing: `AttribBits` omits `GL_CURRENT_BIT`, so the pop ending a Lua callin does not restore the colour, and BAR drives `gl.Color` 53k times a run. Killing one of two sites retires nothing.
  - **What this leaves.** Every remaining function is now either (a) held by BAR content — fog, clip planes, alpha test, `gl.Normal`, `gl.Rotate`, `gl.LineStipple`, the Lua immediate family, and the FF matrix group behind it — or (b) structurally blocked, like the display-list cluster (`gl.CreateList` bodies that create textures can never be command-list captured). There is no remaining engine-only, contract-free retirement that this session's methods can reach.

- 2026-07-31 — **Fixed-function lighting retired: `glLightfv` + `glLightModeli` (39 → 37), 28 sites, no content change needed.**
  - **The reachability lesson again.** Static grep finds `glLightfv` all over `LightHandler.cpp`; the live census shows **zero** of those reachable — all 11 reachable sites are `LuaOpenGL`'s (`SetupScreenLighting`, `EnableDrawWorld`, `EnableDrawGenesis`, `EnableDrawWorldPreUnit`), plus `glLightModeli` from `EnableCommon`/`SetupScreenMatrices`.
  - **Why it is inert for BAR:** `GL_LIGHTING` is enabled at exactly **one** site in the entire engine — `gl.Lighting` — and BAR calls it **zero** times. No BAR shader reads `gl_LightSource`, `gl_LightModel` or `gl_FrontMaterial`; only two engine content files do, and the relevant one (`ModelFragProg.glsl`) is never compiled in a BAR run.
  - **That reasoning was not trusted on its own**, because a compatibility shader can read the light builtins with lighting disabled. Measured with experiment 4: **746 compared frames, 0 pixels**, and `ab_unitshape_driver.lua` was extended to drive `gl.UnitShape` through **both** the Lua-material and engine-shader paths first, so the shader family that *does* read those builtins was covered. Falsified: `glLightfv` 16848 → 13284, `glLightModeli` 19756 → 15658 with the experiment on.
  - **`SetupLegacyFFLighting` (default off)** restores the calls for a game that lights through the engine's legacy `ModelFragProg`. Second config escape hatch of the programme, same shape as `BindLegacyModelTexUnits`.

- 2026-07-31 — **`glClientActiveTexture` retired (40 → 39) — after manufacturing the coverage that made the measurement mean anything.**
  - **The blocker verdict was wrong, and so was the first attempt to disprove it.** Experiment 3 drops tex units 1/5/6 from `S3DModelVAO`'s legacy bind. On stock content it read 0/0 — a **vacuous zero**: the path binds ~31 times per run on watertest, idletest *or* a full replay, i.e. at most ~7 compared frames. Busier content does not help, because a backtrace showed the only thing reaching this path in BAR is **`gl.UnitShape`**, and BAR calls that when a build menu or ghost placement is on screen — the driver is UI state, not combat.
  - **So the coverage was manufactured:** `test/gl-ab-compare/ab_unitshape_driver.lua` draws 8 unit shapes every frame, raising the path from ~31 binds/run to **6845**. Re-run: **688 compared frames, 0 pixels**, with `useLuaMat=true` so BAR's own material shaders were bound — the very ones reading `gl_MultiTexCoord5/6`. They guard it (`if (dot(T,T) < 0.1) T = vec3(1,0,0)`), and BAR's main unit paths bind the **modern** VAO, not this one. Falsified as usual: `glClientActiveTexture` 38560 → 30840 and `glTexCoordPointer` 19280 → 16385 with the experiment on, so it demonstrably fires.
  - **Landed with an escape hatch, because this one genuinely can bite another game.** A game whose model shaders read `gl_MultiTexCoord1/5/6` on this path *without* a tangent guard would lose its tangent frame. `BindLegacyModelTexUnits` (default **off**) restores the old bind. This is the one place so far where the capture goal and Constraint #1 actually trade against each other, and the trade is a one-line config rather than a silent regression.
  - **Two bugs the gates caught, worth noting as a pattern:** reading `configHandler` at namespace scope segfaulted the engine before the window appeared (static-init order — make it function-local), and an inverted condition left `glClientActiveTexture(GL_TEXTURE0)` still firing. The pixel gate caught the first, the **meter** caught the second: it still read 40 while the gate was passing 692 frames clean. Neither instrument alone would have caught both.
  - **The remaining five in this bind pair are load-bearing, and now provably so — this is where the client-array cluster actually stops.** `glVertexPointer`, `glNormalPointer`, `glTexCoordPointer` (unit 0 only now), `glEnableClientState`, `glDisableClientState`. BAR's material template reads **`gl_Vertex`** (`defaultMaterialTemplate.lua:213`), **`gl_Normal`** (215) and **`gl_MultiTexCoord0`** (233) — read from source, not inferred, so unlike units 5/6 there is nothing to measure here. Retiring them means moving those three builtins to generic attributes in **both** BAR's material template **and** the engine's `ModelVertProg.glsl`, i.e. a coordinated engine + content change against the pinned checkout. Rerouting `gl.UnitShape` onto the modern submission path instead (`SetStaticModelMatrix` + `SubmitImmediately`, as `CUnitDrawer`'s build icons do) is a real option but not pixel-neutral: it swaps `ModelVertProg` for `ModelVertProgGL4`, so it would need its own parity campaign rather than a gate run.

  - **Superseded — the earlier claim that the gate content could not settle this.** Experiment 3 (`ModelLegacyTexUnits`) binds only unit 0 on the legacy model path, dropping units 1/5/6 and every `glClientActiveTexture` with them. It reads **0/0 on both watertest (789 frames) and idletest (523 frames)** — but that is a **vacuous zero**: `BindLegacyVertexAttribsAndVBOs` runs only **28 times per run** on either content, i.e. at most ~7 compared frames under the 4-pass harness. The experiment demonstrably fires (`glClientActiveTexture` 224 → 144), so the wiring is good; there is simply nothing drawing through the path to compare. Two things weaken the original blocker claim as well: BAR's main unit paths bind the **modern** VAO (`S3DModelVAO::Bind()`), not this one, and the template's tangent read is **guarded** (`if (dot(T,T) < 0.1 || dot(B,B) < 0.1) { T = vec3(1,0,0); B = vec3(0,0,1); }`) — exactly the fallback you would write for attributes that may be absent. **Do not land this on the current evidence, and do not trust the earlier "hard contract blocker" verdict either.** What it needs is content that actually exercises the path: ghosted buildings (enemy structures seen then lost to fog), AI temp units, and model weapon/piece projectiles. A replay is the obvious candidate. The experiment is committed and ready to run against one.
  - **The earlier reasoning, for the record — the six client-array functions may be a content contract.** `S3DModelVAO::BindLegacyVertexAttribsAndVBOs` exposes tex units 0/1/**5**/**6** as uv0, uv1, sTangent, tTangent, and **BAR's own default material template reads `gl_MultiTexCoord5.xyz` / `gl_MultiTexCoord6.xyz`** for tangent/bitangent (`modelmaterials/Templates/defaultMaterialTemplate.lua:284-285`, plus `gfx_unit_glass.lua` and `unit_icongenerator.lua`). That template shades essentially every unit in the game. Swapping the bind for `vao.Bind()` therefore breaks BAR's unit rendering, not just other games' — the FF texcoord channels are a **published attribute contract to game-supplied shaders**, and retiring them is a coordinated engine + content change against a BAR checkout deliberately pinned to Release 2026.06.30-3. This was called "the best ratio on the board" twice on call-site counts alone; it is actually the most contract-entangled item left. Mechanically the swap is trivial (the VAO supplies the element-array binding too, so `S3DModelPiece::DrawElements` needs no change) — the cost is entirely back-compat.
  - **The six remaining client-array functions, for the record.** `glVertexPointer`, `glNormalPointer`, `glTexCoordPointer`, `glClientActiveTexture`, `glEnableClientState`, `glDisableClientState` are now reachable **only** through `S3DModelVAO::Bind/UnbindLegacyVertexAttribsAndVBOs`. The client arrays exist because `ModelVertProg.glsl` — the **legacy** model shader, attached at `ModelDrawerState.cpp:122`, the one `CUnitDrawerGLSL::DrawStatic` draws through — reads `gl_Vertex`, `gl_Normal` and `gl_MultiTexCoord0`. Converting means a generic-attrib shader *variant* (never restructure the proven one) plus re-plumbing the drawer's uniform state onto it, with regression risk for every non-BAR game that uses the legacy drawer. Six functions for one call-site pair is still the best ratio left — but it is a project, not a step.
  - **`glColor3f` retired anyway (41 → 40): its colour had no reader, even though its matrices do.** The startup group needed no new gate after all — `ClearScreen`'s trailing `glColor3f(1,1,1)` is unread by **every** one of its five callers, which is provable by inspection without a pixel gate. `CPreGame::Draw` draws nothing but font text and calls `SetTextColor` (the font emits per-vertex colours, never the FF current colour); `CLoadScreen::Draw` and `CLuaMenuController::Draw` hand straight to a Lua draw callin whose `ResetGLState` sets the colour absolutely; `SelectMenu::Draw` hands to aGui, whose widgets carry their own `ucolor` (its only two `glColor4f` calls, in `LineEdit`, *set* rather than inherit); and `SpringApp::Init` draws nothing before the next full setup. The remaining two `glColor3f` sites are unreached in a BAR run and both precede a depth-only shadow pass — the same inert situation already proven for `glShadeModel`. Gate after: 797 frames, 0/0.
  - **The startup group's MATRIX half is still blocked on verification — and `ClearScreen` is not as dead as it looks.** `glColor3f`'s only 3 reachable sites are all `ClearScreen()`, from `SpringApp::Init`, `CPreGame::Draw` and `CLoadScreen::Draw`. Two things make this harder than the other "sets the GL default" cases:
    - **Its matrices are consumed.** `CPreGame::Draw` calls `ClearScreen()` and then immediately draws font text, and the font renderer reads its MVP off the fixed-function stack (`GetCurrentFixedFunctionFontMVP`) instead of setting one — so pregame text position depends on `ClearScreen`'s `gluOrtho2D`. `SelectMenu::Draw` likewise hands straight to `agui::gui->Draw()`. Only the `CLoadScreen` path is safe, because `LuaOpenGL::EnableDrawScreenCommon` overwrites projection and modelview with **absolute** `glLoadMatrixf` and the colour with `glColor4f`. Do not assume `ClearScreen`'s FF setup is vestigial; it is live for two of its five callers.
    - **`glColor3f(1,1,1)` is doing real work, so the Batch-A trick does not apply.** `AttribBits` omits `GL_CURRENT_BIT`, so the `glPopAttrib` at the end of a Lua draw callin does **not** restore the current colour — it stays whatever the last `gl.Color` set. The reset is therefore not a write-of-the-value-already-held, and can only be justified by "no consumer reads it", which is a weaker claim needing a pixel gate.
    - **No gate covers these frames.** The whole-frame harness lives in `CGame::Draw`; pregame, loading and menu frames are never compared. Retiring the startup group needs that coverage built first — the one remaining piece of contract-free engine work in the whole programme.

  - **The display-list cluster will not fall to the "add a recordable Op" trick — tried and reverted.** `glTexParameteri` is not deprecated and merely lacked an `Op`, so it looked like a contained way to shrink the 16 materialising lists. Implemented (enum + recorder + both executors, gate clean at 687 frames): materialisation stayed at **16**. The same lists simply fell back on **`glTexImage2D`**, which had been masked behind `glTexParameteri` in the once-per-name log. Those bodies create textures inside `gl.CreateList`, which no command-list layer can capture — so the fallback is structural, not a missing opcode. Reverted, since it retired nothing and changed capture semantics for no measured gain. Remaining causes to beat: `glTexImage2D`, `glNormal3f`, `glPushAttrib`.

  - **Lua display lists are already capturing; three calls still force fallback.** `LuaCommandLists=1` is on, and only **16** bodies materialise into real GL lists. The engine logs why: `glTexParameteri`, `glNormal3f`, `glPushAttrib`. `glTexParameteri` is not even deprecated — it is a plain state setter that simply has no recordable `Op`, so adding one is a contained way to shrink the fallback. Note this retires no *function* on its own: `glGenLists`/`glNewList`/`glEndList` also serve the font's `ffTextureSpaceMatrix` list, which has to go too.

- 2026-07-31 — **`CLineDrawer` off client arrays via a new float-colour vertex type (42 → 41).**
  - **`VA_TYPE_C4`** is `VA_TYPE_C` with a `float4` colour. It exists because every other `VA_TYPE_*` colour is an 8-bit `SColor`, and `CLineDrawer`'s vertices come straight from the float arrays its `glColorPointer(4, GL_FLOAT, …)` stream fed — an 8-bit attribute would round each component by up to 0.5 LSB. The RenderBuffer shader machinery is fully generic over `attributeDefs`, so the new type needed only a struct, its attribute defs, a `GetFragOutput` specialisation and an appended registry slot (appended, so no existing index shifts).
  - **The transform needed no care at all, and that is the reusable part.** The RenderBuffer shader reads the compat `gl_ModelViewProjectionMatrix` builtin, so the driver composes the MVP exactly as it does for the fixed-function draw, and reading a builtin is not a GL call. Engine-side conversions can therefore be transform-exact by construction; the dense-MV problem is specific to CPU-composed uniform MVPs.
  - **Proven, then falsified.** `GLFFRemovalExperiment 2` ran both paths in the same frame: 788 frames, 0 px. Then the buffered path was confirmed to actually run by censusing LineDrawer's client-array counts off-vs-on — **3744 → 3178**. Post-landing: meter 41, gate 782 frames 0/0.
  - **`glColorPointer` retired; the other six did not come with it.** They are all in `S3DModelVAO::Bind/UnbindLegacyVertexAttribsAndVBOs`, and they exist because `ModelVertProg.glsl` reads `gl_Vertex`/`gl_Normal`/`gl_MultiTexCoord0`. That is a shared-content-shader rewrite affecting every game, not a local swap — see the corrected costing above.

- 2026-07-31 — **State at 42, with the next targets costed from the live census.**
  - **Engine-side FF-array draws have an exact conversion route: `gl_ModelViewProjectionMatrix`.** The dense-MV composition problem (never matchable, do not restructure proven shaders) only arises when the MVP is composed **on the CPU** and fed as a uniform. A GLSL shader that reads the compat builtin gets the driver's own composition, bit-for-bit — and *reading* it is not a GL call, so it does not block capture. `RenderBuffers.inl` already has that path (`uUseMVP ? uMVP : gl_ModelViewProjectionMatrix`). So engine-side client-array draws can move to a RenderBuffer with the transform exact by construction, and the screen-aligned-MV gate that constrains the Lua backend does not apply to them.
  - **What each remaining cheap function actually costs** (reachable sites, from `rdoc_site_census.sh`):
    - **Client-array cluster — half done, and the other half is not what it looked like.** `CLineDrawer::DrawAll` is converted (below), retiring `glColorPointer`. The remaining six (`glEnableClientState`, `glDisableClientState`, `glVertexPointer`, `glNormalPointer`, `glTexCoordPointer`, `glClientActiveTexture`) all live in `S3DModelVAO::Bind/UnbindLegacyVertexAttribsAndVBOs` — and the client arrays are there because **`ModelVertProg.glsl` reads `gl_Vertex`, `gl_Normal` and `gl_MultiTexCoord0`**. Retiring them is a rewrite of a shared content shader in `cont/base/springcontent/` that every game uses, against Constraint #1, not a local draw-path swap. It is no longer the cheap 7-for-2 it appeared to be from call sites alone.
    - **`glColor4f` is NOT cheap despite 2 sites.** Both write the GL default `(1,1,1,1)`, but unlike material/texenv the current colour is genuinely mutated by BAR (`gl.Color`, 53k calls/run), so the reset is load-bearing — and the only alternative spelling, `glColor4fv`, is equally unsupported. It retires with the Lua immediate colour path, not before. (`CShadowHandler`'s one is a plausible `--ff-experiment` candidate, but retires nothing alone.)
    - **Fog (`glFogfv`/`glFogi`/`glFogf`, 1+1+6 sites) is live state, not dead.** `LuaImmediateBuffer`'s parity-proven fog shader variant reads `gl_Fog.color/end/scale` and `FogIsEffective` queries `GL_FOG_START/END`. Retiring the setters means rehosting fog as explicit uniforms everywhere it is consumed, and touching a parity-proven shader — treat as a real conversion.
    - **`glLineStipple` (2 sites) needs a shader dash**, `glNormal3f`/`glRotatef` belong to the Lua immediate and FF-matrix scaffolding groups respectively, and `glColor4ub`/`glCallList`/`glNewList`/`glEndList`/`glGenLists`/`glDeleteLists` are entangled with the display-list story (font recordable path + `gl.CreateList`).

- 2026-07-31 — **Decision: FULL capturability (not a capture mode). Baseline = 49 distinct unsupported functions, with a 1-minute meter to drive them to zero.** *(Superseded above: the true baseline is 47; two of the 49 were the A/B harness's own blit.)*
  - **The meter.** `test/gl-ab-compare/run_rdoc_offenders.sh` runs the engine under a `librenderdoc` built from source with `renderdoc-unsupported-log.patch` (a five-line `fprintf` in `UseUnusedSupportedFunction`), and prints the distinct offending functions **in first-use order** — the first is the one that kills capture. It exits 0 only when a `.rdc` actually appears, so the meter and the acceptance test are the same command. Measuring used to cost a ~20-minute apitrace; this is ~1 minute, which is what makes a 2.6M-call program tractable. Build recipe is in the script header (`-DENABLE_XCB=OFF` avoids needing `libxcb-keysyms1-dev`; xlib/GLX is what the engine uses).
  - **Baseline: 49 functions.** In first-use order: `glShadeModel`, `glGenLists`, `glNewList`, `glEndList`, `glMatrixMode`, `glLoadIdentity`, `glTranslatef`, `glScalef`, `glOrtho`, `glColor3f`, `glPushAttrib`, `glPopAttrib`, `glAlphaFunc`, `glTexEnvi`, `glColor4f`, `glMaterialfv`, `glMaterialf`, `glLightModeli`, `glColor4fv`, `glTexCoord2f`, `glPushMatrix`, `glPopMatrix`, `glBegin`, `glVertex2f`, `glEnd`, `glGetTexEnviv`, `glFogfv`, `glFogi`, `glFogf`, `glNormal3f`, `glVertex3f`, `glCallList`, `glColor4ub`, `glDeleteLists`, `glLoadMatrixf`, `glRotatef`, `glClipPlane`, `glMultMatrixf`, `glLightfv`, `glLineStipple`, `glWindowPos2i`, `glDrawPixels`, `glEnableClientState`, `glDisableClientState`, `glColorPointer`, `glVertexPointer`, `glNormalPointer`, `glClientActiveTexture`, `glTexCoordPointer`.
  - **Note `glGetTexEnviv` is on the list** — a *query* that blocks capture. `PlainModulateTexturing` calls it on every textured Lua flush, so the texenv gate is itself a blocker and needs replacing with tracked state, not just the draws it guards.
  - Function-count is the right unit for this program, not call-count: one `glShadeModel` and 362k `glTexCoord2f` are equally fatal, so 49 → 0 is the burn-down, and each entry retires when its last call site is converted.

- 2026-07-31 — **Whole-process census against RenderDoc's actual blocklist: 2,605,050 offending calls (17.67% of all GL), and the very first one is `glShadeModel` at call #3317, before the first swap.**
  - **Method:** the whole 4,398-frame trace streamed against the 1,904-function unsupported list extracted from `renderdoc/driver/gl/gl_dispatch_table_defs.h` (`UnsupportedWrapper*` macros). This is the real gate — not "deprecated-looking", but the exact set that trips `UseUnusedSupportedFunction`.
  - **Refinement that shrinks the target:** `glEnable`/`glDisable`/`glGetFloatv`/`glGetIntegerv`/`glIsEnabled` are **NOT on the list**. So `glEnable(GL_TEXTURE_2D)` and the uMVP `glGetFloatv` bridge do not block capture, and the per-frame target is the **2,036** figure, not 3,271. The `ff-enable` (961) and `ff-query` (274) classes are cleanliness, not blockers — which also de-prioritises task #6's glGetFloatv bridge as a *capture* concern.
  - **Startup is 20 calls, not a new continent.** I had warned that startup/loading was now unmeasured mandatory scope and might be large (the old "Chili/Chobby, 159 sites, all at f=-1" finding). Measured: **20 offending calls before the first swap**, from about five identifiable engine-side sources — `glShadeModel(GL_SMOOTH)` (#3317), a `glGenLists`/`glNewList`/`glEndList` list-id probe (#3329-31), screen-matrix setup `glMatrixMode`/`glLoadIdentity`/`glTranslatef`/`glScalef`/`glOrtho`/`glColor3f` (#3469-79), and the **font renderer** — its texture-space list `glNewList`/`glScalef(1/512, 1/388)`/`glEndList` (#3610-12) plus `PushGLState`'s `glPushAttrib`/`glPopAttrib` (#3561, #3613-3728). Caveat: this run goes straight into a game from a startscript, so it never touches the menu/Chobby layer a real launch would.
  - **Whole-process totals.** By scope: `Draw::Screen::DrawScreen` **1,462,166** (56%), `Draw::World::DrawWorld` 334,588, `Draw::Screen` 176,280, `Draw` 166,722, `<no group>` 125,303, `Water::DrawReflections` 104,725. By function: `glTexCoord2f` 362,343, `glColor4fv` 317,501, `glMatrixMode` 280,384, `glVertex3f` 204,772, `glVertex2f` 164,264, `glPushMatrix`/`glPopMatrix` 133,408 each, `glLoadIdentity` 131,480, `glPushAttrib`/`glPopAttrib` 99,334 each, `glMaterialfv` 84,648, `glLoadMatrixf` 72,395, `glBegin`/`glEnd` 55,310 each, `glLightModeli` 51,931, `glLightfv` 41,300. The FF **lighting** volume (`glMaterialfv` + `glLightfv` + `glLightModeli` ≈ 178k) barely showed in the single-frame census (149) — worth knowing it is a real class, not a rounding error.
  - **What it means for sequencing.** Capture requires zero offending calls from process start *through the end of the target frame*, so there is no ordering trick: the 20 startup calls are the first gate and a cheap bounded fix, but clearing them only arms capture until the first in-frame offender. Screen-side Lua is now 56% of the entire problem, and it is the same conversion pattern as the reclaim hulls with the call sites already named.

- 2026-07-31 — **First RenderDoc capture attempt: it hooks, it refuses. And the post-conversion frame is 13,517 calls / 15.1% rejected (was 66,794 / 84.7%).**
  - **Re-census of a pure-modern frame, after the reclaim-field conversion.** Total GL calls **66,794 → 13,517**; RenderDoc-rejected **56,577 (84.7%) → 2,036 (15.1%)** on the same classification basis. The immediate class is the whole of the change: **55,399 → 883**. Everything else is untouched engine scaffolding, near-identical call for call — FF matrix 697 → 677, attrib stack 194 → 188, FF lighting 149 → 149, point sprite 56 → 56, alpha test 18 → 18, client arrays 6 → 6, line stipple 1 → 1. (Counting `glEnable`/`glDisable` of FF caps and FF-state queries too, which the earlier census did not, the totals are 3,271 / 24.2%.)
  - **The scope ranking has inverted.** `Draw::World` was 94.5% of all rejected calls; it is now second. Current leaders: `Draw::Screen::DrawScreen` 1,442 rejected / 2,842 calls, `Draw::World::DrawWorld` 587 / 3,007, `Draw::Screen` 221 / 555, `Draw::World::Water::DrawReflections` 206 / 868, `Draw::World::Models::Opaque` 196 / 1,036. Screen-side Lua and the FF scaffolding are the work now, not world geometry.
  - **RenderDoc 1.45 hooks the engine but never starts a capture.** New `/renderdoccapture` action (RenderDoc's own trigger is a keypress, useless to a scripted gate) drives the in-application API. Observed under `renderdoccmd capture`: the engine detects the tool (`GL_EXT_debug_tool`), the API resolves at **1.7.0**, the capture-file template is correct, `TriggerCapture()` is accepted — and then `IsFrameCapturing()` stays 0 and `GetNumCaptures()` stays 0 across two triggers and hundreds of frames. No `.rdc`, and RenderDoc emits no diagnostic even with `--opt-api-validation --opt-api-validation-unmute`. So the refusal is silent, which is worth knowing: **"no capture appeared" is the only symptom there is.**
  - **REFUTED: it is not the profile, and not the deprecated call mix.** Both hypotheses died the same day.
    - *Not the legacy GLX path.* The trace does show `glXChooseVisual` + plain `glXCreateContext` at calls 7–13, but that is SDL's probe context and it is **destroyed at call 49**. The real context is call 53, `glXCreateContextAttribsARB(..., MAJOR=3, MINOR=0, PROFILE_MASK=COMPATIBILITY, FLAGS=DEBUG)`, made current at 54 and never replaced.
    - *Not the context version.* Requesting compat **4.5** instead of 3.0 changes nothing — still no capture.
    - *Not the compatibility profile.* A **minimal 60-line SDL2 app** (same SDL2, same Mesa, same `librenderdoc` trigger path, 3.0 compat + DEBUG flag — i.e. the engine's exact request) **captures fine**: `rdtest_compat_frame31.rdc`. Core captures too. So RenderDoc supports compat contexts on this machine, and the ~2,036 remaining rejected calls are *not* what is blocking a capture — RenderDoc never gets far enough to look at them.
    - *Not RenderDoc's frame-boundary detection either.* Explicit `StartFrameCapture(nullptr, nullptr)` from inside the engine (`/renderdoccapture start`) is refused outright: `IsFrameCapturing()` reads 0 immediately after the call, and `EndFrameCapture` returns 0 with no file.
  - **So the blocker is something about the engine PROCESS, not its GL.** This matters for planning: converting more content cannot, on its own, produce a capture.
  - **ROOT-CAUSED (from the RenderDoc source, then confirmed by experiment): using ANY deprecated GL function permanently disables capture for the whole process.**
    - `WrappedOpenGL::UseUnusedSupportedFunction()` (`renderdoc/driver/gl/gl_driver.cpp:1034`) fires the first time the app calls a function on RenderDoc's unsupported list, and **immediately removes every frame capturer** — `RemoveDeviceFrameCapturer` plus `UnassociateWindow` for each window. From then on `m_UnsupportedFunctions` is non-empty, so the re-registration at each SwapBuffers is skipped: *"only associate windows if no unsupported functions have been used"* (`gl_driver.cpp:2094`). With nothing registered, `MatchFrameCapturer` returns NULL and both `TriggerCapture` and `StartFrameCapture` become silent no-ops (`core.cpp:1077,1118`).
    - **Why it is invisible:** the explanation goes to `SetDriverUnsupportedMessage`, which surfaces only in the RenderDoc UI. Nothing is written to any log, and `m_UnsupportedFunctions` is never cleared — the kill is permanent for the process.
    - **Confirmed by experiment**, same binary and same run configuration, one line apart: `rdoc_profile_test ... noff` → `captures = 1`; with a single `glMatrixMode(GL_MODELVIEW)` at frame 10 → `captures = 0`, no `.rdc`. That one call is the entire difference.
    - **The list is 1,894 functions**, 458 of them in the fixed-function families the census counts (`glBegin`/`glEnd`/`glVertex*`/`glColor*`/`glMatrixMode`/`glPushAttrib`/`glLight*`/`glTexEnv*`/`glAlphaFunc`/`glFog*`/`glNewList`/`glCallList`/…).
  - **This corrects the previous entry's conclusion, which was wrong.** It said the remaining rejected calls "are not what is blocking a capture" because RenderDoc "never gets far enough to look at them". The truth is the exact opposite: the deprecated call mix is **precisely and solely** what blocks it. Three consequences for planning:
    1. **The target is zero, not "small".** 2,036 rejected calls a frame and 1 are equally fatal; there is no partial credit and no threshold.
    2. **Scope is the whole process, not the frame.** The kill is permanent from the first offending call, so fixed-function used during *startup and loading* — long before the frame anyone wants to capture — disables capture for the rest of the run. Cleaning only the in-match frame cannot work.
    3. **The census is now a direct progress metric** against a known finish line, and every remaining class in it (including the `ff-enable`/`ff-query` ones the older census basis excluded) has to reach zero.
  - **Superseded: the elimination table below.** Fifteen hypotheses were eliminated before the source read; they are kept because each is a genuine negative result, but the cause was none of them. Recorded so the next attempt does not repeat any of them. The two diagnostics that would end the guessing are both unavailable in the shipped build: `RENDERDOC_DEBUG_LOG_FILE` writes nothing (logging is compiled out — verified against the app that *does* capture, under both `renderdoccmd` and direct `LD_PRELOAD`), and `librenderdoc.so` is **stripped**, exporting only the C API, so there are no internal symbols to break on in gdb. **The concrete next step is a RenderDoc built from source with logging enabled**, which turns a silent refusal into a stated reason; further black-box guessing has hit diminishing returns.

    Additional eliminations beyond the six below: **thread pool** (32 threads spawned before GL init — captures), **context detach/reattach** (the `LoadLock` `MakeCurrent(nullptr)` pattern — captures), **window flags/size** (borderless + resizable at 2560×1440 — captures), **SDL video-driver env** (engine's exact env — captures), **injection method** (`renderdoccmd capture` and direct `LD_PRELOAD` both work for the app, both fail for the engine), **`--opt-hook-children`** (no change), **GL entry-point hooking** (`glXMakeCurrent`, `glXSwapBuffers`, `glXCreateContextAttribsARB` all resolve into `librenderdoc.so` in *both* processes), and **timing** — the engine is refused at the *earliest possible* probe, 0.207 s, immediately after `MakeCurrentContext` and before the engine has issued a single GL call of its own, as well as at swap 1, swap 5 and mid-game. A false lead worth recording: `ldd build/spring | grep -i sdl` appearing empty suggested a statically-linked SDL2; it was a cwd-reset artefact, and the engine uses the same system `libSDL2` as the test app.

    New infrastructure from the dig: `SPRING_RDOC_PROBE=<n>` (n=0 probes right after context creation, n>0 brackets swap n) and `/renderdoccapture start|stop`.
  - **Bisection — the first six, each with a clean experiment:**

    | # | hypothesis | experiment | verdict |
    |---|---|---|---|
    | 1 | legacy `glXCreateContext` context | trace: it is SDL's probe, destroyed at call 49; real context is `glXCreateContextAttribsARB` | not it |
    | 2 | context version too low | request compat 4.5 instead of 3.0 | not it |
    | 3 | compatibility profile unsupported | minimal app, engine's exact request, captures | not it |
    | 4 | RenderDoc's frame-boundary detection | explicit `StartFrameCapture(nullptr,nullptr)` | refused too |
    | 5 | RenderDoc's active-window tracking | explicit GLX `Display*` + `Window` handles, plus `SetActiveWindow` | refused too |
    | 6 | mimalloc `operator new/delete` override | minimal app linked against the engine's mimalloc | captures fine |

    RenderDoc refuses even when the device and window are named explicitly, so it has not registered the engine's context as capturable **at all**. Note `RENDERDOC_DEBUG_LOG_FILE` produces no file even for the app that captures successfully, so RenderDoc's own log is not available as a diagnostic in this build.
  - **Still untested, in suspicion order:** the glad loader's function-pointer resolution (though `GL_EXT_debug_tool` reads back true, so *some* calls are hooked), threading, SDL window flags / window re-creation during startup. Keep bisecting the minimal app *towards* the engine — it reproduces everything except the refusal, and `test/gl-ab-compare/rdoc_profile_test.c` is the scaffold.
  - **`ForceCoreContext=1` is a silent no-op on its own** (unrelated engine finding, worth fixing independently): `CreateGLContext` requests `GLContextMajorVersion`/`MinorVersion`, which default to **3.0**, and a core profile needs ≥3.2 — so the driver hands back a compatibility context and the log still says `(Compat)`. Getting core requires raising those two knobs as well (4.5 works, and then the engine crashes at 0.4 s in Mesa because startup is still fixed-function — which is the honest state of the port).

- 2026-07-31 — **DENSE_MV is gone from the world: the reclaim-field hulls are converted, BAR-side. Total immediate-mode fallback vertices 68.3M → 0.85M per 880 frames (-98.8%).**
  - **The census had to be taught to name names first.** A fallback *reason* says which gate rejected a stream, never whose content produced it, so `LuaImmediateBuffer`/`LuaOpenGL` now tag each flush with the Lua `file:line` and `/luaimmfallback` ranks the top sites per reason by vertices. Command-list replays needed their own tag — a captured stream reaches the flush with no Lua stack, so its fallbacks were being credited to whichever live draw ran last, which put the entire dense-MV mass on an unrelated `gl.TexRect` helper (visible as 352 verts per "flush" on a line that draws one 4-vertex quad). `LuaCommandList` now carries the `gl.CreateList` site.
  - **Result: 100% of dense-MV WORLD vertices were `gui_reclaim_field_highlight`**, from two captured lists — 88,913 replays of the `GL.TRIANGLES` gradient fill (60.7M verts, ~683/flush) and 88,913 of the `GL.LINE_LOOP` edge (6.7M, ~76/flush), about 101 clusters a frame. The standing guess had named the right widget and the wrong path: this is captured-list replay, not live `gl.BeginEnd`.
  - **The conversion (BAR branch `bruno/ab-compare-widget-guards`): per-cluster VBO + shader.** Geometry uploads once per SHAPE change; colour, both alphas, the fade and the pop-in scale are uniforms. A vertex carries only position + an alpha selector (0 = fill, 1 = gradient), so the shader interpolates exactly what FF interpolated between the same two per-vertex colours. This also deletes the rebuild-on-fade machinery outright — alpha used to be baked into the recorded `glColor`, so every fade tick rebuilt the geometry under a per-frame rebuild budget; both are now unnecessary and removed.
  - **It needs nothing from this engine branch.** `gl.GetVBO`/`GetVAO`/`gl.LuaShader` and the `cameraViewProj`/`fogParams` engine UBO are all released-engine surface, so the change is upstreamable to BAR independently of the migration.
  - **Two things a `cameraViewProj` shader cannot inherit, both caught before they shipped:** the gradient layer was wrapped in a fixed-function `glTranslate(0,-1,0)`, and FF fogged these draws whenever `GL_FOG` was on for the pass. Both are now explicit — a y-offset in the transform uniform, and a fog factor read once per pass from `gl.GetFixedState("fog")` using the engine's own LINEAR-fog math.
  - **Verified three ways**, because the census alone cannot tell "converted" from "drawing nothing": census `DENSE_MV` WORLD 67.5M verts → **0**; a screenshot at a fixed frame is unchanged from the original widget (5,371 px differ by >8, all HUD counters/fps text/moving units); and a control run with only `hullGL.Draw` suppressed differs by 2,803,517 px — 522× more — proving the hulls dominate that view and really are being drawn.
  - Remaining fallbacks are all SCREEN: `PERSPECTIVE_P` 129,594 flushes / 844k verts (`r2thelper` `gl.TexRect`, `gui_top_bar`, `gui_pip`) and `DENSE_MV` 2,618 / 10k (`gui_top_bar`).

- 2026-07-31 — **`TEX_MIP_INCOMPLETE` converted (the second SAMPLES-NOTHING reason), and the gate runners now assert they ran.**
  - `b623d7c68e` — FF treats an INCOMPLETE texture as texturing DISABLED for the whole unit (GL 2.1 3.8.15) and rasterizes the plain vertex colour, which is what the parity-proven untextured program draws — so these streams convert instead of falling back, exactly as `TEX_2D_DISABLED` did. Since the unit is disabled, every *sampling* gate is moot, so the completeness check moved ABOVE texgen/texenv/texture-matrix/alpha-format instead of sitting after them; it keeps the "nothing else textures this fragment" guards (other unit-0 target, other FF unit) and additionally refuses an enabled 1D target — the spec takes 1D down with the unit, but that corner is untestable on BAR content and refusing it costs no coverage. Censused watertest 880 frames: `TEX_MIP_INCOMPLETE` **8,809 flushes / 36,340 verts → 0**, neither guard firing; total fallbacks 320,256 → 309,958. Gates: watertest signal 0/770, idletest 0/529, `AB_FORCE_LEGACY` null test 0/741.
  - **Remaining fallback census after this** (watertest, 880 frames, draw-frame counts drift ±5% run to run): `DENSE_MV` 182,012 / 68.1M verts (WORLD 179,442 — the volume king, and BAR-side work), `PERSPECTIVE_P` 127,946 / 833k (SCREEN only). Every texture-gate reason is now zero on local content; the two structural blockers are all that is left.
  - `94f6d361e9` — **the gate runners assert a minimum compare count** (`test/gl-ab-compare/run_gate.sh`, `run_census.sh`). A run that compared nothing greps identically to a clean one, and the previous session read "clean" off two no-op runs: the engine STRIPS `GLFrameABCompare` from the write-dir config on exit, and a `cd`-reset once made a run exit 127 while the stale infolog still held the previous run's passes. The runners re-apply the config keys, pin `GLFrameABCompareInterval=1` so the log count IS the compare count, truncate the infolog, use an absolute binary path, and fail on a nonzero exit, on fewer than `--min-compares` frames (default 400), and on any nonzero control or signal.

- 2026-07-31 — **Residual A/B control flake characterised: an async single-pass perturbation, NOT the modern backend.** The gate had a rare nonzero control (~1 frame in 750, roughly 1 run in 3). Findings, in the order they settled it:
  - **Null test (`AB_FORCE_LEGACY=1`, all four passes legacy) reproduces it**, so it is content/engine/driver nondeterminism with no modern-path involvement — every `signal=0` result stands.
  - **Frames are bit-identical apart from a handful of pixels**: 4-17 px, always on thin lines/edges (dashed command lines, metal-spot rings, LOS-boundary arcs), with *zero* sub-threshold (1 LSB) differences anywhere else in the 3.7M-px frame. Symptoms split into position shifts and blend-strength changes in both directions.
  - **`gui_metalspots` eliminated by controlled experiment** (write-dir stub shadowing BAR.sdd): identical rate with the widget removed, 3 flake frames per 10 runs either way. An apparent "all samples are on metal spots" fingerprint was a false lead.
  - **The pairwise breakdown is decisive** (`f05c3df581`, logged on any nonzero compare): exactly ONE pass of four differs and the other three are bit-identical — and *which* pass varies randomly across catches (pass 1 x3, pass 2 x2, pass 3 x1). That rules out warmup (isolates pass 0), per-pass state advance (monotonic 0!=1!=2!=3), accumulation (progressive), and differing depth/z-fighting (three passes would not agree bit-exactly). The per-pass snapshot/restore of the unsynced RNG, `guRNG` and the draw clock is confirmed working by the same result.
  - `ABCapture` is `glFinish()` + synchronous `glReadPixels` — no PBO ring — so the perturbed pass genuinely *rendered* differently rather than being read back wrong.
  - **Remaining explanation: an asynchronous event landing inside one render** (driver-side residency/recompile, or a worker thread mutating data the draw reads). Deterministic logic cannot produce a random outlier pass.
  - **Practical status: tolerated by design, not a blocker.** `ABBuildNoiseMask` marks control-differing pixels and `ABMaskedDiff` excludes them, so it costs one frame of control validity per ~750. Pass 0 legitimately differs (lazy once-per-frame work) and is not compared.
  - **CORRECTION (2026-07-31): the mask does NOT make a flake harmless in every case.** It protects only while the perturbed pass is 1 or 2, where the delta lands in the control and gets masked. When the flake hits pass **3** — the modern pass — the control is clean, nothing is masked, and it reads as a pure one-frame SIGNAL. Caught in the act: a gate run reported `signal(L<->M)=4 px (max 127)` on 1 frame of 790 with `1v2=0/0`, i.e. exactly the one-outlier-pass signature, and the same content with the change reverted produced 3 CONTROL frames at the same max delta 127. So roughly one flake in three surfaces as false signal. A real backend divergence is deterministic and repeats; the flake does not — **re-run before believing a one- or two-frame signal**, which `run_gate.sh` now says out loud when it sees one.

- 2026-07-31 — **Task #8 done: apitrace census of a pure-modern frame, plus a per-reason fallback census.** Traced a `LuaModernGLBackend`+`LuaCommandLists`+`FontUseMVPUniform`+`RenderBufferUseMVPUniform` frame with `GLFrameABCompare=0` (the 4-pass gate would have put its two LEGACY passes in the trace). One settled in-game frame = 66,794 GL calls, **84.8% RenderDoc-rejected**, attributed by walking the engine's `glPushDebugGroup` stack:
  - **Display lists are dead, measured**: `glNewList` = **0**, a single `glCallList`. Phase 2 validated against a real frame.
  - By class: immediate 55,399 (82.9%), FF matrix 697, attrib stack 194, FF lighting 149, texenv/logicop/clipplane 57, point sprite 56, alpha test 18, client arrays 6, line stipple 1.
  - By scope: **`Draw::World` alone is 94.5% of every rejected call** (53,452 / 54,228; 132 `glBegin` blocks averaging ~415 verts). `Draw::Screen::DrawScreen` 2,289 / 3,680. The engine's own drawers are already clean — ROAM 0/1153, particles 0/68, opaque models 8/1144 (attrib stack only).
  - **The modern backend's own legacy footprint is 472 calls (0.7%)**, all reads: 172+172 `glGetFloatv(GL_{PROJECTION,MODELVIEW}_MATRIX)` (the uMVP bridge) + 128 `GL_CURRENT_COLOR` (FF color seeding). It emits no FF state — but those reads are why the FF matrices must still be set (Phase-0 dependency).
  - **Fallback-reason census** (new: config `LuaImmediateFallbackStats`, dump via `/luaimmfallback`; `PlainModulateTexturing` now returns the rejecting sub-gate). 880 watertest frames: `DENSE_MV` 188,872 flushes / 66.6M verts (WORLD 186,474, ~357 verts/flush — the volume king), `PERSPECTIVE_P` 190,033 / 2.6M (**SCREEN only**, 13.6 verts/flush), `TEX_2D_DISABLED` 3,725 / 14,900 (SCREEN), `TEX_MIP_INCOMPLETE` 48 / 1,296 (SCREEN).
  - **Finding that redirects the screen work:** the screen path's blocker is the **projection**, not texturing — perspective is ~97% of screen fallbacks by count, texture gates only ~2%. The tilted top-bar UI was a known parity hazard but had never been quantified as *the* screen-path cost. Two leads follow: `TEX_2D_DISABLED` streams carry texcoords while texturing is DISABLED, which is exactly what the parity-proven untextured shader already does (so they should convert, not fall back); and the perspective finding was measured on LINEAR-sampled high-frequency textures while flat fills stayed byte-exact, so the gate may be splittable to admit UNTEXTURED perspective streams.
  - Caveat when sizing conversions from these numbers: the fallback counters include `gl.Rect`/`gl.TexRect`, not just `gl.BeginEnd`, so they do not share a denominator with the apitrace frame's `glBegin` count.

- 2026-07-04 — **Coverage breadth (divergence-plan step 2) — dev-content gates all 0/0; three engine parity-bug families found and fixed by the replay content.** Ran the gate beyond fightertest: a Supreme Isthmus 24-player replay (draft mode, reflection/refraction, chat/playerlist churn), a local Supreme Isthmus water test (BumpWater reflections + PiP + camera waypoints over water + buildmenu/build orders) and an idle base-building start. Engine fixes (all found by the gate, localized via the null test + apitrace pass-diff + dump PNGs, exactly per the documented recipe):
  1. **Font uniform-cache desync via display-list compiles** (`778abc2866`): glUseProgram/glUniform are RECORDED into `gl.CreateList` bodies, not executed (earlier comments had this backwards!) — `CglShaderFontRenderer::PushGLState`'s cached `SetUniform("uUseMVP",…)` during a compile updated the CPU cache but never the GPU, so later "redundant" legacy writes were skipped and whole text elements vanished from legacy passes (replay pregame "Victory condition" text, ~12.5k px on 40% of frames, control=0, null-clean; apitrace showed the identical draw with no glUniform reaching the legacy pass). Fix: skip the entire shader path when `GL_LIST_INDEX != 0` (the recordable-FF flush needs no shader).
  2. **Modern gl.Rect/TexRect/BeginEnd inside gl.CreateList** (`92c205e135`): the modern flush recorded glDrawElements aliasing the stream VBO (replays whatever occupies those offsets later — vanishing startbox polygons, phantom playerlist icons) + recorded program/uniform calls (bug 1's class). All modern dispatch sites now compile the legacy FF path (glBegin/glEnd IS the recordable representation).
  3. **Polygon-mode + FF-current-color + texture-enable parity** (`92c205e135`, texture-enable follow-up): glRectf honors `glPolygonMode` (unit_share_tracker draws GL_LINE outline rects — per-call compare delta 232; fix dropped replay in-game signal from 904/1043 frames to 7/177); legacy fills/modulates with the FF CURRENT color (not LuaOpenGL's tracked `color[]`, which is blind to dlist replays/recordable font flushes) and leaves it unchanged unless the body called glColor — the flushes now seed from `glGetFloatv(GL_CURRENT_COLOR)` and restore the exact legacy end-state (new `SeedColor`/`Color` split); gl.TexRect only samples when `GL_TEXTURE_2D` is enabled (modern shader would sample regardless).
  - **Replay-gating infrastructure discovery:** demos mount the demo's pinned release `.sdp` archive, NOT `games/BAR.sdd` — BAR-branch guards are invisible in replay runs. Replay gating needs write-dir shadow widgets = release widgets (GitHub raw at the recorded commit, e.g. `test-30440-535fc79` → `535fc79`) + the guard patterns. Shadow set built for screencopy (per-passIndex key — REQUIRED for the signal to be meaningful), guishader, flowui, bloom, pregameui×2; with it the replay pregame control went 9 leaking frames → 0/2934. See `test/gl-ab-compare/README.md` (drivers + local content scripts committed there; `AB_DUMP_MIN_FRAME` env keeps the dump budget for in-game frames).
  - BAR-branch guards (`f90a198daa`): pregameui/draft countdown blink timer advanced per DrawScreen = per pass (233↔255 flip = the max-22 fightertest pregame leak); guishader stencil/delete-queue per-pass freeze; FlowUI guishader-dlist lifecycle freeze.
  - **Results after the first batch:** fightertest 279/279, water-map 644/644, idle-start 410/410 — control=0 AND signal=0 (incl. pregame). The replay still had a modern-linked residual tail, burned to zero the same day (below).
- 2026-07-04 (later) — **REPLAY GATE FULLY BYTE-PERFECT: control=0 AND signal=0 on all 3871 pregame + 1071 in-game compared frames of the Supreme Isthmus multiplayer replay (to f=30000), plus fightertest 271/271, water-map 652/652, idle-start 410/410 with the same binary.** The residual tail decomposed into four more engine fixes, all localized with the null test + a 31 GB apitrace of the pregame→in-game window (glretrace crashed on the trace, so the decisive evidence came from a full normalized call-stream diff of pass 2 vs pass 3 plus blob extraction — vertex payloads identical, index shifts uniform, leaving only the color calls):
  1. **Overbright color wrap** (`fe76b4acb0`, the dominant class): widgets set out-of-range colors (minimap camera box: `glColor4fv(.., b=1.15)`). FF clamps at rasterization (blue→255) but keeps the unclamped value in the CURRENT-color state; SColor's raw float→uint8 cast WRAPS (1.15·255=293→37) — the box drew blue in legacy, mustard in modern (the max-218 class, apitrace-confirmed digit for digit: trailing `glColor4ub(186,170,37)`). Also explained the playerlist-icon and late-game classes. Fix: `ClampedColor` (clamp + round) everywhere the modern path quantizes.
  2. **Quantized fade alphas** (same commit): animated fades (α=0.1595653…) aren't 8-bit-representable → max 4–12 px tails. Single-color streams (gl.Rect, gl.TexRect, single-color BeginEnd = every fade) now pass the exact float color via a new `uColor` uniform (vertex colors white); `FlushLegacy` replays original float colors; flushes restore the FF current color from floats (`glColor4fv`), so unclamped state round-trips.
  3. **GameSetupDrawer countdown per-pass advance** (`ad8ff67244`): the engine's "Starting in n" pregame text (drawn as fallback in replays — BAR's pregame widget doesn't claim GameSetup for spectators) advanced `readyCountdown` by raw wall clock once per PASS; the digit could flip between compared passes. Pinned to the first pass via `LuaUnsyncedRead::IsABDuplicatePassRaw()`.
  4. **TypedRenderBuffer mid-frame grow corrupted the VAO** (`05cfcd8901`, latent vanilla bug): `Resize()` recreates the buffer object but the VAO (initialized once) kept referencing the deleted old buffer — uploads went to the new storage while draws sourced the stale old one. Found because 4 in-frame passes accumulate 4× stream data. Fix: re-`InitVAO` + full re-upload after a grow.
  - Also `452d3d4ce1`: modern gl.TexRect falls back to legacy when `GL_TEXTURE_2D` is disabled (legacy doesn't sample then).
- 2026-06-30 — initial consolidation from static analysis. Key conclusions below are static-grep-derived and **must be confirmed at runtime** (`DeprecatedGLWarnLevel=2` + RenderDoc reject log). Nothing built yet.
- 2026-06-30 — closed the tracer-coverage gap: added `CondWarnDeprecatedGL(L, __func__)` to `Rect`, `TexRect`, `DrawGroundQuad`, `PushAttrib`, `PopAttrib` in `rts/Lua/LuaOpenGL.cpp` (they previously fired no warning). `DeprecatedGLWarnLevel=2` now covers all relevant Lua-side blockers. Deliberately NOT added to `gl.PolygonMode` and `gl.Texture` — both are core-valid (not RenderDoc blockers) and would flood the log with false positives.
- 2026-06-30 — adopted a **test-forward, oracle-backed** method: legacy path (behind the flag) is the oracle; added a Compatibility contract (C1–C6), a write-test-first loop with a green-suite commit gate, harness positive/negative controls, and per-phase done-gates. Legacy is not deleted until the whole contract is green over unit scenes + a replay bake.
- 2026-06-30 — **In-engine compare is now a committed, repeatable integration test.** `test/engine/Rendering/glcompare_integration.sh` (+ `glcompare_startscript.txt`, `README.md`) runs the GL `spring` with `LuaGLCompareMode=1` over a BAR startscript and exits non-zero if any wired `gl.*` callin diverges >1 byte, the compare FBOs fail, or nothing was compared. Turns the ad-hoc validation into a one-command PASS/FAIL artifact. Current result: **PASS — 89 wired callins, max byte delta 1.** (Manual/integration: needs a display + local BAR data + the GL binary; not a ctest unit test.)
- 2026-06-30 — **Broadened emitter unit coverage** (`testGLImmediateEmitter`, now 13 cases/68 asserts): all primitive modes (TRIANGLE_FAN/STRIP, QUAD_STRIP, LINE_STRIP/LOOP, POINTS) A/B ≤1 LSB; **C4 translucent draw-order** (two overlapping half-alpha quads, SRC_ALPHA blend, legacy vs modern composite identically); **C5 edge cases** (empty BeginEnd, single vertex, degenerate zero-area triangle — no crash, no GL error). - 2026-06-30 — **Attrib-stack swap analyzed — deferred, bundle with FF-reset removal (not a standalone win).** Investigated replacing `glPushAttrib`/`glPopAttrib` (the one seemingly content-independent Phase-0 piece). Conclusion: it can't improve RenderDoc-cleanliness on its own, because restoring the saved `AttribBits`/`ResetGLState` state (mostly fixed-function: alphaFunc/shadeModel/logicOp/texEnv/lighting/texgen/lineStipple/clipPlanes/pointSprite/material/current-color) requires re-emitting those deprecated FF calls. Also `State.h` is a RAII state-*management* system (`GL::SubState`), not a save/restore snapshot. **Remaining task: do the attrib-stack swap together with turning `ResetGLState`'s FF resets into no-ops/shader-defaults**, once content no longer depends on FF state — see the annotated Phase-0 fix list. Until then there's no incremental Phase-0 win; the lever is converting content off FF (uniform/UBO-MVP, display lists, residuals).
- 2026-06-30 — **Phase-0 uniform-MVP prerequisite documented; general textured BeginEnd attempted and (correctly) deferred.** Added the Phase-0 ⚠ TODO above (uniform/UBO MVP conversion is the real blocker — the font renderer + RenderBuffer shaders read `gl_ModelViewProjectionMatrix`). Then tried modernizing *general* textured `gl.BeginEnd` (`VA_TYPE_TC` + the MODULATE shader). The in-engine compare immediately caught it: `gui_pip`'s textured overlays (`DrawWaterAndLOSOverlays`, a `drawFn`) diverged Δ255 — real widgets use texture-unit/texenv setups a single MODULATE-on-unit-0 shader can't reproduce, and `gui_pip` leans on `gl.UseShader` + multitexture. Reverted textured BeginEnd to the exact legacy replay (Δ0); `gl.TexRect` (a controlled single MODULATE quad) stays modernized. Net: `TriangulateForModern` is now generic (`VA_TYPE_C`/`VA_TYPE_TC`) for when textured BeginEnd is revisited with texenv/unit awareness. The harness catching this pre-merge is exactly the test-forward payoff.
- 2026-06-30 — **Smooth-shaded QUADS/POLYGON now tested — no gap.** A per-vertex-colored `GL_QUADS` and `GL_POLYGON` A/B vs legacy come out **≤1 LSB** on llvmpipe: the modern path's `v0-v2` quad / fan triangulation matches Mesa's FF decomposition diagonal even with interpolated colors. (A driver whose FF used the *other* diagonal could differ near the diagonal for smooth fills, but the modern triangulation is the canonical result going forward — the legacy compare is only the transitional oracle.) All Tier-1/2 contract dimensions now have coverage.
- 2026-06-30 — **C3 deepened to a full state snapshot.** The state-leak test now captures program, VAO, `GL_ARRAY_BUFFER_BINDING`, blend enable + func, depth test + write-mask, active texture unit and bound `GL_TEXTURE_2D` before/after `FlushModern` and asserts the whole set is preserved (with a distinctive non-default pre-state). Result: the modern flush leaves all observable restorable GL state intact (incl. the array-buffer binding) — FF-only state it intentionally abandons (current color, matrix stack) is excluded as an expected divergence, not a leak.
- 2026-06-30 — **Matrix-tracker randomized property test** (`testGLMatrixStateTracker`, now 18 cases / ~4285 asserts): 3000 random ops (Translate/Scale/Rotate/Push/Pop/mode-switch) mirrored into an independent per-mode `std::vector<CMatrix44f>` stack, asserting the active matrix matches after every op — exercises the stack/mode bookkeeping far beyond the fixed cases. Fixed seed for reproducibility.
- 2026-06-30 — **`gl.BeginEnd`/`Vertex`/`Color`/`TexCoord` wired (gated) + validated in-engine.** `gl.BeginEnd` now uses capture-then-render when `(LuaModernGLBackend || LuaGLCompareMode)` and no shader is bound: the callback's `gl.Vertex`/`Color`/`TexCoord` route into `LuaImmediateBuffer` (no FF calls) via an `inModernBeginEnd` flag; after the callback the captured stream is compared (both flushes) and/or rendered (modern or legacy). The emitter now stores `VA_TYPE_TC` (captures texcoords) + a `textured` flag; `FlushLegacy` replays texcoords for an exact legacy equivalent, and `FlushModern` falls back to `FlushLegacy` for textured streams (general textured BeginEnd deferred). MVP from `glGetFloatv` (Phase-0 bridge); `NoShaderBound()` guard. Unit tests extended (textured-BeginEnd capture/fallback; 10 cases/43 assertions). **In-engine compare re-run (Starwatcher, f400): 86 wired call-sites — 83×Δ0, 3×Δ1, zero Δ>1, no FBO-unavailable** — `gl.BeginEnd` across real BAR widgets (LuaIntro, unit_seismic_ping, gfx_unit_shield_effects, …) is pixel-identical (≤1 LSB) to legacy. Legacy path byte-unchanged when both flags off.
- 2026-06-30 — **FIRST IN-ENGINE VALIDATION over a real BAR frame — passed, and caught+fixed a real bug.** Ran `build/spring` (GL/legacy) with `LuaGLCompareMode=1` on a short auto-quitting BAR startscript (Starwatcher map, `setspeed 20`, `quitforce` at f400; `DISPLAY=:0 SDL_VIDEODRIVER=x11`, `--isolation --write-dir … --config <tmp>`), reached **in-game frame ~410**, and the comparator logged real widgets' `gl.Rect`/`gl.TexRect` (`gui_top_bar`, `gui_given_units`, `unit_respawning`, LuaIntro loading screen, …). **First run: 27×Δ0, 1×Δ1, and 2×Δ255** in the offline LUT bakes `GenBrdfLut.lua`/`GenEnvLut.lua`. Root cause (a real wiring bug): `gl.UseShader` calls `glUseProgram` **directly** (LuaShaders.cpp:835) without notifying `shaderHandler`, so the `GetCurrentlyBoundProgram()==nullptr` guard missed the bound LUT shader and the modern path overrode it. **Fix:** guard on the live GL state via `glGetIntegerv(GL_CURRENT_PROGRAM)` (`NoShaderBound()`), which catches engine *and* Lua-bound shaders. **Re-run: 27×Δ0, 2×Δ1, zero Δ>1** — the modern `gl.Rect`/`gl.TexRect` path is now pixel-identical (≤1 LSB) to legacy across every real BAR UI call-site, and shader-bound primitives correctly fall back to legacy. (This is the C6 evidence the unit tests couldn't give.)
- 2026-06-30 — **In-engine `LuaGLCompareMode` (closes the wired-shim validation gap).** New config `LuaGLCompareMode` (bool, default off). When on, each wired callin (`gl.Rect`/`gl.TexRect`, no user shader bound) is drawn **both ways** — legacy + modern — into two viewport-sized offscreen FBOs over a common cleared bg, read back, and the **max per-byte delta is logged once per Lua call-site** (`[file]:line`); the real frame still renders normally. Implemented as `LuaGLCompare::CompareDraws(w,h, legacyFn, modernFn)` in `LuaImmediateBuffer.cpp` (saves/restores FBO binding, viewport, clear color) + a dedup logger in `LuaOpenGL.cpp`. The `CompareDraws` helper is unit-tested (confirm: identical draws → Δ0; deny: shifted rect → Δ>0). This is the live-replay vehicle (contract C6) to pixel-validate the wired path over real BAR frames — run a replay with `LuaGLCompareMode=1` and watch the log. `gl.Rect`/`gl.TexRect` refactored to legacy/modern draw lambdas reused for both compare and the normal draw. Engine-headless + engine-legacy compile/link clean; full suite green (bar the 2 env failures). (Still can't be *run* in this sandbox — needs BAR content.)
- 2026-06-30 — **First live-path wiring: `gl.Rect` + `gl.TexRect` dispatch to the modern emitter (gated).** Added config `LuaModernGLBackend` (bool, default **off**). When on (and no user shader is bound — else legacy must feed that shader), `gl.Rect`/`gl.TexRect` draw via `LuaImmediateBuffer` (modern) instead of `glRectf`/`glBegin`; off ⇒ legacy path byte-unchanged. `LuaImmediateBuffer.cpp` added to the engine build (`rts/Lua/CMakeLists.txt`); engine-headless + engine-legacy both compile/link clean. MVP bridge: until Phase 0, the wiring reads the current MVP off the fixed-function stack via `glGetFloatv(GL_PROJECTION/MODELVIEW_MATRIX)` (a query, not a deprecated set-call) so the modern shader gets the engine-scaffolding transform; setting `LuaModernGLBackend` also forces matrix tracking on. **Validation gap (honest):** compile-verified + gate-off-safe + the emitter is unit-validated, but the *wired shim* (Lua args→emitter, glGetFloatv MVP, shader-bound guard) is not yet pixel-validated in a running engine — needs the in-engine `LuaGLCompareMode` or a manual BAR run with the flag on. Deferred: `gl.BeginEnd`/`Vertex`/`Color` wiring (stateful routing + custom-shader interaction).
- 2026-06-30 — **Emitter C3 (GL state side-effects) check.** Added a test asserting the modern flush leaks no transient GL state — after `FlushModern`, `GL_CURRENT_PROGRAM` and `GL_VERTEX_ARRAY_BINDING` are restored to their pre-flush values and `glGetError()` is clean (shader `Enable`/`Disable` and VAO bind/unbind balance). Note: FF-only state the modern path intentionally abandons (`GL_CURRENT_COLOR`, the matrix stack) is an *expected* divergence from legacy, not a leak — C3 here verifies the modern backend restores what it temporarily touches. Emitter now covers (all A/B green on llvmpipe): BeginEnd pos+color, QUADS/QUAD_STRIP/POLYGON triangulation, LINES, sticky color, TexRect (MODULATE), and no-state-leak.
- 2026-06-30 — **Emitter textures: `TexRect` (the dominant textured primitive, 46 BAR files).** Added `LuaImmediateBuffer::SetTexRect` + `FlushTexRect{Legacy,Modern}`: legacy = `glBegin(GL_QUADS)` + `glTexCoord`/`glColor`/`glVertex` (FF MODULATE); modern = `TypedRenderBuffer<VA_TYPE_TC>` (2 tris) + a textured uniform-MVP shader (`outColor = texture(tex,uv) * vcolor`, MODULATE), sampler on unit 0, no FF matrix/enable. Caller binds the texture (as `gl.Texture` does). Test: a 2×2 NEAREST texture, white color, A/B legacy vs modern ≤1 LSB, plus an absolute texel check (lower-left quadrant = red) proving the modern path is actually textured.
- 2026-06-30 — **Emitter primitive coverage: QUADS/QUAD_STRIP/POLYGON triangulation.** `LuaImmediateBuffer::FlushModern` now triangulates the non-core modes (QUADS→2 tris, QUAD_STRIP, POLYGON→fan) since the modern/core path can't draw them; other modes pass through. Tests added: `GL_QUADS` flat fill is **bit-exact** vs legacy (triangle union == FF quad coverage), `GL_POLYGON` flat pentagon and `GL_LINES` (width 1) within ≤1 LSB. Smooth-shaded quads/polys can still differ by the diagonal/fan choice — deferred until needed (flat fills are the common case).
- 2026-06-30 — **Unified `lua_State`-free emitter landed (`LuaImmediateBuffer`).** New `rts/Lua/LuaImmediateBuffer.{h,cpp}`: accumulates a pos+color immediate-mode stream (sticky color), then flushes it either **Legacy** (`glBegin`/`glColor`/`glVertex`, fixed-function matrix) or **Modern** (`TypedRenderBuffer` + its own uniform-mat4-MVP shader, zero FF matrix calls; MVP via `SetMVP`, in-engine from `GLMatrixStateTracker`). Both flushes consume the SAME accumulated stream, so the two paths get byte-identical input by construction — this is the backend-select-by-flag + run-both-to-compare spine the doc's method needs. Test `testGLImmediateEmitter` drives one scene and flushes it both ways into two FBOs: flat quad, smooth-colored triangle, and sticky-color cases all match within ≤1 LSB under llvmpipe. The shader handles the explicit/non-explicit attrib-location cases like the engine's RenderBufferShader. Scope of this iteration: pos+color (VA_TYPE_C) + core primitive modes; **next:** QUADS/POLYGON triangulation, textures (TexRect/`VA_TYPE_TC`), GL-state side-effects (C3), then wire `gl.BeginEnd`/`Rect`/`TexRect` to dispatch to the emitter (flag-gated, legacy fallback). Test CMake refactored to a shared `add_engine_gl_test` macro + engine-source list.
- 2026-06-30 — **Modern backend validated against the real engine geometry path.** Added a case to `testGLRenderBufferCompare`: engine `TypedRenderBuffer<VA_TYPE_C>` geometry + a **uniform-`mat4`-MVP engine shader** (built via the real `shaderHandler`; attribs at explicit locations 0/1 matching `VA_TYPE_C::attributeDefs`, so it consumes the engine VAO directly) that reads ONLY the `uMVP` uniform and **makes zero fixed-function matrix calls** (ignores `gl_ModelViewProjectionMatrix`). A/B vs legacy `glBegin`: ≤1 LSB, quad-color asserted at center. This is the RenderDoc-clean modern draw proven on the engine's real vertex machinery. **Decision: do NOT add the uniform to the shared `RenderBuffers.inl` shader** — `gl_ModelViewProjectionMatrix * transformMatrix * v` would impose a per-vertex `mat4*mat4` on every engine RenderBuffer user (fonts/particles/UI). The modern path gets its OWN shader (zero blast radius); its permanent home is the Lua-immediate backend (next: factor the `lua_State`-free emitter and wire `gl.BeginEnd`/`Rect`/`TexRect`, feeding `uMVP` from the `GLMatrixStateTracker`).
- 2026-06-30 — **Tier-2 A/B against the REAL engine renderer landed** (`test/engine/Rendering/testGLRenderBufferCompare.cpp`, target `testGLRenderBufferCompare`). Renders a flat quad via the actual engine path — `RenderBuffer::GetTypedRenderBuffer<VA_TYPE_C>()` + `Shader`/`VAO`/`VBO`/`StreamBuffer` — and A/B-compares vs legacy `glBegin` with the same MVP: **matches within ≤1 LSB**, and the engine buffer is asserted to actually contain the quad color at center (guards against a both-draw-nothing false pass). **This solves "how to unit-test the engine GL path" — the recipe:** (1) put the test in a subdir (`test/engine/Rendering/CMakeLists.txt`) that does `remove_definitions(-DUNIT_TEST)` (engine GL headers `#error` under `UNIT_TEST`); (2) compile with `-DNOT_USING_CREG` (drops `CMatrix44f`'s creg) and `-ffunction-sections -fdata-sections` + link `-Wl,--gc-sections` (strips `Shader.cpp`'s file/Lua shader-loading paths — `CFileHandler`, `LuaMatTexture`, `ParseTextureImage`, `LoadFromLua` — that the inline-shader RenderBuffer path never calls); (3) link the few light real sources the draw path needs (`MemoryOverride`, `StringUtil`, `Config/ConfigVariable`, plus the GL/Shaders `.cpp`s); (4) provide tiny inert stubs in `GLEngineStubs.cpp` for the rest — a zeroed `globalRendering` with `supportExplicitAttribLoc`/`active` set, `configHandler=nullptr` (only used behind a short-circuited `oldValid &&`, never dereferenced first compile), a no-op `glClearErrors`, and the never-executed `CFileHandler` ctor/Close/Read/virtuals (so its vtable links). No VFS, no window system, no config files. SKIPs headless; green under llvmpipe.
- 2026-06-30 — **Tier-2 modern-vs-legacy A/B landed + key shader finding.** Added `test/engine/Rendering/testGLMatrixDrawCompare.cpp` (target `testGLMatrixDrawCompare`) and factored the shared harness into `test/engine/Rendering/GLTestHarness.h` (context/FBO/comparator/legacy helpers; `testGLImmediateCompare` refactored onto it, still green). The new test renders the *same geometry* two ways — legacy fixed-function `glBegin` vs a modern **VAO + uniform-`mat4`-MVP GLSL-150 shader** — both fed the **identical `CMatrix44f` MVP built by the Phase-1 `GLMatrixStateTracker`** (loaded into the FF stack for legacy, set as the uniform for modern, so any delta is purely backend). Results under llvmpipe: **flat axis-aligned quad is bit-exact (maxAbsDelta=0)**; smooth-colored triangle and a tracker-transformed (Translate+Rotate) quad within ≤1 LSB. This exercises the Phase-1 tracker end-to-end on the GPU and proves a RenderDoc-clean draw reproduces legacy output. **IMPORTANT architectural finding:** the engine's stock `TypedRenderBuffer` shader (`RenderBuffers.inl`) transforms via `gl_ModelViewProjectionMatrix` — the deprecated fixed-function builtin, set by exactly the `glMatrixMode`/`glLoadMatrixf` calls we must remove for RenderDoc. So "re-back `gl.BeginEnd` onto `TypedRenderBuffer`" is **not** sufficient as-is; the modern backend needs a **uniform-MVP shader variant fed by the matrix tracker** (which is why this increment validated that path with a minimal hand-rolled VAO+shader rather than the stock TypedRenderBuffer). **Next:** bring up `globalRendering`(+configHandler)+`CFBO`+`TypedRenderBuffer` and add a uniform-MVP shader variant, then A/B the *engine* path; and factor the emitter `lua_State`-free for the LuaOpenGL integration.
- 2026-06-30 — **Tier-2 harness + controls landed** (`test/engine/Rendering/testGLImmediateCompare.cpp`, target `testGLImmediateCompare`). Hidden SDL2 compat context + glad + raw-GL FBO + glReadPixels + the comparator's positive/negative controls (incl. a 1-texel-flip deny case). Green under `xvfb-run`/llvmpipe; `SKIP`s with no display so GL-less CI stays green. SDL2+glad only (no engine globals yet) — the `globalRendering`+`CFBO`+`TypedRenderBuffer` "new backend" A/B cases are the next increment, gated on factoring the emitter `lua_State`-free. See the Tier-2 section for details.
- 2026-06-30 — **Phase 1, iteration 0 (matrix tracker) landed test-first.** Wrote `test/engine/System/testGLMatrixStateTracker.cpp` (Catch2, wired via `add_spring_test` in `test/CMakeLists.txt`, modeled on `testMatrix44fRotation`) covering C1/C5-matrix: post-multiply order/side for `Translate`/`Scale`/`Rotate`/`MultMatrix`, glRotatef-style axis normalization, bit-exact push/pop round-trip + nesting, three independent mode stacks, `Ortho`/`Frustum` vs `CMatrix44f::OrthoProj`/`PerspProj` goldens, and depth over/underflow vs `HasMatrixStateError`. Then extended `GLMatrixStateTracker` (`rts/Rendering/GL/MatrixStateTracker.h`) from depth/mode-only to hold real `CMatrix44f` value stacks per mode (the existing depth counters stay the canonical balance check and are kept in sync). Group-B Lua callouts (`gl.Translate/Scale/Rotate/Ortho/Frustum/LoadIdentity/LoadMatrix/MultMatrix/Billboard`) now drive the tracker, gated behind a new `LuaMatrixTracking` config flag (default **off** ⇒ byte-identical to the legacy FF-only path). To keep Tier-1 truly GL-free (`myGL.h` `#error`s on `UNIT_TEST`), the tracker's value-math compiles without the GL loader under `UNIT_TEST` (3 mode enums defined locally; the GL-calling `HandleMatrixStateError` is `#ifndef UNIT_TEST`). **Known limitations (follow-ups):** (1) the new value stacks are not yet isolated across nested callins / display lists (the `PushMatrixState`/`PopMatrixState` swap only moves the depth counters) — Phase 0 will seed/restore them per callin; (2) `Ortho`/`Frustum` track the plain `OrthoProj`/`PerspProj`, not the engine's clip-space-control variant (`__spring_glOrtho`'s extra translate/scale) — reconcile when the shader path actually consumes the MVP.
- 2026-06-30 — added **Testing & validation** section: Tier 1 = pure Catch2 matrix-tracker unit tests (no GL, like `testMatrix44f.cpp`); Tier 2 = offscreen dual-FBO A/B render test (new infra — engine has no GL-context test today; needs SDL compat context + `globalRendering` cap init + CFBO + Xvfb/llvmpipe). Prereq for Tier 2: factor the immediate emitter `lua_State`-free.
- 2026-06-30 — **first real runtime trace** (GL 4.6 Compat, master `2026.06.06-80-gf898efc`, includes the 5 new hooks — confirmed `Rect`/`TexRect`/`PushAttrib`/`PopAttrib` fire). See Runtime findings below. Headline: **Chili/Chobby/LuaMenu is a whole immediate-mode UI layer the static inventory missed** (menu-scope), and the static matrix undercounted because it never tracked groups B/Vertex/Color.
- 2026-07-01 — **Font renderer uMVP conversion landed + validated.** Both font vertex shaders (`vsFont330`/`vsFont130`, now in `rts/Rendering/Fonts/glFontRendererShaders.h`) gained `uniform mat4 uMVP; uniform bool uUseMVP;` with `gl_Position = (uUseMVP ? uMVP : gl_ModelViewProjectionMatrix) * vec4(pos,1)`; gated by config `FontUseMVPUniform` (default **off** ⇒ byte-identical legacy builtin path). MVP from the `glGetFloatv(PROJECTION)*glGetFloatv(MODELVIEW)` Phase-0 bridge, set per-draw in `PushGLState` (runtime-switchable). Validated two ways: Tier-2 unit test `testGLFontMVPCompare` (uUseMVP=0 vs =1 on a VA_TYPE_TC quad, ≤1 LSB) **and** an in-engine same-frame self-comparator (config `FontShaderMVPCompare`) that renders both branches and logs the delta — result **`[Font MVP compare] uMVP path matches builtin: max byte delta = 0`**. This is the #1 Phase-0 consumer of the FF matrix builtin; still needs the bridge swapped for a real CPU/UBO source to be FF-independent.
- 2026-07-01 — **Whole-frame A/B harness built (`GLFrameABCompare`) — and found a real modern-backend bug the per-call compare missed (contract-C4 class).** Splits `CGame::Draw`, re-renders the visible frame 3× flipping a global `LuaOpenGL::SetModernImmediate` + `CglShaderFontRenderer::SetUseMVPUniform` toggle (legacy/legacy/modern), reads back each via `glReadPixels`, and reports `net = |pass2−pass3| − |pass1−pass2|` (adjacent-pass diff cancels temporal drift; control = pass1↔pass2). On BAR it flagged the **gui_pip PiP minimap rendering ~1.4–2.4× brighter (washed out)** under the modern immediate backend — a state leak onto a *later* draw that per-call `LuaGLCompareMode` cannot see (every individual wrapped draw compared identical; the leak hits the engine/gui_pip minimap render that runs after).
- 2026-07-01 — **RenderDoc is blocked until FF is gone (verified), so debugging uses apitrace.** Engine creates a **compatibility-profile** context (`rts/Rendering/GlobalRendering.cpp:512`, `SDL_GL_CONTEXT_PROFILE_COMPATIBILITY`) because it still uses FF immediate mode + the FF matrix stack; RenderDoc only supports core 3.2+, so it can't capture the current engine — RenderDoc is the *destination* of the migration, not a tool available now. **apitrace does work** (traces compat/FF/immediate mode). Recipe used: prebuilt `apitrace-14.0-Linux` (GitHub release, no root); `apitrace trace -o t.trace -- env … ./spring …`; `apitrace dump --grep/--calls` to find frame/draw boundaries; **`glretrace -D <call> --dump-format=json`** to dump full GL state at a call; compare two dumps' `parameters` blocks (or `apitrace diff-state`). This is the concrete "GL state diff" method for the "identical pixels, different leaked state" bug class.
- 2026-07-01 — **Minimap-wash root-caused via apitrace and FIXED.** apitrace state-dump *at the minimap terrain draw* (`glDrawArraysInstanced` under gui_pip's shader) showed the only meaningful difference: **`GL_CURRENT_COLOR = [1,1,1,0.09]` modern vs `[1,1,1,1]` legacy**. gui_pip's minimap shader (compatibility profile) reads the built-in `gl_Color` for opacity, so the terrain drew at ~9% alpha and blended toward the bright background (lifted blacks / washed). **Mechanism:** gui_pip draws the minimap background quads with `gl.Color(1,1,1,1)` set *outside* the BeginEnd and a body that only emits TexCoord+Vertex — legacy `glBegin/glEnd` inherits that current color, but the modern capture reused `LuaImmediateBuffer`'s **stale `curColor`** (0.09 from a prior BeginEnd) because `Begin()` never seeded it. **Fix (3 legacy-parity changes):** (1) `LuaOpenGL::BeginEnd` seeds the capture's current color from the inherited `color[]` before running the callback; (2) `FlushModern` leaves FF current color = last vertex color (like legacy's per-vertex `glColor`); (3) `FlushTexRectModern` calls `glColor4ub(texRect.c)` matching `FlushTexRectLegacy`. **Validated by clean single-run screenshot** (harness-independent): minimap brightness ratio 1.4–2.4× → **1.000**. Earlier failed hypotheses (all tested + rejected before apitrace): stray non-vertex calls in the body, nested BeginEnd, FF-current-color-sync-on-gl.Color, FlushModern-last-vertex — none worked because the color was inherited from *outside* the body, invisible to all of them.
- 2026-07-01 — **The `GLFrameABCompare` harness itself is unreliable for whole-frame validation — its modern re-render pass corrupts the UI.** Comparing the harness's saved passes: legacy pass renders a perfect frame, but the **modern pass draws a full-screen gray wash with the command panel/build-icons missing** — yet a **plain modern render (`LuaModernGLBackend=1`, no harness) is completely correct** (full UI, correct minimap, crisp text). So the breakage is the harness flipping `SetModernImmediate` **mid-frame during a re-render** (pass 3), not the modern backend; its `net` numbers therefore overstate differences and its "residuals" (the ~8–30k net, a ~10% brightness lift on the "Game starting…" countdown text) are largely **harness artifacts**, not real modern-vs-legacy deltas. **Implication:** trust clean single-run screenshots + per-call `LuaGLCompareMode` (both harness-independent, both green) — not the whole-frame `net` — until the harness re-render is fixed. **In progress: root-causing why the mid-frame modern toggle + re-render stretches the UI panel background to full-screen** (likely screen-matrix / `SetupScreenMatrices` state not re-established for the extra passes).
- 2026-07-03 — **A/B harness restructured: in-frame 3-pass [L,L,M] (one Update, three renders).** Replaces the paired-consecutive-frames design wholesale. Each iteration now runs sim + `UpdateUnsynced` ONCE, then renders the visible frame three times — legacy (reference, presented), legacy (same-frame control), modern (test) — and compares in-frame. Draw-prep (drawFrame, timeOffset, camera, shadow fit, cull flags, particle draw-pos bake, per-draw UBO, buffer uploads) is identical for all passes **by construction**, killing the cross-iteration pin whack-a-mole (the draw-radius leak, camera-frustum pin, and `newSimFrame==false` alternate-path bugs are structurally impossible now; those pins are deleted). Sim runs FULL speed (no `abSuppressSim`/half-fps). Retained pins: exactly two — draw clock (`PinDrawTime`/os.clock) and unsynced RNGs (guRNG + Lua math.random), snapshot before pass 0, replayed before passes 1–2. Per-pass work is **from scratch** (own clear, DrawGenesis, **shadow-map render** — now backend-compared too — world + screen); once-per-iteration exceptions: `minimap->Update()` (wall-clock-throttled cache) and the reflection-cubemap round-robin + consume-once sky/shading updates. The same-frame L↔L control builds a per-frame noise mask AND is itself the pixel-perfect gate: any nonzero control px is a real per-pass leak with an in-frame repro. `AB_FORCE_LEGACY` ⇒ [L,L,L] null test. Also fixed a regression: `SetABCompareActive` was never wired (lost in the 2026-07-03 checkout accident) — `Spring.GetABCompareActive()` and the BAR CAS/bloom/diaglines guards were dead; now set from `CGame::Draw` after a new `GLFrameABCompareWarmup` (default 120 frames, lets accumulators build state). New Lua API: `Spring.GetABPassIndex()` (0/1/2); `Spring.GetABDuplicatePass()` redefined as passIndex>0. BAR-side: `api_screencopy_manager.lua` dedup now keys on (drawFrame, passIndex) so each pass re-copies its own backbuffer (else the modern pass composites legacy pixels through CAS/DoF/glass); `gfx_ssao.lua` SLOWFUSE periodic fuse frozen while compare active. Engine ordering change in the normal frame (compare off): DrawGenesis now runs after minimap/IBL updates, and the shadow map renders after DrawGenesis. All three engine targets compile+link. **Status: NOT yet runtime-validated — next is the [L,L,L] null gate on fightertest (control must be 0), then [L,L,M].**
- 2026-07-04 — **Modern MVP now comes from a CPU-side FF matrix mirror (`GL::ffMirror`, commit `487d2c5fd3`) — the Phase-0 prerequisite step.** The per-callin scaffolding seeds it with exact values (incl. the `__spring_glOrtho` clip-space composition via new `ComposeSpringOrtho/Frustum`; `gl.Ortho`/`gl.Frustum` tracking fixed to match — closes the tracker's documented plain-OrthoProj limitation), every `gl.*` matrix callout replays into it (incl. `gl.PushPopMatrix`, previously untracked, and `gl.RenderToTexture`'s identity push/pop), `gl.CreateList` compiles skip it, and `gl.CallList` taints it (consumers fall back to the `glGetFloatv` bridge until the next reseed). Under the A/B gate every mirror read is shadow-verified against `glGetFloatv`. **Validated: gate byte-perfect with the mirror live (control 0/165, signal 0/165) and ZERO shadow-compare divergences** — CPU matrix composition is pixel-compatible with the driver's FF stack. The bridge is now only the taint-fallback + shadow reference; the font's uMVP still uses the bridge (fonts also fire from unmirrored engine paths — migrates with the UBO phase). Next per the plan: coverage breadth, then the shared RenderBuffer/font uniform-MVP (UBO) conversion fed from the mirror, then removing the FF matrix set-calls.
- 2026-07-03 — **BOTH GATES GREEN, BYTE-PERFECT: the whole-frame compare now reports control=0 AND modern-vs-legacy signal=0 on every in-game frame of the fightertest mass-combat benchmark.** Three iterations got there: **(1) First 3-pass null run**: calm frames 0 but combat median control ~500 px with 10k–45k spikes. apitrace of the run (the per-pass `glReadPixels` are perfect pass boundary markers; segment + histogram draws per pass) showed EVERY recurring leak was first-render-of-the-drawFrame lazy work — widget dlist rebuilds (which shift the font stream-VBO aliasing recorded inside lists), chat's run-twice RTT workaround, a one-time `fxShader` `SMOOTH_PARTICLES` `SetFlag`-after-`Enable` relink landing mid-pass — and that passes 1↔2 were already draw-identical everywhere. **(2) Restructured to 4 passes [settle, L, L, M]** (commit `a819b62b17`): pass 0 is an uncompared settle pass absorbing all once-per-drawFrame lazy work (and is what's presented); control = p1↔p2, signal = p2↔p3; dumps now save the control pair when it leaks. Result: control 0 on 70% of combat frames, residual ~100–330 px = the fightertest `dbg_benchmark.lua` overlay's smoothed ms digits advancing per pass (guarded on `GetABDuplicatePass`, also fixes its stats quadruple-counting) → **[L,L,L,L] null gate: 0/0 on all 153 in-game frames**. **(3) First real modern-backend catch**: [L,L,L,M] showed 6 frames (2.2k–8.6k px) where the modern pass DROPPED `gui_messages`' typewriter event text ("Battle Started/Enemy Units Detected") — `font:Print` compiled into `gl.CreateList` bodies breaks under `FontUseMVPUniform` because `glUseProgram`/`glUniform*` execute immediately during list compile and are NOT recorded, so the replayed glyph draw uses stale shader state (text transforms by another draw's uMVP → vanishes); plus the latent stream-VBO offset-aliasing fragility that affects the legacy path too. **Fix (commit `1e74b7a554`)**: `CglShaderFontRenderer` detects `GL_LIST_INDEX != 0` and emits the accumulated glyph quads as recordable fixed-function immediate mode (texel→UV via a shared texture-space-matrix dlist recompiled on atlas resize, the `CglNoShaderFontRenderer` trick) → **[L,L,L,M]: control 0/160, signal 0/160**. The modern gl.Rect/TexRect + font-uMVP backend is pixel-identical to legacy over a full mass-combat run; the whole-frame gate is trustworthy and CHEAP to re-run (`GLFrameABCompare=1`, fightertest, ~2 min). BAR-side uncommitted: screencopy per-pass key, ssao SLOWFUSE guard, dbg_benchmark pass guard + the 3 existing freeze guards.
- 2026-07-01 — **Modern `gl.BeginEnd` status:** the capture path (FF-free for non-textured geometry — renders through the `uMVP` shader) is correct with the 3 fixes above and validated in a plain render; it is currently behind a **temporary `MODERN_BEGINEND_CAPTURE` env gate** while the harness is untrustworthy, with the default being deferral to legacy `glBegin/glEnd`. Promotion to default is pending the harness re-render fix (so whole-frame `net` can confirm no regressions). `gl.Rect`/`gl.TexRect` remain modern.
- (add dated entries as we learn more)

## TL;DR — scope & sequencing

The work splits into one centralized engine core plus a small per-widget tail. Order:

- **Phase 0 — per-callin scaffolding (the gate, content-independent).** LuaOpenGL wraps *every* draw callin in fixed-function setup that fires regardless of what the widget draws. Until this is modern, no BAR frame is capturable even if every widget is clean. Centralized in ~10 functions in `rts/Lua/LuaOpenGL.cpp`. **Do this first.**
- **Phase 1 — primitives + matrix machinery (A + B).** Re-back `gl.BeginEnd`/`Shape`/`Vertex`/`Rect`/`TexRect` onto `GL::TypedRenderBuffer`; add CPU-side matrix tracking so a shader gets the MVP. Shares the matrix code with Phase 0.
- **Phase 2 — display lists (E).** Re-back `gl.CreateList`/`CallList` onto an engine-managed retained VBO, same Lua API. Largest file footprint, but migrates with zero widget changes if done engine-side.
- **Phase 3 — residuals.** Per-widget: alpha-test→shader-discard, `gl.LineStipple` (dashed lines), `gl.PushAttrib`/`PopAttrib`, `gl.PointSprite`. Concrete file lists below.
- **Phase 4 — engine C++.** SMF void-edge `GL_ALPHA_TEST` (void maps only).

Group **D (fixed-function lighting) is not used by BAR at all — skip it.**

The big lever: Phase 0 + Phase 1 are centralized in `LuaOpenGL.cpp`. Done engine-side behind the unchanged `gl.*` API, the 110 widget files below mostly come along for free; the genuine per-file work is only the Phase 3 residuals.

## Working method: test-forward, oracle-backed

This work is unusually well-suited to a test-first workflow because **the legacy immediate-mode path is a perfect oracle** — it stays behind a config flag, so for any input we can render/compute both ways and assert equality. We exploit that to make "compatible" a *measurable gate*, not a judgment call. Rule: **no change is committed unless the full test suite is green**, and the legacy path is not deleted until the entire contract below is green over both unit scenes and a replay bake.

### Compatibility contract (the definition of "the same")

Every migrated primitive must preserve all of these. Each is a test type, each must be green to call a phase done:

| # | Dimension | "Same" means | Test type |
|---|---|---|---|
| C1 | **Transform values** | tracked `CMatrix44f` == GL FF matrix, bit-exact | Tier-1 unit + shadow assert |
| C2 | **Pixel output** | framebuffer identical (bit-exact where expected; bounded+investigated ±1 LSB for rotated-quad/interp) | Tier-2 dual-FBO A/B |
| C3 | **GL state side-effects** | after a callin, observable state (current color, active texture, blend/enable bits, matrix mode + stack depth) matches legacy | state-snapshot test |
| C4 | **Draw order / compositing** | translucent overlaps and rect↔text interleave composite identically within a frame | Tier-2 order cases + live replay compare |
| C5 | **Error / edge behavior** | empty/degenerate prims, Lua error mid-`BeginEnd`, stack over/underflow → same outcome, no crash, balanced stack | unit + state test |
| C6 | **Coverage** | every call site seen in the `DeprecatedGLWarnLevel=2` trace is exercised by a unit scene or the live replay compare | coverage checklist vs trace |

### The loop (per increment = one matrix-op group or one primitive)

1. **Write the test first**, from the contract. It is red (assertion fails) or, if the new backend for that primitive doesn't exist yet, explicitly **pending/SKIP** — never silently absent.
2. Implement the minimal code (or flip the per-primitive flag).
3. Test goes green (new ≡ legacy).
4. **Run the full suite** — all prior tests still green = no regression.
5. **Commit gate:** commit only when the whole suite is green.

### Validate the harness before trusting it

Before the A/B comparator is allowed to gate anything, prove it can both confirm *and* deny:
- **Positive control:** render the same primitive twice via the legacy path → must be byte-identical (proves readback/determinism).
- **Negative control:** feed the comparator two deliberately-different renders → must FAIL (proves it isn't a no-op that always passes).

A comparator that can't fail gives false confidence; these two controls are themselves committed tests.

### Per-phase done-gates

- **Phase 1 (matrix):** Tier-1 suite green (C1, C5-matrix) — pure unit, no GL, authored before the tracker code.
- **Phase 0 (scaffolding) + Phase 1 (primitives A):** Tier-2 A/B suite green for all primitive cases (C2, C4); state-snapshot tests green (C3); harness controls green.
- **Phase 2 (display lists), Phase 3 (residuals):** their own A/B + state cases green.
- **Whole-program gate (before deleting legacy):** C6 satisfied — `LuaGLCompareMode` over a representative replay reports zero pixel deltas across the full `DeprecatedGLWarnLevel` caller set, plus a clean RenderDoc capture.

---

## The migration-unit model

A re-backed `gl.BeginEnd` draws with a **shader**, which needs as uniforms/inputs everything the fixed-function pipeline supplied implicitly. So the geometry functions and the state functions that feed them are one coupled unit.

| Group | Lua API | Underlying GL | Becomes | BAR uses? |
|---|---|---|---|---|
| **A. Geometry** | `BeginEnd`, `Shape`, `Vertex`, `Normal`, `TexCoord`, `MultiTexCoord`, `Color`, `SecondaryColor`, `FogCoord`, `EdgeFlag`, `Rect`, `TexRect` | `glBegin`/`glEnd`, `glVertex*`, `glColor*`, `glTexCoord*`, `glNormal*`, `glRectf` | `TypedRenderBuffer.AddVertex` + `DrawArrays(mode)` | yes (heavy) |
| **B. Transform** | `MatrixMode`, `Push/PopMatrix`, `LoadIdentity`, `LoadMatrix`, `MultMatrix`, `Translate`, `Rotate`, `Scale`, `Ortho`, `Frustum` | `glMatrixMode`, `glPush/PopMatrix`, `glLoadMatrixf`, `glMultMatrixf`, `glTranslatef`, `glRotatef`, `glScalef`, `glOrtho`, `glFrustum` | CPU-tracked `CMatrix44f` stack → shader MVP uniform | yes (heavy) |
| **C. Fragment/raster state** | `TexEnv`, `AlphaTest`, `Fog`, `ShadeModel`, `PolygonMode`, `LineWidth`, `LineStipple`, `PointSize`, `ClipPlane` | `glTexEnv*`, `glAlphaFunc`, `glShadeModel`, `glPolygonMode`, `glLineWidth`, `glLineStipple`, `glPointSize`, `glClipPlane` | shader flags/uniforms; some are core-valid no-ops | partial (see below) |
| **D. Lighting** | `Light`, `LightModel`, `Material`, `Lighting` | `glLight*`, `glMaterial*`, `glLightModeli` | FF lighting in shader | **NO — 0 files** |
| **E. Display lists** | `CreateList`, `CallList`, `RunList`, `DeleteList` | `glGenLists`/`glNewList`/`glCallList` | engine-managed retained VBO behind same API | yes (heavy) |

### Group C detail (what BAR actually uses)

Confirmed BAR usage of the C/residual functions (static grep):

| Function | Files | RenderDoc blocker? | Action |
|---|--:|---|---|
| `gl.AlphaTest` | 8 | **Yes** (`glAlphaFunc`/`GL_ALPHA_TEST` removed) | shader `discard` |
| `gl.LineStipple` | 6 | **Yes** (removed in core) | shader dashed-line (1D tex or `gl_FragCoord` discard) |
| `gl.PushAttrib`/`PopAttrib` | 3 | **Yes** (removed in core) | explicit state save/restore |
| `gl.PointSprite` | 1 | **Yes** (implicit in core) | drop enable, use core point sprites |
| `gl.LineWidth` | 33 | No — core entry point | none (thick lines may not render in strict core, but capture is fine) |
| `gl.PolygonOffset` | 12 | No — core | none |
| `gl.PolygonMode` | 6 | No — core | none |
| `gl.PointSize` | 2 | No — core | none |
| `gl.TexEnv`, `gl.Fog`, `gl.ClipPlane`, `gl.ShadeModel`, `gl.Lighting`, `gl.LogicOp`, `gl.PointParameters` | 0 | — | none |

---

## Phase 0 — the per-callin scaffolding (the real gate)

Every Lua draw callin is wrapped by `EnableCommon`/`Reset*` in `rts/Lua/LuaOpenGL.cpp`, which unconditionally emits RenderDoc-rejected calls **regardless of widget content**. A widget drawing only text (already on the modern shader font renderer) still triggers all of this every frame:

- `EnableCommon` (`:599`) → `glPushAttrib(AttribBits)` (`:604`) … `glPopAttrib()` (`:620`).
- `ResetGLState()` (`:497`) → `glAlphaFunc`, `glShadeModel`, `glLightModeli`, `glTexEnvi(GL_MODULATE)` (`:543`), `glLogicOp`, `glDisable(GL_LIGHTING / GL_TEXTURE_GEN_*)`.
- `SetupScreenMatrices()` (`:980`) → `glLightModeli`, `glMatrixMode`, `glLoadMatrixf`. `RevertScreenMatrices()` → `glMatrixMode`/`glLoadIdentity`/`gluOrtho2D`. Same for the `EnableDrawWorld*`/`Shadow`/`Reflection`/`Refraction` matrix setups.

This is why Phase 0 is mandatory and first. The fix (all three are gated on the FF content being gone — see the finding below):

- **Attrib-stack swap** — replace `EnableCommon`'s `glPushAttrib(AttribBits)` / `DisableCommon`'s `glPopAttrib()` with explicit modern state save/restore (engine state abstraction in `rts/Rendering/GL/State.h`, which is RAII `GL::SubState`, not a snapshot API). **Finding (2026-06-30): this is NOT a content-independent win.** `AttribBits` (COLOR_BUFFER|DEPTH_BUFFER|ENABLE|LIGHTING|LINE|POINT|POLYGON|VIEWPORT) and what `ResetGLState` changes is mostly **fixed-function** state (`glAlphaFunc`, `glShadeModel`, `glLogicOp`, `glTexEnvi(MODULATE)`, `glDisable(GL_LIGHTING)`, texgen, line stipple, clip planes, point sprite/params, current color, material). Restoring that without `glPopAttrib` means re-emitting those same deprecated FF calls — so swapping the attrib stack removes `glPushAttrib`/`glPopAttrib` but adds back `glAlphaFunc`/`glShadeModel`/… for the restore: **net zero RenderDoc benefit while any frame content still needs FF state**. So this only pays off once `ResetGLState`'s FF resets are gone (next bullet) and no widget needs FF state restored. Do it together with — not before — the FF-reset removal.
- Turn `ResetGLState`'s FF resets into shader-state defaults / no-ops **for frames whose content is already modern** (depends on no widget relying on FF state). Per Constraint #1 this is conditional, not a removal — legacy-content frames keep the full FF reset.
- Route `SetupScreenMatrices` and the world/shadow matrix setups through the CPU-tracked matrix system (shared with Phase 1 / group B) — **blocked by the uniform/UBO-MVP conversion below**.

> ### ⚠ TODO — Phase 0 prerequisite: uniform/UBO MVP shader conversion (the actual blocker)
> Phase 0 is the **final** gate, not an incremental step: removing the FF matrix scaffolding (`SetupScreenMatrices`'s `glMatrixMode`/`glLoadMatrixf`) is impossible while **any shader in the frame still reads `gl_ModelViewProjectionMatrix`** — they'd all transform by identity. Confirmed consumers (2026-06-30):
> - **The font renderer** — `rts/Rendering/Fonts/glFontRenderer.cpp:30,90` (`gl_Position = gl_ModelViewProjectionMatrix * …`, already tagged `// TODO: move to UBO`). BAR draws *all* text through this, so it fires every frame.
> - **The stock `TypedRenderBuffer` shader** — `rts/Rendering/GL/RenderBuffers.inl` (same builtin); used engine-wide.
> - **Our `LuaImmediateBuffer` modern path already avoids this** (uniform `uMVP`), but it currently sources that MVP via `glGetFloatv(GL_*_MATRIX)` — a bridge that only works *because* the FF matrix is still set by the scaffolding.
>
> **Prerequisite work, in order:** (1) convert the font renderer + the shared RenderBuffer shader to a uniform/UBO MVP (a global UBO updated once per view is cleanest, so per-callin code needn't set a uniform); (2) feed that UBO from the matrix tracker (Phase 1) instead of `glGetFloatv`; (3) only *then* can `SetupScreenMatrices`/`ResetGLState`/`glPushAttrib` be removed. Until (1)+(2), Phase 0 stays blocked and the modern primitive path keeps using the `glGetFloatv` bridge. The `glPushAttrib`/`glPopAttrib` attrib-stack replacement (via `State.h`) is the one Phase-0 piece that *is* content-independent and could land earlier.

Chokepoint functions to modernize (all in `LuaOpenGL.cpp`): `ResetGLState` (`:497`), `EnableCommon` (`:599`), `ResetDrawGenesis` (`:652`), `EnableDrawWorld` (`:666`)/`ResetDrawWorld` (`:682`), `EnableDrawWorldPreUnit` (`:696`), `EnableDrawWorldShadow` (`:726`), `EnableDrawWorldReflection` (`:768`), `EnableDrawWorldRefraction` (`:798`), `EnableDrawScreenCommon` (`:827`), `SetupScreenMatrices`/`RevertScreenMatrices` (`:980`).

---

## The matrix-tracking gap (infra audit)

**Exists and reusable:**
- `GL::TypedRenderBuffer<VertType>` (`rts/Rendering/GL/RenderBuffers.h`) — VAO/VBO/IBO accumulator (`AddVertex`/`DrawArrays`/`DrawElements`, quad-index helpers). The draw target.
- `CMatrix44f` — full translate/rotate/scale/ortho/frustum/multiply math already in C++.
- `LuaOpenGLUtils::GetNamedMatrix` + `gl.GetMatrixData` — Lua can already read known engine matrices.
- `GLMatrixStateTracker` (`rts/Rendering/GL/MatrixStateTracker.h`), stored per-Lua-context (`LuaContextData.h:118 glMatrixTracker`) — already hooks `MatrixMode`/`Push/PopMatrix`, tracks mode + stack depth, validates balance.
- `rts/Rendering/GL/State.h` — modern GL state abstraction (for Phase 0 attrib save/restore).

**The gap (the one enabling change):**
> `GLMatrixStateTracker` tracks stack **depth and mode only**, not matrix **values**. The actual modelview/projection/texture values live solely in the GL fixed-function stack. `gl.Translate` is literally `glTranslatef(...)` (`:5150`) with no CPU mirror; `gl.PushMatrix` bumps a depth counter then `glPushMatrix()` (`:5582`).

So a re-backed `gl.BeginEnd` has no CPU-side MVP for its shader. **Fix:** extend `GLMatrixStateTracker` to hold real `CMatrix44f` stacks for the three modes (it already models the three modes + their stacks; today they're `int` depths where they need `std::stack<CMatrix44f>`), and have group-B functions mutate those stacks using existing `CMatrix44f` math. On flush, read `tracker.GetMatrixState()` → compose proj·modelview → upload as the shader uniform.

---

## Residuals — concrete per-file lists (Phase 3)

**`gl.AlphaTest` (8) → shader discard:**
- `luarules/gadgets/gfx_energy_explosion_particles_gl4.lua`
- `luarules/gadgets/gfx_nano_particles_gl4.lua`
- `luarules/gadgets/gui_display_dps.lua`
- `luarules/gadgets/mo_battle_royale.lua`
- `luaui/Widgets/gfx_airjets_gl4.lua`
- `luaui/Widgets/gui_com_nametags.lua`
- `luaui/Widgets/gui_pip.lua`
- `luaui/Widgets/gui_rank_icons_gl4.lua`

**`gl.LineStipple` (6) → shader dashed-line:**
- `luarules/gadgets/unit_target_on_the_move.lua`
- `luaui/Widgets/cmd_customformations2.lua`
- `luaui/Widgets/gui_pip.lua`
- `luaui/Widgets/gui_pregame_build.lua`
- `luaui/Widgets/gui_selectionbox.lua`
- `luaui/Widgets/unit_waypoint_dragger_2.lua`

**`gl.PushAttrib`/`PopAttrib` (3) → explicit state save/restore:**
- `luarules/gadgets/unit_target_on_the_move.lua`
- `luaui/barwidgets.lua`
- `luaui/Widgets/cmd_customformations2.lua`

**`gl.PointSprite` (1):**
- `luaui/Widgets/gfx_snow.lua`

---

## Engine-side (Recoil) inventory

### BAR triggers these (must change for #1)

| File | Site | Call | Notes |
|---|---|---|---|
| `rts/Lua/LuaOpenGL.cpp` | scaffolding (Phase 0) | attrib stack, matrix, alpha/texenv/shademodel | Fires every callin. See Phase 0. |
| `rts/Lua/LuaOpenGL.cpp` | `BeginEnd` 2389, `Shape` 2337, `Rect` 2822, `TexRect` 2877, `DrawGroundQuad` 2189 | `glBegin`/`glRectf` | Group A. |
| `rts/Lua/LuaOpenGL.cpp` | matrix family (`Translate` 5150, `PushMatrix` 5582, `LoadMatrix` 5488, `MultMatrix` 5547, `MatrixMode` 5411, …) | `glTranslatef`/`glLoadMatrixf`/… | Group B. |
| `rts/Lua/LuaOpenGL.cpp` | display-list API | `glGenLists`/`glNewList`/`glCallList` | Group E. |
| `rts/Map/SMF/SMFGroundDrawer.cpp` | 241-242, 290-291 | `GL_ALPHA_TEST` + `glAlphaFunc` | Void maps only (`voidAlphaMin`). Shader discard. Phase 4. |

### Inert under BAR (no action for #1) — confirmed by `springconfig.lua`

`GrassDetail=0`, `ForceDisableShaders=0`, `LuaShaders=1`, BumpWater, GL4 materials.

| File | Why inert |
|---|---|
| `rts/Rendering/Env/GrassDrawer.cpp` | `GrassDetail=0` → early-return before list creation; BAR uses `map_grass_gl4.lua`. |
| `rts/Rendering/Units/UnitDrawer.cpp` 301/325, `Features/FeatureDrawer.cpp` 157/163 | `if(preList!=0)`; BAR materials never install lists. |
| `rts/Rendering/Models/LocalModelPiece.cpp` 294 | Legacy no-shader model path; BAR is GL4/GLSL. |
| `rts/Rendering/Fonts/glFontRenderer.cpp` 280-380 | No-shader renderer only on AMD-hack/fallback. |
| `rts/Rendering/Env/DynWater.cpp` 681/748/1002 | `Water=2`; BAR uses BumpWater. |
| `rts/Rendering/Env/Particles/ProjectileDrawer.cpp` 1023 | `glAlphaFunc` commented out; uses shader `alphaCtrl`. |
| `rts/Rendering/Common/ModelDrawer*.{h,cpp}` | Behind `IsLegacy()`. |
| `rts/Game/UI/GuiHandler.cpp` (~40), `rts/aGui/*`, `TooltipConsole.cpp`, `GameControllerTextInput.cpp`, `Game.cpp` 2083 | Engine default UI / menus / chat / loading bar; BAR replaces or not in-frame. |
| Debug drawers (QTPFS/HAPFS/ROAM/SmoothHeightMesh/GeometryBuffer/Combiner), `HUDDrawer.cpp` | Debug toggles / FPS-control only. |

---

## BAR-side master work-list (per-file primitive matrix)

Columns: `BE`=BeginEnd, `Sh`=Shape, `Rc`=Rect, `TR`=TexRect, `CrL`=CreateList, `CaL`=CallList, `FUI`=WG.FlowUI.Draw consumer. Sorted by total. A file with only `FUI`>0 migrates for free once the engine path is modern. Static counts (BAR branch `bruno/init-nano-particles-during-load`).

Aggregate file counts: `gl.BeginEnd` 41 · `gl.Shape` 4 · `gl.Rect` 25 · `gl.TexRect` 46 · `gl.CreateList` 62 · `gl.CallList` 60 · `WG.FlowUI.Draw` 40. **110 unique files.** Text (`gl.Text`, 128 callers) already modern — excluded.

### Keystone

| File | BE | Sh | Rc | TR | CrL | CaL | FUI |
|---|--:|--:|--:|--:|--:|--:|--:|
| `luaui/Widgets/gui_flowui.lua` | 11 | 1 | 0 | 2 | 1 | 0 | 70 |

### High weight

| File | BE | Sh | Rc | TR | CrL | CaL | FUI |
|---|--:|--:|--:|--:|--:|--:|--:|
| `luaintro/Addons/main.lua` (loading screen, not in-game) | 14 | 0 | 11 | 12 | 0 | 1 | 0 |
| `luaui/Widgets/gui_pip.lua` | 4 | 0 | 0 | 1 | 18 | 4 | 3 |
| `luaui/Widgets/gui_options.lua` | 0 | 0 | 2 | 1 | 8 | 1 | 8 |
| `luaui/Widgets/gui_top_bar.lua` | 1 | 0 | 1 | 2 | 1 | 1 | 13 |
| `luaui/Widgets/gui_buildbar.lua` | 2 | 0 | 0 | 1 | 8 | 2 | 4 |
| `luaui/Widgets/gui_gridmenu.lua` | 1 | 0 | 0 | 1 | 4 | 1 | 8 |
| `luaui/Widgets/camera_player_tv.lua` | 0 | 0 | 0 | 0 | 7 | 6 | 1 |
| `luaui/Widgets/widget_selector.lua` | 0 | 0 | 2 | 0 | 5 | 3 | 3 |
| `luaui/Widgets/gui_buildmenu.lua` | 0 | 0 | 0 | 1 | 2 | 2 | 7 |
| `luaui/Widgets/gui_chat.lua` | 0 | 0 | 4 | 0 | 1 | 1 | 4 |
| `luarules/gadgets/unit_icongenerator.lua` (icon bake) | 0 | 1 | 0 | 9 | 0 | 0 | 0 |
| `luarules/gadgets/cmd_get_player_data.lua` | 0 | 0 | 3 | 4 | 1 | 2 | 0 |
| `luaui/Widgets/gui_pregameui_draft.lua` | 1 | 0 | 1 | 0 | 3 | 1 | 3 |
| `luaui/Widgets/gui_ordermenu.lua` | 1 | 0 | 1 | 1 | 1 | 0 | 5 |
| `luaui/Include/AtlasOnDemand.lua` (shared text/atlas helper) | 0 | 0 | 1 | 8 | 0 | 0 | 0 |

### Medium weight

| File | BE | Sh | Rc | TR | CrL | CaL | FUI |
|---|--:|--:|--:|--:|--:|--:|--:|
| `luaui/Widgets/gui_mission_info.lua` | 0 | 0 | 0 | 0 | 5 | 1 | 3 |
| `luaui/Widgets/gui_changelog_info.lua` | 0 | 0 | 0 | 0 | 5 | 1 | 3 |
| `luaui/Widgets/gui_scavenger_info.lua` | 0 | 0 | 0 | 0 | 4 | 1 | 3 |
| `luaui/Widgets/gui_keybind_info.lua` | 0 | 0 | 0 | 1 | 4 | 1 | 2 |
| `luaui/Widgets/gui_gameinfo.lua` | 0 | 0 | 0 | 0 | 4 | 1 | 3 |
| `luaui/Widgets/map_startbox.lua` | 1 | 0 | 0 | 1 | 3 | 2 | 0 |
| `luaui/Widgets/gui_unit_stats.lua` | 0 | 0 | 0 | 0 | 3 | 1 | 3 |
| `luaui/Widgets/gui_rejoinprogress.lua` | 1 | 0 | 0 | 0 | 2 | 1 | 3 |
| `luaui/Widgets/gui_advplayerslist_music_new.lua` | 0 | 0 | 0 | 1 | 1 | 1 | 4 |
| `luaui/Widgets/gui_advplayerslist.lua` | 1 | 0 | 1 | 0 | 1 | 1 | 3 |
| `luaui/Widgets/gui_vote_interface.lua` | 0 | 0 | 0 | 0 | 2 | 1 | 3 |
| `luaui/Widgets/gui_spectator_hud.lua` | 0 | 0 | 1 | 0 | 2 | 2 | 1 |
| `luaui/Widgets/gui_selectionbox.lua` | 2 | 0 | 3 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_pregameui.lua` | 0 | 0 | 0 | 0 | 2 | 2 | 2 |
| `luaui/Widgets/gui_ecostats.lua` | 0 | 0 | 0 | 1 | 2 | 1 | 2 |
| `luarules/gadgets/dbg_gadget_profiler.lua` (debug) | 0 | 0 | 6 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/map_start_position_suggestions.lua` | 1 | 0 | 0 | 0 | 2 | 2 | 0 |
| `luaui/Widgets/gui_scavStatsPanel.lua` | 0 | 0 | 0 | 1 | 2 | 2 | 0 |
| `luaui/Widgets/gui_replaybuttons.lua` | 0 | 0 | 0 | 0 | 2 | 1 | 2 |
| `luaui/Widgets/gui_raptorStatsPanel.lua` | 0 | 0 | 0 | 1 | 2 | 2 | 0 |
| `luaui/Widgets/gui_info.lua` | 1 | 0 | 0 | 1 | 0 | 0 | 3 |
| `luaui/Widgets/gui_converter_usage.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 2 |
| `luaui/Widgets/gui_awards.lua` | 0 | 0 | 0 | 1 | 2 | 1 | 1 |
| `luaui/Widgets/cmd_factoryqmanager.lua` | 0 | 0 | 3 | 0 | 0 | 0 | 2 |
| `luarules/gadgets/gui_display_dps.lua` | 0 | 0 | 0 | 0 | 4 | 1 | 0 |
| `luaui/Widgets/gui_teamstats.lua` | 0 | 0 | 0 | 0 | 1 | 1 | 2 |
| `luaui/Widgets/gui_reclaim_field_highlight.lua` | 1 | 0 | 0 | 1 | 1 | 1 | 0 |
| `luaui/Widgets/gui_advplayerslist_unittotals.lua` | 0 | 0 | 0 | 0 | 1 | 1 | 2 |
| `luaui/Widgets/gui_advplayerslist_gameinfo.lua` | 0 | 0 | 0 | 0 | 1 | 1 | 2 |
| `luaui/Widgets/dbg_jitter_timer.lua` (debug) | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| `luaui/RmlWidgets/gui_quick_start/gui_quick_start.lua` | 1 | 0 | 0 | 0 | 2 | 1 | 0 |
| `luarules/gadgets/unit_respawning.lua` | 0 | 0 | 0 | 1 | 2 | 1 | 0 |
| `luarules/gadgets/unit_evolution.lua` | 0 | 0 | 0 | 1 | 2 | 1 | 0 |
| `luarules/gadgets/gfx_unit_shield_effects.lua` | 1 | 0 | 0 | 0 | 2 | 1 | 0 |

### FlowUI-only (migrate with FlowUI backend; no direct primitives)

`gui_unitgroups.lua` (3), `gui_idle_builders.lua` (3), `gui_factionpicker.lua` (3), `gui_tooltip.lua` (2, +1 CaL), `gui_minimap.lua` (2).

### Low weight (1–2 deprecated calls each)

| File | BE | Sh | Rc | TR | CrL | CaL | FUI |
|---|--:|--:|--:|--:|--:|--:|--:|
| `luaui/Widgets/gui_selfd_icons.lua` | 0 | 0 | 0 | 1 | 2 | 0 | 0 |
| `luaui/Widgets/gui_projectile_target_aoe.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_mouse_fx.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_mapinfo.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_infolos.lua` | 0 | 0 | 0 | 3 | 0 | 0 | 0 |
| `luaui/Widgets/gui_given_units.lua` | 0 | 0 | 0 | 1 | 2 | 0 | 0 |
| `luaui/Widgets/gui_com_nametags.lua` | 0 | 0 | 0 | 1 | 1 | 1 | 0 |
| `luaui/Widgets/gui_commanderhurt.lua` | 0 | 0 | 0 | 1 | 1 | 1 | 0 |
| `luaui/Widgets/gui_clearmapmarks.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_build_placement_extension.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_attack_aoe.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_ally_cursors.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_advplayerslist_mascot.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gfx_volumetric_clouds.lua` | 0 | 0 | 0 | 3 | 0 | 0 | 0 |
| `luaui/Widgets/gfx_snow.lua` (also `gl.PointSprite`) | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gfx_guishader.lua` | 0 | 0 | 1 | 1 | 0 | 1 | 0 |
| `luaui/Widgets/dbg_ffa_startpoints_picker.lua` (debug) | 0 | 0 | 0 | 0 | 1 | 1 | 1 |
| `luaui/Widgets/cmd_share_unit.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luarules/gadgets/unit_seismic_ping.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luarules/gadgets/unit_juno_damage_mini.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luarules/gadgets/unit_juno_damage.lua` | 1 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/unit_share_tracker.lua` | 0 | 1 | 1 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/snd_volume_osd.lua` | 0 | 0 | 1 | 0 | 0 | 0 | 1 |
| `luaui/Widgets/gui_transport_weight_limit.lua` | 1 | 0 | 0 | 0 | 1 | 0 | 0 |
| `luaui/Widgets/gui_show_orders.lua` | 0 | 0 | 1 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_pregame_build.lua` (also `gl.LineStipple`) | 0 | 2 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/gui_pausescreen.lua` | 0 | 0 | 1 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_messages.lua` | 0 | 0 | 0 | 0 | 1 | 1 | 0 |
| `luaui/Widgets/gui_cache_icons.lua` | 0 | 0 | 0 | 2 | 0 | 0 | 0 |
| `luaui/Widgets/dbg_startbox_editor.lua` (debug) | 2 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/dbg_deferred_buffer_visualizer.lua` (debug) | 0 | 0 | 0 | 2 | 0 | 0 | 0 |
| `luaui/Widgets/cmd_customformations2.lua` (also Stipple/PushAttrib) | 2 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/api_los_combiner_gl4.lua` | 0 | 0 | 0 | 2 | 0 | 0 | 0 |
| `luaui/Widgets/unit_waypoint_dragger_2.lua` (also Stipple) | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/unit_loop_select.lua` | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/unit_alt_set_target_type.lua` | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/gui_tax_buildspeed_debuff.lua` | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_sensor_ranges_sonar.lua` | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_sensor_ranges_radar.lua` | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_sensor_ranges_los.lua` | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_sensor_ranges_jammer.lua` | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gui_prospector.lua` | 0 | 0 | 1 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/gui_mapmarks_fx.lua` | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/gui_easyFacing.lua` | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/gfx_unit_stencil_gl4.lua` | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gfx_dof.lua` | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/gfx_deferred_rendering_GL4.lua` | 0 | 0 | 1 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/gfx_darken_map.lua` | 0 | 0 | 1 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/dbg_widget_profiler.lua` (debug) | 0 | 0 | 1 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/dbg_quadtree_atlas_tester.lua` (debug) | 0 | 0 | 1 | 0 | 0 | 0 | 0 |
| `luaui/Widgets/dbg_deferred_test.lua` (debug) | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luaui/Widgets/cmd_extractor_snap.lua` | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luarules/gadgets/unit_target_on_the_move.lua` (also Stipple/PushAttrib) | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| `luarules/gadgets/include/GenEnvLut.lua` (offline bake) | 0 | 0 | 0 | 1 | 0 | 0 | 0 |
| `luarules/gadgets/include/GenBrdfLut.lua` (offline bake) | 0 | 0 | 0 | 1 | 0 | 0 | 0 |

---

## Runtime verification

Static grep proves presence, not absence, and can't see runtime frequency, dynamically-built call names, or other LuaOpenGL helpers that internally use immediate mode (`gl.DrawGroundQuad` does; ground-circle/shape-style helpers might). Confirm with:

### Run the tracer
1. Set `DeprecatedGLWarnLevel = 2` in `springsettings.cfg`.
2. Play/replay a normal match; capture `infolog.txt`. Level 2 logs each deprecated `gl.*` with its Lua caller (`[file]:line Caller: <fn>`), deduplicated.
3. **Coverage (done 2026-06-30):** `Rect`, `TexRect`, `DrawGroundQuad`, `PushAttrib`, `PopAttrib` now call `CondWarnDeprecatedGL`, so the tracer catches all relevant Lua-side blockers. Note `gl.PolygonMode`/`gl.Texture` are intentionally unhooked (core-valid, not blockers). Also note the tracer is Lua-side only — it will NOT report the engine's own per-callin scaffolding (Phase 0), which fires from C++; that's known statically and doesn't need the tracer.

### RenderDoc cross-check
- RenderDoc's first-frame API/error log names the exact rejected call. Compare against this doc.
- Repeat across: void vs non-void maps (engine void-edge alpha test), water modes, and representative widget loadouts (active set varies per player config).

---

## Runtime findings

### Run 2026-06-30 — GL 4.6 Compat, master `2026.06.06-80-gf898efc`, `infolog.txt`

A full match (reached `f≈64405`). **1350 deprecated-GL warnings, 26 distinct `gl.*` functions.** Frame split: **994 at `f=-000001`** (pre-game: loading screen + Chobby menu), **~356 at `f≥0`** (in-game). Warnings are deduplicated per call-site (first occurrence only) — so the `f≥0` set is a *lower bound* on in-game callers (a widget first seen drawing the loading screen is logged at `f=-1` even though it also draws in-game; e.g. `gui_flowui` shows only 7 `f≥0` but is obviously the #1 in-game UI).

**Function histogram (all frames):** Vertex 361 · Color 236 · DeleteList 126 · BeginEnd 90 · CreateList 76 · Translate 70 · TexCoord 54 · TexRect 53 · MultiTexCoord 43 · PushMatrix 39 · PopMatrix 39 · Scale 38 · LineWidth 36 · CallList 32 · Rect 16 · Rotate 9 · AlphaTest 9 · MatrixMode 7 · LoadIdentity 5 · Normal 3 · Shape 2 · Ortho 2 · PushPopMatrix 1 · PushAttrib 1 · PopAttrib 1 · Billboard 1.

#### Headline finding — a missed UI layer: Chili / Chobby / LuaMenu

The single largest non-BAR-content source is the **Chili UI framework** (the Chobby lobby/menu), which the static inventory never scanned (`libs/chiliui/`, `LuaMenu/` are outside `luaui/luarules/luaintro`):

- `libs/chiliui/chili/headers/skinutils.lua` — **159 sites** (overall #3)
- `LuaMenu/Widgets/api_chili.lua` (14), `LuaMenu/widgets/chobby/components/priority_popup.lua` (12), `modules/graphics/r2thelper.lua` (7), `libs/chiliui/chili/controls/{control,font,image}.lua` (7/5/4)

**All Chili/LuaMenu warnings fire at `f=-000001` and zero in-game** — so this is **menu/lobby scope, a separate goal from an in-game frame capture.** If we ever want to RenderDoc the menu, Chili is the dominant immediate-mode target; for in-game capture it's irrelevant.

#### In-game callers (`f≥0`, definitely draw during a match)

Authoritative work-list for the in-game goal (lower bound per dedup caveat). Functions firing in-game: DeleteList 80 · Color 65 · Vertex 45 · LineWidth 24 (core-valid, not a blocker) · TexRect 23 · Translate 20 · CallList 12 · BeginEnd 12 · PushMatrix/PopMatrix 11 · CreateList 10 · TexCoord 9 · Scale 8 · Rotate 8 · Rect 8 · AlphaTest 8 · Shape 2.

| Caller | f≥0 sites | | Caller | f≥0 sites |
|---|--:|---|---|--:|
| `gui_pip.lua` | 72 | | `gui_messages.lua` | 8 |
| `gui_mapinfo.lua` | 44 | | `gui_awards.lua` | 8 |
| `gui_ecostats.lua` | 24 | | `gui_flowui.lua` (undercounted) | 7 |
| `gui_chat.lua` | 22 | | `gfx_guishader.lua` | 7 |
| `gui_projectile_target_aoe.lua` | 17 | | `unit_seismic_ping.lua` (gadget) | 7 |
| `gui_com_nametags.lua` | 17 | | `gui_tooltip.lua` | 6 |
| `gui_top_bar.lua` | 15 | | `gui_options.lua` | 5 |
| `gui_converter_usage.lua` | 14 | | `gui_build_eta.lua` | 5 |
| `gui_info.lua` | 10 | | + ~25 more at 1–4 sites | |
| `unit_share_tracker.lua` | 9 | | (map_startbox, gui_attackrange_gl4, gui_defenserange_gl4, gui_commands_fx, gui_rank_icons_gl4, gfx_airjets_gl4, gfx_nano_particles_gl4, gfx_energy_explosion_particles_gl4, gui_advplayerslist*, widget_selector, gui_teamstats, gui_sensor_ranges_jammer, gui_gridmenu, gui_keybind_info, gui_gameinfo, gui_commanderhurt, gui_clearmapmarks, gui_changelog_info, cmd_share_unit, unit_respawning, unit_evolution, gfx_unit_shield_effects, gui_transport_weight_limit, gui_mouse_fx, gui_cache_icons, gui_pregameui_draft) | |
| `gui_advplayerslist_music_new.lua` | 9 | | | |

### Reconciliation with static inventory
- **Confirmed:** `gui_flowui`, `gui_pip`, `gui_top_bar`, `gui_chat`, `gui_ecostats`, `map_startbox` etc. all present — static inventory's BAR set is broadly right.
- **Static matrix undercounted everywhere:** it only tracked BeginEnd/Shape/Rect/TexRect/CreateList/CallList/FlowUI, never group B (matrix) or `gl.Vertex`/`gl.Color`. Runtime shows Vertex (361) + Color (236) + Translate (70) are the *highest-volume* calls. This is why `gui_build_eta` (uses only `local glColor/glTranslate/glScale` aliases) and `gui_mapinfo` (44 in-game!) barely registered or were absent in the matrix. **The caller files are right; the per-file weights in the matrix are not — trust this runtime list for the in-game set.**
- **In-game callers not in the primitive matrix — but almost all already known.** Diffing the 47 in-game callers against the matrix flagged 8 "new" files; on inspection the trace revealed **no significant unanticipated in-game source**:
  - 4 (`gfx_airjets_gl4`, `gfx_energy_explosion_particles_gl4`, `gfx_nano_particles_gl4`, `gui_rank_icons_gl4`) fire `gl.AlphaTest` and are **already in the Phase-3 AlphaTest residual list** — they were absent from the *matrix* only because it doesn't have an AlphaTest column.
  - 2 (`gui_attackrange_gl4`, `gui_defenserange_gl4`) fire **only `gl.LineWidth`** — core-valid, not blockers; correctly excluded.
  - 2 are **genuinely new and real, both trivial**: `gui_build_eta` (`gl.Color` + `gl.PushMatrix`/`Translate`/`PopMatrix` via aliases) and `gui_commands_fx` (a lone `gl.Color`). Group A/B, covered by the engine-side re-backing with no special handling.
  - `gui_mapinfo` (44 in-game) was in the matrix but weighted near-zero because its calls are matrix/Color — a weighting error, not a new file.
- **Hooks validated:** `gl.TexRect` (53), `gl.Rect` (16), `gl.PushAttrib` (1), `gl.PopAttrib` (1) appear → the 5 new `CondWarnDeprecatedGL` calls compiled and fire.
- **Noise to ignore:** `gl.LineWidth` (36 total / 24 in-game) is logged but core-valid — not a RenderDoc blocker.
- **Engine void-edge alpha test** (SMFGroundDrawer) — not visible here (Lua-only tracer); check the map used and RenderDoc's reject log separately.

### Open follow-ups from this run
- Decide scope: **in-game frame only** (ignore Chili/Chobby) vs **also menu** (Chili `skinutils.lua` becomes the #1 target). The engine-side `RenderBuffer` re-backing helps both for free since Chili also calls the same `gl.*`.
- The dedup hides true in-game frequency for widgets that also draw at load. If we want exact in-game-only attribution, add a frame-phase tag to the warn (or clear `deprecatedGLWarned` at `GameStart`).

---

## Testing & validation

Goal: prove each migrated `gl.*` primitive renders identically to the legacy immediate-mode version, reproducibly, in CI. Two tiers, because the risk splits cleanly.

### Tier 1 — matrix tracking: pure Catch2 unit tests (no GL, runs in normal CI)

**Landed 2026-06-30:** `test/engine/System/testGLMatrixStateTracker.cpp` (target `testGLMatrixStateTracker`). Authored test-first, then made green by extending `GLMatrixStateTracker` with `CMatrix44f` value stacks (see changelog). Note `myGL.h` `#error`s when `UNIT_TEST` is defined, so the tracker header was made to compile GL-free under `UNIT_TEST` rather than pulling the GL loader into the test.

The highest-risk piece (the `GLMatrixStateTracker` value stack from Phase 1) is pure CPU math and needs no GL context. Add a Catch2 test beside the existing `test/engine/System/testMatrix44f.cpp` / `testRotationMatrix44f.cpp` (same `add_spring_test` macro, no extra libs).

What it pins:
- **Multiply order/side** — `glTranslatef` post-multiplies (`M' = M·T`, vertex transformed as `M·T·v`). The likeliest bug. Assert op sequences against hand-built `CMatrix44f` goldens. `CMatrix44f` is column-major `m[16]` with `operator==` (bit-exact SSE) and `equals()` (epsilon).
- **Push/pop round-trip** — `m0 = Get(); Push(); mutate; Pop(); CHECK(Get() == m0)`.
- **Mode independence** — `GL_MODELVIEW`/`PROJECTION`/`TEXTURE` are three independent stacks.
- **`MultMatrix`/`LoadMatrix`/`LoadIdentity`/`Ortho`/`Frustum`** vs goldens; depth over/underflow matches `HasMatrixStateError`.

Why this suffices for the transform layer: `CMatrix44f` is already the engine's matrix type (with its own passing tests), so its GL-convention correctness is established; these tests only prove the tracker *drives* it with GL semantics. Optional belt-and-suspenders: a one-time shadow assert in a GL build comparing the tracker against `glGetFloatv(GL_MODELVIEW_MATRIX)` — note `gl.GetMatrixData` already does exactly that `glGetFloatv`.

### Tier 2 — pixel equivalence: offscreen dual-FBO render test (new infra)

**Harness + self-validation landed 2026-06-30:** `test/engine/Rendering/testGLImmediateCompare.cpp` (target `testGLImmediateCompare`). This first increment is deliberately the *harness and its controls only* — it brings up a hidden SDL2 **compatibility** context + glad, renders into a raw-GL RGBA8 FBO, reads back, and runs the comparator's **positive control** (same legacy `glBegin` scene twice ⇒ byte-identical) and **negative control** (different scenes ⇒ unequal; plus a 1-texel-flip case so the comparator can't pass by ignoring small diffs). Verified green under `xvfb-run … LIBGL_ALWAYS_SOFTWARE=1` (Mesa llvmpipe) and it `SKIP`s cleanly with no display (4 skipped, exit 0), so GL-less CI stays green. CMake only builds it `if (TARGET SDL2::SDL2 AND TARGET glad)`.

Scoped out of this increment (next step): it depends only on SDL2 + glad — **no** `globalRendering`/`configHandler`/`CFBO`/`TypedRenderBuffer` yet, because the "new backend" side needs the immediate-mode emitter factored `lua_State`-free first (see below). The context bring-up, comparator, readback, SKIP behavior and the xvfb/llvmpipe recipe all carry forward; only the FBO-creation lines (raw GL → `CFBO`) and the addition of the real legacy-vs-new A/B cases change.

Pixels need a real context — the engine has **no GL-context test today** (all current tests run on the headless stub). This is the one new piece to build. Design it as an in-process **A/B compare** (render legacy backend and new backend in the *same* context, same frame, assert equal) rather than golden-PNG comparison — so GPU/driver pixel quirks cancel and there are no frozen artifacts to maintain or drift.

**Dependency reality (scoped):**
- `shaderHandler` — lazy singleton (`#define shaderHandler (CShaderHandler::GetInstance())`), self-initializing. Free.
- `globalRendering` (`CGlobalRendering`) — **the only real bring-up.** `TypedRenderBuffer`'s shader-header generation reads `globalRendering->supportExplicitAttribLoc`. The test must instantiate `globalRendering` and set its GL-capability fields after the context is current (or run its capability detection).
- Legacy backend (`glBegin`) needs only the context; new backend (`TypedRenderBuffer` + the migration shader) needs the two globals above.

**Design implication (do this in the migration):** factor the new immediate-mode emitter into a **`lua_State`-free class** (e.g. `LuaImmediateBuffer` that `BeginEnd`/`Vertex`/`Rect`/`TexRect` call into). Then the test drives it directly — no `LuaHandle`, no drawing-enabled gating, no Lua. The legacy comparator is raw `glBegin`/`glVertex`. This keeps the test small and makes the backend independently testable; the `LuaOpenGL` functions become thin shims.

**Harness components:**
1. **Context** — `SDL_Init(VIDEO)` + hidden window + `SDL_GL_CreateContext` with `SDL_GL_CONTEXT_PROFILE_COMPATIBILITY` (the engine's own fallback path), then `glad` load. Compat is required so both `glBegin` *and* GLSL work in one context.
2. **`globalRendering`** — instantiate, make context current, populate capability flags.
3. **Render target** — `CFBO` (`rts/Rendering/GL/FBO.h`: `AttachTexture`/`Bind`/`Unbind`/`IsValid`) with a color texture, fixed size (e.g. 512×512), fixed clear color.
4. **Per case:** render legacy → FBO_A → `glReadPixels` → bufA; render new → FBO_B → bufB; `CHECK(bufA == bufB)` (or `maxΔ` within a documented per-case bound).
5. **Graceful skip** — if context creation fails (CI box without GL), Catch2 `SKIP`, don't fail. Keeps existing CI green.

**CMake wiring:** new `add_spring_test(GLImmediateCompare ...)` target linking the rendering object lib + `SDL2` + `glad` + `Catch2`, with a `main` that creates the context (not the shared `catch_main.cpp`). Model on the existing `add_spring_test` macro but add the GL/SDL libs.

**CI reproducibility recipe:** run under **`xvfb-run`** with **`LIBGL_ALWAYS_SOFTWARE=1`** → Mesa **llvmpipe**, a deterministic, GPU-independent compatibility context that supports FF + GLSL. `xvfb-run ctest -R testGLImmediateCompare`. llvmpipe output is stable across runs/machines, so the A/B compare is reproducible; and because both backends render on the same llvmpipe, vendor differences are irrelevant anyway.

**Cases (one primitive per FBO render, exactness expectation per case):**

| Case | Expect |
|---|---|
| `gl.Rect` opaque; `gl.TexRect` (default/flipS/flipT/explicit s,t) | bit-exact (AE=0) |
| `BeginEnd` TRIANGLES / FAN / STRIP; `gl.Shape` mixed attrs | bit-exact |
| `GL_QUADS` axis-aligned; rect inside Push/Translate/Rotate/Scale/Pop | bit-exact (transform already proven in Tier 1) |
| `GL_QUADS` rotated 30°; `GL_POLYGON` pentagon | ≤±1 LSB at diagonal — investigate, don't tolerate silently |
| smooth tri (3 colors); textured+per-vertex color (MODULATE) | ≤±1 LSB interpolation |
| attribute stickiness (color on vert 1 only) | bit-exact |
| LINES/LINE_LOOP/POINTS (width 1) | document; width-1 should match |
| two overlapping translucent rects (A then B); rect→text→rect | correctness (draw order / blend / z-interleave) |
| empty `BeginEnd`; single vertex; callback that errors | no crash, no state corruption |

### Tier 3 — GL state side-effects (contract C3)

A primitive can produce identical pixels yet leave different *state* behind, silently corrupting the next draw. So alongside the pixel A/B, snapshot observable state after a legacy callin vs a new callin and assert equality: current color (`GL_CURRENT_COLOR`), active texture + binding, blend/enable bits, matrix mode + per-mode stack depth (`GLMatrixStateTracker` already exposes this), scissor/viewport. Same offscreen harness, `glGet*` after each case. This catches the "batched buffer flush left blend enabled" / "didn't restore current color" class of bug that pixels alone miss.

**Reuse:** the same A/B comparator is the in-engine **`LuaGLCompareMode`** for running over real replays (render each callin to two FBOs, report `maxΔ`) — coverage driven by the `DeprecatedGLWarnLevel=2` caller list. One comparator, two harnesses: fixed unit scenes (CI) + live replay (broad coverage).

### Scope / effort summary

- **Tier 1:** small — a single Catch2 file, no new infra, runs everywhere. Land it with Phase 1.
- **Tier 2:** one-time harness build (~SDL offscreen context + `globalRendering` cap init + CFBO + readback + `add_spring_test` target + Xvfb/llvmpipe CI job). The main prerequisite is factoring the emitter `lua_State`-free. After that, adding cases is trivial and it's CI-enforceable.
- **Risks/unknowns:** `globalRendering` capability init outside full engine boot may need a few fields set by hand (enumerate them when building); llvmpipe must be present in the CI image; confirm `CFBO` doesn't pull additional globals (it shouldn't — it's a thin GL wrapper).

## Whole-frame A/B "test mode" (config `GLFrameABCompare`) — in-frame 4-pass design

A validation harness that renders each display frame **four times within the same iteration** — [legacy settle (presented, uncompared), legacy, legacy (same-frame control pair), modern (test)] — and compares the readbacks in-frame. One `CGame::Update` + one `UpdateUnsynced` per iteration means every pass consumes byte-identical draw-prep (drawFrame, timeOffset, camera, shadow fit, cull sets, particle draw-pos bake, per-draw UBO, buffer uploads) **by construction**; the sim runs at full speed and the cost is ~4× GPU per displayed frame. The settle pass absorbs all once-per-drawFrame lazy work (widget dlist rebuilds, RTT cache refreshes, run-twice workarounds, lazy shader recompiles) — trace analysis showed every recurring L↔L divergence class lands there and is settled by pass 1. **Status: byte-perfect — control=0 and signal=0 on every in-game frame of the fightertest benchmark (see 2026-07-03 changelog).** (History: the previous design drew each sim state on two consecutive real frames with the sim frozen on the duplicate — it worked, but needed 8 fragile cross-iteration pins and produced a whole bug class of pin leaks.)

Only two things advance while a pass renders, so only two pins remain, snapshot before pass 0 and replayed before passes 1–2:

- **`guRNG` + Lua `math.random`** snapshot/restore (`spring_lua_unsynced_rand_save/restore_state`) — FX draw jitter consumed during draw callins
- **draw clock** — `Spring.GetTimer`/`GetTimerMicros` pinned; **`os.clock`** replaced in unsynced states (`LuaLibs::OpenUnsynced` → `LuaUnsyncedRead::OsClock`) — wind-turbine etc.

Per-pass work is redone **from scratch**: own FBO/viewport restore, `DrawGenesis`, **shadow-map render** (so `DrawWorldShadow` Lua is backend-compared too), `worldDrawer.Draw()` (own clear + water RTT), and the full screen block. Two deliberate once-per-iteration exceptions (identical for all passes, just not backend-compared): `minimap->Update()` (wall-clock-throttled RTT cache) and the reflection-cubemap face round-robin + consume-once sky/specular/shading updates.

The same-frame L↔L control serves double duty: its >1-LSB pixels build the per-frame noise mask for the L↔M signal, and its count is the **pixel-perfect gate** — any nonzero control is a real per-pass leak (stateful Lua draw callin, temporal RTT accumulator, per-pass GL nondeterminism) with an in-frame repro (one apitrace captures all three passes). `AB_FORCE_LEGACY=1` (env) turns the run into [L,L,L] — the null test where control **and** signal must be zero. `GLFrameABCompareWarmup` (default 120) renders that many frames normally first so Lua temporal accumulators build real state before the freeze guards engage.

**Lua pass protocol.** `Spring.GetABCompareActive()` — true for all passes of an active compare; widgets with same-frame temporal-feedback accumulators (bloom `historyTex`, diaglines `coverageTex`, CAS, ssao SLOWFUSE fuse) freeze those updates while true. `Spring.GetABPassIndex()` (0/1/2) — for per-pass *recompute* dedups: `api_screencopy_manager.lua` keys its copy on (drawFrame, passIndex) so each pass re-copies its own backbuffer (a frozen copy would make the modern pass composite legacy pixels through CAS/DoF/glass). `Spring.GetABDuplicatePass()` (= passIndex > 0) — for stateful draw-callin animation (`self.fade += n`): guard the advance so repeat passes redraw pass 0's phase.

Historical results from the paired-frame era (still-relevant conclusions): calm/settled frames byte-identical; combat residual was 94–99.8 % ±1–2 LSB and **backend-independent** (same under force-legacy) — GPU/driver-level, below the API; visible content divergence (delta>32) small and localized. The in-frame design eliminates the cross-iteration sources (different draw-prep paths, buffer re-upload vs reuse), so the [L,L,L] null gate must be re-measured — it is the new baseline for what "pixel-perfect" can reach.

## Divergence-hunting plan (post-gate, 2026-07-03)

The gate is byte-perfect and cheap; the loop for every step below is: run `GLFrameABCompare=1` → control must be 0 (a nonzero control is a per-pass widget/engine leak — fix via the three patterns: `GetABCompareActive` freeze / `GetABPassIndex` dedup key / `GetABDuplicatePass` advance guard) → any signal is a real modern-backend bug; localize via the dump PNGs → `LuaGLCompareMode` per-call attribution → apitrace pass-diff (segment on the per-pass `glReadPixels`, multiset-diff (prog,fn,count) per FBO).

1. ~~**Re-gate with modern `gl.BeginEnd` promoted**~~ done 2026-07-03, green.
2. ~~**Coverage breadth**~~ DONE 2026-07-04, fully green (see changelog): ALL gates byte-perfect — the Supreme Isthmus multiplayer replay (0/4942 frames incl. pregame, draft UI, reflections, chat/playerlist churn), fightertest (0/271), local water test (0/652, BumpWater+PiP), idle start (0/410). Seven engine parity-bug families found and fixed by the breadth content. Fast local loop established (~1 min/gate cycle via `test/gl-ab-compare/` scripts — replays cost ~10 min because the recorded lobby replays at 1x, so iterate locally and confirm on the replay).
3. **Daily-drive modern** — `LuaModernGLBackend=1` + `FontUseMVPUniform=1` in normal play (no compare cost); anything that looks off gets reproduced under the gate.
4. **Expand the modern surface**, each increment closed by the same loop: textured `gl.BeginEnd` (texenv/multi-unit aware — currently exact legacy replay), `gl.Shape`/`gl.DrawGroundQuad`, group-B uMVP fed from `GLMatrixStateTracker` instead of the `glGetFloatv` bridge (Phase-0 prereq), then Phase 2 display lists (the font-in-list lesson: lists need real capture semantics, not stream aliasing), then the Phase-0 endgame (UBO MVP everywhere → remove `SetupScreenMatrices` FF + attrib stack + `ResetGLState` FF resets) → first RenderDoc capture attempt.

## Open questions / to-verify

- Which LuaOpenGL helpers beyond the A/B/E set internally emit immediate mode? (`gl.DrawGroundQuad` confirmed; audit ground-circle, shape, unit/feature draw helpers.)
- Does `gl.CreateList`'s engine implementation capture recorded calls in a way that can be re-backed onto a retained VBO without changing the Lua API? (Determines whether Phase 2 is zero-BAR-change.)
- Exact `AttribBits` set saved by `EnableCommon`'s `glPushAttrib` — needed to build the explicit modern save/restore.
- Are the offline/bake gadgets (`unit_icongenerator`, `Gen*Lut`) ever run during a live frame, or only at load? (If load-only, they don't affect in-match capture.)
- Tier-2 test: exactly which `globalRendering` capability fields must be set for `TypedRenderBuffer::GetShader()` to build (enumerate when wiring the harness; `supportExplicitAttribLoc` confirmed, likely also GL version/extension flags).
- Is Mesa `llvmpipe` available in the project's CI image? If not, the Tier-2 test only runs locally / on GL-capable runners (it SKIPs gracefully elsewhere).
- Does `CFBO` pull any globals beyond a current context? (Spot-check when building the harness — expected to be a thin GL wrapper.)

## History

`flowui_gl4.lua` (2589 lines) was a prior GL4 reimplementation of the FlowUI `Draw.*` API by Beherith — born 2021 on the GL4 branch, reverted once (2021), re-landed and developed 2022–early 2024, then sat dormant/unloaded (only an inert `gfx_ssao.lua` hook referenced it) and was removed in PR #7990 (2026-06-17, empty body, self-merged in 10s). Zero of the 40 `WG.FlowUI.Draw` widgets ever adopted it. Lesson: the migration died of non-adoption, not infeasibility — which is why the engine-side (behind unchanged `gl.*` API) approach is preferred here over a per-widget rewrite. Related: BAR PR #7813 (open) adds no-geometry-shader paths for ~22 GL4 widgets on macOS/Apple Silicon — a portability tax specific to the per-widget GL4 approach.
