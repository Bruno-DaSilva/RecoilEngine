# Research: decoupling sim and draw into two threads

Status: research only (2026-07-04). No code changes. Companion docs: `ENGINE_PERFORMANCE.md`, `move-unsynced-gameframe-work-todo.md`, `replay-seeking-architecture.md`.

## Update 2026-07-05: decisions and refinements

Outcome of a deep-dive on the Lua boundary and the strategic sequencing. The compatibility invariants and the concrete PR plan are at the end of this doc; the seeking-specific design (stream format, stages S0–S4, scrub UX) lives in `replay-seeking-architecture.md`.

1. **Driving use case: replay seeking.** The boundary work ships as a product (instant replay scrubbing) instead of an invisible refactor. Stage order: extraction v1 + stream format (S1) → stream-fed drawers (= Phases 0–1 here) → seek UX (S3) → sim thread slots in later as background-resim engine. The thread split becomes the capstone, not the driver.
2. **Lua mechanism settled: redirect the callout glue, not the VM.** No sim state lives in any `lua_State`; the entire sim-touching surface is the C callout layer (~250 `LuaSyncedRead` + ~177 `LuaUnsyncedRead` + ~169 `LuaUnsyncedCtrl` entry points). Snapshot-backed reads = flat arrays keyed by unitID (IDs are already dense and already the Lua-facing handle). No interpreter changes; API surface byte-compatible.
3. **No ECS prerequisite.** Extraction is a hand-written field copy running at the quiescent barrier — it walks live pointers and emits values; no pointers cross the boundary, so the sim graph stays pointer-rich. ECS remains an optional follow-up: once all draw-side consumers of a field read the extracted array, moving the authoritative copy to SoA is a small per-field step (the boundary is the incremental migration path, not the obstacle).
4. **Unbounded structures (command queues, paths): dirty-versioning + immutable shared blocks.** Copy cost is bounded by mutation rate (order-rate, not frame-rate); snapshot generations share unchanged blocks via refcount. `GetUnitCommands` already walks the queue per call today, so one boundary copy costs ~one current call. Interest tracking is an optimization only, not load-bearing. Spatial queries are boundary-computed against a draw-side grid.
5. **DECISION — no lock fallback, boundary must be complete.** Both blanket sim-locking (old option E-1) and a cold-miss lock are rejected: they convert functional gaps into load- and widget-dependent timing edges (unattributable hitches in the field). The boundary serves everything it claims to serve; the residual tail gets a documented deterministic contract (stale/nil, identical every time, dev-mode warning naming the callout). Consequence: full classification of the callout surface, driven by call-count telemetry, is a *precondition* of enabling the split — not lazy per-miss work.
6. **Claims corrected after review.** (a) Snapshot hashing localizes desyncs in *space* (which unit/field), not time — per-frame detection already exists (`NETMSG_SYNCRESPONSE`), and deep-state divergence can precede observable divergence by many frames, so root-causing still needs deep tools; hierarchical hash ring (~unit-level, ~25 MB for a ~10 s window) is the always-on tier, field-level values via opt-in ring or offline deterministic resim. (b) Render-replay is a byproduct, not a pillar: useful for GL-migration parity, sim-noise-free draw benchmarks, and crash-repro iteration (share the demo + frame; regenerate the stream locally; clip a few seconds around the crash for hand-off). (c) The observable stream **cannot** serve reconnect-to-live-game — reconnect needs bit-identical deep sim state; the stream is a viewing artifact only.
7. **GL4 migration will not come first and is not a dependency.** The boundary serves the current widget call pattern at full fidelity; migration, if/when it happens, shrinks demand as a bonus only.
8. **Size estimates are gated on measurement.** Two known holes in the Fermi numbers: per-piece pose data (~10–20 MB/frame raw at 10k units; `transformsMemStorage` is the in-tree proxy to instrument) and projectile lifecycle churn (mitigation: ballistic projectiles re-derived from spawn params; guided ones need updates). Hard gate: instrument sizes and dirty rates over 2–3 late-game BAR replays before designing the stream format. **GATE CLEARED 2026-07-05** — S0 measured over two full-length replays (Wave-3 PRs 1–2): piece-pose raw was a 4–8× overestimate (~0.5–0.8 MB/frame at 2–3k units, 33–35% per-frame churn), projectile churn is a non-issue (synced spawns ~2/frame, ≥96% re-derivable, guided 2–4%); measured numbers now in `replay-seeking-architecture.md` "Sizes".

## TL;DR / verdict

It is feasible, but it is a large multi-phase project, and the hard part is not the threading — it is the ~8 places where draw and sim alias the same objects, plus Lua. The engine already contains about half of the required "extraction layer" (transform/uniform SSBO copies, heightmap synced/unsynced split, one-frame `preFrameTra` interpolation base). The blockers, in increasing order of pain:

1. Draw code **writes into sim objects** every frame (`drawPos`, `drawFlag`, icon state, `drawAlpha`, `losStatus &= ~LOS_PREVLOS`, localModel dirty flags) — must move to render-owned side structs.
2. Sim fires **synchronous render events** mid-sim-frame (`RenderUnitCreated` etc. mutate drawer containers) — must become a queued delta applied at a frame boundary.
3. The **CPU draw pass and picking read live sim objects** directly (localModel, collision/selection volumes, quadfield) — needs extraction (locks rejected, see decisions above).
4. **Lua draw callins** (`DrawWorld` etc.) call `Spring.GetUnitPosition`-class callouts that dereference live sim objects arbitrarily — the single biggest obstacle, with no cheap fix.

The realistic architecture is **pipelined frames with a synchronized extract boundary** (the Factorio model), not free-running threads: sim thread simulates frame N+1 while the main thread renders frame N; they meet once per draw frame at a short "extract" barrier. GL stays on the main thread (mandatory), so it is the *sim* that moves off-main.

Recommendation (updated 2026-07-05): execute Phases 0–1 as the PR plan at the end of this doc, shipped as the replay-seeking product (`replay-seeking-architecture.md`); the thread split (Phase 2+) is the capstone, gated on boundary completeness for the draw-time callout surface. The BAR GL4 migration is explicitly **not** a prerequisite and will not come first — the boundary must serve the current widget call pattern as-is; any future migration only shrinks demand as a bonus.

## Prior art

- **GML / Spring MT (zerver fork, ~91–94 era)**: threaded GL wrapper + partially-threaded sim ("ASIM") + async pathfinding ("APATH"). Never merged; removed. Failure modes: licensing, fragility, and — decisive — Lua-driven rendering made the GL translation layer slower than single-threaded for the games people actually played. The docs literally warned "LUA-BASED GRAPHICS WILL CAUSE HIGH CPU LOAD AND SEVERE SLOWDOWNS". Nothing of it remains in the tree (grep is clean); this is a clean slate, not an untangling job.
- **Current maintainer direction** (issues #2316 ECS, #1210 MT loading, threadpool/affinity work): keep one main thread for deterministic sim + GL, push data-parallel work onto the worker pool (`for_mt`), run QTPFS in the background, move render-prep off the sim frame. #1210 states the two invariants directly: *Lua is not multi-thread safe* and *OpenGL calls must occur on the main thread*. No open issue proposes a sim/render split.
- **Factorio** (FFF #215, #281): same lockstep-determinism constraints; their landed design is exactly the pipelined model — render runs parallel to update, synchronizing only a short "prepare" step, leaning heavily on interpolation. That is the shape to copy.

## Current architecture (what the seam looks like)

One thread does everything, in this order per iteration (`SpringApp::Run` loop, `rts/System/SpringApp.cpp:905,919`):

```
SpringApp::Update (SpringApp.cpp:871)
├─ CGame::Update (Game.cpp:1370)            [SIM SIDE]
│   └─ ClientReadNet (NetCommands.cpp:259)
│       └─ 0..N × NETMSG_NEWFRAME → SimFrame (Game.cpp:2000)
│            (budgeted: msgProcTimeLeft, capped by minDrawFPS so catch-up
│             can't starve rendering — NetCommands.cpp:239-241)
├─ CGame::Draw (Game.cpp:1640)              [DRAW SIDE]
│   ├─ UpdateUnsynced (Game.cpp:1418)
│   │   ├─ timeOffset = fraction of a sim frame elapsed (Game.cpp:1472)
│   │   └─ worldDrawer.Update → DrawerData extraction, drawPos interpolation
│   └─ drawOnePass(): shadows, DrawWorld, DrawScreen
└─ SwapBuffers
```

Useful properties already in place:

- **Draw FPS is already decoupled from sim rate logically** — draw interpolates between the last two sim states via `preFrameTra` (snapshotted in `UpdatePreFrame`, `Game.cpp:2070`) and `globalRendering->timeOffset`. The one-frame transform double-buffer a sim thread needs *for positions* already exists.
- **GameServer already runs on its own "netcode" thread** (`GameServer.cpp:263`) and flow-controls NEWFRAMEs against the client's consumption (`GameServer.cpp:2694-2700`). Sim pacing needs no changes; the sim thread just becomes the consumer of the net queue instead of the main thread.
- **The heightmap is the reference pattern**: synced heightmap owned by sim, unsynced copy owned by draw, dirty-rect queue drained at draw time (`ReadMap.cpp:69-70,96,496-514`). This is what "done right" looks like for every other shared structure.
- **The GPU data path is already an extraction layer**: `UpdateObjectUniforms`/`UpdateObjectTrasform` copy sim fields into `transformsMemStorage`/`modelUniformsStorage`, streamed to triple-buffered persistent SSBOs with dirty-lists (`ModelDrawerData.h:161-218`, `ModelsDataUploader.cpp`), guarded by `CModelsLock`, deduped per sim frame via `lastSyncedFrameUpload`.

## Threading infrastructure status

- There is **no sim-thread concept**: thread registry is `MAIN/LOAD/SND/VFSI/WDOG` only (`Threading.h:30-37`). Adding `THREAD_IDX_SIM` + `SetSimThread/IsSimThread` + a watchdog slot is trivial and mechanical.
- The **game-load thread** (`GameLoadThread.cpp`, `LoadLock.h`, `WrappedSync.h`) is the working in-tree template for "second thread that shares state with main under a handoff lock", including streflop FPU init on the new thread and the runtime `SetThreadSafety` toggle to make the locks free when the feature is off. A sim thread should copy this pattern (minus the GL-context handoff — the sim thread must never touch GL).
- **ThreadPool is a shared blocking fork-join pool** used by *both* sim (`LosHandler`, QTPFS/HAPFS, `GroundMoveSystem`, `UnitHandler`) and rendering (`UnitDrawerData`, `FeatureDrawerData`, `RoamMeshDrawer`, `ProjectileDrawer`). Today these serialize through the main thread; with two threads calling `for_mt` concurrently they contend, and `GetThreadNum()`-indexed scratch buffers (`ThreadPool.h:475,494,538,553`) assume one fork-join in flight. Needs either two pools, pool partitioning, or making the per-thread scratch fork-join-instance-local. Sim correctness asserts (`assert(!ThreadPool::IsInMultiThreadedSection())`) keep working unchanged since they are thread-local.
- **One GL context, main thread only.** That is fine for this design — the sim thread does no GL. The `IsMainThread() || IsGameLoadThread()` asserts in `IModelParser.cpp:238,262,467` and `ExplosionGenerator.cpp:1090` mark places where *sim code triggers GL-resource creation* (model loading on unit build, explosion generators); these become cross-thread requests that must be queued to the main thread (model preload already has machinery pointing this direction).

## The coupling inventory (what actually has to change)

### A. Draw→sim write-backs (must move to render-owned storage first)

These are fields that live inside `CUnit`/`CFeature`/`CProjectile` but are authored by draw code every draw frame. With a sim thread they are races on day one:

| Field | Written at | Fix |
|---|---|---|
| `drawPos`/`drawMidPos` | `UnitDrawerData.cpp:367-379`, `FeatureDrawerData.cpp:212-217`, `ProjectileDrawer.cpp:379` | **DONE 2026-07-05 (PR 3)** — fields evicted from `CSolidObject`/`CProjectile` into drawer-owned storage: id-keyed `drawPositions` arrays in `CModelDrawerDataBase<T>` (units/features; zeroed on AddObject, stale-on-death, unregistered objects read zero — all matching the old member semantics), a `renderIndex`-parallel vector in `CProjectileDrawer`. Rode along in the same eviction: `CFeature::transMatrix[2]`'s unsynced half (was written by `FeatureDrawerData.cpp:212` at draw rate into a creg-serialized sim member) → `CFeatureDrawerData::unsyncedTransforms`, and `CProjectile::GetTransformMatrix` ("UNSYNCED ONLY") → `CProjectileDrawer::GetTransformMatrix`. Pattern statement for PRs 4-6/9: **sim objects expose only synced transforms** — `GetTransformMatrix()` lost its `synced` parameter entirely so every call site broke at compile and was explicitly re-audited (no silent semantic flips); draw-time variants are drawer statics (`CUnitDrawer::GetDrawPos/GetDrawMidPos/GetObjDrawMidPos/GetObjDrawErrorPos/GetObjectSpaceDrawPos/GetUnsyncedTransformMatrix`, `CFeatureDrawer::` equivalents). Audit result in sync-audit item 1 below |
| `drawFlag`/`previousDrawFlag` | `ModelDrawerData.h:224-225` + cull passes | render-side visibility bitset |
| `SetIsIcon`/`currentIconIndex`/`iconRadius` | `UnitDrawerData.cpp:263-364` | mostly render-side, **but `iconRadius` is used for click-picking** — keep in the picking snapshot. `SetIsIcon` **DONE (PR 4)**; the rest **DONE 2026-07-05 (PR 5)** — all remaining icon state evicted from `CUnit` (`definedIconName`, `currentIconIndex`, `customIconIndex`, `iconRadius`, `drawIcon` — the last two surfaced by the compile-break sweep) into drawer-owned id-keyed storage: `CUnitDrawerData::UnitIconState` slots, default-initialized at the `RenderUnitPreCreated` drain (where `definedIconName` is captured from the unitDef, replacing the sim-side `CUnit::UpdateRenderParams` creation-time copy — that method is gone), stale-on-death, unregistered ids read defaults. Accessors are drawer-data methods exposed via `CUnitDrawer` statics (`GetUnitIconIndex/GetUnitIconRadius/GetUnitDrawIcon/SetUnitDrawIcon/SetUnitCustomIcon`); `iconRadius` stays picking-visible through `CUnitDrawer::GetUnitIconRadius` (`TraceRay.cpp` draw-side picking reads the drawer value — no sim member reintroduced). Sync audit clean: zero synced readers; the only sim-context reference was the write-only `UpdateRenderParams` copy. Enumerated deviations (all unsynced, sliver-width): creation-sliver Lua icon writes reset at the drain, `definedIconName` freeze point moved to the drain, the five members left creg (recomputed after load; `customIconIndex`/`drawIcon` reset to defaults), `Spring.GetUnitIcon` on an invalid unitID now returns nil instead of dereferencing null |
| `CFeature::drawAlpha` fade | `FeatureDrawerData.cpp:150-176` | render-side. **DONE 2026-07-05 (PR 6)** — evicted from `CFeature` (field + `CR_MEMBER`) into drawer-owned id-keyed storage `CFeatureDrawerData::drawAlphas`: slot init 1.0f at the `RenderFeaturePreCreated` drain **and reset to 1.0f on destroy** — a deliberate deviation from the PR-3/5 stale-on-death convention, because the decal-fade reader touches arbitrary features incl. non-model ones that never register, so an id reused by a non-model feature must read the fresh-member 1.0f; unregistered ids read 1.0f. Accessor `CFeatureDrawer::GetDrawAlpha`. Reader set is small — the feature alpha render passes use the global `IModelDrawerState` alpha, not this field: `IsAlpha`, the fade writer itself, `LuaObjectDrawer::DrawBinObject` (Lua-material teamcolor alpha), `GroundDecalHandler` decal fade. Zero synced or sim-context readers; no Lua surface (`Spring.SetFeatureFade` writes `alphaFade`, untouched) |
| `p->SetSortDist` | `ProjectileDrawer.cpp:403` | render-side sort key array. **DONE 2026-07-05 (PR 6)** — `sortDist[3]` + `Get/SetSortDist` evicted from `CProjectile` into a renderIndex-parallel `sortDists` vector in `CProjectileDrawer` (maintained alongside `drawPositions`/`drawFlags`: zero on registration as the old member default, swap-erase on destroy); the two z-sort comparators read the drawer storage. `sortDistOffset` stays a `CProjectile` member — sim-authored (`SmokeTrailProjectile` ctor), only read at draw time when the drawer computes the key, exactly as the old setter did. Was already `CR_IGNORED`; zero synced or sim-context readers |
| `LocalModelPiece` dirty-flag reset/recompute | `ModelDrawerData.h:182-197` | **DONE 2026-07-05 (PR 8)** — transform upload is now a defined extraction pass (`CModelDrawerDataBase::ExtractTransforms`, full contract in the comment block there): runs once per new sim frame at the first drawer-data `Update()` after the sim burst and covers every object in LOS for the local allyteam (everything under full-view/replay), camera-independent — the old per-object `lastSyncedFrameUpload` dedup and the drawFlag gate are gone — so no post-extraction consumer ever recomputes a dirty piece in place; `wasUpdated`/`noInterpolation` are consumed and reset only at the extraction point. The LOS (not drawFlag) gate is deliberate: extracting *hidden* objects would push live enemy poses into the Lua-shader-readable SSBO, widening master's information surface (same gate as `UpdateObjectUniforms`); out-of-LOS objects keep master's accumulate-until-seen behavior, and per-allyteam extraction is the boundary contract a player-POV client will need anyway. Key audit result: every LMP mutator is synced-sim-side (UnitScript ticks, `Move/TurnNow`, `Spring.SetUnitPieceMatrix`, `SetScriptVisible`), so nothing can mutate between extraction and draw — legacy `GetModelSpaceMatrix()` reads and the extracted SSBO copies are frame-consistent by construction. Layout for PR 15: per-object slots `[0]`=prev root (preFrameTra), `[1]`=curr root, `[2+2i]/[3+2i]`=piece i prev/curr model-space, script-invisible pieces = `Transform::Zero()` |
| `losStatus[allyTeam] &= ~LOS_PREVLOS` | `UnitDrawerData.cpp:637,720-721` | **DONE 2026-07-05 (PR 7)** — audit found the doc premise wrong: the clears fire at sim-event time (not draw rate) and LOS_PREVLOS has synced readers (GetErrorVector→weapon aim, GenerateWeaponTargets, synced GetUnitLosState), so a render-side shadow would desync; the clears moved *into* sim (`CUnit::SetLeavesGhost`, bit-identical ordering) and the dying-unit clear was dropped as provably unobservable — drawer is now a pure losStatus reader |
| `tempNum`/`mtTempNum` quadfield scratch | `WorldObject.h:82,104` stamped by draw-time `GuiTraceRay` | dedicated scratch id space for draw-thread queries, or picking runs at the boundary |

Each row is an independently landable refactor with zero behavior change. This is Phase 0.

### B. Synchronous sim→render events

`RenderUnitPreCreated/Created/Destroyed`, `RenderFeatureCreated/...`, LOS-transition events fire from inside the sim frame and synchronously mutate drawer containers, ghost lists, decals, and the SSBO alloc maps (`Unit.cpp:381-404`, `UnitHandler.cpp:292`, `UnitDrawerData.cpp:662-723`, `GroundDecalHandler.cpp:1737-1753`). These must become a per-sim-frame **delta queue** (create/destroy/LOS-change records) drained by the draw side at the extract boundary. The heightmap's `unsyncedHeightMapUpdates` queue is the in-tree precedent. Deletion is the sharp edge: drawer containers hold raw `CUnit*`/`CFeature*` (`ModelDrawerData.h:82`), so object lifetime must be extended past the boundary (deferred-delete list owned by sim, freed only after the draw side acks the destroy delta).

**DONE 2026-07-05 (PR 13)** — deferred-deletion epoch, landed as `DeferredObjectDeleter` (`rts/Sim/Objects/DeferredObjectDeleter.{h,cpp}`; full contract in the class comment). The destroy sharp edge is resolved by splitting sim-object destruction: the sync-observable destructor half moved verbatim into a virtual `PreDestruct()` (`CObject` severing, `~CUnit` wreck/weapon/script/AI teardown, `~CBuilding`/`~CExtractorBuilding` unblock/extraction, `~CFeature` unblock/quadfield/geotherm, `~CProjectile` quadfield, nano-counter, damage-array decref) that still runs at the exact old `pool.free()` call sites — sim timing bit-identical to master by construction — while the undestructed shell (plain fields + localModel) parks in the deleter so every queued record, including the destroy itself and any earlier creation/LOS records of the same object, dispatches against valid memory in exact fire order at the drain. The three `Render*Destroyed` methods became the plain `Push` the PR-12 handoff note promised; the mid-sim flushes are gone. Ack point is a single explicit function: `AckDrainedDestroys()`, called right after `renderEventQueue.Drain()` in `CGame::Draw`, runs the real (body-skipping, guarded by `CObject::detached`) destructors and overwrites each slot with `0xDE` poison; `ReleaseAcked()` at the end of the same Draw returns the slots to the pools (which zero on free — the poison is the detector for the pooled-UAF bug class ASan cannot see, since any draw-side dereference after the ack faults on `0xDEDE..` instead of reading a zeroed/recycled slot; a DEBUG-only scan at release also catches stray writes). Deferring member teardown and slot reuse is sync-safe by address-blindness; verified by both master-demo resim gates. Pool-pressure valve: the pools are fixed-size, so `Defer()` checks headroom and below 64 free pages flushes the queue in place (the pre-PR-13 destroy semantics) and releases everything — under the future split this becomes "sim waits for the boundary". Post-drain invariant handlers may now rely on: drawer containers hold live objects only. Enumerated Lua deviation (doc'd tier (a)): the `RenderUnitDestroyed` callin now fires at the boundary drain, so callouts querying the dead unitID inside it get nil (the pushed id/defID/team args are read from the shell and unchanged).

**DONE 2026-07-05 (PR 12)** — landed as `RenderEventQueue` (`rts/Rendering/Common/RenderEventQueue.{h,cpp}`; full semantics in the class comment). Creation and LOS-transition events append plain-data records during the sim phase (window opened by `CGame::Update`) and drain — everything, in exact fire order, once — at the top of `CGame::Draw`; catch-up bursts therefore apply N frames of records at one boundary. Facts that sim overwrites before the drain are captured into the record at fire time: the LOS records carry the event-time `leavesGhost` bit, and `UnitLeavesGhostChanged` (the direct `unitDrawer->` call from `CUnit::SetLeavesGhost`, which reads `losStatus` that sim clears immediately after firing) is now also a queued record carrying a per-allyteam dead-ghost mask — with every ghost-container mutation flowing through the queue in order, drawer end-state replays master op-for-op. Destruction stays synchronous behind the same API per the PR-13 gate: `Render*Destroyed` flushes the queue (an early drain, order-exact) then dispatches in place, because all three destroy sites free the object immediately after; the PR-13 handoff point is exactly those three methods (flush+dispatch → plain Push once the deferred-deletion epoch extends lifetimes). Outside the sim phase the queue dispatches immediately (empty-queue invariant preserves order): game-load creation and Lua draw-callin CEG spawns keep master timing. The record types are the seed of the recorded-stream event schema (`replay-seeking-architecture.md`); the embedded object pointer was a transitional dispatch handle that PR 14 (ID-keyed drawer containers) removed — records are plain data since, dispatch resolves ids (see the PR 14 row).

### C. Direct sim reads during the draw pass

Beyond the Update-phase extraction, the actual render pass still dereferences live objects: `u->drawFlag`, `GetIsIcon()`, `luaDraw`, `localModel.Draw()`, `GetTransformMatrix()`, minimap's `losStatus`/radar radii reads (`UnitDrawer.cpp:188-451`, `MiniMap.cpp:862,1943-1963`; the losStatus reads are snapshot-served since PR 15 — the radar-radii/selection reads in `DrawUnitRanges` remain live pending their fields), health-bar reads, `CUnitTracker`/`DollyController`/`DynWater` position reads, info textures reading sim LOS/metal state, `SmoothHeightMeshDrawer` reading the sim mesh. Under the pipelined model these all read **frame-N state while sim mutates frame N+1** — every one must either (a) read from the extracted snapshot, or (b) be tolerable-if-torn (some are: health bar off by a frame is invisible; LOS texture a frame late is invisible) with the field made atomic-relaxed. An explicit torn-read policy per field beats trying to snapshot everything.

**DONE 2026-07-06 (PR 24) — the §C torn-read policy table.** A manual sweep of `rts/Rendering/`, `rts/Game/UI/`, and the draw-side halves of `rts/Game/` (the PR-10 constify pass made these reads const-clean, so the compiler no longer finds them — the sweep was grep-driven over the sim-object member reads and the handler globals `losHandler`/`metalMap`/`readMap`/`smoothGround`/`quadField`/`teamHandler`/`playerHandler`). Every site is classified below with its split-time action; the conversions for the (a) rows landed in this PR. This table is the torn-read contract PR 27b enforces.

*Framing — the read-context axis decides everything.* Three contexts, only one of which is the hazard:
- **DRAW** (per-frame render pass / camera / UI draw) — runs on the future draw thread while the sim advances frame N+1. This is the actual §C torn-read surface and the only context that gets an (a)/(b)/(c) verdict below.
- **UPD** (the drawer *extraction* passes — `CUnitDrawerData::Update*`, `CModelDrawerDataBase::ExtractTransforms`/`UpdateObjectUniforms`, `CFeatureDrawerData::Update*`, `CProjectileDrawer::UpdateDrawFlagsAndPos`, and `GetObjDrawErrorPos`→`GetErrorVector`) run once per new sim frame at the drawer `Update()`, i.e. they *are* the boundary extraction layer (§ "GPU data path", PR 8). Under the split they run inside the extract barrier with the sim paused → **class (c) by construction, not a torn read**; PR 26 only has to pull them under the one formal barrier function. Not reclassified, not converted here.
- **EVENT** (`Render*Created/Destroyed`, `Unit/FeatureMoved` decal callbacks) — fire from inside the sim frame and are already the boundary-drained `RenderEventQueue` / event-time-captured records (PR 12/13). Out of scope, handled.

*Two whole families are owned by sibling PRs and excluded here* (they surfaced in the sweep; listed so the table is complete, not re-litigated):
- **Picking / selection queries** → **PR 25**: `CGameHelper` `ClosestUnit`/`ClosestFriendlyUnit` `AddUnit`/`Filter` paths, `CMiniMap::GetSelectUnit`/`SelectUnits`/`ProxyMouse*`, `CSelectionKeyHandler::DoSelection` + its filter predicates, the box-select `midPos` loops, and the `quadField` walks in `DebugColVolDrawer`/`DebugDrawerQuadField`. §D decided these read a draw-side snapshot + spatial grid.
- **`teamHandler`/`playerHandler`/`CTeam`/`CPlayer` reads** → **PR 26** (§E.3 player/team boundary copy, already classified): team-colour lookups (`teamHandler.Team(team)->color` in `UnitDrawer`/`FeatureDrawer`/`ModelDrawerHelpers`/`LuaObjectDrawer`/`3DModelVAO`/`UniformConstants`), `CTeamHighlight`, `InMapDrawView` owner colours, `ResourceBar`/`ShareBox`/`QuitBox`/`EndGameBox`/`PlayerRoster*`/`GameSetupDrawer`/`StartPosSelecter` dialogs, and the tooltip/`SelectedUnitsHandler` team-text reads. The per-unit `team`/`allyteam` reads that feed these are a boundary-copy input, not a SimSnapshot row.
- **`readMap`/`CGround` height & shading reads** from draw (`GetCurrMin/Max/AvgHeight`, `GetShadingTexture`, `GetHeightMapTexture`, `CGround::GetHeightReal`/`GetHeightAboveWater`, `GetTypeMapSynced`, `Height`-info-texture) are **already draw-safe** via the existing synced/unsynced heightmap split (the reference dirty-rect pattern, § "Current architecture") — no §C action. Noted, not tabled.

*Classification principle for the remaining DRAW-context sim-object + LOS/metal/smooth-mesh reads.* A site is **(a)** and converted **only when the read maps to an already-extracted SimSnapshot row *and* converting makes the site sim-object-clean** (no residual live-object deref) — a pure consistency win at zero added extraction cost. It is **(b)** when a one-frame-stale value is visually invisible: leave the live read, mark the field atomic-relaxed at split time. It is **(c)** when the read is a container / unbounded / deep structure that cannot be an atomic scalar and must move to the barrier or a bounded copy. **No new SimSnapshot rows were added**: every candidate for one (the range radii, `activated`, `maxRange`, `mass`, …) is a *cold* UI-overlay scalar whose torn read is invisible — adding it to the hot every-boundary per-unit extraction (which walks all active units) to serve a cold path is the wrong trade; atomic-relaxed is the cheaper, correct mechanism (task rule: "add a row only when a consumer needs one", and a stale-invisible scalar does not). This directly answers §C's "`DrawUnitRanges` … remain live pending their fields": the decision is **not** to add those fields — they are (b).

**(a) snapshot-served — CONVERTED in PR 24.** (Accessors added to `SimSnapshot::UnitRows`: `MidPos`/`Radius`/`Frontdir`/`Rightdir` — pure exposure of existing rows, so no `Resize`/`Extract`/`SnapshotHash`/`SnapshotDiffGate` change; the existing field pass already proves these rows equal live.)

| Site | file:line | Read → accessor | Why (a) |
|---|---|---|---|
| `CUnitTracker::SetCam` | `UnitTracker.cpp:287,295-303` | `u->radius`/`frontdir`/`rightdir` → `Radius`/`Frontdir`/`Rightdir(u->id)` | DRAW camera update; position already drawer-owned (`GetDrawMidPos`), so converting these three leaves `u` used only for its id — sim-object-clean. |
| `CDollyController::Update` | `DollyController.cpp:114` | `u->IsInLosForAllyTeam(myAllyTeam)` → `LosStatus(u->id, myAllyTeam) & LOS_INLOS` | DRAW camera update; the LOS gate was the only sim-object read (position is `GetDrawPos`). Fully clean. |
| `CGuiHandler::DrawCentroidCursor` | `GuiHandler.cpp:4035` | `unit->midPos` → `MidPos(unitID)` | DRAW; single read; selected units are own units (always in LOS) so the raw boundary midPos is exact. |

**(b) tolerable-if-torn — LEFT LIVE, mark atomic-relaxed at split.** The dominant class; a one-frame-stale value is invisible.

| Site(s) | file:line | Fields | Split-time action |
|---|---|---|---|
| `CMiniMap::DrawUnitRanges` scalars | `MiniMap.cpp:1934-1941` | `radarRadius`/`sonarRadius`/`jammerRadius`/`activated` (`pos`/`beingBuilt` have rows) | atomic-relaxed; selected-unit range circles, stale-invisible. (The weapon-interceptor loop in the same fn is (c) — see below.) |
| `CGuiHandler` range/decloak/facing scalars | `GuiHandler.cpp:3440-3444,3757-3808,3912-3924,2521-2522` | `radarRadius`/`sonarRadius`/`seismicRadius`/`jammerRadius`/`sonarJamRadius`/`maxRange`/`decloakDistance`/`stockpileWeapon`/`buildFacing` | atomic-relaxed; build-cursor/range overlays. |
| `CTooltipConsole` unit-info scalars | `TooltipConsole.cpp:162-333` | `health`/`maxHealth`/`experience`/`cost`/`maxRange`/`resourcesMake`/`resourcesUse`/`harvested`/`harvestStorage`, `losStatus` | atomic-relaxed; hover text. `health`/`maxHealth`/`losStatus` have rows but the reader also needs rowless fields, so the whole hover read stays (b) rather than a partial conversion. |
| `CUnitDrawer` icon/minimap/alpha passes | `UnitDrawer.cpp:113-118,413-475,579-586,687-718,922-970,1797-1843` | `losStatus`, `radius`, `noMinimap`, `IsInVoid`, `isSelected`, `buildFacing`, `buildProgress`, `engineDrawMask`, ghosted-building `lgb->*`; object-space midpos math (`GetObjDrawErrorPos`/`GetObjDrawMidPos` → `frontdir`/`updir`/`rightdir`/`relMidPos`) | atomic-relaxed per scalar/vector; frame-N visuals over the bounded on-screen drawer container. Object-space midpos upgrade path: an **id-keyed snapshot error-pos helper** (the pattern already proven by the PR-18 `GetUnitViewPosition` twin, `LuaSnapshotServe.cpp:390`) if consistency is ever wanted. `isSelected` is unsynced draw-owned (no race). |
| Projectile self-`Draw()` family | `Env/Particles/Classes/*` (~20 classes) | own visual members (`age`/`color`/`size`/`dir`/`pos`/`speed`/…) mutated by sim `Update()`; `RepulseGfx` cross-object `repulsed`/`owner` pos | atomic-relaxed per scalar, or a future projectile-visual-state extraction pass. Projectile world pos already drawer-routed (`GetDrawPos`, PR 3). |
| `LightHandler::UpdateLights` | `LightHandler.cpp:168-206` | tracked `u->frontdir`/`GetObjectSpaceVec`, `po->dir` | atomic-relaxed; light offset, stale-invisible. |
| **LOS maps** (info textures + point queries) | `Los/AirLos/Radar/Path.cpp` Update; `losHandler->InLos/InAirLos` at `UnitDrawerData.cpp:264`, `ProjectileDrawer.cpp:620,701,1079`, `LightHandler.cpp:206`, `GroundDecalHandler.cpp:1562-1586`, `HAPFSPathDrawer.cpp:133,170`, `QTPFSPathDrawer.cpp:404`, `DebugDrawerQuadField.cpp:132` | per-viewAllyTeam los/airLos/radar/jammer maps; positional `InLos` gates | **explicit entry:** a frame-late LOS overlay/gate is invisible → (b). Split action: a **draw-owned unsynced double-buffer of the per-viewAllyTeam LOS/radar/jammer/airLos maps, drained at the barrier** (heightmap dirty-rect pattern), *or* run `CInfoTextureHandler::Update` inside the barrier — it already runs once per new sim frame in `UpdateUnsynced` (boundary-shaped) and keeps no CPU copy today. The scattered point `InLos` queries read the same unsynced copy. |
| **Metal maps** | `Metal.cpp:34`, `MetalExtraction.cpp:106`, `TooltipConsole.cpp:285` | `metalMap.GetDistributionMap`/`GetExtractionMap`/`GetMetalAmount` | **explicit entry:** distribution map is dirty-flagged (`MetalMapChanged` event → boundary-queued), extraction map is 1 Hz; near-static between terrain deforms → (b) atomic/boundary. |
| `CDynWater` ship wakes (legacy water) | `DynWater.cpp:1063-1111` | `pos`/`losStatus`/`frontdir`/`rightdir`/`radius`/`speed` (rows) + `mass`/`IsUnderWater`/`IsInWater`/`moveDef` (rowless) | atomic-relaxed; legacy/optional renderer, wake positions stale-invisible. Not converted — the rowless `mass`/water-state reads mean a snapshot conversion would be partial; if DynWater is kept, route the whole wake read through the snapshot (water-state is derivable from row `pos.y`). |

**(c) boundary-computed / deferred — NOT a scalar; move to the barrier or a bounded copy.**

| Site(s) | file:line | What | Owning work / split-time action |
|---|---|---|---|
| Drawer extraction layer (UPD) | `UnitDrawerData.cpp:264-445`, `ModelDrawerData.h:330-380` (`ExtractTransforms`/`UpdateObjectUniforms`), `FeatureDrawerData.cpp:172-274`, `ProjectileDrawer.cpp:400-459`, `GetObjDrawErrorPos`→`GetErrorVector` `UnitDrawerData.cpp:401` | the sanctioned per-sim-frame extraction (transforms, uniforms, draw-flags, error-pos) | **PR 26** brings these under the one formal barrier function; already boundary-shaped, sim-paused-safe. |
| Unbounded per-unit weapon state | `MiniMap.cpp:1945-1951` (interceptor), `GuiHandler.cpp:3524-3536` (`DrawWeaponArc`), `3751-3924` (`DrawMapStuff`), `HUDDrawer.cpp:143-259`, `DebugColVolDrawer.cpp:208-268` | `unit->weapons[]`, `w->range`/`numStockpiled`/`HaveTarget`/`GetCurrentTarget`/`wantedDir`/… | command-queue class (decision 4): **dirty-versioned bounded copy** of the per-unit weapon summary, or boundary compute of the range set. Can't be (b) — iterating a live `std::vector<CWeapon*>` under mutation is a race, not a torn scalar. |
| Command queues | `CommandDrawer.cpp` (all branches) | `cai->commandQue` walks, `owner->allyteam`/`pos`/`selfDCountdown` | command-queue-class dirty-versioned copy (decision 4). |
| Legacy CPU `localModel.Draw()` | `UnitDrawer.cpp:292`, `FeatureDrawer.cpp:303`, `HUDDrawer.cpp:74` | immediate-mode model draw reads live `LocalModelPiece` transforms | piece transforms are already extracted for the SSBO/GL4 path (PR 8); the legacy CPU path must read the **extracted** piece transforms (or be GL4-only under the split). |
| **SmoothHeightMesh** | `SmoothHeightMeshDrawer.cpp:61-109`, `SpringController.cpp:107` | `smoothGround.GetHeight/mapChangeTrack/…`; sim-owned mesh rebuilt in `SimFrame` on the `mapChangeTrack` dirty flag | **explicit entry:** the drawer is a debug overlay; the camera-smoothing read is scalar-ish but the mesh *array* is sim-mutated. Split action: a **dirty-rect unsynced copy of the smooth mesh** following the heightmap pattern (`mapChangeTrack` already supplies the dirty tracking), or compute at the barrier. → (c). |
| `quadField` walks (debug) | `DebugColVolDrawer.cpp:304-317`, `DebugDrawerQuadField.cpp:87-133` | live quad unit/feature lists + collision volumes | **PR 25** draw-side spatial grid, or run at the barrier. Debug overlays, low priority. |
| `GroundDecalHandler::UpdateDecalsVisibility` | `GroundDecalHandler.cpp:1547-1600` | per-frame `losStatus`/`buildProgress`/`IsInLosForAllyTeam` over the pointer-keyed `decalOwners` map | the `decalOwners` rekey is already flagged for **PR 26** (last pointer container, PR-14 note); the visibility reads follow it (mixed unit+feature — features have no rows yet, §E.3). |
| `CHUDDrawer` FPS HUD (deep) | `HUDDrawer.cpp:59-259` | `moveType`, `weapons`, target objects, `localModel` | FPS-control-only (single unit); (c) deferred with the weapon/localModel actions above; `health`/`maxHealth` are (b). Niche mode, low priority. |

*Atomic-relaxed field list for PR 27b* (the (b) scalars that become cross-thread reads at the flip; mark on the sim-object members): `CUnit`: `radarRadius`, `sonarRadius`, `jammerRadius`, `seismicRadius`, `sonarJamRadius`, `activated`, `maxRange`, `decloakDistance`, `buildFacing`, `experience`, `mass`, `xsize`/`zsize`, `noMinimap`, `stockpileWeapon` bit; `CProjectile` subclass visual members (`age`/`color`/`size`/`dir`/…). The row-backed reads (`pos`/`midPos`/`health`/`maxHealth`/`losStatus`/`radius`/`frontdir`/`rightdir`/…) do **not** need marking — the (a) sites read the snapshot, and the remaining (b) render-pass reads of those same fields are torn-tolerant and covered by the same atomic-relaxed sweep only if the split later chooses consistency over the id-keyed-helper path.

### D. Picking / TraceRay

`GuiTraceRay` walks the sim quadfield and reads live collision/selection volumes + transforms (`TraceRay.cpp:352,426-432,453,495`). Called from mouse, GuiHandler, minimap, and Lua `TraceScreenRay` — i.e. from the draw/input side, potentially many times per frame. DECIDED: picking runs draw-side against the boundary snapshot — positions, radii, collision-volume params, piece transforms (if it renders, it raycasts) — with box-select served by a draw-side grid over snapshot positions; a shared read lock on the quadfield is rejected (no-lock decision). Synchronous callers like Lua `TraceScreenRay` read the previous boundary's snapshot (≤1 draw frame of pick latency, same staleness as everything else they see). **LANDED as PR 25 (see the PR-plan entry below for the full sync-audit table and deferrals): `SnapshotPickGrid` broadphase + snapshot selection-volume/feature rows + a `CSolidObject`-free `CCollisionHandler::MouseHit`; piece-tree selection volumes deferred behind a documented live-read fallback (unused in BAR).**

### E. Lua — the big one

- `LuaRules`/`LuaGaia` already have **separate synced and unsynced `lua_State`s** (`LuaHandleSynced.h:180-268`) and LuaUI is its own state — the state-separation groundwork exists. But `GetSyncedHandle(L)` cross-hops exist and must be barred cross-thread.
- The killer: unsynced draw callins (`DrawWorld`, `DrawUnit`, `DrawScreen`, … `LuaHandle.cpp:2727-2909`) freely call `LuaSyncedRead` callouts that dereference live sim objects (`LuaSyncedRead.cpp:537-544,2948,3260`). BAR calls these tens of thousands of times per frame (see `project_lua_boundary_profiler_findings`). Resolution (2026-07-05 decisions above):
  1. **Locking is rejected** — both blanket sim-locking during draw callins (the GML failure mode) and a cold-miss lock (widget/load-dependent timing edges). The boundary must be complete for everything it serves before the split can enable.
  2. **Snapshot-backed reads are the mechanism**: on the draw thread, `Spring.GetUnitPosition`-class callouts read the extracted frame-N snapshot. Requires classifying the LuaSyncedRead surface (hundreds of callouts) into snapshot-served / boundary-computed / contracted stale-nil — driven by call-count telemetry, completed *before* the flip. Start extraction with the ~30 callouts that dominate call counts.
  3. **GL4 migration is not a dependency** (decided 2026-07-05: it will not come first). The boundary must serve today's call pattern — tens of thousands of per-unit getters per frame — at full fidelity. If widgets later migrate to engine-side instancing they stop calling per-unit getters at draw time, which shrinks demand as a bonus, but no serving work is deferred in anticipation of that.
  Note `gl.CreateList`-era semantics and the A/B parity work assume draw-time sim reads are frame-consistent — the snapshot actually *improves* consistency (today a mid-draw sim frame can't happen, but mid-*Update* Lua sees half-updated frames during catch-up).

### E.1 S0 draw-time callout census (measured 2026-07-05)

The callout-context telemetry (PR 2, `/calloutcensus`: every callout invocation bucketed as draw-callin / sim-phase / other) run full-length over the two S0 replays (headless resim, engine-2026.06.10 demos; see `replay-seeking-architecture.md` "Sizes" for the setup). Top 30 callouts by draw-context demand, normalized per draw frame:

| Callout | Rosetta draw calls | /draw frame | ATG draw calls | /draw frame | sim-phase share |
|---|---|---|---|---|---|
| `GetMouseState` | 3,158,161 | 8.00 | 2,310,579 | 8.00 | 0% |
| `IsGUIHidden` | 2,763,187 | 7.00 | 2,021,551 | 7.00 | 0% |
| `GetUnitViewPosition` | 736,767 | 1.87 | 1,627,994 | 5.64 | 0% |
| `Blending` | 2,003,458 | 5.07 | 1,476,881 | 5.11 | 0% |
| `GetGameFrame` | 1,269,128 | 3.21 | 938,543 | 3.25 | 39% |
| `Translate` | 918,431 | 2.33 | 703,041 | 2.43 | 0% |
| `CallList` | 894,141 | 2.26 | 665,857 | 2.31 | 0% |
| `PopMatrix` | 845,655 | 2.14 | 608,842 | 2.11 | 0% |
| `PushMatrix` | 845,655 | 2.14 | 608,842 | 2.11 | 0% |
| `GetActiveCommand` | 819,404 | 2.08 | 610,654 | 2.11 | 0% |
| `GetModKeyState` | 789,482 | 2.00 | 577,586 | 2.00 | 0% |
| `CreateTexture` | 784,333 | 1.99 | 573,745 | 1.99 | 0% |
| `Begin` | 761,249 | 1.93 | 546,178 | 1.89 | 0% |
| `End` | 761,249 | 1.93 | 546,178 | 1.89 | 0% |
| `GetMapDrawMode` | 759,331 | 1.92 | 544,287 | 1.88 | 0% |
| `IsUnitVisible` | 269,416 | 0.68 | 414,253 | 1.43 | 0% |
| `Color` | 445,969 | 1.13 | 327,965 | 1.14 | 0% |
| `GetCameraPosition` | 424,493 | 1.08 | 321,708 | 1.11 | 0% |
| `Scale` | 420,786 | 1.07 | 286,755 | 0.99 | 0% |
| `SetTextColor` | 396,201 | 1.00 | 290,222 | 1.00 | 0% |
| `GetMouseCursor` | 394,741 | 1.00 | 288,793 | 1.00 | 0% |
| `GetSelectionBox` | 394,741 | 1.00 | 288,793 | 1.00 | 0% |
| `SetOutlineColor` | 394,741 | 1.00 | 288,793 | 1.00 | 0% |
| `Print` | 366,966 | 0.93 | 257,847 | 0.89 | 0% |
| `GetGameSeconds` | 364,826 | 0.92 | 255,739 | 0.89 | 4% |
| `GetUnitPosition` | 125,108 | 0.32 | 63,489 | 0.22 | 26% |
| `GetFeaturePosition` | 12,517 | 0.03 | 90,599 | 0.31 | 5% |
| `AlphaTest` | 59,504 | 0.15 | 65,830 | 0.23 | 0% |
| `WorldToScreenCoords` | 69,141 | 0.18 | 32,459 | 0.11 | 0% |
| `IsUnitIcon` | 55,967 | 0.14 | 31,030 | 0.11 | 0% |

Classification takeaways for the boundary contract:

- **The draw-context surface splits cleanly into three serving classes.** (1) GL/font calls (`Blending`, `Translate`, `CallList`, `Begin/End`, `Print`, ...) never touch sim state — free on a draw thread. (2) Input/UI/camera state (`GetMouseState`, `IsGUIHidden`, `GetActiveCommand`, `GetModKeyState`, `GetSelectionBox`, `GetCameraPosition`) is unsynced main-thread state — also free. (3) Sim-state reads that need snapshot serving are concentrated in a *small* family: `GetUnitViewPosition`, `IsUnitVisible`, `GetUnitPosition`, `GetFeaturePosition`, `IsUnitIcon`, `WorldToScreenCoords`(-adjacent), plus low-rate `GetUnitDefID`-class lookups — the positions family predicted by decision 2 dominates exactly as assumed.
- **Family shares of draw-context demand**: `GetUnit*` is 3.9% (Rosetta) / 9.6% (ATG) of all draw-context calls — and essentially the *only* synced-state family with volume; `GetFeature*` ≤0.5%, `GetProjectile*` literally zero in draw context, `GetTeam*`/`GetPlayer*` ≤0.02%. Snapshot-serving the positions/visibility family covers the demand cliff; the long tail (hundreds of callouts) is candidate stale/nil-contract territory at these volumes.
- **Context split is real and matters**: `GetGameFrame` is 39% sim-phase (gadget event handlers), `GetUnitPosition` 26% — the same callout name serves both sides, so the redirect must be per-*handle* (which lua_State/thread), not per-name; counts here justify per-callin glue redirection (decision 2) rather than global function swaps.
- **Headless caveats** (this census is a classification set + ranking, not an absolute-load forecast): GL-stubbed widgets self-disable some render paths, so real-GL per-frame demand is higher — the live-game boundary profiler measured ~27k callouts/sim-frame (`project_lua_boundary_profiler_findings`) vs the ~2.4k/draw-frame seen here; the fast-forward draw:sim ratio (6–9 draws/sim frame) inflates "other"-context totals; per-draw-frame columns are ratio-independent. A live-GL census (one `/calloutcensus` in a real game with `LuaTrackCalloutCounts=1`) should be captured before finalizing the PR-18 family order, but the *shape* — positions family on top, projectiles absent, teams/players negligible — is robust. **Live-GL addendum (2026-07-06): the "projectiles absent" claim was a headless artifact — see below.**

**E.1b Live-GL callout census (measured 2026-07-06).** Two `/calloutcensus` runs on the live legacy build (real GL, overview camera; captured via the S0 driver's new `S0AllowLive`/`S0Overview` knobs): the ATG gate replay full-length (44,856 sim frames / 8,140 draw frames, fast-forward; `livecensus_atg_callouts.csv`) as the representative real-game dataset, plus the fightertest benchmark scene (projectile-heavy air battle, 2,000 frames at 1x) as a stress shape. ATG draw-context sim-state demand per draw frame: `GetUnitBasePosition` **298**, `GetUnitHealth` **108**, `GetUnitIsStunned` **108** (the healthbar/status widget loops), `GetPlayerInfo` **95**, `GetProjectileDefID` **62**, `GetProjectilePosition` **25**, `GetUnitViewPosition` 20, `GetTeamInfo` 15, `GetUnitPosition` 13, `IsUnitVisible`/`IsUnitIcon` ~6 each, `GetProjectileVelocity` 1.8, `GetUnitDefID`/`GetUnitTeam` ~1.2, `GetFeaturePosition`/`GetProjectileTarget`/`GetProjectileOwnerID` ~1. Fightertest confirms the same families with the ratio flipped toward projectiles (`GetProjectileDefID` 785/frame, healthbar triple 469/frame, `GetUnitLosState` 235/frame — LOS-state reads appear when non-spectating). Corrections to the headless takeaways: (1) **the `GetProjectile*` family is a first-class draw-context family under real GL** (~90/frame in a real game) — the projectile-visual widgets self-disable headless, which produced E.1's "literally zero"; the PR-18 serving surface needs a projectile family (defID/projectileType, pos, velocity, target, owner; per id-namespace), i.e. a SimSnapshot projectile-row extension (v1 is units-only). (2) **The unit status family dwarfs everything**: BasePosition+Health+IsStunned+ViewPosition+Position ≈ 550/frame ≈ 10-40x the headless per-frame numbers for the same demo — pos/health/maxHealth are v1 rows already; stunned/paralyze (and the paralyzeDamage/captureProgress components `GetUnitHealth` returns) need adding; `GetUnitViewPosition` is drawer-owned drawPos, served without sim access already. (3) `GetPlayerInfo`/`GetTeamInfo` volume is real (95+15/frame) but is player/team-table state, not per-unit sim state — classify for main-thread/unsynced serving, not snapshot rows. Resulting PR-18 family order: **unit status+positions family first** (mostly v1-covered; establishes the glue pattern), **projectile family second** (new snapshot rows), then the player/team-table classification, features and the tail last (stale/nil-contract territory at ~1/frame). Same-run finds: the census runs crashed/error-flooded until two mid-sim-phase drawer-read windows were closed (PlayerChanged resolving dead-but-undrained ids -> pending-destroy-shell fallback in DrawerGetObjectByID; TransformsUploader::GetElemOffset on just-created objects -> lazy GetOrCreateTransformMemAlloc), see the 2026-07-06 commit "Close the mid-sim-phase drawer-read windows".

### E.2 The PR-18 glue-redirection pattern (first family landed 2026-07-06)

The positions/status family (`GetUnitPosition`/`GetUnitBasePosition`/`GetUnitHealth`/`GetUnitIsStunned` in LuaSyncedRead, `GetUnitViewPosition` in LuaUnsyncedRead) is snapshot-served from draw context. This section is the recipe every remaining family follows; the full serving-side contract lives in the `LuaSnapshotServe.h` header comment and the masking-policy block in `SimSnapshot.h`.

- **Redirect predicate, per call not per registered function**: `LuaSnapshotServe::ShouldServe(L)` = unsynced handle context (`CLuaHandle::GetHandleSynced(L)` false — per-handle, exactly as decision 2 requires) AND inside a `Draw*` callin (the PR-2 `ScopedDrawCallinContext` TLS bracket). Sim-phase and other-context invocations of the same callout keep the live path (E.1: `GetUnitPosition` is 26% sim-phase).
- **Entry-point shape**: the registered function body moves verbatim into a `static int <Name>Live(lua_State*, const char* caller)` and the entry becomes one line: `return LuaSnapshotServe::Route(L, __func__, &<Name>Live, &LuaSnapshotServe::<Name>);`. Alias callouts that call another entry (`GetUnitBasePosition`) inherit its routing.
- **Serving twin rules** (in `LuaSnapshotServe.cpp`): line-by-line mirror of the live body with every sim-object read replaced by a snapshot row read. Identical argument parsing and error text (NB LuaSyncedRead's `ParseRawUnit` and LuaUnsyncedRead's `ParseUnit` raise *different* error strings — mirror the owning file's), identical float expression order (bit-equality is the gate's pass criterion), identical return shapes. UnitDef/def-table derefs stay direct (immutable game data, not sim state). Drawer-owned reads (drawPos) go through id-keyed drawer accessors — a twin never touches a `CUnit*`.
- **Masking (the policy PR 15 deferred, now settled)**: serving-layer masking over POV-complete raw rows. The snapshot carries the masking *inputs* for every allyteam (`losStatusAll`/`posErrorBits` stride rows, `posErrorVector`, `leavesGhost`, alliance matrix, radar-error scalars) and `SimSnapshot::UnitRows` owns the bit-exact mirrors of the live formulas (`PovAlliedUnit`/`PovUnitVisible`/`PovUnitInLos`/`ErrorVector`/`LuaErrorVector`); twins gate and mask through those with the handle's `(readAllyTeam, fullRead)`. Rationale: one snapshot serves every handle POV at once (LuaUI player POV, LuaRules-unsynced, `/specteam` flips — no per-POV copies, no re-extraction on POV change), and the values are directly bit-comparable against live. CPU-side raw rows widen no information surface (process memory already holds the whole lockstep sim; the leak boundary is what crosses into Lua, and the gates are master's own). Extraction-time masking remains mandatory for GPU-visible surfaces (`UpdateObjectUniforms`/`ExtractTransforms` LOS-gate precedent) — do not port this policy there.
- **Stale/nil contract**: an id the snapshot does not cover returns the exact nil shape of the live path's "no such unit", deterministically, with NO fallback read of live sim state. No per-miss dev warning — widgets legitimately query dead ids constantly; the detector for genuine snapshot-vs-live set divergence is the armed diff gate (a validity mismatch names the id and frame).
- **Verification (masking-aware, three layers, no third formula copy)**: (1) `LuaSnapshotServe::Route` dual-run — while the PR-17 gate is armed every redirected invocation executes BOTH real paths and bit-compares the actual Lua return slots at the handle's real POV, then serves the snapshot values; (2) `SnapshotDiffGate::CheckBoundary` field pass over every extracted row incl. the per-allyteam strides and the global block; (3) the masked-value sweep — per unit x per allyteam (plus the invalid-allyteam branch), `UnitRows::ErrorVector` vs live `CUnit::GetErrorVector`, so the masking math is verified for ALL POVs regardless of what the local handles run. The diff-gate driver widget must exercise the family from a real `Draw*` callin (`widget:Update` does not set the draw-context flag) and includes a mid-replay single-team-POV segment (`specfullview 0` + `specteam N`, knobs `DiffGatePov*`) so the dual-run comparator also runs at a non-fullRead POV.
- **Snapshot growth per family**: follow the SimSnapshot.h add-a-field recipe (now 5 steps: rows, extract, accessor + SnapshotHash word, diff-gate compare, consumers). This family added midPos/aimPos, paralyzeDamage/captureProgress, beingBuilt/stunned, relMidPos + frontdir/updir/rightdir (object-space math for the drawer midpos variant; also the future `GetUnitDirection`/`GetUnitVectors` rows), and the masking inputs above; the losStatus row went from single-viewAllyTeam to all-allyteams stride (consumers pass `gu->myAllyTeam` and read the same byte as before; the viewAllyTeam re-extraction trigger is gone).
- **E.1b correction**: `GetUnitViewPosition` was listed as "served without sim access already" — only its drawPos read was; its `ParseUnit` visibility gate, `GetLuaErrorVector` masking and the `GetObjDrawMidPos` orientation math (`relMidPos`, front/up/rightdir) were live sim reads. It is redirected like the rest of the family.

Same-run finds (both fixed in the PR-18 commit):
- **The PR-2 draw-context bracket missed every argless draw callin.** `isDrawCallin` is detected by `strncmp(callin, "Draw", 4)` on the dispatch function's `__func__`, but DrawWorld/DrawGenesis/DrawWorldPreUnit/... all dispatch through the shared `CLuaHandle::RunDrawCallIn` (whose `__func__` defeats the check); only the argful draw callins (DrawScreen/DrawScreenEffects/DrawUnit/DrawInMiniMap/...) with dedicated dispatchers armed the TLS flag. Fixed by opening the bracket explicitly in `RunDrawCallIn`. Census correction: the E.1/E.1b draw-context columns UNDER-count callouts fired from the argless draw callins (they were bucketed "other") — the family ranking stands (real draw-context demand is strictly higher, concentrated in the same families), but absolute per-draw-frame numbers are lower bounds.
- **Armed dual-run stack pollution.** Running the live body then the twin on the same stack lets the twin's optional-arg reads (`luaL_optboolean` at index nargs+1..) see the live path's return values instead of "none" (found live: BAR's `gui_com_nametags` 1-arg `GetUnitPosition` made the twin read the live x as its midPos flag — a hard error under the widgetHandler's pcall since `luaL_SpringOpt` goes strict when `L->errorJmp` is set). `Route` now stashes the live returns in the registry and resets the stack to the original arguments before the twin runs.

Remaining family order (from E.1b, unchanged by this PR): **projectile family next** (new snapshot row namespace: defID/projectileType, pos, velocity, target, owner), then the player/team-table classification (main-thread unsynced state, not snapshot rows), then features and the long tail (stale/nil-contract territory at ~1/frame).

### E.3 Remaining-family status (2026-07-06, second pass)

**Projectile family — SERVED.** `GetProjectilePosition`/`GetProjectileVelocity`/`GetProjectileDefID`/`GetProjectileTarget`/`GetProjectileOwnerID` (the E.1b census set) redirect via the §E.2 pattern over a new `SimSnapshot::ProjectileRows` namespace: synced projectiles keyed by synced projectile id (free-list ints, rows grow-only to the high-water id), fields validity/pos/speed/allyTeam/ownerID/isWeapon/weaponDefID/target{Type,ID,Pos}, masking input `inLosAll` — the positional LOS-map answer `losHandler->InLos(pro->pos, at)` captured per allyteam at extraction, `PovVisible` mirroring `LuaUtils::IsProjectileVisible` (own-allyteam bypass + positional LOS; note projectile gating is positional, unlike the units' stored losStatus rows). Target capture resolves the CWorldObject with the same dynamic_cast chain as the live callout. `ParseProjectile` arg semantics are `luaL_checkint` (standard Lua error, unlike the unit parsers). Unsynced projectiles have no rows: they are draw-side objects and not visible through the synced callouts. The projectile section folds into the SnapshotHash root (all synced state; unit-granularity localization stays with the unit rows). Tail projectile callouts (Direction/Gravity/Type/TeamID/AllyTeamID/TimeToLive/IsIntercepted/PieceProjectileParams/...) stay live-served — ~0/frame in draw context in both censuses; convert by the same recipe if a future census ranks them.

**Unit-family stragglers — SERVED.** `GetUnitLosState` (the non-spectating hot read: 235/frame in the fightertest census; served from losStatusAll incl. the `GetEffectiveLosAllyTeam` POV-argument mirror with its exact luaL_argerror), `IsUnitVisible` (losStatus gates + radius default were the sim reads — new `radius` row; camera and icon state are draw-side) and `IsUnitIcon` (gate only) — all LuaUnsyncedRead ones via id-keyed drawer accessors.

**Player/team-table family — CLASSIFIED (not snapshot rows; implementation deferred to the boundary-copy PR).** Read-set audit result (full table in the analysis below): the family splits into
- *(c) already draw-thread-owned:* `GetLocalPlayerID/GetMyPlayerID`, `GetLocalTeamID/GetMyTeamID`, `GetLocalAllyTeamID/GetMyAllyTeamID`, `GetSpectatingState` — pure `gu` reads (the `GetMy*` names are aliases onto the `GetLocal*` bodies); nothing to do.
- *(b) boundary-copied small tables:* everything reading `teamHandler`/`playerHandler`/`CTeam`/`CPlayer` — `GetTeamInfo` (leader/isDead event-rate, rest static), `GetTeamList`/`GetAllyTeamList`/`GetTeamAllyTeamID` (static maps), `GetPlayerInfo` (ping/cpuUsage at net rate — the 95/frame hot one), `GetPlayerList`, `GetTeamColor`/`GetTeamOrigColor` (event-rate; registered unsynced but dereferences live CTeam), `GetTeamResources` (per-sim-frame `res` — must copy every boundary; allied-only gate via IsAlliedTeam), `GetTeamUnitCount` (allied fast-path scalar `NumUnitsByTeam`; the enemy branch is a per-unit IsUnitVisible loop already covered by the unit snapshot), team/ally stats callouts (TeamStatistics), `GetGaiaTeamID` (fixed at start). Sizing: a per-team row is < 256 B x realistic ≤ 16 teams (2–4 KB/boundary) + a per-player row ≤ ~64 B fixed x ≤ 251 — trivially boundary-copyable in one memcpy-scale pass. The masking inputs (alliance matrix, readTeam/readAllyTeam/fullRead POV) are already in the snapshot/serving layer.
- *(d) stale/nil or dirty-versioned later:* `GetTeamRulesParams`/`GetPlayerRulesParams`/singular variants (unbounded `modParams` maps — same class as command queues), `GetTeamStatsHistory` (unbounded vector).
The (b) tier is deliberately NOT implemented as SimSnapshot rows now: pre-split, live reads from draw context are frame-consistent (single thread), so unlike the unit/projectile families there is no behavior to lock in and no masking to settle — the copy is mechanical and belongs to the boundary PR that formalizes the extract barrier (Phase 1/2), using the field list above as its spec.

**Second-pass gates (engine 2026.06.10-38-gda3ef94 + tree):** diff gate armed full-length over both replays (parallel runs, second hardlink-copy write-dir `bar-data2` with its own HostPortDefault/cache) — ATG PASS 0 mismatches (proj field pass 130k rows / 390k inLos-per-allyteam checks; serving dual-runs 258.7k per projectile callout, 20.6M GetUnitLosState, 10.3M each IsUnitVisible/IsUnitIcon), Rosetta PASS 0 mismatches (234k per projectile callout, 11.1M LosState, 5.5M each IsUnitVisible/Icon). Both master-demo resims clean full-length uncontended: Rosetta f=44537 @ 2:56, ATG f=44859 @ 4:07 — inside the 3:52-4:07 historical band; extraction avg unchanged vs the first family (0.054/0.109 ms; projectile extraction is noise at these counts), projectile row high-water ~16.4k slots (free-list ids are not compact — sized by max id seen, ~2.5 MB resident for the projectile namespace). Comparator find fixed along the way: `SlotsEqual` compared result tables by `lua_rawequal` (identity), which false-flagged every table-returning callout (`GetUnitLosState` table form, `GetProjectileTarget` ground branch) — structural `TablesEqual` (shallow two-way key sweep, values recurse) replaced it; field passes had been 0-mismatch throughout, confirming the artifact.

**Features + long tail — CONTRACTED TERRITORY, spec'd.** At ≤0.3/draw-frame (both censuses) features do not earn rows yet. When/if converted: feature ids are dense-indexed but sparse-occupancy, bounded by MAX_FEATURES=32000 (`featureHandler.GetFeature(id)` array + `SimObjectIDPool`), so the unit-style SoA-with-validity pattern carries over; needed v1 rows are pos/health/maxHealth (def)/resurrectProgress/team/allyteam/defID + per-(feature,allyteam) visibility bytes, because `IsFeatureVisible` is positional like projectiles but with mod-config branches: `CFeature::IsInLosForAllyTeam` = alwaysVisible || by `modInfo.featureVisibility` ∈ {NONE: InLos(pos,at); GAIAONLY: isGaia||InLos; GAIAALLIED: isGaia||sameAllyTeam||InLos; ALL: true} — the two globals (featureVisibility mode, gaiaAllyTeamID) join the boundary global block when this family converts. Until then — and for the whole remaining callout tail — the Phase-3 flip contract applies: draw-thread calls to unserved sim-reading callouts return the documented deterministic stale/nil (the "no such object" nil shape), with the dev-mode warning naming the callout; the warning mechanism lands with the flip PR itself since pre-split the live path still serves them frame-consistently.

### F. Misplaced work (cheap, do first)

`SimFrame()` runs a block of unsynced work (`waitCommandsAI`, `geometricObjects`, `sound->NewFrame`, `eoh->Update`, group handlers — `Game.cpp:1764-1780`, already commented as misplaced) and `UpdateUnsynced` computes stuff sim needs nothing of. `move-unsynced-gameframe-work-todo.md` already tracks the pattern. Everything in that block must be classified: stays with sim thread, moves to draw thread, or becomes boundary work. **DONE 2026-07-05 (PR 11a)** — full classification table in `move-unsynced-gameframe-work-todo.md` ("PR 11a" section): 5 movers (4 boundary, 1 draw-update), 1 stays (`eoh->Update` — AI cheat callbacks mutate synced state directly in SP, `AICheats.cpp:53-89`, and AIs assume per-sim-frame cadence; the `Game.cpp:1765` "everything here is unsynced" comment is wrong for it), plus adjacent findings (`UpdateGhostedBuildings` → boundary; profiler/alloc-stats sampling stays). Unblocks 11b.

## Proposed architecture (Phase 2 target)

Pipelined, one-frame-deep, barrier-synchronized:

- **Main thread** (unchanged identity): SDL input, all GL, `CGame::Draw`, Lua unsynced states, swap.
- **Sim thread** (new): `ClientReadNet` → `SimFrame`s, `ENTER_SYNCED_CODE`, synced Lua, streflop FPU init at spawn (copy `GameLoadThread.cpp`), watchdog slot `WDT_SIM`.
- **Boundary (once per draw frame, short)**: sim pauses at a frame edge (never mid-SimFrame); drain render-event delta queue; run transform/uniform extraction (`worldDrawer.Update` extraction half) + heightmap `UpdateDraw`; snapshot the small hot-field set (health, losStatus copy for minimap/icons, radar radii); release sim; render frame from snapshot while sim consumes more NEWFRAMEs.
- **Backpressure**: sim runs at most far enough ahead that interpolation stays within one frame (it already targets ~2 frames behind server, `NetCommands.cpp:204-208`); during catch-up/replays sim free-runs and draw just renders the latest boundary — this is where the split pays off most (fast-forward no longer fights rendering for the thread, cf. `project_headless_speed_control`).
- **Feature flag**: config var (à la `LoadingMT`), single-threaded path preserved via `WrappedSync`-style no-op locks. Headless keeps the current model.

Determinism note: the sim code itself is unchanged and stays on one thread, so lockstep sync is not inherently at risk — the risks are indirect (FPU state on the new thread, any sim code that accidentally reads draw-authored fields — which Phase 0 eliminates, and event *timing* of unsynced observers). `ASSERT_SYNCED`/sync-checker machinery is thread-agnostic.

## What it buys

Main-thread frame time today is `Σ(simFrames) + draw`. The split makes it `max(sim, draw)` plus the boundary cost (Amdahl on the boundary). Concretely:

- Steady 30 Hz play: draw stutter from expensive sim frames disappears; sim spikes (big battles, path floods) no longer eat draw frames and vice versa.
- Catch-up / replays / fast-forward: currently capped by `minDrawFPS` budgeting sharing one thread; split, sim saturates a core while draw stays smooth.
- It does **not** speed up sim itself. Under the no-lock decision the split does not enable at all until the draw-time callout surface is served/contracted (§E), so there is no degraded lock-mode intermediate — the flip is all-or-nothing per the completeness precondition.

## Phasing and effort

| Phase | Content | Effort | Standalone value |
|---|---|---|---|
| 0 | Move all §A write-backs to render-owned storage; split `tempNum` scratch; relocate §F misplaced work | ~8 independent medium refactors | Yes — cleans aliasing, enables `const` sim during draw, helps current MT asserts |
| 1 | Render-event delta queue + deferred object deletion (§B); formalize the extract boundary as one function; torn-read policy for §C fields; boundary-deferred picking (§D) | Large; the deletion-lifetime work is the riskiest part | Partial — queue also fixes today's "sim frame mutates drawer containers mid-catch-up" ordering quirks |
| 2 | Sim thread itself: `THREAD_IDX_SIM`, spawn/join, boundary barrier, feature flag, ThreadPool partitioning, GL-resource request queue (model loads), watchdog | Medium once 0–1 are done | The actual payoff, behind a default-off flag; cannot enable before Phase 3's classification is complete (no-lock decision) |
| 3 | Lua: cross-hop bans, snapshot-backed callout serving, per-callout contract (served / boundary-computed / stale-nil) | Scope bounded by the callout census | Precondition of the Phase-2 flip, not a follow-up |

Rough total: Phases 0–2 are on the order of a couple of engine-dev months of focused work; Phase 3's scope is set by the callout census (the GL4 migration is explicitly not a prerequisite — see decisions).

Mapping to the PR plan below: Tracks 1–5 refine Phases 0–1 plus the telemetry/classification half of Phase 3; Track 6 is the seeking product built on them; Track 7 is Phase 2 — concretely planned as **Wave 4 (PRs 24–27b)** at the end of the PR plan, independent of Track 6.

## Compatibility invariants

**Sync compatibility with master is a hard invariant for Tracks 1–6** (a branch client must not desync in a game with a master client). Structural argument: only synced-code changes can desync; every PR below is draw/unsynced-side or read-only. §A evictions are sync-safe *by construction* — those fields are authored at draw rate, which already differs across machines on master, so sync is provably blind to them (else master would already desync between a 60 and a 240 fps player). Deferred deletion is safe by address-blindness: cross-machine sync working today proves synced code cannot observe object addresses or allocation order.

**Enforcement, per PR:** demos recorded on master must resim clean on the branch with matching per-frame checksums (the `test/replay-rewind` harness runs exactly this). A demo is "same net input" by definition, so a clean resim *is* the sync-compat proof. Live same-lobby dogfood vs a master client as end-to-end confirmation. (Lobby version pinning may prevent actual mixed-version play; the invariant's practical value is master demos as regression corpus, bisectability, and dogfooding.)

**Build & gate runbook** (how the per-PR gates are actually run on this branch):

- Build: plain non-docker `build/` (`cd build && ninja engine-headless`; `engine-legacy` and `engine-dedicated` should also link before committing). Use the GCC toolchain — clang without `-frounding-math` desyncs replays around f=300.
- Resim gate: `SPRING_BIN=$REPO/build/spring-headless S0_BOUNDARY=0 S0_CALLOUTS=0 test/s0-measurement/run_s0.sh <demo.sdfz> <label>` per demo. The gate demos are the two S0 replays (Rosetta 1.4.4 and All That Glitters Extended, engine-2026.06.10 demos, `replays/` in the repo root); data dir defaults to `/www/projects/bar-data` (`--isolation --write-dir`, BAR content must be in the pool — see the restore notes in the S0 README).
- Speed: gates run fast-forwarded (`/setspeed 20` + `/speedcontrol 0`, the driver widget's default) — this is the catch-up stress case and the standard; 1x runs add nothing sim-side (resim is speed-invariant) and take ~25 min each vs ~3 min.
- Verdict: `run_s0.sh` greps the infolog for `DESYNC` and copies it to `<label>_infolog.txt`. A clean full-length run (Rosetta f=44537, ATG f=44856) with zero DESYNC is a pass. **Always confirm the `Spring Engine Version:` line in the infolog names the branch/commit you think you built** — a stale binary under `build*/install/` once hijacked a gate run and produced a convincing-looking "desync" (the script now excludes install trees from its binary search, but the version check stays mandatory).
- Environment: stash the A/B driver widgets out of `<write-dir>/LuaUI/Widgets/` before gate runs (one of them force-quits at f=30000 and they don't reflect stock BAR); they currently live in `Widgets.ab-stash/` next to it.

**Sync audit list** — the four places the structural argument has an asterisk; each is the review focus of its PR:
1. Does `slowUpdate` icon-distance logic read `drawPos`? If any synced path reads a draw-authored field, that's a latent master desync bug — find and fix (read sim `pos`) before/with the eviction. **DONE 2026-07-05 (PR 3)** — full reader audit of all 177 `drawPos`/`drawMidPos` references: **no synced reader exists**; the icon-distance suspicion was unfounded (`DrawAsIconByDistance` reads sim `pos`, `UnitDrawerData.cpp:309`; `iconRadius`'s only non-render reader is draw-side picking, `TraceRay.cpp:459`). One sim-context read found: `CUnitScript::Shatter` (`UnitScript.cpp:962`) called `GetTransformMatrix()` whose *default* was the unsynced/drawPos branch, to place shatter-piece debris — not a desync (output is unsynced-only flying pieces, `guRNG` not `gsRNG`) but a sim-thread read of render data post-split; the signature change fixes it silently and correctly (debris now spawns from the synced matrix — sub-frame placement difference, imperceptible). Enumerated visual deviation: none other; every other former unsynced-default call site was explicitly migrated to the drawer helpers |
2. `LOS_PREVLOS`: eviction stops draw code clearing a bit inside synced `losStatus`; needs an explicit no-synced-readers audit. **DONE 2026-07-05 (PR 7)** — synced readers DO exist (GetErrorVector, GenerateWeaponTargets, synced Lua LOS queries), which killed the shadow design; resolution and full reader table in the PR-7 commit message.
3. Each §F relocation item needs a touches-synced-state/RNG check before moving (e.g. confirm `waitCommandsAI` is genuinely unsynced-side despite the engine's "misplaced" comment); anything synced stays put.
4. Track-1 instrumentation must be side-effect-free reads (no streflop perturbation).

**Lua API compatibility tiers:** signatures identical by construction (same registered C functions). Synced Lua bit-identical (non-negotiable). Draw-callin reads: snapshot-served values are the same frame's values as today (draw already only ever sees the last completed sim frame) — semantics preserved, consistency improved. Enumerated deviations only: (a) unsynced event handlers deferred from mid-sim-frame to the boundary; (b) the contracted sim-only tail returns deterministic stale/nil on the draw thread (chosen via telemetry to be what games don't call at draw time, dev-mode warning on trip); (c) unsynced writes apply at the boundary. Verified by the callout differential gate (PR 17) + the single-threaded feature-flag path preserving exact legacy behavior.

## PR plan (individually reviewable, each standalone-valuable)

Ordered **Fable-first**: Fable work is front-loaded to the maximum the dependency graph allows, because every Fable PR emits the artifact (pattern, audit table, contract, inventory) that converts its followers into Opus-grade pattern-following. Opus work runs *behind* the Fable frontier in parallel — the frontier never waits for it. Original track labels (T1–T7) kept for cross-reference; PR numbers are stable identifiers, not order.

Model tiers — **[F] = Fable**, **[O] = Opus**. Rule of thumb: Fable where the work is design-setting (first of a pattern), proof-carrying (sync audits), or semantic-edge-heavy (lifetime, ordering, lazy→eager changes); Opus where a pattern already exists, the spec here is complete, and the mechanical gates (demo-resim, diff gate) catch errors. Nothing before PR 19 changes game-visible behavior. Verification: master-demo resim gate (above), benchmark suite. (Decided 2026-07-05: the GL4-parity A/B pixel gate is not used for these PRs — it lives on the gl4 harness branch and compares render backends, not sim/draw refactors; visual checks here are manual where relevant.)

**Wave 1 — Fable frontier (no upstream deps; start immediately, mutually parallel)**
3. [F] (T2) `drawPos`/`drawMidPos` → render-side array. Sets the Track-2 eviction pattern; includes sync audit item 1. Unblocks PRs 4–6, 9. **DONE 2026-07-05** — drawer-owned id-keyed storage + synced-only `GetTransformMatrix()` (parameter removed ⇒ compile-driven call-site audit); audit item 1 closed (no synced readers; `CUnitScript::Shatter` default-arg read found and fixed by construction); bonus evictions: feature unsynced `transMatrix`, projectile draw matrix. See §A row for the pattern PRs 4-6/9 follow.
7. [F] (T2) `LOS_PREVLOS` clearing → render-side prev-LOS shadow (audit item 2; synced-reader audit + ghost-bookkeeping semantics). **DONE 2026-07-05** — audit overturned the shadow design (synced readers exist; clears are sim-event-time, not draw-rate); landed as sim-side ownership move instead, full audit table in the commit message (`Move LOS_PREVLOS ghost accounting from draw code into sim`). See §A row.
8. [F] (T2) `LocalModelPiece` dirty-flag recompute → extraction-time snapshot of piece transforms (lazy→eager semantic change; animation-correctness and perf edges). Feeds PR 15's piece-transform layout. **DONE 2026-07-05** — landed as `CModelDrawerDataBase::ExtractTransforms` (defined once-per-new-sim-frame pass over all objects); see §A row and the extraction-site comment block for the contract and PR-15 layout.
11a. [F] (T3) Synced-state/RNG classification table for every §F item (audit item 3) — lands as reviewable doc/table before any code moves. Unblocks 11b. **DONE 2026-07-05** — table + 11b constraints in `move-unsynced-gameframe-work-todo.md`; headline: `eoh->Update` is NOT unsynced (stays), everything else moves (4 boundary / 1 draw-update), `geometricObjects` expiry is exact-frame-keyed (sweep-range required).
12. [F] (T4) Render-event delta queue — ordering semantics and catch-up edge cases are the substance. With 13, defines the rules PR 14 follows. **DONE 2026-07-05** — landed as `RenderEventQueue`; see the §B DONE note for the semantics (event-time capture for ghost bookkeeping, destroy-flush PR-13 handoff, immediate mode outside the sim phase).
13. [F] (T4) Deferred-deletion epoch + freed-slot poisoning — the sharpest lifetime edge in the plan; wants the strongest model *and* the strongest review. **DONE 2026-07-05** — landed as `DeferredObjectDeleter` (PreDestruct split keeps all sync-observable teardown at master timing; shells stay readable until the boundary drain; slots poisoned `0xDE` between ack and release); see the §B DONE note for the full design.
15. [F] (T5) `SimSnapshot` v1 + extraction pass with one converted consumer — sets the contract every later consumer/callout PR follows. **DONE 2026-07-05** — landed as `SimSnapshot` (`rts/Rendering/Common/SimSnapshot.{h,cpp}`; full contract — layout, validity, losStatus row semantics, extraction timing, swap/generation, add-a-field recipe — in the class comment): double-buffered SoA parallel arrays keyed by dense unitID (validity, pos, speed, health, maxHealth, team, allyTeam, defID, buildProgress, losStatus row for the extraction-time `gu->myAllyTeam`), extracted once per new sim frame in `CGame::Draw` right after the render-event drain, so snapshot and drawer containers agree on the same completed frame; re-extracts on viewed-allyteam change, out-of-cadence spawns and backwards frame jumps (rewind); published by pointer swap + generation bump. Converted consumer: the minimap losStatus reads (`CMiniMap::GetSelectUnit` select gate, `DrawUnitMiniMapIcons` radar-dimming gate). Uncovered ids read as deterministic defaults (the stale/nil contract); v1 rows are raw and unmasked — the hidden-enemy masking policy lands with the first consumer that could leak one (PR 18), cf. the `UpdateObjectUniforms` LOS-gate precedent. (Settled 2026-07-06 with PR 18's first family: serving-layer masking over POV-complete raw rows, the losStatus row generalized to an all-allyteams stride — see §E.2 and the SimSnapshot.h masking block.) Measured over both gate replays (fast-forward resim, sync-clean full-length): extraction mean ~0.02 ms / max ~0.35 ms per sim frame at 1.8–3k peak units; resident 2 × ~1.5 MB at MaxUnits=32000.
21a. [F] (T6) Draw-side accumulator inventory for the reset hook — doc-only, completeness-critical (a missed accumulator = subtle post-seek corruption); no code deps, can start anytime. **DONE 2026-07-05** — full table in `replay-seeking-architecture.md` ("Reset-hook inventory"): ~20 genuine accumulators + verified non-accumulator list + hook ordering; headline surprises: the creg checkpoint already restores ghosts/decals/unsynced-projectiles (`CR_MEMBER(projectiles)` covers both containers), and four sim-side checkpoint gaps found (`grassMap` is synced-readable but serialized nowhere, `SmoothHeightMesh` unserialized but synced-consumed, no projectile `Render*Created` re-emit on load, `GeometricObjects` absent from the collector). Unblocks 21b; stream flags feed PR 19.

**Wave 2 — Opus follow-through (each consumes a Wave-1 artifact; runs behind the frontier)**
4. [O] (T2) `drawFlag`/`previousDrawFlag` → render-side visibility bitset (PR-3 pattern). **DONE 2026-07-05** — evicted from `CWorldObject` into drawer-owned storage: an id-keyed `{flag,prev}` array in `CModelDrawerDataBase<T>` for units/features (SO_NODRAW on AddObject, stale-on-death, unregistered→SO_NODRAW), a renderIndex-parallel vector in `CProjectileDrawer`; accessors are drawer-data methods exposed via `CUnitDrawer`/`CFeatureDrawer` statics (`ShouldDrawProjectile` became a const member for the same reason). Fields + all six accessors removed from the sim object so every call site broke at compile and was re-audited; `CUnit::GetIsIcon`/`SetIsIcon` rode along to the drawer. Sync audit closed: no synced reader and no sim-context read at all (cleaner than PR-3's Shatter case). Projectile `previousDrawFlag` was a verified per-frame dead store and was dropped; DrawFlags enum stays in `WorldObject.h` for include locality.
5. [O] (T2) Icon state → render-side; `iconRadius` kept picking-visible (PR-3 pattern). **DONE 2026-07-05** — `definedIconName`/`currentIconIndex`/`customIconIndex`/`iconRadius`/`drawIcon` evicted from `CUnit` into `CUnitDrawerData::UnitIconState` (id-keyed, slot re-init at the `RenderUnitPreCreated` drain, stale-on-death, unregistered→defaults); fields + `UpdateRenderParams()` removed from the sim object so every call site broke at compile and was re-audited; accessors as drawer-data methods behind `CUnitDrawer` statics, `iconRadius` picking-visible via `CUnitDrawer::GetUnitIconRadius` (TraceRay). Sync audit closed: no synced reader, no sim-context read (the sole sim-side touch was the write-only `UpdateRenderParams` copy, now done drawer-side at registration). Deviations enumerated in the §A row.
6. [O] (T2) `drawAlpha` fade + `SetSortDist` → render-side (PR-3 pattern; two trivial rows, one PR). **DONE 2026-07-05** — both fields evicted per the §A rows above (feature fade → id-keyed `CFeatureDrawerData::drawAlphas` with a destroy-reset closing the non-model id-reuse edge; projectile sort keys → renderIndex-parallel `CProjectileDrawer::sortDists`; `sortDistOffset` stays sim-authored). Gates clean full-length on the GCC build (engine 2026.06.10-24-g7352ab1): Rosetta f=44537 / ATG f=44857, 0 DESYNC, wall 2:46 / 3:52 — no frame-time regression vs the PR-5 runs (2:46 / 3:44).
9. [O] (T2) Draw-time `tempNum` scratch → dedicated scratch id space (design settled here; execution mechanical). **DONE 2026-07-05** — `CWorldObject::tempNum` renamed to `syncedTempNum` (sim, still creg) and a draw-only `unsyncedTempNum` + `CGlobalUnsynced::GetTempNum()` counter added, so draw/unsynced QuadField-style dedup (5 LuaUnsyncedRead visibility scans, MiniMap picking) no longer reads or writes sim scratch. The one mixed helper reached from both sides, `GameHelper::QueryUnits`, became `template<bool Synced>` and `GetClosestFriendlyUnit` gained a required `synced` param (3 callers re-audited: Builder/LuaSyncedRead synced, MiniMap unsynced); sim single-threaded and mt scratch paths byte-identical apart from the rename. TraceRay confirmed tempNum-free (iterates quads directly). Full classification table (~30 sites, 11 files) in the commit message.
10. [O] (T2) Capstone: constify draw-pass access to sim objects — compiler-driven churn; requires 3–9 landed. **DONE 2026-07-05** — draw code now sees sim objects as `const T*` end-to-end and the compiler enforces it: all drawer containers hold pointers-to-const (`unsortedObjects`, `ModelRenderContainer` bins, `renderProjectiles`/`drawParticles`, `liveGhostBuildings`, `LuaMatBin` unit/feature lists, `ModelUniformsStorage` keys), `CProjectile::Draw()` + all 32 overrides are const, `ShouldDraw*`/`Draw{Opaque,Alpha,Shadow}*`/`LuaObjectDrawer::Add*`/`SetObjectLOD` take const, and all 17 sim-object `const_cast`s in `rts/Rendering/` are deleted (none added). Real draw-side writes the chase surfaced (the point of the PR): `CTracerProjectile::Draw` wrote its length clamp back to the member (now a local; drawn output identical) and `CRepulseGfx::Draw` wrote `pos` to track the repulsed projectile (tracking moved to `Update()`; Draw interpolates locally — enumerated deviation: the cull position also tracks while off-screen, where master left it stale). Discovered-and-deferred eviction candidates, marked `mutable` with in-place ownership comments (the LocalModelPiece-transform-cache idiom): the Lua-material/LOD family (`LocalModel::luaMaterialData`, `LocalModelPiece::lodDispLists` — render-owned, CR_IGNORED, authored by draw passes + unsynced Lua), the `CExpGenSpawnable` draw-rate animation state (`animProgress1-4`/`rotVal`/`rotVel`; `UpdateRotation`/`UpdateAnimParams` now const), `CWorldObject::drawRadius` (projectile cull state, grown per draw frame by `BitmapMuzzleFlame::Draw`), and `CProjectile::renderIndex` (drawer backref, removed outright by PR 14's id-keyed containers). Gates clean full-length on the GCC build (engine 2026.06.10-25-g694deac): Rosetta f=44537 / ATG f=44857, 0 DESYNC, wall 2:52 / 4:01 (`pr10_*_infolog.txt`); headless/legacy/dedicated + tests all build.
11b. [O] (T3) The §F relocations per 11a's table. Value now: honest profiler buckets. **DONE 2026-07-05** — executed the 11a table: six unsynced items moved out of `CGame::SimFrame`'s misplaced-work block into `CGame::UpdateUnsynced` (five at the sim-frame boundary gated on `newSimFrame && !PreSimFrame()`, `uiGroupHandlers` free-running at draw rate), plus `UpdateGhostedBuildings` from the Sim timer to the post-drain boundary; `eoh->Update()` stays sim-side with the block comment corrected. Frame-modulo/exact-key cadences converted to boundary-crossing / range-sweep form (waitCommandsAI %3, teamHighlight %30, geometricObjects exact-key→`<=frameNum` sweep) via a latched `prevSimFrame`; `waitCommandsAI`→`sound->NewFrame` soft order preserved. Movers write no synced state and consume no gsRNG — confirmed by the gate (below).
14. [O] (T4) Drawer containers `CUnit*` → ID, one container family per PR (rules from 12/13). **DONE 2026-07-06** — five commits, one container family each, every one gated green (both master-demo resims clean full-length; final walls 2:48/3:53 vs 2:52/4:01 PR-10 baseline). (1) `RenderEventQueue` records dropped the transitional object pointer the PR-12 note promised: records are plain data, dispatch resolves ids through the handlers with a `pendingDestroyShells` FIFO (keyed kind+namespace+id, populated at destroy-record queue time) overriding the live lookup, which is generation-exact under id reuse because fire order interleaves generations strictly and the destroy dispatch pops its generation's shell; immediate mode and the PR-13 pool-pressure flush need no shells (all three destroy sites fire before deregistering). (2) Render containers: `CModelDrawerDataBase<T>::unsortedObjects` + the `ModelRenderContainer` bins hold ids (`ModelRenderContainerTraits<T>::Handle`); all unit/feature pass loops, icon/minimap loops, `Spring.GetRenderUnits/Features`, DynWater resolve once per object per loop via `DrawerGetObjectByID<T>` (indexed handler lookup); container op order unchanged so bin iteration replays master exactly. (3) Allocation maps: `scTransMemAllocMap` and `ModelUniformsStorage` (per-kind id maps behind typed CUnit/CFeature overloads; dummy slot 0 reserved explicitly; the dead auto-adding projectile `GetElemOffset` path neutered). (4) `liveGhostBuildings` → unit ids, destroy-erase made unconditional (master leaked a dangling pointer when Lua set `leavesGhost` with `ghostedBuildings` off; only mempool zeroing kept it benign); creg checkpoint format changes for that member. (5) Projectiles: `CProjectile::renderIndex` removed; drawer keeps packed `(id<<1|synced)` `renderHandles` + `renderIndices[synced][id]` for O(1) swap-erase, parallel storages rekeyed `[synced][id]` (hot per-particle `GetDrawPos(this)` stays one indexed load), and `renderProjectiles` becomes the per-draw-frame pointer resolution rebuilt inside `UpdateDrawFlags` (one handler lookup per projectile per frame under for_mt; all passes run after it in the same `CGame::Draw`). Post-drain invariant everywhere: an id in a drawer container resolves to a live object, asserted at every resolution site (queue-side misses also LOG_L(L_ERROR)); the one documented deferred-window reliance outside destroy dispatch is `CUnitDrawerData`'s dtor resolving ids for decal cleanup (render tears down before sim). Deliberately out of scope, verified safe: `LuaMatBin` unit/feature lists (per-pass transient — filled and cleared inside one `DrawMaterialBins`), `drawParticles` (per-`DrawAlpha` transient), `GroundDecalHandler::decalOwners` (persistent pointer-KEYED map, event-paired add/remove keeps it live-only — the remaining pointer container, candidate follow-up for stream mode). War story: `float3 f = {}` binds to `operator=(const float f[3])` as a null pointer — crashed every replay at f=0 until spotted in the disassembly (`movss 0x0` + `ud2`); slot inits use `ZeroVector`.
16. [O] (T5) Hierarchical snapshot hashing + debug command, wired into `test/replay-rewind` (consumes 15; design spec'd above). **DONE 2026-07-05** — hierarchical XXH3-64 hashing of `SimSnapshot` rows (unit → 1024-id bucket → root; floats hashed as bit patterns, fixed field order) in `rts/Rendering/Common/SnapshotHash.{h,cpp}`, armed via `/snaphashdump <f0> <f1> [path] [root|bucket|unit]`. Hashes every completed sim frame from `CGame::SimFrame` (decoupled from the draw-time publish cadence so fast-forward doesn't skip frames) into a private scratch extraction that leaves the published double-buffer and all game-visible behavior untouched; unarmed cost is one bool load. The dump survives checkpoint-rewind reloads so forward and resim passes accumulate into one file; `test/replay-rewind/compare_snaphash.py` diffs them to name the first diverging sim frame + diverging id-range buckets/unit ids — the spatial-localization tier the QTPFS rewind-accumulation drift needed (localizes the unit; v1 carries no pathID/heading, so pathing drift surfaces via pos/speed). NB `BUCKET_SIZE=1024` duplicated in SnapshotHash.cpp and compare_snaphash.py (keep in sync).
17. [O] (T5) Lua callout differential gate (test-only harness; consumes 15). **DONE 2026-07-05** — `SnapshotDiffGate` (`rts/Rendering/Common/`), armed via `/snapshotdiffgate [arm|disarm|dump]`, hooked in `CGame::Draw` right after `simSnapshot.Update()`. Field pass bit-compares (memcmp, NaN-safe) every v1 field + validity for all unit ids against live sim objects each boundary; a callout comparator (`CheckCalloutUnitPosition`, positions-family proof-of-use) is the plug PR 18 fills to dual-path each converted family through the same counters/logging. Mismatches log-and-count (never crash); pass = zero mismatches over a full replay via `test/callout-diff-gate/` (unsynced driver widget + s0-style runner). Masking (errorVec/LOS) deliberately out of scope pending PR 18.

**Wave-2 verification (2026-07-05):** all five PRs (4, 9, 11b, 16, 17) landed together on `bruno/poc-split-sim-draw` (tip `a51957d1c7` + `ShouldDrawProjectile` fixup); both master-demo resim gates clean full-length on the GCC build — Rosetta 0 DESYNC to f=44537, ATG 0 DESYNC to f=44856 (`wave2_*_infolog.txt`, engine `2026.06.10-21-ga51957d`). PRs 16/17 instrumentation was compiled in but unarmed for the gate; arming runs: the PR-17 diff gate ran armed over both full demos on 2026-07-06 — first run FAILed with 229 team-field mismatches at one ATG boundary (net-message-driven resign transfers mutate team/allyteam/losStatus between sim frames, invisible to the frameNum due-check), fixed by `SimSnapshot::MarkMutatedOutsideFrame()` poked from `CUnit::ChangedTeam`; both demos now PASS (0 mismatches, 26.4k boundaries / 48.7M values per field on ATG) with clean resims. The PR-16 snaphash arming run: the full pipeline (per-sim-frame hashing under fast-forward -> /snaphashdump tsv -> compare_snaphash.py cross-pass comparison) was validated 2026-07-06 by hashing the ATG demo twice at bucket detail over frames 0..40000 and comparing the concatenated dumps — 40,001 frames, every root hash bit-identical, comparator exit 0. The *rewind-window* variant (forward vs post-checkpoint resim in one run) cannot run on this branch: the checkpoint-rewind feature (`/cp`, `/rew`, RewindAudit) lives on the `gabba-replay-rewind` branches and was never merged here — the harness's rewind flow no-ops (commands unknown), which the first arming attempt surfaced. That comparison re-arms for free once the QTPFS rewind-accumulation hunt happens on a branch that has both PR 16 and the rewind feature.

**Wave 3 — Fable integration (dependency-gated; in order)**
Just-in-time telemetry (Opus, deferred from the start of the plan — nothing in Waves 1–2 needs it):
1. [O] (T1) Boundary-size instrumentation: `transformsMemStorage`/`modelUniformsStorage` sizes + dirty rates, command-queue mutation rate, projectile churn; surfaced via `/boundarydump` (a `/profiledump` sibling — its own fixed-width per-frame row buffer, so full-game dumps stay small). The S0 dataset; must land before 19. **DONE 2026-07-05** (`BoundaryStats`, commit on `bruno/poc-split-sim-draw`). S0 numbers in `replay-seeking-architecture.md` "Sizes"; harness `test/s0-measurement/`. Verification: both S0 replays (44.5k frames each) resim sync-clean with instrumentation compiled in and armed (audit item 4); overhead over a fixed 8k-frame fast-forward segment: base 1.933 ms/frame, instrumented-idle 1.932 (counters unmeasurable), dump-active 1.946 (+0.7%, sampler self-measure 0.02–0.05 ms/frame).
2. [O] (T1) Draw-time callout census: per-callout call counts from draw callins (extends existing per-callin/callout profiler). Classification dataset for the boundary contract; must land before 18. **DONE 2026-07-05** (draw-callin context axis + `/calloutcensus` + `count_draw/count_sim` dump columns; same sync/overhead gates as PR 1). Census + classification takeaways in §E.1; a live-GL census remains recommended before finalizing PR-18 family order.

18. [F first family / O rest] (T5) Snapshot-serve hot callout families — positions family establishes the glue-redirection pattern (Fable; needs 15 + 17); remaining families follow (Opus), each validated by 17. With GL4 not coming first, the positions/piece-transform families carry the full draw-time load permanently — highest-priority serving work. **First family DONE 2026-07-06** — the unit positions/status family (E.1b order) is snapshot-served from draw context: `GetUnitPosition`/`GetUnitBasePosition`/`GetUnitHealth`/`GetUnitIsStunned` (LuaSyncedRead) + `GetUnitViewPosition` (LuaUnsyncedRead; its gate/errorVec/midpos-orientation reads were live sim despite the E.1b note). Landed as `rts/Lua/LuaSnapshotServe.{h,cpp}` (redirect predicate + `Route()` entry-point glue + serving twins) over new SimSnapshot rows incl. POV-complete masking inputs; masking policy settled as serving-layer over raw rows (SimSnapshot.h masking block); verification = Route's armed dual-run of both real paths + the gate's extended field pass + the per-POV masked-value sweep. **Remaining families DONE 2026-07-06 (second pass)** — projectile family served over a new `ProjectileRows` namespace, unit stragglers (`GetUnitLosState`/`IsUnitVisible`/`IsUnitIcon`) served, player/team family classified (boundary-copy spec, deferred to the extract-barrier PR by design), features + tail contracted; full status, field lists and gate numbers in §E.3. First-family gates (engine 2026.06.10-37-g7e266fd + working tree): diff gate armed full-length over BOTH replays with the driver exercising the family per draw frame and a f6000-12000 single-team-POV segment — ATG PASS 0 mismatches (5,566 boundaries; dual-runs: 29.0M GetUnitPosition + 29.0M GetUnitViewPosition + 14.4M each Health/IsStunned; ~31M/field pass, 124M masked-errorVec, 93M per-allyteam LOS/errorBit compares), Rosetta PASS 0 mismatches (7,233 boundaries; 16.7M/16.7M/8.3M/8.3M dual-runs); both master-demo resims clean full-length (Rosetta f=44537 @ 2:52, ATG f=44856 @ 4:00-4:07 vs 3:52-4:01 historical band — no regression beyond noise). Extraction with the PR-18 rows: unarmed avg 0.05 ms (Rosetta, 1.8k peak units) / 0.11 ms (ATG, 3k peak) per extraction, max 0.86 ms, resident 2 x 4.6 MB at MaxUnits=32000 (was ~0.02 ms / 2 x 1.5 MB for v1).
19. [F] (T6) Stream format + headless index-pass writer — encoding decisions (piece-pose quantization, projectile spawn-param re-derivation) are open design problems; gated on Wave-0 S0 numbers. (S0–S4 stages defined in `replay-seeking-architecture.md`.)
20. [F] (T6) Stream-fed render mode — the big integration; needs Track 2 complete + 12/13/14.
21b. [O] (T6) Reset-hook implementation from 21a's inventory.
22. [F] (T6) Seek UX + background resim + feed swap — interpolation continuity across the swap is the subtle part; needs 19/20/21b.
23. [O] (T6) Live ring-buffer rewind (reuses everything above).

**Wave 4 — the road to the split (T7; planned 2026-07-06, after PR 18 completed the Lua serving surface).** This is the remaining work for the Phase-2 sim/draw thread split itself, independent of Track 6 (replay seeking, PRs 19-23 — that track can proceed in parallel or later; nothing below depends on it). With PR 18 done, the Lua callout boundary — the "single biggest obstacle" — is served/classified; what remains is the §C/§D engine-side read surface, the formal barrier, the flip-time Lua mechanics, and the thread infrastructure. Order matters: 24 and 25 are independent of each other, 26 wants both, 27 wants everything. Every PR keeps the master-demo resim gate (both replays, full-length, 0 DESYNC) and the diff-gate where it applies; nothing before 27b changes game-visible behavior.

24. [O] **§C torn-read policy + remaining draw-pass conversions.** Deliverable one: a policy table classifying every remaining direct sim read in draw code into (a) snapshot-served (convert via the SimSnapshot accessor, add rows per the add-a-field recipe if missing), (b) tolerable-if-torn (off-by-one-frame invisible; mark the field atomic-relaxed at split time, document why), or (c) boundary-computed. Known reader list to start from (§C): minimap `DrawUnitRanges` radar-radii/selection reads, health-bar readers (health/maxHealth/buildProgress rows exist), `CUnitTracker`/`DollyController`/`DynWater` position reads (pos/midPos rows exist), info textures reading sim LOS/metal maps (likely (b) — a frame-late texture is invisible; the LOS maps themselves are NOT snapshotted and must be classified explicitly), `SmoothHeightMeshDrawer` reading the sim mesh (candidate (b) or a dirty-rect copy like the heightmap pattern). Sweep method: the PR-10 constify work already forced draw code onto `const T*` — grep draw-side code for sim-object member reads plus the handler globals (`losHandler`, `metalMap`, `smoothGround`, `quadField`) and classify each site; the compiler no longer finds these (reads are const-clean), so the sweep is manual but bounded. Deliverable two: the conversions for every (a) row. Gates: resim x2; diff gate covers any new snapshot fields. **DONE 2026-07-06** — full policy table landed as the §C DONE note above (read-context axis DRAW/UPD/EVENT; picking→PR 25, team/player→PR 26, heightmap already split, all excluded; LOS/metal maps + smooth mesh classified explicitly as (b)/(c) with dirty-rect-copy split actions). Deliverable-two conversions (3 fully-cleanable (a) sites, no new rows — only `MidPos`/`Radius`/`Frontdir`/`Rightdir` accessors over existing rows): `CUnitTracker::SetCam`, `CDollyController::Update`, `CGuiHandler::DrawCentroidCursor`. Decision recorded: no new SimSnapshot fields — every rowless candidate (range radii, `activated`, `maxRange`, `mass`, …) is a cold stale-invisible UI scalar, so (b)+atomic-relaxed beats bloating the hot per-unit extraction. No new snapshot fields ⇒ diff gate unchanged; gates run = build (headless/legacy/dedicated link) + resim x2.
25. [O] **§D draw-side picking against the snapshot.** Design-setting (first consumer of snapshot collision data): `GuiTraceRay` + box-select served from boundary data — needs new snapshot rows for collision-volume params (per-unit/feature `CollisionVolume` scale/offset/type, `crushable`-class flags are NOT needed; see `TraceRay.cpp:352-495` for the exact read set), piece-transform raycast support (piece matrices are already extracted per PR 8 — `ExtractTransforms` layout has per-piece model-space transforms; picking needs them resolvable by id), and a draw-side spatial grid over snapshot positions replacing the quadfield walk (box-select + ray coarse phase). Feature rows land here too if picking needs them (features are pickable) — use the §E.3 feature row spec. Callers to convert: mouse/GuiHandler/minimap picking, Lua `TraceScreenRay` (reads the previous boundary — ≤1 draw frame pick latency, decided §D). Sync-audit item: `GuiTraceRay` has synced callers too (gadget `TraceScreenRay` variants?) — audit and keep any synced path on live reads. Gates: resim x2 + a manual picking sanity pass (windowed: select/click/box-select under motion). **DONE 2026-07-06** — the whole draw-side pick surface now resolves against the SimSnapshot + a new draw-side broadphase grid (`Rendering/Common/SnapshotPickGrid.{h,cpp}`, 128-elmo cells mirroring `CQuadField`, rebuilt per boundary from the front buffers, DDA ray-cell traversal + radius query, ascending-id deterministic candidate order). Snapshot growth (add-a-field recipe, all 5 steps incl. `SnapshotHash` words + `SnapshotDiffGate` field passes): unit rows gained `selVol` (the live per-object selectionVolume, copied by value — an unsynced widget, BAR's `cmd_no_self_selection`, mutates it at runtime, so it must be the current value not the def), `noSelect`, `inVoid`, and `inRadarAll` (per-allyteam `losHandler->InRadar` answer — folds sonar/jammer/water, so we store the computed answer like the projectile/feature `inLosAll`); and a whole new `FeatureRows` namespace (validity/pos/midPos/relMidPos/radius/allyTeam/defID + `selVol`/`noSelect`/`inVoid` + per-allyteam positional-LOS `inLosAll` + the two `IsInLosForAllyTeam` globals `featureVisibility`/`gaiaAllyTeam`, grow-only to the max feature id like the projectile rows). The transform story needed no new drawer state: the unit pick matrix is reconstructed bit-exactly from the snapshot basis + drawer `GetDrawPos(id)` + snapshot `ErrorVector(id, at)` (= `CUnitDrawerData::GetUnsyncedTransformMatrix` via `CSolidObject::ComposeMatrix`), features read the drawer's id-keyed `unsyncedTransforms[id]`; new id-keyed accessors added for `GetUnitIconRadius`, `CFeatureDrawer::GetUnsyncedTransformMatrix`, and the drawer icon state. Draw-side collision entry point: a new `CCollisionHandler::MouseHit(relMidPos, isInVoid, m, p0, p1, v, cq)` overload takes the object's midpos-offset + in-void state explicitly (no `CSolidObject`) and reuses the exact `Intersect(v, mr, ...)` math. Callers converted: `GuiTraceRay` body (covers mouse click-select, GuiHandler command targeting, minimap frustum trace, tooltip, and Lua `TraceScreenRay` — the single choke point), the minimap closest-unit path (`CGameHelper::GetClosestUnit` fully + `GetClosestFriendlyUnit`'s `synced==false` branch, reproducing the exact `Filter`/`Query` predicates over snapshot rows via the grid radius query), and the engine box-select `midPos`/`pos`/`radius` loops (`CSelectedUnitsHandler::HandleUnitBoxSelection` + double-click, `CSelectionKeyHandler` Visible/FromMouse, minimap ProxyMouse) — those iterate own/selectable/spectator-visible units so the raw boundary value is exact. **Sync audit (clean fault line): `MouseHit` is the SOLE draw entry (only `GuiTraceRay` calls it), `DetectHit` is the synced entry.** MUST-STAY-LIVE (verified untouched): every synced `TraceRay::TraceRay`/`TraceRayShields`/`TestCone`/`TestTrajectoryCone` and their weapon callers (`LightningCannon`/`BeamLaser`/`Rifle`/`Weapon::HaveFreeLineOfFire`+`GetTargetBorderPos`/`StarburstLauncher`/`Cannon`/`MissileLauncher`), `ProjectileHandler::Check{Unit,Feature,Shield}Collisions`, QTPFS `PathCache`/`PathSearch` box tests, `FPSUnitController::Update`, `AICheats`/`AICallback` ray queries, and the synced `GameHelper::QueryUnits<true>` closest-* family (`GetClosestEnemyUnit`/`ValidTarget`/`NoLosTest`/`Aircraft`/`Friendly(synced)`); Lua `Spring.TraceRayGround*` are synced but ground-only (no sim-object reads, left as-is). No synced caller resolves to `GuiTraceRay`/`MouseHit`. SAFE-REDIRECTED (draw/unsynced): `GuiTraceRay` + all 15 UI/render callers, `Spring.TraceScreenRay`, minimap `GetSelectUnit`/`SelectUnits`, the box-select loops. **Deferred, documented:** (1) piece-tree selection volumes (`usePieceSelectionVolumes`) — `0` uses in BAR (checked the content repo: `usePieceSelectionVolumes`/`useFootPrintSelectionVolume` unused; `SetUnitPieceCollisionVolumeData` is a synced gadget on the *collision* volume, `SetUnitSelectionVolumeData` an unsynced widget on the top-level *selection* volume already captured by the by-value copy) — so the piece-tree pick path is inert for the gate; the draw-side `MouseHit` returns false for `DefaultToPieceTree` and the caller falls back to a live sim read for those objects (the one remaining live touch on the pick path), TODO removed at split-enable (27b); building an unverified piece-tree pick path no gate exercises was judged worse than the documented fallback. (2) Debug overlays (`DebugColVolDrawer`/`DebugDrawerQuadField`) stay on live quadfield/colvol reads — §C low-priority, dev-only, read weapons/shield/factory fields out of scope. (3) `Spring.GetUnitsInScreenRectangle`/`GetFeaturesInScreenRectangle` already project via drawer draw-pos; their `GridVisibility` quad walk + `IsUnitVisible`/team gate remain live — Lua-serving-tail class, convert with the E.2/E.3 remaining callouts. **Enumerated deviations (all imperceptible):** candidate iteration order changed from sim-quad order to ascending snapshot-id order (deterministic, required by §D) — affects only exact-distance / factory-overlap unit tie-breaks; `selVol` is excluded from the synced-desync `SnapshotHash` (it is UNSYNCED per-object state a local widget can diverge) but IS covered by the diff gate; the terminal winning-id → `CUnit*`/`CFeature*` lookup in `GuiTraceRay`/`GetClosest*` is the id→owner seam left for 27b (the pick DECISION is fully snapshot-driven). Coordination with PR 24: `SimSnapshot.{h,cpp}`/`SnapshotHash.cpp`/`SnapshotDiffGate.cpp` appended cleanly; re-run the diff gate after any rebase. Gates: **PENDING USER BUILD/RUN** — build (legacy + dedicated link), diff gate over both replays covering the new unit + feature rows (0 mismatch), resim x2 full-length (0 DESYNC), and the windowed manual picking pass (click/box/hover/minimap under motion + fast-forward).
26. [O] **The extract barrier as one function + small-table boundary copies.** Formalize the `CGame::Draw` boundary sequence (`renderEventQueue.Drain()` → `AckDrainedDestroys()` → `simSnapshot.Update()` → gate hook, plus the PR-11 boundary movers) into a single named barrier function with a documented contract — under the split this becomes the sim-pause window, so everything that must happen "at the boundary" has to already live inside it. Fold in: the player/team boundary copy per the §E.3 field spec (global block + per-team row + per-player row; serve `GetPlayerInfo`/`GetTeamInfo`/`GetTeamResources`/... from it via the §E.2 twin pattern — POV gates from the already-snapshotted alliance matrix), the `GroundDecalHandler::decalOwners` pointer-keyed map rekey (last pointer container, flagged in PR 14), and `readMap` unsynced-copy drain ordering (already boundary-shaped via `UpdateDraw`; just bring it under the same roof). Gates: resim x2; diff gate extended to the team/player copies.
27a. [F] **Flip-time Lua contract mechanics (Phase 3 close-out).** The pieces deliberately deferred to the flip: (1) the deterministic stale/nil tail — every sim-reading callout NOT snapshot-served returns the documented "no such object" nil shape when invoked from the draw thread, with a dev-mode warning naming the callout (a per-callout-name once-only LOG gated on a config var; the census tells us games don't call these at draw time, the warning catches the ones that do); mechanism: the same `ShouldServe`-style branch, but rather than wrapping ~600 entry points by hand, gate at the shared parse helpers (`ParseRawUnit`/`ParseFeature`/`ParseProjectile`/handler-global accessors) + an audit for callouts that bypass them. (2) Cross-hop bans: `GetSyncedHandle(L)`/`SYNCED` proxy reads from the draw thread must hard-error (they'd touch the synced lua_State the sim thread owns). (3) LuaUnsyncedCtrl classification: the ~169 entry points split into draw-side-only state (fine), net-command senders (fine — queue is thread-safe or boundary-flushed), and direct sim pokes (must become boundary-applied; census-style telemetry pass to find the hot ones). Deliverable is the classification table + the mechanisms, verified by running a full replay with the draw-thread contract ENFORCED while still single-threaded (a "pretend-split" assert/flag mode: everything CGame::Draw runs under the contract) — this is the cheap dress rehearsal for 27b and the real completeness proof (decision 5).
27b. [F] **The split itself (Phase 2).** `THREAD_IDX_SIM` + `SetSimThread`/`IsSimThread` + watchdog slot `WDT_SIM`; sim thread runs `ClientReadNet`→`SimFrame`s (copy the `GameLoadThread` spawn pattern: streflop FPU init on the new thread, `WrappedSync`-style no-op locks when the flag is off); the Wave-4-26 barrier becomes the handshake (sim pauses at a frame edge, never mid-SimFrame; boundary work runs; sim releases); backpressure per the architecture section (sim runs ≤1 frame ahead of the last boundary; catch-up free-runs). Infrastructure risks called out up front: ThreadPool partitioning (both sides call `for_mt`; either two pools, a partition, or fork-join-instance-local scratch — `ThreadPool.h:475-553`), GL-resource request queue for sim-triggered model/CEG loads (`IModelParser.cpp:238-467`, `ExplosionGenerator.cpp:1090` asserts mark the sites), net-message consumption order bit-for-bit (the sync-compat risk), `eventHandler` unsynced-event timing (tier (a) deviations already enumerated), and the profiler/watchdog accounting. Feature flag (à la `LoadingMT`) default-off; headless keeps the single-threaded model. Gates: resim x2 with the flag ON and OFF (flag-off must be bit-identical to 27a's tree); live windowed dogfood both replays; the PR-1 boundary-size + frame-time telemetry quantifies the win (fast-forward and widget-heavy scenes are where it shows).

Sync-compatibility note for the whole wave (unchanged from the invariants section): only 27b changes execution structure, and sim code itself stays single-threaded and untouched — the two real risk points remain streflop/FPU state on the new thread and net consumption order, both covered by the resim gates + mixed-lobby dogfood.


## Key files

`rts/System/SpringApp.cpp` (871-932), `rts/Game/Game.cpp` (1370, 1418, 1640, 2000-2138), `rts/Net/NetCommands.cpp` (161-267, 593-618), `rts/Net/GameServer.cpp` (263, 2630-2724), `rts/System/Platform/Threading.{h,cpp}` (30-37, 461-506), `rts/System/{GameLoadThread.cpp,LoadLock.h,Threading/WrappedSync.h,Threading/ThreadPool.h}`, `rts/Rendering/Common/ModelDrawerData.h` (82, 116-225), `rts/Rendering/Units/UnitDrawerData.cpp` (179-451, 589-723), `rts/Rendering/Features/FeatureDrawerData.cpp` (94-217), `rts/Rendering/Env/Particles/ProjectileDrawer.cpp` (377-421), `rts/Rendering/ModelsDataUploader.cpp`, `rts/Map/ReadMap.cpp` (69-96, 496-514), `rts/Game/TraceRay.cpp` (352-495), `rts/Lua/LuaHandleSynced.h` (180-268), `rts/Lua/LuaHandle.cpp` (2727-2909), `rts/Lua/LuaSyncedRead.cpp` (537-544), `rts/Sim/Objects/{WorldObject.h,SolidObject.h}` (67-104, 431-434).
